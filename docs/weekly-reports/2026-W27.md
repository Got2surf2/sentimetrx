# Weekly Governance Report — 2026-W27 (Week of Jun 22 to Jun 28)

**Generated**: 2026-06-29T08:11:09Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W27-devlog.md
**Spec-drift**: 0 drifted — see 2026-W27-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 50 commits, 1 author (Sanjay Patel), 1,894 file-changes across the week. Top-level dirs: app/ (524 files), lib/ (214), sql/ (163), components/ (114), scripts/ (108), tests/ (97), docs/ (56).

### Narrative (from devlog)

**PulseIQ Round-Based Pacing — Full Feature (6 feature commits + 1 test commit, all Jun 29)**
- Six-phase implementation of synchronous tasting-mode for PulseIQ: model layer (round_number on themes + config.rounds), creator rounds UI, moderator control + live engine gate (POST /api/townhall/sessions/[id]/round), participant hold screen (round_hold flag + branded hold card + 4s polling resume), round-grouped exports (CSV/XLSX + PPTX per-round section dividers), and final polish (cross-round move dropdown, AI Generate-append-to-rounds, resume lead-in message).
- Core use case: menu-research client runs synchronous tastings and needs the moderator to gate all participants between items. Moderator console shows round status/buttons; participants see a hold card and auto-resume when the moderator advances.
- sql/140 applied to prod.
- WHY: client-specific business requirement that was blockers on the pilot; pilot P1–P5 now COMPLETE.

**Multi-Tenancy Gate Tests for New Route**
- Added integration tests for POST /api/townhall/sessions/[id]/round and the resume endpoint: 401 unauth, 401 no-org-non-admin, 400 invalid round, 404 cross-org non-admin, 200 owning org, 200 admin bypass.
- Coverage gate: statements 23.35% / branches 15.99% / functions 23.06% / lines 23.91% (all above enforced thresholds); 1,068 tests pass (+9 from 1,059).

**Smart Dimensions & Competitive Report (prior commits in range)**
- Smart Dimensions: detect restaurant data at theme-time + auto-classify, one-click Enable+Classify, suppress flag for non-restaurant data.
- Competitive report: score members against shared themes, two-proportion significance test, Dimensions comparison matrix, normalize cells + narrative to %, proper radio modal for focus picker, section divider slides.
- Analytics: collapsed member themes when no collection theme set.

**Entity Catalog & Brand Correction (prior commits in range)**
- Entity-catalog provenance + source authority: discovery stamping, glossary filter, agent write-side, foundation.
- Shared brand-correction layer Phases 1–5: lib/correction shared across Town Hall / bots / surveys / brand-glossary editor; manual add at brand scope.

**Questions Page & KB Hygiene (prior commits in range)**
- Questions: full-conversation modal, inline answer/accept, AI-drafted answers, guided Review mode, Unanswered queue newest-first.
- KB hygiene: near-duplicate guard on answer→KB, Knowledge-base Health view.

**Collections (prior commits in range)**
- Manage Members unified (add + remove + admin-aware), freshness badge + refresh, purpose persistence, Collection source pill + refresh.
- Purpose-typed collection reports: competitive + brand-360 + shared PDF design system, PDF template standard (branded header/footer, keep-together pagination).

### Risk signals
- All W27 work is **LOCAL / unpushed** — no Vercel deploy yet. Owner visual QC of auth-gated UI surfaces (moderator console, participant hold, PPTX round dividers) is the stated gate before push authorization.
- PPTX round divider slides flagged in devlog for a visual pass against a real rounds session (auth-gated route, can't headless-render).
- Resume endpoint serves the lead-in question verbatim (no AI call) — deliberate for poll speed; future enhancement is noted.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 13.5 |
| Security | 8/10 | 15% | 12.0 |
| Dependencies | 8/10 | 15% | 12.0 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 7/10 | 15% | 10.5 |
| Documentation | 9/10 | 10% | 9.0 |
| Maintainability | 9/10 | 20% | 18.0 |
| **Total** | | **100%** | **83.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- None. 0 critical / 0 high CVEs (down from 2 high in W25). No hardcoded secrets detected. No eval/exec with user input. New round route is org-gated with integration tests covering the multi-tenancy invariants. requireAdmin called in 85 app/ locations + 2 lib/ locations across all internal routes.

**MEDIUM**
- `xlsx` sourced from `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` (SheetJS Pro CDN) — `npm install` returns 403 Forbidden in clean checkout environments without credentials. Breaks CI on fresh clones and any remote dev environment. Supply-chain reliance on a proprietary CDN is a risk if the CDN changes or the license lapses.
- 14 moderate npm CVEs — no critical/high path identified; likely dev/build-time chain. Not individually audited for exploitability.
- Test coverage at ~23% statements / ~16% branches — enforced thresholds exist (15% stmt / 10% branch) but are conservatively set. Coverage gap is the main quality risk for route-handler logic.
- Bare service-role `eq('id')` pattern in `app/api/bots/[id]/route.ts` (two-step read-to-get-org_id) — flagged in W25, not swept this week. Write paths should confirm `id + org_id` pairing per CLAUDE.md invariant.

**LOW / informational**
- Google Fonts dependency in PDF deck routes — no bundled fallback; serverless render fails silently if fonts unreachable. Documented, unresolved.
- 0 `.claude/agents`, 0 `.claude/rules`, 0 `.claude/skills` — governance relies on CLAUDE.md + hooks + commands only. Persists from W25.
- `scripts/` directory (108 files) — systematic audit of one-off vs maintained tooling not done.
- `lib/filterUtils.ts` UTC period resolver — deliberate, documented in SAVED_VIEWS.md §3.

### Progression plan (top 3 to fix this week)

1. **Document or replace `xlsx` CDN dependency** — add a `README` note or package.json comment that SheetJS Pro credentials are required for `npm install`, OR evaluate replacing with a public npm alternative (e.g. `exceljs`). Blocks clean checkouts in CI and remote environments. Estimated: 30–60 min investigation, decision point.
2. **Raise CI coverage thresholds** — 1,068 tests pass; bump `vitest.config.ts` statement threshold from 15% → 20% and branch from 10% → 15%. Locks in test gains and prevents regression. Estimated: 10 min.
3. **Audit `app/api/bots/[id]/route.ts` write paths** — confirm every service-role UPDATE/DELETE after the two-step read pairs both `id` and `org_id` in the mutating call, matching the CLAUDE.md invariant. Flagged in W25, now in a second report. Estimated: 30 min.

## 3. Correlation: dev work → audit findings

- **HIGH CVEs (@vercel/sandbox / @workflow)** — addressed. W25 flagged 2 remaining HIGH CVEs; current `npm audit` shows 0 high (resolved somewhere in the W26–W27 range).
- **Bare service-role id lookups (`bots/[id]`)** — not mentioned in devlog. New round route was correctly gated from day one, but the W25-flagged bot route was not revisited. Not addressed.
- **Test coverage gap** — incrementally addressed. W27 added 9 gate tests for the new round route (1,059→1,068); ratio 0.098→0.10. Coverage ~23% unchanged; not a focus week.
- **xlsx CDN dependency** — not mentioned in devlog; newly surfaced this audit run (403 on clean install). Not addressed.

## 4. Trend

Previous governance report: **2026-W25: 81.5 / 100** (2026-W26 had a devlog but no governance report was filed).
This week (2026-W27): **83.0 / 100**
Delta: **+1.5 points**

Driver: Dependencies score moved 7→8 as the 2 HIGH CVEs present in W25 no longer appear in `npm audit`. All other category scores held steady.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Review the spec-drift companion (2026-W27-spec-drift.md) — any spec debt?
- [ ] Check correlation section — are open findings being worked on?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
