# 2026-W28 — Dev log (Week of Jul 6 to Jul 12)

## 2026-07-06 — Google reviews response dashboard scoped (spec only, DATA_SOURCES.md §7.x)

WHY: owner wants to close the loop on Google reviews — not just analyze them but respond: near-real-time alerts to management and AI-drafted replies. Scoping found ~70% of the substrate already standing (DataForSEO sync + cadence, auto-classify with severity/emotion alert signals, Resend email, and the client-review draft→approve→send workflow as the exact UX template), and ONE hard external dependency: posting replies requires the Google Business Profile API (DataForSEO is read-only), which needs a Google-approved application (~2 weeks) from an org holding its own verified Business Profile ≥60 days old. Spec records a 3-phase plan — P1 dashboard/alerts/AI-drafts with copy-to-Google manual posting (no external deps), P2 GBP OAuth for one-click posting + minutes-level polling, P3 auto-thank-you/digests/benchmarks — plus the honesty note that "real time" via DataForSEO bottoms out around hourly. Owner action item captured: verify/create the Datanautix Business Profile now, because the 60-day age requirement, not our build, is the critical path. Application URLs web-verified. No code.

## 2026-07-06 — Auto-classify emotion at theme-mining time (the universal tier's forward path)

WHY: the backfill covers existing datasets, but without this every NEW non-restaurant dataset would need a manual Dimensions toggle to get emotion tags. The mine-themes foodService judgment is already the moment we decide what kind of data this is — so the false branch (which stamps taxonomy_suppressed) now also runs `autoTagEmotion` (TextMineModule, mirror of the 6/28 `autoEnableDimensions` restaurant path but with the order inverted): classify FIRST via the pendingOnly loop (the route picks emotion mode because suppression was just stamped), then flip taxonomy_enabled ONLY if emotion language actually fired — the genre gate means an ideation survey never grows an empty "0% emotion" tab. "Tagging emotion language…" banner while it runs; zero clicks. Housekeeping in the same commit: the WDK-generated `app/.well-known/workflow/**` routes (untracked, regenerated by every dev/build) were being linted — their own eslint-disable headers count as "unused directive" warnings and jittered the ratchet ±the regeneration — now eslint-ignored + gitignored, and the ratchet ceiling drops 3062→3061. tsc clean, 1,238 tests.

## 2026-07-06 — Emotion goes universal: the emotion axis decouples from the restaurant gate

WHY: owner caught a scoping gap — disappointment/blame/churn-intent language is vertical-neutral, but the emotion axis only computed inside the restaurant-gated taxonomy classify, so surveys/uploads/PulseIQ datasets could never get it. Fix = a server-side classify tier (never client-passed): `full` (restaurant ABSA + emotion) keeps its exact gate — unsuppressed google-reviews sources and taxonomy-capable orgs — and every other dataset now gets `emotion` mode, which skips the restaurant dictionary entirely (menu vocabulary on a donor survey would be invented taxonomy) and embeds only the emotion axis. The per-dataset taxonomy_enabled flag is redefined as "Dimensions on" for ANY dataset (Settings/Upload/Enable-Dimensions copy rewritten to describe both tiers); taxonomy_suppressed (the mine-themes non-food judgment) now also forces emotion mode, so a hotel's google reviews get emotion instead of being hidden. The rollup zero-suppresses EVERY axis that never fired (was emotion-only), so an emotion-only dataset renders a single Emotion dimension card rather than seven empty restaurant cards. All §2a discipline holds (expressed-language framing, negation guard, evidence spans, genre zero-suppression, neg-row denominators degrading to null without ratings). Known corner documented in TAXONOMY §2a.0: the foodService upload auto-detect in a non-restaurant org now yields emotion-only — restaurant client orgs should carry restaurant primaryIndustries (org-setup norm). Verified: 3 new rollup tests (1,238 total), tsc clean, lint 3062 held, and a live TEST run on a real survey dataset ("liked LEAST", 11,420 rows): stored rollup contains ONLY the emotion axis — disappointment 5.4% of negative rows / churn 0.8% / blame 0.6%, avg rating 2.1 on flagged rows, evidence spans intact.

## 2026-07-06 — Research Probes UI: setup editor + results dashboard (§14 units 3–4 — feature complete)

WHY: probes without an owner-usable surface would be config-by-JSON — not acceptable for a non-developer owner. Setup: a "Research Probes" section in the agent editor (mirrors the Prompt Focuses idiom) covering question/delivery-mode/answer-format/sample-%/quota/earliest-turn/follow-up + concept construct; the §14.2 rule-7 version bump is automatic and previewed inline ("v1 → v2 on save (wording changed)"); un-exposed JSONB fields survive saves via the carried raw object. Results: /bots/[id]/probes ("Probes" card link) reads GET /api/bots/[id]/probes — per (probe, version) funnel (assigned→asked→answered with the full outcome breakdown), response/decline rates, quota progress, yes/no + emotion-language chips, concept wording variants, verbatim drill-down; historical versions and deleted probes with data still render (results are never orphaned by a config edit). Auth mirrors the bot [id] route (admin-org may view any; service-role reads pair agent_id+org_id); responses page past the 1000-row cap via fetchAllRows. Verified: tsc clean, 1,235 tests, lint 3062 held, dashboard cards render-QC'd via a static harness screenshot.

## 2026-07-06 — Research Probes runtime: ambient market research inside agent conversations (BOTS.md §14, units 1–2)

WHY: the owner's build order after the Fable-window list — the §14 spec (committed 7/5) completes the elicitation ladder: passive emotion flags on reviews → dedicated adaptive surveys → sampled research questions woven into ordinary service chats. This lands the substrate + runtime + mandatory disclosure; the setup UI and results dashboard are the next unit. sql/159 (TEST-applied; prod rides the next batch): `agents.research_probes` library, outcome-accounted `agent_probe_responses` (RLS org-read; every assignment resolves to exactly one of asked_answered/asked_declined/asked_ignored/never_fit/quota_closed — non-response is data, not survivor bias), `agent_probe_quota` + atomic counter RPC (sql/154 pattern). `lib/researchProbes.ts` is pure and deterministic: djb2 hash assignment (reproducible — no model discretion over WHO gets asked), field windows, eligibility gates, verbatim-containment delivery matching, decline/yes-no keyword coding. chatCore wiring uses the Decision-Study state-injection pattern with one design improvement over the spec: probe state rides the response row itself (provisional `asked_ignored` at delivery, upgraded by asked_turn arithmetic) instead of turn content_flags, because the focus classifier overwrites flags — race-free by construction. The live harness caught a real gap the spec missed: the sentiment gate only saw prior STORED turns, so an angry first message didn't gate the ask — now the current message is scored inline too. Disclosure line (§14.3) renders in the widget composer footer whenever any probe is enabled, server-computed. cooldown_days is documented as NOT enforced in v1 (no stable cross-session participant identity). Verified: 17 unit tests (1,235 total), tsc clean, lint 3062 held, and a 13/13 live end-to-end run against the TEST project through the real handleChatTurn with real AI calls (ask → capture → coding → quota → decline → quota_closed → gated skip → cron never_fit sweep).

## 2026-07-06 — Town Hall Participation tab: who held the floor, deterministically

WHY: #5 on the owner's Fable-window list — the ML-SPEAK-inspired participation-dynamics idea scoped conceptually on 7/4. Town Hall reports summarize WHAT was said (Q&A, coverage, actions) but nothing showed the meeting's conversational shape: who dominated, how often the floor changed, how much of the meeting was panel vs community voice. New `lib/recordings/participation.ts` computes it purely and deterministically (no AI) from the diarized transcript segments — per-speaker talk time/share/turns/longest-turn/pace, floor changes, crosstalk moments, quiet time — handling both identity systems (stereo channel+channel_labels, mono speaker+speaker_names) and reusing the conservative `isPanelMember` roster matcher so the panel-vs-community split reports null (not a fake 0%) until names are assigned. New Participation tab in the report (between Action items and Transcript) renders a KPI strip, the split bar, a per-speaker floor ribbon, and a stats table whose longest-turn cell plays that span through the shared audio modal. Zero new fetches/routes/migrations — everything computes client-side from props already in ReportData; Whisper-only mono recordings get an honest empty state. Verified: 10 new unit tests (1,218 total), tsc clean, lint held at 3062, and a full render QC via a synthetic 7-speaker meeting screenshot (untracked scripts/_participation_qc.ts).

## 2026-07-06 — Deck style picker: the palette becomes per-request data (DN-const refactor)

WHY: #4 on the owner's Fable-window list, and the long-standing blocker recorded when the style/personality picker was scoped 2026-06-02: the renderer hard-coded ONE palette, and a *selectable* palette must be per-request. The landscape had moved since the scoping — the 6/25 cream flip already made `lib/pptx/slideRenderer.ts` the single styling source of truth for every renderDeck consumer — so the refactor landed there, not in the route: the cream constant moved verbatim to `lib/pptx/styles.ts` as the **Datanautix Modern** preset (DEFAULT), joined by **Datanautix Classic** (the original navy/teal/gold DN palette), **Warm** (terracotta/olive) and **Ocean** (blue/cyan). The resolved palette rides the per-request pptx instance (`pal(pptx)` alias at the top of all 25 builders) — deliberately NOT a mutable module global, which on Fluid Compute would bleed colors between two concurrent exports of different styles. ExportModal gains a swatched "Deck Style" picker (PPTX only); `body.style` is validated against the preset registry in the route and recorded on the recap slide. Proof of no regression: the same 9-slide spec rendered by the pre-refactor and post-refactor code compared **pixel-identical** across all slides (LibreOffice→pdftoppm harness, kept as untracked scripts/_style_qc.ts); all four presets eyeballed on covers/sections/theme-cards/quotes/bar-charts — readable and Datanautix-marked in every style. tsc clean, 1,208 tests, lint held at the 3062 ceiling (the two `any`s the first draft added were typed away instead).

## 2026-07-06 — W28 governance follow-up: 3 drifted specs cured, dev-dep CVEs cleared, inventory docs de-staled

WHY: the W28 report dropped 83.0→79.0 on 3 drifted specs (SEARCH/SOCIAL/CAMPAIGNS — missed by the 7/3–7/4 sweep commits, which bypassed the pre-commit hook after updating 13 OTHER specs; process rule adopted: sweeps that SKIP_SPEC_CHECK get a spec-drift-tool pass before done). Each spec now records its actual code change (admin auto-grant via resolveOrg on social/campaigns gates; paged campaign-scheduler respondents — the silent-1000-cap class; bounded-concurrency Facebook comment fetch; `_tx` reserved-key skip in search detail). npm audit fix cleared the 8 auto-fixable moderate dev-dep CVEs incl. @babel/core (12→4; the 4 survivors are the deliberate isomorphic-dompurify/jsdom pin — owner judgment, not auto-fixable); typecheck + 1,208 tests green. ALSO: SPEC.md + FEATURES.md were stale through the 7/4 taxonomy-embed migration (still described the dropped sidecar tables and lacked the emotion axis) — both inventory docs now describe the `data._tx` embed and the emotion-language flags feature.

## 2026-07-06 — LLM-tier prototype: regret is viable, cost is $0.005/review (research, no wiring)

WHY: #3 on the owner's Fable-window list, and the decision evidence the RC pilot needs. 300-review deterministic gold sample (150 neg/150 pos, RC on TEST) through the dormant lib/taxonomyExtractor (Haiku fast tier, prompt v4 + a prototype emotion-axis addendum), ~$1.40 total. Headline: REGRET — dark at the keyword tier since the 7/4 validation (50–60% precision) — reads ~75–80% precise at the LLM tier (8 candidates, ≈6 clean incl. textbook menu regret "next time I will go with my first choice, T-bone"; two 5★ hits prove the negative-context reporting gate earns its keep). LLM recalls 90–100% of validated keyword emotion hits and finds 2–5× more; taxonomy merged lift 9.76 vs 7.60 assertions/review with additions concentrated in inference buckets; only 50.8% of keyword assertions LLM-confirmed (learned-dict FP tail flagged for triage). Measured $0.0047/review → RC full ≈$97 live / ≈$48 Batch API. Full findings + wiring plan recorded in TAXONOMY.md §2a.1; per-review results with regret evidence in untracked scripts/_llm-tier-prototype-results.json (owner spot-check = ship gate). Also researched public regret-vs-disappointment data: GoEmotions (separate remorse/disappointment labels) fits as an external precision benchmark; ReDDIT (action/inaction regret) has no confirmed public download. EA decision same night (TAXONOMY.md §2a.2): membership vertical runs keyword-tier always-on with a non-renewal overlay (scoped, ~20 phrases, spot-check on first real wave); LLM as per-wave pass.

## 2026-07-06 — First deploy-behind-CI exercise: gate proven in both directions

WHY: first real push since ENGINEERING §12 armed. The 16-commit batch (owner-authorized) auto-built NOTHING on push (deploymentEnabled main:false working); CI run 1 failed the check:sql-tx guard (sql/158 was applied to prod pre-wrap) and correctly deployed nothing; fix = BEGIN/COMMIT wrap + CREATE OR REPLACE for idempotent re-apply, re-applied via npm run migrate to re-sync the ledger sha (snapshot byte-identical — zero functional delta); CI run 2 green in 4m02s → the deploy job fired the ci-main hook → exactly ONE production build of the right sha. Also resolved an owner alarm about "all these deployments": a stale Vercel dashboard tab from Friday 07:45Z — every deployment shown was created 3s after its authorized 7/4 push (verified against the GitHub push-event log); zero unauthorized builds. Housekeeping in the same batch: .claude/scheduled_tasks.lock untracked (machine-local scheduler state committed by accident in 1c1b03ce) and scripts/_*.json added to the untracked-harness gitignore patterns.

## 2026-07-06 — Emotion-language flags shipped as the embed's 8th axis (disappointment / blame / churn intent)

WHY: the 2026-07-04 research session validated the framework (30-sample spot-check: disappointment 12/12 precision, blame 6/6; churn ~7% of negatives vs 0.3% of positives across brands) and #2 on the owner's Fable-window priority list was "emotion-flags build". Built it as an **8th `emotion` axis inside `data._tx`** rather than a parallel sidecar key — that one decision makes every existing surface inherit the feature: Dimensions tab card, TextMine dimension cloud + Comments facet, Charts/Stats `__dim_emotion__`, drill-down with evidence spans, and dimension×dimension crosstabs (disappointment × product = "which dishes drive expectation-gap language" came free). `lib/emotionFlags.ts` ports the validated probe lexicons with two upgrades over the probe: a negation guard on disappointment ("you won't be disappointed" no longer counts — positive-row false hits halved to 1.7%) and regret kept dark (first-person "should have" DROPPED; third-party→blame, passive/impersonal→disappointment per the spot-check fixes). LLM lockout is structural: `AXES` (extractor closed vocab) stays 7-axis, new `ALL_AXES` feeds embed/read. Rollup grows an optional `emotion` block — per-flag counts split by neg (rating≤3) / pos denominators + disappointment×churn-intent co-occurrence — and **omits the axis entirely at zero** (the genre gate: never "0% emotion" on ideation prompts). Operational Review deck gets a kpi_grid tile "Emotion Language in Negative Reviews" (negatives-denominated, expressed-language captions, co-occurrence KPI = the recovery list; emotion excluded from the ABSA aspect-coverage chart). sql/158 widens the five taxonomy RPC axis guards + adds `p_sub_emotion` to get_rows_by_filters (client omits the param unless an emotion chip is selected, so pre-migration prod can't break — deploy-order safe). TAXONOMY_VERSION v3→v4. Verified: 1,208 unit tests (12 new lexicon cases incl. all spot-check fix classes), read-only rate check on the RC clone matches validation (disap 30.7% of 1–3★, blame 0.8%, churn 5.9% neg / 0.3% pos, 59 disap+churn co-occurrence rows), then live e2e on TEST after applying sql/158 there: full re-classify (20,774 rows) → rollup v4 + emotion block + all five RPC surfaces + Comments facet + old-signature backward-compat, 10/10 checks. sql/158 NOT yet applied to prod (owner-gated, must land before/with the next deploy — but code degrades safely either way). Captive verticals (`suppressChurn`) and per-vertical blame overlays are parameters awaiting their first non-restaurant vertical; regret rides the RC LLM tier.

## 2026-07-06 — Universal emotion tier backfilled across ALL prod datasets (keyword tier, zero AI)

WHY: `8acf9a95` decoupled the emotion axis from the restaurant gate in code, but the axis only lights up on data that's actually been classified — every existing prod dataset predated it. Owner authorized the prod backfill. Built `scripts/_backfill-emotion-all.ts` (untracked harness): for every dataset with open-ended text it replays the taxonomy route's EXACT server-side tier decision — `full` (restaurant ABSA + emotion) iff NOT `taxonomy_suppressed` AND (`google_reviews` OR `orgTaxonomyEnabled`), else `emotion`-only — over the Dimensions view's default ANALYZE field (saved theme-model fieldNames, else first open-ended field, else the route's sampling heuristic), then flips `datasets.taxonomy_enabled` on ONLY where emotion actually fired (genre gate — never surface "0% emotion"). Idempotent (blocks overwrite in place), reuses the real `classifyByKeyword`/`detectEmotionAssertions`/`buildFieldBlock`/`apply_taxonomy_verdicts`/`updateStoredTaxonomyRollup` path, no dictionary re-implementation.

One real perf gotcha found + worked around: `classifyDatasetKeyword` keysets on `id` (`.gt('id',cursor).order('id')`) but there is no `(dataset_id, id)` index — from a direct connection that blob-select costs ~3.1s on the first page of a big dataset and trips the DB statement timeout (the in-app route survives inside its warm request; a script doesn't). The script instead enumerates ids via the `(dataset_id, row_index)` index (~170ms/1K) and fetches blobs by PK with `.in('id', …)` (~240ms/1K), so it never touches the slow path. (Candidate follow-up for the lib itself: a `(dataset_id, id)` index or a row_index keyset — not done here, out of scope for a backfill.)

Result: 38 datasets with text classified (~210K rows), zero AI spend. Full-tier restaurants re-classified (this closes the pending "re-classify RC/Chuy's to light the emotion axis" item): RC 1,311 emotion hits (disappointment 30.7% of ≤3★ — matches the 7/04 validation), Chuy's 75 (disap/blame/churn all present), Capital Grille 632/619, Nobu 519, Flemings 476, Cheddar's 1,018. 14 non-restaurant datasets flipped `taxonomy_enabled`→ON where emotion fired (Olive Garden/Sample Google Comments 178, Universal Studios 303, MCO airport 42, two Substack comment sets 9 each, Reddit 6, Coalition google-reviews-suppressed 5, an EA MVP+ sample 2, several small NOWOCATS recordings); genre gate correctly LEFT off the datasets where nothing fired (USDA regulations comments, bot-side conversation text, the tiny Coalition NPS-followup studies). Verified read-only across tiers: emotion-only datasets carry ONLY the emotion axis (no invented restaurant cards — Carabbas GSS, Olive Garden), evidence spans present and on-target both tiers (disappointment/blame/churn verbatim), full-tier datasets keep all 7 ABSA axes + emotion. Prod writes only (authorized); no code shipped, no push.

## 2026-07-06 — AI-cost accounting: every call now metered + org-attributed (audit + remediation)

WHY: owner asked to confirm ALL AI calls are logged and assigned to an org for cost accounting. A full audit (100+ callAI sites + every direct-fetch vendor path, cross-checked against docs/USAGE_ACCOUNTING.md) found three failure modes, since `usage_logs.org_id` is nullable and nothing enforced attribution. Fixed all of them.

UNMETERED → METERED: (1) ask-ana's direct Anthropic **streaming** endpoint logged nothing — now parses SSE `message_start`/`message_delta` usage and logUsage's on stream end under `dataset.org_id`. (2) Browser live Deepgram captions (mic streams browser→Deepgram, never hits our server — a real charge on top of batch ASR) — transcribe.ts now emits a new `recording_live_stream` flat cost from the recorded duration at Deepgram's per-minute rate, booked to the recording's org. (3) entityDiscovery set `usage:undefined` on null org (dropped the row) → always logs now.

ORPHANED (org_id omitted though resolvable) → ATTRIBUTED: agentReadout ×3 + agentStudy ×3 (thread bot.org_id via new orgId params + call-site updates); bot conversation report/insights-deck/review-cron (bot.org_id); datasets insights/expand-keywords + signals-pptx export (getCallerOrgContext / orgId); the town-hall setup helpers suggest-topic/guide/sensitive/expand-terms/simulate + participant join-translate (getCallerOrgContext / session.org_id); deflect (survey widget now sends studyGuid → route resolves the study's org, mirroring clarify); temple-events cron (looked up Guru agent org, was org_id:''); reoExtractor + gradeEventDescription gained orgId params.

DOUBLE-LOGGED → single: export/scoreComments, export/pptx, export/html each had callAI({usage}) auto-log PLUS a redundant manual logUsage (2× token counts). Dropped the manual one.

CONCURRENCY: personaExtractor (persona/demographics) and the legacy townhall/chat language-switch path attributed via a mutable module-global usage ctx — a cross-request org-misattribution hazard on Fluid Compute. Both refactored to pass the ctx per call (removed the globals + the setPersonaUsageCtx API).

GUARD: logUsage/logFlatCost now console.warn when a customer resource type (bot|townhall|social|dataset|study|recording) logs with null org_id, so regressions surface. `system` stays the one org-less bucket. HONEST system cases: nora-chat (Tabla) and clara-chat (Craniometrix) are hardcoded-KB platform assistants with no org binding in code — reclassified from mislabeled dataset+null to resource_type:'system' (hardcode a client org_id later if they should bill). Moderation + health probes are free endpoints, left unlogged.

Verified: tsc clean, 1238 tests pass, lint held at ceiling. docs/USAGE_ACCOUNTING.md gets a dated hardening addendum + the ~19 event types that had drifted undocumented. No push.

## Bareburger demo conversational survey (Jul 6)

WHY: Owner wanted a ready-to-show conversational survey for Bareburger to demo the by-score follow-up + AI-clarifier flow.

`scripts/seed-bareburger-demo.ts` upserts an active study (slug `bareburger`) from a hand-built StudyConfig — 5-star experience rating (NPS off); per-score `experienceFollowUp` (per-response mode) where 1–3 probe what fell short and 4 & 5 ask a "what could we improve" question; `useAIClarify:true` so the /api/clarify pass deepens thin answers; 8 Bareburger-tailored psychographics (organic/plant-based/sourcing lean), 3 shown per session; legacy q3/q4 off so the per-score follow-up is the sole open-end. Green/earthy theme. Seeded to TEST + verified /s/bareburger serves HTTP 200 with the intended config. `--prod`/`--org` flags exist but were NOT run against prod. No new survey behavior — reuses existing engine fields, so no spec change. No push.

## Bareburger demo — brand colors + shipped to prod (Jul 6)

WHY: Owner asked if the colors were right and to get the demo onto production.

Pulled Bareburger's official logo SVGs — real palette is deep green `#0C5C43` + accent pink `#E584B0` (my first pass used an off-brand olive/lime). Corrected the theme in `seed-bareburger-demo.ts` and added a `--slug` flag. Found a pre-existing generic Bareburger survey already live on prod (`/s/bareback`, 2 responses, no per-score follow-ups, cyan theme); owner chose to upgrade it in place rather than duplicate. Ran `--prod --slug bareback --org <datanautix>` — updated the existing study's config (same guid `07aee641`, responses preserved). Verified live: https://www.sentimetrx.ai/s/bareback serves 200 with the new config (brand palette, per-score follow-ups incl. 4★/5★ "what can we improve", AI clarify, 8 psychographics). DB update only — no Vercel deploy. No push.

## Reusable sales-pitch deck template (Jul 6)

WHY: Owner wanted the Bareburger capability deck productized into a standard, client-agnostic template so we're not "reinventing the deck" for each pitch — and asked whether restaurant vs non-restaurant needs two templates or one smart one.

Answer shipped: ONE smart template, `lib/pptx/salesPitchDeck.ts` (`buildSalesPitchDeck(pptx, cfg)`), in the EA-membership house style (navy header bands, sarinaBlue accent, orange kicker lines, navy stat cards, Sentimetrx-forward + "powered by Datanautix"). Fixed universal SPINE (cover → two-ways-to-hear [Listen=reviews in the wild / Ask=conversational surveys] → why-conversational [the 40–50% open-end data-loss argument] → the conversational advantage [illustrative static-vs-conversational] → try-it QR → the ask). Restaurant-specific EVIDENCE modules (stars-&-dogs leaderboard, score decomposition, Dimensions↔Themes coin, mined themes, food-safety early-warning) are all OPTIONAL + data-driven, and table headers are configurable, so a non-restaurant pitch is the same template with those modules omitted and labels swapped. Two seed configs: `SAMPLE_BAREBURGER` (restaurant, real 2022–23 review data — teaser strategy, 2024–26 held as paid unlock) and `SAMPLE_GENERIC` (spine + KPI grid). Admin-gated `app/api/sales-pitch-deck/route.ts?client=<slug>` (defaults bareburger) + two entries on `/admin/decks`. Bareburger QR points at the live prod survey `/s/bareback` (verified 200). One-off `scripts/_bb_deck.ts` is now a thin wrapper over the shared builder so the meeting file and the route never diverge. tsc clean. No push.

## forceClarify flag — always-on AI follow-up (Jul 6)

WHY: Owner wanted the Bareburger demo to always show a tailored AI clarification, even when a respondent gives a long/detailed answer (default engine SKIPs those).

New opt-in `config.forceClarify` (default off — no change to any existing study). Two gates relaxed only when set: `shouldClarify` in useSurveyEngine returns true regardless of answer length (declines/off-topic still skip), and `/api/clarify` swaps its "SKIP if 3+ points" rule for "always ask ONE new-angle question; SKIP only for unsafe/off-topic". Client passes `force` through buildClarify. Enabled on the Bareburger demo (seed script), re-seeded TEST + prod bareback. Verified against the live route: an exhaustive "nothing to improve" 5★ answer → no-force SKIPs 4/4, force returns a relevant tailored question 4/4 (menu-addition / values angles). tsc clean. SURVEYS.md config list updated. NOTE: the live prod demo (/s/bareback) needs a deploy for the code to take effect — owner will push.

## CI fix: forceClarify push went red — floating-promise error + lint ceiling (Jul 6)

WHY: The forceClarify push failed CI (deploy-behind-CI → nothing deployed). Two causes: (1) `scripts/seed-bareburger-demo.ts` had a hard `no-floating-promises` error on the bare `main()` call → wrapped `.catch(e => { console.error(e); process.exit(1) })`; (2) the parallel-session sales-pitch deck template (`194baea1`) shipped `lib/pptx/salesPitchDeck.ts` with ~9 `no-explicit-any` warnings, pushing the total 3061→3069 over the ratchet ceiling. Typed the deck's pptxgenjs params properly (`import type PptxGenJS` → `PptxGenJS.Slide` / `PptxGenJS`, matching sibling deck builders). Total now 3060; lowered `lint:ci` ceiling 3061→3060. tsc clean.

## ESLint burn-down batch 1 — pptx Slide/instance params (Jul 6)

WHY: Owner wants the ~3000 `no-explicit-any` warnings driven down. Batch 1 = the mechanical pptx cluster: typed `slide`/`s`/`pptx` params across 7 deck/export builders (`projectInsightDeck`, `reviewIntelligenceDeck`, `mcoListeningDeck`, `eaMembershipDeck`, `shared`, `recordingDeck` [aliased `PGJS` — runtime const collision], `studyDesignPptx`) as `PptxGenJS.Slide`/`PptxGenJS`. Type-annotation-only (no runtime change). Deliberately EXCLUDED `slideRenderer.ts` — it augments `pptx` with `_deckPalette`, so a plain `PptxGenJS` type is wrong there (left for a later, careful pass). 3060→3015 warnings; ceiling lowered to 3015. tsc clean, 1238 tests pass.

## ESLint burn-down wave 2 — no-explicit-any across 21 files (Jul 6)

WHY: Owner opted into the parallel workflow approach to drive down ~3000 `no-explicit-any` warnings.

Ran a multi-agent workflow (`scripts/_wf-eslint-burndown.js`, untracked): one agent per file in an isolated git worktree (node_modules symlinked), each types its file with real interfaces/imports (annotation-only — no runtime logic changes), verifies `npx tsc --noEmit` clean in isolation, and writes the verified file out. 21 of the top any-heavy files done (chatCore 134→2, export/pptx 96→1, ChartsModule 81→35, useSurveyEngine 51→0, the townhall/pulseiq/deck routes, StatsModule, statsUtils, etc.). Consolidation caught one cross-file conflict the isolated agents couldn't see — StatsModule's local `RegressionResult` (`ci: number[]`) vs statsUtils' exported one (`ci: [number, number]`); reconciled by aligning the tuple. Agents deliberately LEFT genuinely-untypeable anys (e.g. chatCore's dynamic `agent`/`body` surface, discriminated-union results accessed before narrowing) rather than risk incorrectness. Verified: global `tsc` 0 errors, 1238 tests pass, warnings 3015→2194 (−821); ceiling lowered to 2194. (Full-project `eslint .` OOMs on this machine now, so the count was summed per-directory.) Not pushed.

## ESLint burn-down wave 3 — 26 more files to zero-any (Jul 6)

WHY: Continue the burn-down (owner: "continue").

Same parallel workflow, next 26 top files — ALL to 0 no-explicit-any (412 removed): slideRenderer.ts 39→0 (typed as `PptxGenJS & {_deckPalette?}` intersection — the augmentation batch-1 deliberately skipped), the route-gate integration tests (mock builders + CallerOrgContext + NextRequest casts), TextMineModule, DownloadMonitor, dataforseo, the dashboard pages, deck/bot/admin routes. Consolidation caught one cross-file conflict: dashboard/page.tsx typed study.config minimally (`{experienceRatingLabel?}`) while DashboardClient reads config.theme/industry → typed both as `StudyConfig | null`. Verified: global tsc 0 errors, 1238 tests pass, warnings 2194→1782 (−412). Ceiling → 1782. Not pushed.

## Fix: service_health missing in prod — apply skipped migration 126 (Jul 6)

WHY: Sentry fired `PGRST205 — Could not find the table 'public.service_health'` on `GET /api/cron/service-balance` (every 6h). Root cause: sql/126 (vendor-credit monitor) shipped in code and was recorded in the migration ledger by the 2026-07-03 bulk **backfill** (`applied_by:"backfill"`) — but the DDL was never actually run against prod, so the table didn't exist (nothing dropped it; the backfill just assumed it applied). Impact was ops-only (no customer/data/security impact): the credit-alert monitor was blind + a recurring Sentry alert. Fix: `npm run migrate sql/126_service_health.sql` (idempotent, CREATE TABLE IF NOT EXISTS) — table now queryable in prod (0 rows; cron repopulates next run). Schema snapshot refreshed. sql/126 left byte-identical (its ledger sha must stay stable; its "already applied" comment is now true). Snapshot commit isolated from an in-flight parallel eslint burn-down in the working tree. No push.

## ESLint burn-down wave 4 — 26 more files (Jul 6)

WHY: Continue the burn-down.

Same workflow, 26 more files (−276): townhall analyze/resume/join/export routes, datasets/aggregate, ask-ana, agentStudy, entityFilter/Discovery, orgTransfer, outletReport, projectReportLoad, more route-gate tests, AskAnaPanel, recordings/m pages, admin simulator. Fixed one self-contained cast the isolated agent's tsc missed (townhall/join `projectHallAsSession(hall) as unknown as ProjectedSession`). Before running: verified the tree was clean + confirmed the ~30 session-start "dirty" files were the already-committed AI-cost sweep (064a39ed, ancestor) — no uncommitted work at risk. Global tsc 0 errors, 1238 tests pass, warnings 1782→1506. Ceiling → 1506. Not pushed.

## ESLint burn-down wave 5 — 30 files (Jul 7)

WHY: Continue to zero.

30 files (−236): pulseiq/bots/favorites clients, share/recordings/datasets/entities routes, more route-gate + unit tests, uiHints, studyDraft, creator steps, deploy/glossary pages, etc. Reconciled one cross-file conflict: admin/downloads/page.tsx enriched DB rows (Record<string,unknown>) → DownloadMonitor's typed props; exported the 4 prop interfaces and cast at the boundary (`as unknown as RedditSource[]` etc — DB-to-component boundary, no `any`). Global tsc 0 errors, 1238 tests pass, warnings 1506→1270. Ceiling → 1270. Not pushed.

## ESLint burn-down wave 6 — 30 files (Jul 7)

WHY: Continue to zero.

30 files (−171): lib/ai, agentReadout, reviewSync, reddit, brandRules, serviceHealth, townhallAnalytics; settings/dataset/bot clients; review-sources/favorites/entities/compute/bots routes; unit + integration tests. Reconciled one cross-file conflict: townhallAnalytics now types computeSessionAnalytics `themes: LegacyThemeRow[]` (indexed) vs townHallAdapter's LegacyTheme (no index sig) → boundary `as unknown as LegacyThemeRow[]`. Global tsc 0 errors, 1238 tests pass, 1270→1099. Ceiling → 1099. Not pushed.

## ESLint burn-down wave 7 — 32 files (Jul 7)

WHY: Continue to zero.

32 files (−147): townhall/chat (frozen legacy — annotation-only, allowed), studies/reviews/recordings/deck/datasets/bots/campaigns routes, usageLog, taxonomyExtractor, phase3DualWrite, entityMentionDetector, analyticsCompute, wizards. No cross-file conflicts in committed code (one incidental break in the untracked scripts/_llm-tier-prototype.ts fixed locally, gitignored/not in CI). Global tsc 0 errors, 1238 tests pass, 1099→952. Ceiling → 952. Not pushed.
