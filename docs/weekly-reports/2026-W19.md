# Weekly Governance Report — 2026-W19 (Week of May 4 to May 10)

**Generated**: 2026-05-11T04:00:00Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W19-devlog.md

## 1. Development Summary

**Quantitative**: 71 commits (7-day window May 4–11; primary week May 4–10), 2 authors (Sanjay Patel, Claude Weekly Audit), top-level dirs: app/, lib/, components/, sql/, tests/, docs/, .claude/

### Narrative (from devlog)

**Security sprint — heaviest area this week**
- 8-sprint full-codebase security sweep ahead of first customer: cross-tenant route gating, auth hardening, account-takeover mitigations, webhook signing, iframe defenses, CSRF middleware, RLS migration, requireAdmin sweep, SSRF + PostgREST hardening, cron auth, mass-assignment guards, XSS in formatHtml, magic-link strict-check, function `search_path` pinning, rate-limit buckets, xlsx swap, security headers, CORS
- Fixed CRITICAL XSS in `useSurveyEngine.ts` (6 `innerHTML` sites) via DOMPurify (`isomorphic-dompurify`)
- Fixed attribute-injection XSS in `public/clara-widget.js` / `nora-widget.js`
- Closed 6 cross-tenant leaks across 4 campaign routes (`export`, `respondents`, `send`, `clone`) — all used service-role with bare `id` lookup, no `org_id` check; new campaign-routes egress test locks the fix
- Fixed magic-link email-enumeration vector: `/api/auth/magic-link` always returns `{ ok: true }` regardless of Supabase outcome
- Fixed `public.users.id → auth.users.id` FK (no FK existed; migration `sql/046_users_auth_fk.sql` applied to prod)
- Bumped `eslint-config-next` 14.2.5 → 15.5.18, closing 6 of 8 npm audit findings; fixed 3 `react-hooks/rules-of-hooks` bugs surfaced by lint

**Test infrastructure — foundational additions**
- Cross-org data egress test (`tests/integration/cross-org-egress.test.ts`, env-gated): seeds Org A rows, signs in as Org B, asserts no read leak — 27 tests, all green against linked Supabase
- Auth-flows integration test (`tests/integration/auth-flows.test.ts`, env-gated): real Supabase round-trips — sign-in, password reset, OTP, invite flow, sign-out; switched test emails to Gmail `+suffix` aliasing to stop NXDOMAIN bounces
- High-traffic-routes integration test (`tests/integration/high-traffic-routes.test.ts`, always-on, mocked): 22 tests covering most-trafficked public endpoints
- Campaign-routes egress test (env-gated): validates handler-level `org_id` gate since service-role bypasses RLS
- Strategy: prod-linked + prefix-namespaced cleanup for real-Supabase tests; mocks for pure validation logic

**AI governance infrastructure**
- Weekly governance audit routine stood up: `/audit-codebase`, `/security-check`, `/security-audit` slash commands; routine runs Monday, opens a PR for human review — the merge is the SOC 2 / NIST AI RMF / EU AI Act evidence artifact
- Added repo-root `CLAUDE.md` (71 lines) with multi-tenancy invariants, naming conventions, data model anchors, content rules
- Session-start hook and stop-queue-prompt hook committed to `.claude/hooks/`; `.claude/settings.json` now shared in repo

**Product / infrastructure**
- Admin Phase E: cross-org read/edit/delete for all major entities, standardized transfer with audit log; org filter on /bots, /analyze, /townhall
- Bot share links: rich Open Graph unfurl; bot editor TopNav consistency, sticky save/cancel, dirty gate; legacy bots migrated to dynamic pipeline
- Auth: cookie-only auth (Supabase bearer-token rate-limit mitigation); password reset routed through `/auth/callback`
- Performance: collapsed N+1 queries, memoized TextMine chain, lazy-loaded TextMine modal, ISR on `/b/<slug>`, session-count dedup in Postgres
- `dataset_rows_flat` sole source of truth; dual-write to `dataset_rows` dropped
- Full-text search + AI re-ranking layer in TextMine; search modal in dataset header
- Town Hall: prompt-cached static prompts, per-`participant_id` rate limit, k6 + Playwright load simulator
- Governance Reports link added to gear menu (desktop + mobile, admin-only)

### Risk signals
- **38 bare service-role `.eq('id', ...)` calls without `org_id` pairing** across ~20 files remain — exact class of bug responsible for the 6 CRITICAL campaign fixes; not all are exploitable but none have been individually reviewed
- 2 npm CVEs unresolved (1 high, 1 moderate) after this week's bump
- 71 commits in 7 days, single primary author; changes touch auth, RLS, CSRF, route access control — human review at each PR merge is the only control

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 8/10 | 15% | 1.20 |
| Security | 5/10 | 15% | 0.75 |
| Dependencies | 7/10 | 15% | 1.05 |
| Structure | 7/10 | 10% | 0.70 |
| Tests | 4/10 | 15% | 0.60 |
| Documentation | 8/10 | 10% | 0.80 |
| Maintainability | 7/10 | 20% | 1.40 |
| **Total** | | **100%** | **65.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- **[HIGH] Bare service-role `id`-only lookups in ~20 route files (38 instances)** — `service.…eq('id', x)` without a paired `org_id` guard. CLAUDE.md identifies this as the source of every prior CRITICAL finding. Confirmed in: `app/api/bots/[id]/route.ts:67,89,114`, `app/api/bots/[id]/chat/route.ts:603`, `app/api/share/analytics/route.ts:147–157`, `app/api/share/route.ts:300`, `app/api/social/comments/bulk/route.ts:55,61`, `app/api/regulations-sources/download-comments/route.ts:60–133`, and ~14 additional files. Admin/cron routes may be intentionally cross-tenant but must be individually confirmed.
- **[HIGH] 5 `/api/admin/*` routes missing `requireAdmin`**: `admin/orgs/[id]/route.ts`, `admin/orgs/[id]/users/route.ts`, `admin/questions/[id]/route.ts`, `admin/agent-tester/route.ts`, `admin/questions/route.ts`. All verify login (`getAuthUser`) but do not assert the caller is from an admin org. Per CLAUDE.md: "URL obscurity is not a defense."

**MEDIUM**
- **npm audit: 1 high + 1 moderate CVE unresolved** — `eslint-config-next` bump closed 6 of 8 prior findings; 2 remain (likely upstream transitive deps requiring ESLint 9 for the 16.x bump). Run `npm audit` for current package names.
- **Test ratio 0.04** (18 test files / 440 source files) — significant improvement over last week's 0.025 but well below 0.5 target. Most route handlers and components still lack unit tests.
- **TypeScript tsc errors (5) in vitest config and test files**: `Cannot find module 'vitest'`, `vitest/config'`, `@vitejs/plugin-react'`; `Cannot find name '__dirname'` (vitest.config.ts:1–9, tests/unit/usageLog.test.ts:1). Root tsconfig likely needs vitest test files excluded or `@types/node` scoped.

**LOW / informational**
- `*.pem`, `*.key`, `*.p12` not in `.gitignore` — no such files found in repo, but the gap exists
- No `.claude/rules`, `.claude/agents`, or `.claude/skills` defined — next AI governance maturity tier
- `execSync` in `next.config.js:6` is build-time only (`git rev-list --count HEAD`); not a runtime risk but appears in automated scans
- `tsconfig.tsbuildinfo` is committed; it is a build artifact that bloats diffs

### Progression plan (top 3 to fix this week)

1. **Audit all bare service-role `id` lookups across ~20 files** — for each instance, determine: admin/cron route (intentionally cross-tenant, document it) or user-facing route (add `org_id` gate or use a `gate*Access` helper). This closes the highest-risk open class. Security finding, CRITICAL pattern.
2. **Add `requireAdmin` to the 5 admin routes** — `admin/orgs/[id]`, `admin/orgs/[id]/users`, `admin/questions/[id]`, `admin/agent-tester`, `admin/questions`. One-line change each using the existing `requireAdmin` helper. Security finding, easy fix.
3. **Fix tsc vitest errors + add `*.pem/*.key/*.p12` to `.gitignore`** — exclude vitest config from root tsconfig or install `@types/node` for the test tsconfig; add three gitignore patterns. Quick wins on Structure and Secrets.

## 3. Correlation: dev work → audit findings

- **[HIGH] Bare service-role `id`-only lookups (~20 files, 38 instances)** — partially addressed: campaign routes (4 files, 6 leaks) explicitly fixed and tested; the broader set of 38 bare-id instances in other files was not mentioned in devlog.
- **[HIGH] 5 admin routes missing `requireAdmin`** — not mentioned in devlog.
- **[MEDIUM] npm CVEs (1 high, 1 moderate)** — partially addressed: `eslint-config-next` bump explicitly targeted the npm audit; devlog notes 2 findings could not be closed without the ESLint 9 upgrade.
- **[MEDIUM] Test ratio 0.04** — partially addressed: 3 new integration suites added; ratio improved but still low; component and route-handler unit tests not mentioned.
- **[MEDIUM] tsc vitest errors** — not mentioned in devlog.

## 4. Trend

Preliminary W19 report (generated 2026-05-09, mid-week baseline): **55.0 / 100**

This end-of-week report: **65.0 / 100** — **+10.0 points**

Score delta driven by: XSS fixed (+Security), CVEs reduced from 8 to 2 (+Dependencies), campaign cross-tenant leaks fixed (+Security), CLAUDE.md added (+Documentation, +Maintainability), session-start hook and governance routine wired (+Maintainability), 3 new test suites (+Tests). Security remains the primary drag (5/10) because the 38 bare service-role id lookups represent a systemic unresolved risk class despite targeted campaign-route fixes.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Check correlation section — are open findings being worked on?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine. Requires human review before being recorded.*
