# Weekly Governance Report — 2026-W19 (Week of May 04 to May 10)

**Generated**: 2026-05-10T00:00:00Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)

## 1. Development Summary

- **Commits**: 49 (non-merge)
- **Authors**: Sanjay Patel (49)
- **Areas changed**: app/ (397 file-touches), components/ (92), lib/ (79), sql/ (53), docs/ (31), tests/ (20), supabase/ (12), public/ (10), .claude/ (4)

### Notable changes

- **Security sprint × 8** (pre-customer hardening): cross-tenant route gating, account-takeover hardening, webhook signing, iframe sandboxing, CSRF middleware, RLS migrations, requireAdmin sweep, SSRF guard, PostgREST `.or()` hardening, predictable-ID fix, mass-assignment guard, formatHtml XSS fix, secrets-in-headers removal, server-only isolation, materialized-view RLS, magic-link strict-check, DB function `search_path` hardening, rate-limit buckets, xlsx library swap, security headers, CORS annotation, final RLS tests.
- **Deleted orphaned phishing-relay route** `/api/%20notify/closed-study`.
- **Governance infrastructure**: `docs/weekly-reports/` directory, README, and three periodic audit slash commands (`/audit-codebase`, `/security-check`, `/security-audit`) added.
- **Full-codebase security review report** committed (`docs/2026-05-09-security-review.md`).
- **Foundational test suite + CI** added: unit, integration, and e2e tests covering requireAdmin, guardrails, rate-limit, usage-log, persona-extractor, RLS isolation, deck download.
- **Town Hall**: rate-limit per `participant_id` + IP backstop; prompt-cache static system prompts; k6 + Playwright load simulator.
- **Admin Decks**: per-deck Last Updated timestamps, auto-refresh after download, time-of-day in metadata.
- **Login redesign**: email-wordmark brand, Datanautix parent attribution, support address corrections (four rapid iterations).
- **Bot/Dataset pipeline**: schema-stat merges on every analyze sync and Regulations.gov batch; categorical filter include/exclude mode; Google Reviews download banner with pre-flight estimate; schema refresh cascades to collection members.
- **Retire staging references** across the repo.

### Risk signals

- **Single-author velocity**: 49 commits in 7 days, all from one author — no independent reviewer visible in commit history. Changes touch auth, RLS, CSRF, rate-limiting, and route access control.
- **Rapid auth churn**: login footer went through 4 commits in quick succession (fabricated email removed, then replaced twice) — suggests rushed production-facing copy.
- **innerHTML without sanitization remains** in `components/survey/useSurveyEngine.ts` (lines 247, 475, 706, 1487, 2379, 2448, and more). Sprint 3 fixed `formatHtml` for the admin chat; the survey engine's direct DOM writes on user/API-sourced content were not fully addressed.
- **8 high-severity CVEs** in npm dependencies including a production Next.js DoS vector.
- **Large blast radius**: security sprints modified auth, DB, and API layers simultaneously without granular separation — hard to audit individual changes.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 8/10 | 15% | 1.20 |
| Security | 6/10 | 15% | 0.90 |
| Dependencies | 5/10 | 15% | 0.75 |
| Structure | 7/10 | 10% | 0.70 |
| Tests | 3/10 | 15% | 0.45 |
| Documentation | 7/10 | 10% | 0.70 |
| Maintainability | 4/10 | 20% | 0.80 |
| **Total** | | **100%** | **55.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**

- **XSS: direct innerHTML on API/user-sourced content** — `components/survey/useSurveyEngine.ts:247` sets `bub.innerHTML = text` where `text` comes from the AI API response; line 475 injects `data.deflection` (API string); lines 1487, 2379, 2448 inject label strings from survey config. If any of these values contain attacker-controlled HTML, users of the survey embed are vulnerable. The `formatHtml` fix in sprint 3 covers only the admin ChatBot component.
- **8 high-severity npm CVEs**: `next` (DoS via Image Optimizer remotePatterns), `glob` (CLI command injection via `-c`/`--cmd`), `minimatch` (ReDoS via repeated wildcards), `fast-xml-builder` (attribute-value injection). The Next.js and minimatch CVEs are production-relevant; `glob` applies to build scripts. Run `npm audit fix` or pin safe versions.

**MEDIUM**

- **`.gitignore` missing `*.pem`, `*.key`, `*.p12`** — private keys or certificates accidentally created in the working directory would not be blocked from commit.
- **No `.env.example`** — onboarding contributors or auditors have no reference for required environment variables.
- **Auth churn with no external review marker**: sprints 1–8 all carry `|Sanjay Patel|` with no co-author or review tag. For code touching cross-tenant gating and CSRF, a second pair of eyes is strongly recommended before the first customer goes live.
- **`exec` in `app/api/bots/deep-crawl/route.ts:61`** uses `RegExp.exec()` on HTML (not `child_process.exec` — low severity, but the grep pattern flags it for manual confirmation).

**LOW / informational**

- **`import * as` wildcard imports** are limited to Sentry (`global-error.tsx`, `sentry.*.config.ts`) and XLSX (`lib/xlsxExport.ts`) — acceptable for these libraries, not a pattern issue.
- **Test ratio 0.025** (11 test files / 433 source files): the foundational suite added this week covers critical auth and RLS paths, but UI components, API route business logic, and data-pipeline code have zero automated coverage.
- **No `CLAUDE.md`** and no `.claude/settings.json` — Claude Code has no project context or permission configuration. Three custom audit commands exist but there are no rules, agents, or hooks defined.
- **`tsconfig.tsbuildinfo` committed** — this is a build artefact; it bloats PRs and diffs. Add to `.gitignore`.
- **`supabase/.temp/` committed** — temp files (pooler-url, project-ref, cli-latest, version pins) are environment-specific and should be gitignored.

### Progression plan (top 3 to fix this week)

1. **Sanitize all innerHTML calls in `useSurveyEngine.ts`** — use DOMPurify or a strict allow-list before assigning any API/config string to `innerHTML`. This is a customer-facing XSS risk; the survey embed runs in third-party sites.
2. **Resolve the 3 production-relevant high CVEs** (`next`, `minimatch`, `glob`) — run `npm audit fix` and manually review breaking changes; pin `next` to a patched version if auto-fix is not available.
3. **Create `CLAUDE.md`** with project conventions, directory map, and env-var reference, and add `.claude/settings.json` with permissions and at least a pre-tool hook for lint — this unlocks the full value of the Claude Code governance infrastructure already scaffolded.

## 3. Trend

First report — baseline established at **55.0 / 100**.

## 4. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine. Requires human review before being recorded.*
