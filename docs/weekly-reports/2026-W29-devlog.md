# 2026-W29 devlog (Jul 13 – Jul 19)

Brief WHY entries for meaningful commits/ops this week. The Monday governance routine reads this.

## Ops: prod "pathetically slow" diagnosed → 785K [SCALE TEST] deleted from prod (Jul 13, no code)

WHY: Owner at ~1am ET: schema hangs, menu switches slow, System Health takes forever — "did breadcrumbs break it?" Diagnosis (all read-only): breadcrumbs innocent (pure link chrome). The DB was I/O bound: `dataset_rows_flat` was **3.56GB = 97% of the whole database** (785,638 of 1,057,110 rows = the [SCALE TEST] dataset) on a ~1GB Micro instance (shared_buffers 256MB) → cache hit ratio 84.6% (healthy ≥99%), so every page's auth/org lookups paid disk I/O; 7/12's heavy day (sql/165 index build scanning the full heap + pre-index classify PK walks + vacuum) additionally drained the Supabase burst-I/O budget. Owner approved the queued deletion: 785,638 rows removed in 16×50K `id IN` batches on idx_drf_id_keyset (mirroring the app's delete path; review_sources/collections/entities confirmed 0 first), then dataset_state + datasets row; ANALYZE run, autovacuum picked up the dead tuples. Table now 273,795 live rows; settings-page queries measured 0.05ms post-delete. Side finding: the nightly org-snapshot cron had been hitting Vercel's 300s timeout paging the bloated table — should self-resolve.

Residual "schema does not come up" after the delete = **stale browser tab**: the owner's tab predated the 23:21 deploy (which changed ~60 pages' chunks); server 200'd every click while the browser never painted. Hard refresh fixed it. Lesson for support: after a deploy, a tab left open overnight can navigate into missing-chunk territory — hard refresh first before diagnosing.

NEW TODO (owner-reported): Filters modal's filterable-value list is sample-derived and misses values on high-cardinality fields (location) — fix is an exact distinct-values pass for filterable fields. Interim workaround already shipped: Schema tab → "↻ Refresh from data". Tracked in the open-work queue.

## e2e smoke suite in CI — and it caught a live infinite render loop on day one (Jul 13)

WHY: Owner: "yes, start" on the governance Tests-score plan (e2e at 0 tests — the legacy specs need E2E_ADMIN_* human creds so they skip everywhere, including CI). New `tests/e2e/smoke.spec.ts` is fully self-contained: global setup mints a throwaway TEST-project login (e2e-smoke@sentimetrx.test, fresh password per run, storageState cookie — one sign-in, no rate-limit churn, hard-refuses the prod ref) and seeds its own 60-row dataset; teardown cleans it. New `e2e` CI job runs it against a PRODUCTION build (clean-worktree simulated locally first: npm ci with only the 3 TEST env vars + vendored xlsx → build green → 4/4 in 9s); the deploy job now gates on it. Covers: authed shell/breadcrumbs, /analyze listing, the dataset tab dance INCLUDING return visits (the "Schema comes up once then never again" class — server-side tests can't see it), Schema editor + Refresh-from-data, Filters modal.

FOUND ON FIRST RUN: **the Statistics tab has been running an infinite React update loop** — "Maximum update depth exceeded", main thread pegged, tab navigation starved for 20s+ (CDP stack: StatsModule's bootstrap-CI effect). Root cause: `themeSetForField()` returns a FRESH object every call (stripFieldEntries spreads — even for the top-level match, i.e. on EVERY dataset with themes), and Stats/Charts called it unmemoized per render → enrichedData memo invalidated every render → effect refire → setMcResults(new object) → render → loop. This is very plausibly a big contributor to the owner's "Statistics → Schema hangs" reports beyond the SW + classify-stomp fixes. Fix: memoize `effectiveThemeModel` in StatsModule + ChartsModule ([themeModel, themeSourceField]). Post-fix the smoke tab-dance dropped from timeout to 3.2s on a prod build. jsdom could NOT reproduce (needs real canvas/plotly timing) — exactly why a browser-level suite earns its 15% audit weight. 1350 tests, clean-cache tsc, lint delta 0 (e2e files clean).

## W29 audit MEDIUMs cleared: bots org_id pairing + vendored xlsx (Jul 13)

WHY: Owner: "fix both" (the two carried-from-W28 MEDIUM findings in the W29 governance report). (1) `app/api/bots/[id]/route.ts` — the PATCH snapshot reads (before/after the update) now org-pair at the query for non-admins (cross-org id returns no row instead of fetching another tenant's row and discarding it post-check); the explicit org-mismatch 404 stays as a second layer; behavior improvement: a non-admin PATCHing a nonexistent id now 404s (previously fell through to a 0-row update that reported success) — new gate test. The transfer-branch read stays deliberately unpaired (admin-only, exists to discover the agent's current org) and now says so — the invariant's documented admin bypass. (2) `xlsx` no longer installs from cdn.sheetjs.com at install time (403'd clean checkouts, including the governance runner): the official 0.20.3 tarball is vendored at `vendor/xlsx-0.20.3.tgz` — **integrity verified byte-identical to the lockfile's sha512** — and package.json references it via `file:`. npm-registry xlsx is frozen at 0.18.5 with unfixed CVEs, so the audit's literal suggestion (registry 0.20.3) was impossible; vendoring keeps the CVE-clean version with zero install-time network. README note updated. 1350 tests (1 new), clean-cache tsc, lint clean.

## Spec-drift cure: 4 specs noted for the breadcrumbs sweep (Jul 13)

WHY: The W29 governance run flagged SOCIAL/CAMPAIGNS/SURVEYS/RECORDINGS as drifted — all from the breadcrumbs commit (`726ae309`, SKIP_SPEC_CHECK'd as "nav chrome only"). The owner had asked the night before whether specs were audit-ready and got a "yes" based on the 00:16 drift run's verdict instead of a fresh full-week tool run — the exact miss the sweep-commits rule exists for. Cured with a short nav-chrome note in each spec's UI section; verified by re-running scripts/spec-drift.ts over the audit's range (0 drifted).

## Theme mining: corpus validation + flexible phrase matching (Jul 13)

WHY: Owner on Carrabba's GSS "Liked Least": "this theme model seems a poor fit — the logic of theme creation and saving it to the right verbatims seems flawed." Confirmed and quantified with the real matcher on 3K prod verbatims: the mining AI estimated 28/25/16/13/12% prevalence per theme but the keywords it invented matched 7/3/2/2/0.3% — 63 of 105 keywords matched ≤1 comment, because the AI emits paraphrase-phrases ("food disappointing", "cold when served") and the matcher required exact contiguous phrases. Nothing ever compared the AI's own estimate against what its keywords actually match, so "Diffuse 12%" shipped silently. Two-part fix: (1) `lib/themeUtils.kwPatternFragment` — ONE canonical POSIX-safe pattern per keyword used by the JS matcher AND passed into all four SQL counting RPCs (count_theme_matches / theme_dimension_counts / cooccurrence take fragments unescaped; sampled_signal_counts gains a `patterns` path in sql/166 with legacy fallback); multi-word phrases now match in order with ≤4 intervening words. (2) `lib/themeMining.ts` + mine-themes route: prompt demands corpus-literal keywords; the route scans the sample with the real matcher, feeds unmatched responses back once for themes under 60% of their estimate, prunes zero-hit keywords, and restates count/percentage from the scan (one method everywhere). Verified end-to-end offline with real AI calls on the 3K prod verbatims (`scripts/_verify_theme_mining.mts`, untracked KEEP): fit 17.9% → 60.3%, per-theme stored percentages now within ~2pts of measured coverage. sql/166 applied to TEST; PROD MIGRATE PENDING (`npm run migrate sql/166_sampled_counts_prebuilt_patterns.sql` with the next push — safe either order via the fallback). 1349 tests (12 new), tsc clean, lint delta 0. Known edge documented: symbol-leading keywords ("$10 deal") still dead in \m-anchored RPCs (pre-existing).

## Fix: service worker no-op fetch handler wedged soft navigation (Jul 13)

WHY: Owner (after the scale-test delete): "Schema comes up the first time after a hard refresh, then when I leave it I can't come back." Prod logs: every Schema click SSR'd 200 (3 in 2s), zero background traffic, browser never painted — and a hard refresh always cured it. Local Playwright repro (throwaway QC user, TEST) could NOT reproduce once dev compiles were warm (2.1s navigation, idle main thread) — the local browser had no service worker. Root cause candidate with every symptom accounted for: public/sw.js (registered from /m with scope "/", so it controls the whole origin in any browser that ever opened the status page) had a NO-OP fetch listener — pure request-path overhead by its own comments — combined with skipWaiting+clients.claim, which swaps workers over live tabs on every deploy; soft-nav RSC responses then 200 on the server but never reach React, while hard refresh (the one navigation that bypasses a SW) works. Fix: fetch listener removed (v2) — browsers skip no-fetch-handler workers entirely; push + installability (the worker's only jobs) are unaffected; already-installed workers self-update on their next check. Same commit: autoTagEmotion in TextMine re-checks classifyAlive after its tail awaits (rollup GET + PATCH ran AFTER the aliveness check, leaving a seconds-wide window to fire router.refresh() from a dead component — the 513ebb3b class, swept properly this time). tsc clean, 1337 tests.

## npm audit → 0 vulnerabilities (Jul 13)

WHY: The W29 report carried "7 moderate CVEs requiring a breaking @sentry/nextjs bump" — stale by audit time: the graph had moved to 4 moderates in 2 chains, and neither needed the Sentry major. (1) postcss <8.5.10 (XSS in stringify output) nested inside next@16.2.10 (pins 8.4.31; no stable Next fix exists — 16.3 is still preview): fixed with a global `postcss` override pinned via `$postcss` to the direct devDep, now `^8.5.10` → the nested copy dedupes away, one postcss@8.5.15 everywhere (patch line — build-safe). (2) uuid <11.1.1 (buffer bounds) inside exceljs@4.4.0 (latest): scoped override `exceljs.uuid ^11.1.1` — uuid 11 still ships CJS so exceljs's require works; the vulnerable v3/v5/v6+buf path isn't even exceljs's usage (v4 only), belt-and-suspenders. Verified: clean-cache tsc, 1350 tests, exceljs writeBuffer smoke on uuid 11, clean-worktree npm ci + production build, e2e smoke 4/4 vs that build. `npm audit`: 0 vulnerabilities.

## Deck PDF fonts embedded — no render-time Google Fonts (Jul 13)

WHY: Governance LOW carried since W27: the three deck HTML templates (pitch v2/v3, community feedback) <link>'d Fraunces + DM Sans from fonts.googleapis.com at render time inside headless Chromium — unreachable fonts silently rendered decks in fallback serif. New lib/decks/embeddedFonts.ts inlines the latin-subset VARIABLE woff2s (DM Sans wght 100-1000 ~37KB, Fraunces opsz/wght ~67KB) as data-URI @font-face; templates swap the <link>s for the embedded <style>. Zero network at render, byte-stable decks. Verified in Blink with fonts.googleapis/gstatic hard-blocked: document.fonts.check true for both families + pixel QC of slide 1 (Fraunces 900 headline, DM Sans body). tsc clean.

## no-explicit-any burned to 0 → rule promoted to error (Jul 13)

WHY: The last governance-progression lever from the audit sweep. The ~83 "deliberate" residuals (recounted: 77 across 22 files) were left by the 7/07 bulk waves as too risky for mechanical annotation — this final wave used the same worktree harness (one agent per file, tsc-verified in isolation) but with derive-don't-guess instructions: every `any` got a real type read off its producers/call-sites (ChartsModule 28 — AggResult/ChartLayout/ThemeModel; StatsModule 12 — a GroupTestResult discriminated union over the statsUtils result interfaces; chatCore's `agent`/`body` — a ChatAgent interface derived from every bot.* access; studyDesignPptx 10 — StudyConfig shapes; plotly shim typed). Exactly ONE scoped justified disable survives (townHallAdapter's legacy payload return, pinned by a stale verify script's field reads). Consolidation: global fresh tsc clean, zero cross-file conflicts, 1349 tests green (the one full-suite timeout on high-traffic-routes "400 on invalid JSON" reproduces identically on HEAD without this diff — pre-existing local load flake, passes in isolation). Rule promoted to `error` in eslint.config.mjs — a new `any` now fails CI outright; ceiling 346 → 269 (all remaining are react-hooks/* + misc). Annotation-only, zero runtime change.

## Architectural performance review — bottleneck map published (Jul 13)

WHY: Owner mandate (7/13): where does the platform break at multi-M total rows, 1M+-row datasets, concurrent users, and QR-TV survey bursts? Method: read-only prod snapshot + a 1,028,952-row dataset seeded on TEST (Outback ×8) with every dataset surface's DB path timed over PostgREST (the real path, 8s statement timeout) + a 41-route code audit. Deliverable: docs/PERFORMANCE_REVIEW.md — a 10-item scored bottleneck map. Headlines: (1) every exact/full-scan RPC 57014s at 1.03M — the whole /aggregate route (all Charts/Stats server ops), filter-options (per-field serial scans incl. two full date sorts), Ask Ana's ORDER BY random() fetch, count_nonempty/count_field_values/numeric_field_stats/count_theme_matches — while all keyset/sampled/stored paths stay 0.1–0.7s; (2) the 50K sample load is SAFE-but-heavy at 1M (44–56s, pages 5–6.4s — near the 8s cliff on cold cache); (3) whole-DB wall = working set vs Micro's 1GB RAM (the 7/13 incident generalized: ~200K hot rows/GB), with a tier ladder recommendation (pg_repack the ~2GB post-delete bloat free now; Medium $60 at first real 500K dataset; Large $110 if 1M+ datasets are sold); (4) prod concurrency choke is the PostgREST backend pool (~5 conns), not Supavisor — app is REST-only; (5) QR-burst breaks first on venue-NAT rate keying (respond 120/min/IP = ~2 saves/s/room; clarify 10/min/IP silently stops follow-ups for the whole room) and findStudy runs twice per /s/[guid] view (no React.cache dedup); (6) study_response_stats MV refresh is O(all responses ever) per 30s during bursts — counter-table fix named. Ask Ana filter compliance (owner question, same day): filters ARE applied but only to a filter-blind ~600-row prefetch — selective filters starve the sample and the prompt's "N of M" misstates the denominator; fix = filtered sampling in SQL. No code changed — review only; fixes are queued items. Harness scripts/_perf_measure_1m.ts untracked KEEP; [PERF TEST] dataset kept on TEST for owner UI feel, delete when done.

## Perf-fix implementation briefs + model-tier split (Jul 13)

WHY: Owner wants the perf-review fix queue executable by non-Fable sessions without them re-deriving the review ("so I'm not a weak link"). PERFORMANCE_REVIEW.md §7 now carries five self-contained briefs (pg_repack runbook; filter-options server+client halves incl. the FiltersModal.tsx:209 sample-derived value-list bug; /aggregate sampled twins w/ scaling + labeling rules; QR-burst rate-keying + findStudy React.cache; long-tail retirements incl. the MV→counter-table migration) plus global rules: sampled-vs-exact verification bar on 56K/128K + no-57014 on the 1M PERF TEST dataset, PGRST202 deploy-order fallbacks, D6 "~" labeling, TEST-first SQL. Tier split recorded in memory: Fable only for Ask Ana filtered sampling (in progress) and the org-snapshot cursor; all briefs = Opus.

## Ask Ana: filter-aware deterministic sampling (Jul 13, sql/167)

WHY: Perf-review fix-queue #8 (the Fable-designated item) + the owner's direct question "does Ask Ana comply with filters rather than sampling the full dataset?". Old pipeline: ORDER BY random() full-partition sort (57014 at 1M → "No rows found" on big datasets) fetching ~600 filter-BLIND rows, filtered in Node afterwards — selective filters starved the sample and the AI was told a survivors-of-600 denominator as if dataset-wide. Worse, verification surfaced two silent bugs in the inline Node filter: cat exclude-mode was treated as INCLUDE (inverted results for anyone filtering "not X") and daterange filters were ignored entirely. New: sql/167 sampled_filtered_rows keyset-pages the idx_drf_sample deterministic order with the serialized filters compiled in SQL (ana_row_matches_filters mirrors applyFilters — include/exclude, blanks, parseFloat-with-sci-notation numerics [real Qualtrics exports store IDs as "1.19152E+11"; the first regex missed it and the verify harness caught the Δ], daterange epoch-ms); ≤50K datasets scan to the end for EXACT denominators (reconciles with every exact surface), >50K early-stops with an unbiased "~" estimate; small datasets filter in Node via the CANONICAL applyFilters; PGRST202 → legacy fallback (deploy-order safe). Both AI prompt notes rewritten to one honest denominator story. Verified: scripts/_verify_ana_filters.mts (untracked KEEP) — 6 filter shapes JS↔SQL exact-equal over the Outback 50K sample, 1.03M filtered ask 4.1s w/ ~356K/1,028,952 flagged estimate, 14K exact 13,318 = full-scan count; 5 new unit tests (1355 total); fresh tsc clean; lint delta 0. sql/167 TEST-applied; PROD MIGRATE ON PUSH (with sql/166).

## Brief F (filters-compliance sweep) + audit registry (Jul 13)

WHY: (1) The Ask Ana fix found 2 silent filter bugs in ONE consumer → sweep-the-class: PERFORMANCE_REVIEW.md §7 Brief F scopes an Opus-runnable sweep of every SerializedFilters/applyFilters consumer + the filters-silently-ignored class (aggregate/signal-stats/decks), with classify-a/b/c/d verdicts, a results table appended to the brief, and Fable-escalation rules (new SQL semantics or unclear product behavior = report, don't guess). Quick scope check: export/html + export/pptx already use canonical applyFilters. (2) Owner: "are we documenting audits so we don't repeat them?" → new docs/AUDITS.md registry — 13 audits with scope, findings location, and re-run triggers (perf review, route classification, capacity/k6, 7/04 efficiency, first-open, security org-pairing, weekly governance, spec-drift, npm audit, respondent chrome, breadcrumb inventory, audio-QA); CLAUDE.md docs list now points at it with a check-before-scoping + register-in-same-commit rule. The fresh-run rule for "audit-ready?" questions still stands — the registry prevents duplicate scoping, not stale answers.

## Brief A: pg_repack bloat reclaim via VACUUM FULL (Jul 13, prod op — no code)

WHY: Perf-review §7 Brief A — the 7/13 785K-row `[SCALE TEST]` delete left ~2 GB dead extent in `dataset_rows_flat` (heap 2,332 MB for 273,814 live rows; total 3,029 MB). Owner confirmed a quiet downtime window and chose VACUUM FULL over the pg_repack CLI (simpler, no extension/permission risk on Supabase; exclusive table lock for the rewrite). Ran `VACUUM (FULL, ANALYZE) public.dataset_rows_flat` via `supabase db query --linked`. Result: total 3,029 MB → **631 MB** (heap 2,332→444, indexes 697→187 — the rewrite rebuilt indexes too), ~2.4 GB returned to the OS (better than the brief's ~1.1 GB estimate); live rows unchanged 273,814; ANALYZE refreshed planner stats. This directly relieves the §1 whole-DB-working-set pressure on Micro's 1 GB RAM (the hot working set now fits far more comfortably). One-off infra op — NOT a ledgered migration (VACUUM FULL is not replayable schema), so no sql/ file. Re-sample the §1 heap-hit delta on an active usage day to confirm ≥99% cache health.

## Brief B: filter-options single-pass sampled RPC + missing-values fix (Jul 13, sql/168)

WHY: Perf-review §7 Brief B. The Filters modal's /filter-options route looped every field calling count_nonempty_rows + count_field_values(cap 200) + numeric_field_stats + TWO `.order('data->>field')` date probes — each a FULL SCAN, all 57014 at 1M, so the modal never opened on large datasets. count_field_values' 200-cap was ALSO the owner-reported "missing location values" bug: a categorical value ranked past 200 was silently absent, and `DatasetShell` only filled `SchemaFieldConfig.values` when empty, so a rare value not in the loaded 50K sample never appeared. NEW: sql/168 sampled_filter_options keyset-pages the deterministic idx_drf_sample order ONCE (same sample every sampled surface serves), accumulating per field in a single heap visit — non-empty count (btrim, count_nonempty_rows parity), categorical distinct values w/ counts (raw data->>field, count_field_values parity; capped 2000/page by frequency, Node merges + caps 500), numeric min/max (numeric_field_stats parity), date text min/max (legacy .order probe parity). ≤50K → the walk reaches every row = EXACT and returns up to 500 distinct values (the fix); >50K → deterministic sample, non-empty/blanks scaled to row_count, response flags `sampled:true` ("~"). Value lists computed for CATEGORICAL ONLY — open-ended is never a checkbox filter (the modal value-filters cat/numeric/date), so its high-cardinality free-text GROUP BY was pure waste. When distinct >500 → `valuesCapped:true`; FiltersModal shows a "Top 500 shown — search to add" note + a per-field search box that filters chips AND lets the user type a rarer value to add it to the allowlist (≥16px input per the iOS rule). Client now PREFERS the server `values` (DatasetShell overwrites, not fill-if-empty). Legacy per-field loop kept as the PGRST202 deploy-order fallback. Verified scripts/_verify_filter_options.mts (untracked KEEP): EXACT on 14K BareBurger (scanned==total, nonempty/values/numeric all exact-equal to legacy), SAMPLED on 56K Carrabba's (nonempty scaled within 0.02%, all top-20 present), missing-value repro (DR Card field: legacy capped 200, new 500, valuesCapped=true), SCALE on 1.03M (completes ~40s over 18 fields, scanned 50000, NO 57014 — per-page ~4s, well under the 8s statement timeout). 4 new unit tests (1359 total green); fresh tsc clean; lint delta 0 (only pre-existing react-hooks warnings on DatasetShell). sql/168 TEST-applied; PROD MIGRATE ON PUSH (docs/db/schema.sql snapshot refreshes then). Specs: ANALYTICS.md Filters + DATABASE.md.

## Brief F: filters-compliance sweep executed (Jul 13)

WHY: Perf-review §7 Brief F — the Ask Ana fix found 2 silent filter bugs in ONE consumer, so sweep the class. Traced EVERY SerializedFilters/applyFilters consumer (4 parallel classification passes, each (b)/(d) finding re-verified against code) vs canonical applyFilters + sql/167. Results table appended to §7. Findings: most consumers canonical (TextMine/Stats-main/Charts-raw+tax/ComparisonStrip/ViewsBar/DatasetShell/exports/ad-hoc) or correctly out-of-scope (FiltersModal editor, signal-stats all-reviews, decks LLM-sample, projectReportLoad no-filter-UI). The brief's `get_rows_by_filters` premise was WRONG — it's a theme/entity/dimension FACET filter, not a cat/range/daterange peer of applyFilters (reclassified c, no bug). TWO mechanical fixes made: (1) **StatsModule dimension tests** (class d) posted tax_crosstab/tax_group_stats to /aggregate WITHOUT rowIds → t-test/ANOVA/chi-square ran over the whole dataset while the sibling Charts tab filtered the same RPCs correctly; fix mirrors ChartsModule exactly — derive filteredRowIds from the full loaded sample (shared.rows, not the 2K stats subsample) ONLY when filters active (else null = whole dataset), pass to the tax posts (route already accepts p_row_ids). (2) **share/analytics** (class b) carried a bespoke rowMatchesFilter copy — faithful today but an independent drift risk; swapped to deserializeFilters + applyFilters, with a 10-case parity regression test (tests/unit/shareAnalyticsFilterParity.test.ts) inlining the deleted logic as the oracle across cat include/exclude/blanks + sci-notation range + daterange. TWO real bugs ESCALATED to owner (needs NEW SQL, Brief C territory, not guessed): the 5 scalar /aggregate ops (crosstab_counts/group_numeric_stats/date_series_stats/count_field_values/numeric_field_stats) and theme-counts render full-dataset numbers under a filtered UI on the server-aggregate path (large datasets + active filters) — the RPCs take no row-id/filter param; clean fix = add p_row_ids mirroring the tax family, recommend folding into Brief C. 1369 tests (+10), tsc clean, lint 0 delta (StatsModule react-hooks count unchanged 33). AUDITS.md registry entry → DONE. Specs: PERFORMANCE_REVIEW.md §7 + AUDITS.md.

## Brief C (counts family): /aggregate p_row_ids + sampled twins (Jul 13, sql/169)

WHY: Perf-review §7 Brief C fix-queue #4, folding in the Brief F escalation #1. The five scalar /aggregate ops (crosstab_counts, group_numeric_stats, date_series_stats, count_field_values, numeric_field_stats) were full O(N) jsonb scans that (a) 57014'd at ~1M rows AND (b) took no filter param, so Charts rendered full-dataset numbers under a filtered UI while the sibling taxonomy family already honored filters. Both closed in one migration + one lib. **Filter-awareness:** each exact RPC DROP+re-CREATE'd with an appended `p_row_ids bigint[] DEFAULT NULL` (`id = ANY(p_row_ids)`, the tax-family predicate; DEFAULT NULL keeps every existing caller — signal-stats/analyticsCompute/taxonomyRollup/filter-options-legacy — byte-identical and is PGRST202-fallback safe). `ChartsModule.useAggregation` drops the `isTax`-only gate so EVERY spec forwards the view's `filteredRowIds` (fieldKey stays tax-only); the route appends p_row_ids + drops it on PGRST202. **Sampling:** five keyset-paged `sampled_*` twins over idx_drf_sample (same 50K sample the bulk-rows route serves + the strip counts over), each returning one page as `{n_scanned, <partial>, last_hash, last_id}`; `lib/sampledAggregate.ts` pages to the cap, scales COUNTS by total/scanned, reports means/medians/stddev UNSCALED (a uniform sample's mean/quantile is a direct population estimate; percentiles ride raw values to Node since partial-aggregate merge can't produce them). Twins ALSO take p_row_ids (narrows numerators, not the sample-walk denominator) so a filtered above-cap view is bounded per page. Route decision: `datasets.row_count > 50K` → twin (scale, sampled:true), else exact + p_row_ids; twin failure (incl. pre-migration PGRST202) falls back to the exact RPC. Charts carry a dataset-level "≈ Estimated from a 50,000-row sample" affordance (strip's "~" doctrine) at totalRows>50K. Collections don't reach this route (client renders their charts from raw rows); StatsModule's scalar tests already compute client-side over the loaded sample, only its dimension tests hit /aggregate (Brief F). Verified scripts/_verify_aggregate_sampled.mts (untracked KEEP) ALL PASS: sampled-vs-exact within ±2% on proportions (Carrabba 56K field_counts Δ0.07pp / crosstab Δ0.05pp; Outback 128K field_counts Δ0.28pp / crosstab Δ0.17pp / group_stats-n Δ0.18pp / numeric-mean Δ0.00%), 1M PERF TEST sampled field_counts + numeric_stats complete (~33s, scanned 50000, NO 57014) while exact field_counts 57014s (twin load-bearing), p_row_ids exact parity (25 explicit ids → counts match) + sampled-twin narrowing (restricted-sum 14 ≤ 15 picked). 8 new unit tests (lib/sampledAggregate). tsc clean; lint 0 delta (only pre-existing ChartsModule react-hooks warnings). sql/169 TEST-applied; PROD MIGRATE ON PUSH (docs/db/schema.sql snapshot refreshes then). Specs: ANALYTICS.md /aggregate section + DATABASE.md. NEXT in Brief C = taxonomy-family sampled twins + theme-counts p_row_ids (escalation #2), then Brief D (QR-burst), Brief E (long-tail).

## Brief C (escalation #2): theme-prevalence bars honor filters (Jul 13, sql/170)

WHY: Brief F escalation #2 — the Charts theme-prevalence bars (ChartsModule liveThemeCounts → /theme-counts) rendered DATASET-WIDE % under a filtered UI because the numerator (count_theme_matches) and denominator (count_nonempty_rows, sql/161) took no filter param, while every other Charts aggregate is filter-aware (raw-row via applyFilters; scalar + taxonomy server ops via p_row_ids). sql/170 DROP+re-CREATE'd both with an appended `p_row_ids bigint[] DEFAULT NULL` (`id = ANY(...)`, the tax-family predicate; DEFAULT NULL keeps signalStats/nonEmptyCount callers byte-identical + PGRST202-fallback safe). lib/nonEmptyCount.countNonEmptyRows gained an optional rowIds arg (RPC p_row_ids when set; legacy fallback applies .in('id',...)). theme-counts route: parses body.rowIds, SKIPS its sampling path when filters active (the id set is a bounded ≤50K sample subset → exact is cheap and a sampled scaling denominator would be wrong for a subset), threads p_row_ids into countNonEmptyRows + a new themeMatchCount helper (p_row_ids + PGRST202 fallback). ChartsModule forwards _filteredRowIds in the POST body (in the cachedRequest key) + a stable content-signature (_filterIdSig = len:first:last) in the effect deps so it re-fetches on filter change including the async null→ids transition once the filtered sample loads. Scope: the theme-card EXTRAS (cooccurrence/topical/dimensions) stay full-dataset — not the reported bug, and TextMine's own bars recount client-side over the filtered sample. Verified (scripts/_tc_tmp, ephemeral): filtered non-empty 49==49 + filtered theme-match 10==10 vs independent JS count over 150 Carrabba ids; whole-dataset (p_row_ids null) unchanged at 48,536. 3 new unit tests (nonEmptyCountFilter — p_row_ids forwarding/omission, legacy .in fallback). tsc clean; lint 0 delta. sql/170 TEST-applied; PROD MIGRATE ON PUSH. Specs: ANALYTICS.md + DATABASE.md + TESTING.md.

STILL OPEN in Brief C (Part 3, deferred — evidence-gated, NOT rushed): the taxonomy-family sampled twins (sampled_taxonomy_sub_counts/group_stats/crosstab/date_series/axis_crosstab). CONFIRMED load-bearing — with a taxonomy rollup registered so taxonomy_field_or_primary resolves, exact taxonomy_sub_counts 57014s at 1M (8.1s, measured; the TEST datasets carry _tx from Outback ×8 but no dataset_state rollup, so they resolve empty/fast today). Deferred because: (a) both filter escalations (#1 scalar, #2 theme-counts) are now fixed — the taxonomy SAMPLING is the last, least-urgent piece; (b) a fully-classified >50K dataset is a future scenario (classification is browser-looped + capped); (c) verifying requires fabricating rollup state + a 5-twin lib/route build, best done fresh not at low context. Next session: build the 5 tax twins (mirror sql/169 + sql/164's taxonomy_field_or_primary resolution + p_row_ids), a lib/sampledTaxonomy pager, route sampled routing for the 5 tax ops, verify sampled-vs-exact on 128K Outback (register its rollup) + no-57014 on 1M, then REVERT the rollup injection.

## Brief D: QR-burst rate-limit hardening + findStudy dedup (Jul 13)

WHY: Perf-review §7 Brief D — the owner's QR-code-on-TV scenario. A venue-WiFi crowd shares ONE NAT IP, where the flat per-IP keys throttled the whole ROOM: /api/respond `respond:<ip>` at 120/min = ~2 saves/s across everyone; /api/clarify `clarify:<ip>` at 10/min = AI follow-ups silently died for every later respondent (survey still completes, data invisibly shallower). Fix = two-tier keying on both: PRIMARY per-(IP, session_id) bucket (respond 20/min, clarify 6/min) so each respondent has their own budget, PLUS a per-IP BACKSTOP (respond-ip 600/min, clarify-ip 120/min) that still stops a single abusive source. session_id already rode in the respond body; ADDED it to the clarify body + ClarifyRequest interface + the useSurveyEngine clarify fetch. Both routes now parse the body BEFORE the rate check (the key reads session_id) and reject bodies >100KB first (413). Cellular scanners (own IP) unaffected either way. Also: /s/[guid] findStudy wrapped in React.cache — the page render AND generateMetadata both looked it up (~3–5 point lookups EACH), so a burst page view paid the study lookup twice; cache() dedupes within one request. Refactored findStudy to create its own service client inside (keyed by identifier only, so the memo hits across both call sites); SurveyPage/generateMetadata updated. 4 new keying unit tests (respond.test.ts: per-(ip,session) + per-IP backstop keys/limits asserted, two sessions same IP get separate buckets, backstop 429s independently, session-less → anon bucket). tsc clean; lint 0 delta. NO SQL, NO new migration. Specs: SURVEYS.md (clarify + respond rate sections) + CAPACITY.md §3. Load-path otherwise unchanged (tests/loadtest/survey-submit.k6.js still valid). Pre-event checklist (compute bump + confirm keying deployed + avoid scheduled imports) stays the ops complement.

## Decision: keep the 1M TEST perf dataset until the perf fix-queue is done (Jul 13)

WHY: owner asked whether the TEST `[PERF TEST] Outback x8` dataset (1,028,952 rows, id d1000000-…-001000, seeded for the perf architecture review) should be deleted. Decision = KEEP until the §7 fix-queue is finished, because it's the verification target for the two still-open briefs — Brief C Part 3 (taxonomy-family sampled twins) and Brief E (long-tail) — whose bar is "no 57014 on the 1M PERF TEST." TEST-only (no prod impact; doesn't touch the mocked always-on npm test), but ~2.6 GB bloats the Micro-tier TEST DB and can slow the env-gated suites, so DELETE + VACUUM ANALYZE once those two briefs land (re-seed = copy Outback ×8 with fresh identity ids, cheap). Recorded in PERFORMANCE_REVIEW.md re-run policy + queue memory. No code change.

## Brief C Part 3: taxonomy-family sampled twins (Jul 13, sql/171)

WHY: Perf-review §7 Brief C Part 3 — the deferred tail, now executed. The five taxonomy `tax_*` chart aggregates (taxonomy_sub_counts/group_stats/crosstab/date_series/axis_crosstab, sql/164) unnest `data->'_tx'->'f'-><field>->'a'` over EVERY row and 57014 at ~1M ONCE a rollup is registered so taxonomy_field_or_primary resolves a classified field — CONFIRMED load-bearing (taxonomy_sub_counts 8.1s at 1M in the review; the harness re-confirmed it 57014s at 1M under an injected rollup). This was the last O(N) family on /aggregate after the counts family (sql/169) + theme-counts (sql/170). sql/171 adds five keyset-paged `sampled_taxonomy_*` twins over the same 50K idx_drf_sample (byte-identical page CTE to sql/169 + sample_dataset_rows so the planner uses the index; MATERIALIZED so the sample is walked once per page). Each resolves its classified field via taxonomy_field_or_primary(p_dataset_id, p_field_key) in a CTE cross-joined into the unnest (a NULL resolution → empty unnest, matching the exact RPC's early RETURN), so the per-question source-field picker keeps driving dimensions; pure LANGUAGE sql (axis validation stays in the route's TAX_AXES / the exact RPC, same posture as the sql/169 counts twins). lib/sampledTaxonomy.ts pages to the cap and scales COUNTS by total/scanned, reporting means/medians/quartiles/stddev UNSCALED (a uniform sample's mean/quantile is a direct population estimate) — reusing the shared pageSample + median/percentileCont/stddevSamp helpers now exported from lib/sampledAggregate.ts. Twins ALSO take p_row_ids (narrows the numerators of a filtered above-cap view, never the sample-walk denominator). Route: the 5 tax ops route to the twin when datasets.row_count > 50K (scale, sampled:true added to each response), else the exact RPC + p_row_ids; twin failure (incl. pre-migration PGRST202) falls back to the exact RPC. Client labeling needed NO change — the dataset-level "≈ Estimated from a 50,000-row sample" banner is keyed on totalRows>50K (op-agnostic), so dimension charts already carry it. Verified scripts/_verify_taxonomy_sampled.mts (untracked KEEP) ALL PASS: it TEMPORARILY injects analytics.taxonomy.fields.<FK>={} into dataset_state for the 128K Outback + 1M PERF TEST so taxonomy_field_or_primary resolves the "…like LEAST…" field (which carries _tx), runs parity, then REVERTS in a finally (confirmed cleared post-run). Sampled-vs-exact within ±2% on 128K (sub_counts Δ0.91pp / crosstab Δ0.61pp / axis_crosstab Δ0.50pp / group_stats-n Δ0.91pp / group_stats mean Δ0.00% unscaled); 1M sampled sub_counts+crosstab+axis_crosstab all complete (~34s each, scanned 50000, NO 57014) while exact sub_counts 57014s (twin load-bearing); p_row_ids narrows the numerator over a sample-resident id subset. sql/169 regression harness re-run green after the export refactor. 1384 tests green; fresh tsc clean; lint 0 delta on touched files. sql/171 TEST-applied; PROD MIGRATE ON PUSH (docs/db/schema.sql snapshot refreshes then). Specs: ANALYTICS.md (Dimensions sampled note) + DATABASE.md (sql/171) + TAXONOMY.md (RPC note). Brief C is now COMPLETE; NEXT = Brief E (long-tail retirements).

## Brief E item 1: entities GET — stored + sampled mention counts (Jul 13, sql/172)

WHY: Perf-review §7 Brief E item 1 (fix-queue #6, forward-looking). The `entities` GET calls count_entity_terms on EVERY read (Schema tab, theme cards, Ask Ana — entities are deliberately not client-cached) to attach a live full-text `mentions` count per catalog entity. count_entity_terms 57014s at ~1M: common terms match hundreds of thousands of rows via the GIN tsv prefilter, × up to ~300 catalog terms (measured: 15 common terms alone 57014 at 30s on the 1M TEST). An EXACT stored count is uncomputable at 1M for the same reason, so — per the review's sanctioned "store ... or sample" — sql/172 does BOTH. (A) STORED: four entity_catalog.mention_count* columns cache the computed count per row keyed by the scope's total row_count (the signal-stats keying signal); a DEFAULT read (no theme, no field-scoping, not the Manage panel — the hot path) serves stored counts with ZERO scans when every row is populated + keyed to the current total, else computes live and fires storeEntityMentionCounts in the BACKGROUND (fire-and-forget) so the next read is a hit; discovery + manual-add re-store at the end (they already rebuild the catalog). Theme/field-scoped/Manage reads always compute live (counts vary per request). (B) SAMPLED compute for a SINGLE-MEMBER scope above the 50K cap — covers a plain dataset AND a branded upload with no siblings (which still resolves to collection scope; the gate is memberDatasetIds.length===1, NOT scopeType==='dataset' — caught in verify: Carrabba is a 1-member brand collection): sampled_count_entity_terms keyset-pages the 50K idx_drf_sample and matches terms per page (same tsv-prefilter + open-ended recheck as count_entity_terms), page size adapting to the term count (clamp(80000/nTerms,500,5000)) so no page nears the 8s timeout (measured 467ms for 15 terms × 2500 rows at 1M); the caller (lib/entityFilter.sampledEntityTermCounts) scales by total/scanned and flags sampled:true. Multi-member collections (many small members) stay on the live path. apply_entity_mention_counts is a one-round-trip batched writer (slug-keyed jsonb → UPDATE). Client: EntitiesResult.sampled flows through the entities route → TextMineModule → EntitiesCard, which shows a "≈ sampled" chip. Verified scripts/_verify_entity_counts.mts (untracked KEEP; seeds a small catalog on Carrabba 56K since TEST has none, then deletes it): read is sampled=true, sampled counts within ±1.6% of exact count_entity_terms (chicken 1974 vs 1953, worst dessert Δ1.6%), counts persisted + row-total keyed, second read serves stored, a stale row-total triggers recompute (not the poisoned stored value). 1384 tests green; tsc clean; lint 0 delta (TextMineModule react-hooks count unchanged). sql/172 TEST-applied; PROD MIGRATE ON PUSH. Specs: ANALYTICS.md (Entity Counting) + DATABASE.md (sql/172). NOTE: at 1M with a huge (~300-entity) catalog the background populate is bounded-but-slow (~pages × ~3s) — acceptable off the user read path and only re-runs on row-total drift; there's no such prod dataset today (the 1M PERF TEST has no catalog, which is why the review could only code-audit this). Next in Brief E: item 2 (theme-counts cooccurrence/topical/dimensions sampled twins), item 3 (project-report pageSampledRows), item 4 (study_response_stats MV→counter table).

## Brief E item 2: theme-card extras — sampled twins (Jul 13, sql/173)

WHY: Perf-review §7 Brief E item 2. The three OPTIONAL /theme-counts extras — the co-occurrence matrix (compute_theme_cooccurrence_matrix, sql/055), per-theme topical words (extract_theme_topical_words, sql/054), and per-theme Dimensions breakdown (theme_dimension_counts, sql/151) — each full-scan dataset_rows_flat per member and 57014 at ~1M (deferred by sql/170's note as "not the reported bug"; closed here). sql/173 adds keyset-paged MULTI-THEME twins over the 50K idx_drf_sample (sampled_theme_cooccurrence_page / sampled_theme_topical_page / sampled_theme_dimension_page): ONE page RPC covers every theme so a member needs ~10 page calls, not ~10×N-themes; each returns a page's partial as jsonb ({n_scanned, <pairs|words|dims>, last_hash, last_id}) for lib/sampledThemeExtras.ts to page + merge + scale counts by total/scanned. A single-call sample aggregation was measured NON-viable (selecting the 50K sample + heap-fetching each row's full `data` jsonb ran 33s at 1M — the review's "50K sample load ~44-56s" caveat), so paging is mandatory, same reason sql/162/169/171 page. Route (app/api/datasets/[id]/theme-counts): each extra routes a member to the twin when row_count>50K, else the exact per-theme RPC unchanged; twin failure falls back to exact (PGRST202-safe). The topical + dimensions blocks were restructured from per-theme×per-member loops to per-member (so the multi-theme twin covers all themes in one call-set) then merge-across-members + top-N — same output shape/ordering as before (verified below-cap path unchanged). Topical's canonical stopword+sentiment list (446 entries) was EXTRACTED from sql/054's inline NOT IN list into a shared topical_stopwords() IMMUTABLE function so the exact and sampled paths share ONE source (killing the "keep in sync" duplication the old comment warned about); sql/054's function keeps its inline copy for now (both produce identical filtering — the shared fn is the canonical going forward). Shared pageSample (lib/sampledAggregate) now OMITS p_row_ids when null (was always sent) so the extras twins — which don't declare that param — resolve; the sql/169/171 twins default it to NULL so omitting-when-null is equivalent (1 unit test updated: toBeNull→toBeUndefined). Verified scripts/_verify_theme_extras.mts (untracked KEEP; injects a rollup so taxonomy_primary_field resolves for the dimensions extra, then reverts): all 3 sampled-vs-exact within ±3% on 128K Outback (cooccurrence Δ0.07pp, dimensions Δ0.73pp, topical top-word overlap ≥60%); 1M sampled cooccurrence/dimensions/topical all complete (~35-43s, no 57014) while exact cooccurrence 57014s. sql/169+171 regression harnesses re-run green after the pageSample tweak. 1384 tests green; tsc clean; lint 0 delta. sql/173 TEST-applied; PROD MIGRATE ON PUSH. Specs: ANALYTICS.md + DATABASE.md. Next in Brief E: item 3 (project-report pageSampledRows), item 4 (study_response_stats MV→counter table).

## Brief E item 3: project-report readMemberRows → pageSampledRows (Jul 13)

WHY: Perf-review §7 Brief E item 3. lib/projectReportLoad.readMemberRows (the substrate for a collection project-report's rating-over-time chart + theme sample + representative quotes) paged a member's flat rows via `.range(from, from+999)` ORDER BY row_index — deep-OFFSET paging that re-scans from row 0 each page and 57014s on a 1M-row member (competitive-set datasets are ~8-30K today, but the review flagged it as the collections project-report O(N) risk). Swapped to `pageSampledRows` (lib/bulkRowSample, sql/160) — the SAME deterministic 50K idx_drf_sample the bulk-rows route + every sampled surface serves, an index-range scan flat in dataset size. At/under the 50K sample size the walk returns every row (identical row SET → the order-independent aggregates monthlyRatings/theme-counts are unchanged; only the "head" quote sample shifts from row_index-first to hash-first, both arbitrary representative picks); above it the report is computed over the 50K sample. Mechanical reuse of an already-verified helper (no new SQL). Verified read paths on Carrabba 56K (50000 rows, 17s) + the 1M PERF TEST (50000 rows, 42s) — both complete with NO 57014 (the old OFFSET path would time out at 1M); rating/date extraction unchanged (null when the field is absent). cap default SIGNAL_SAMPLE_CAP (was 80000, but idx_drf_sample only holds 50K anyway). tsc clean; lint 0 delta; 1384 tests green. No SQL, no migration. Next in Brief E: item 4 (study_response_stats MV → per-study counter table + trigger).

## Brief E item 4: study_response_stats MV → counter table + trigger (Jul 13, sql/174)

WHY: Perf-review §7 Brief E item 4 (the "schedule last" complex one). The study_response_stats MATERIALIZED VIEW's refresh_study_response_stats() did a full platform-wide GROUP BY over `responses`, debounced to one refresh/30s and fired from /api/respond + /api/studies/[id]/responses on every submit/delete. Trivial at today's ~191 responses, but at 1M+ accumulated responses each refresh is a multi-second whole-table scan re-run every 30s during a QR-burst — competing for the exact I/O ingestion needs (the review flagged it as a platform-wide O(all-history) scan to retire before response volume becomes real). Replaced with study_response_stats_live, a per-study COUNTER TABLE maintained INCREMENTALLY by an AFTER INSERT/UPDATE/DELETE FOR EACH ROW trigger on responses (trg_study_response_stats → srs_apply_delta, a signed-delta UPSERT: +1 on insert, -1 on delete, reverse-OLD-then-apply-NEW on update incl. a study_id change) — O(1) per response, no scan. Averages kept as sum+count pairs (avg_experience = exp_sum/nullif(exp_n,0), same for NPS) so a single response's edit updates in constant time; the "complete" and sentiment-bucket predicates are byte-identical to the MV's FILTERs. RLS enabled + org-scoped SELECT policy (studies join, mirroring the reader RPC; trigger writes as SECURITY DEFINER owner → bypass RLS). Backfilled from responses at migration time (6 studies on TEST). get_study_response_stats_for_user (dashboard reader) repointed from the MV to the table; study_stats_for_ids (a direct grouped read over responses, bounded to requested ids — the dashboard fallback) unchanged. MV DROPped; refresh_study_response_stats() kept as a no-op stub so the pre-deploy /api/respond's call is harmless during the deploy window (safe to drop once no caller remains). Code: removed the refresh block + the mv:study_response_stats rate-limit debounce from BOTH /api/respond (dropped the now-unused waitUntil import) and /api/studies/[id]/responses (dropped now-unused waitUntil + checkRateLimit imports); architecture-deck schema listing study_response_stats → study_response_stats_live. Verified scripts/_verify_study_stats_counter.mts (untracked KEEP): backfill parity (counter == study_stats_for_ids GROUP BY), then INSERT/UPDATE/DELETE of a throwaway response each keep the counter table in lockstep with a fresh GROUP BY, DELETE restores the original 181 (net-zero); test row cleaned up. 1384 tests green; tsc clean; lint 0 delta. sql/174 TEST-applied; PROD MIGRATE ON PUSH (backfills prod's responses at apply time). Specs: SURVEYS.md (response stats) + DATABASE.md (sql/174). ⚠ RLS/egress env-gated suites (test:rls/test:egress) should be re-run against Sentimetrx-Test after migrate to confirm the new table's policy. **Brief E COMPLETE; the entire perf review §7 fix-queue (Briefs A-F + C Part 3 + E) is now DONE — the 1M [PERF TEST] dataset can be deleted + VACUUMed once these land.**

## Fix-queue item 7: org-snapshot intra-org checkpoint resume (Jul 13)

WHY: Perf-review fix-queue item 7 (the last one; Fable-tagged backup-correctness item). The nightly org-snapshot cron's continuation was ACROSS orgs only (`?after=<org-id>&hop=N`, 2026-07-04): a SINGLE org whose dump outruns the 240s budget blew past it mid-dump (the budget was only checked between orgs), got hard-killed by Vercel at maxDuration 300s, and left NO manifest — and because each retry restarted the org from scratch, that org failed identically EVERY night (observed on prod 7/13 at 785K rows; the 04:00Z timeout also hammered the DB right before late-night owner sessions). Fix in three layers. (1) lib/orgSnapshot.iterateTablePages now yields a serializable resume cursor with every page (offset / parent+offset / chunk+offset per filter kind) and accepts a startCursor — offsets drift like any non-transactional paging, no worse than the existing within-run behavior. (2) lib/orgSnapshotV2.dumpOrgSnapshotV2Resumable takes a deadlineAt: it bails at a page boundary once past it, closes+uploads the in-flight gzip part as a complete object (large tables become multi-part: tables/<name>.ndjson.gz, .part2…, listed under the manifest entry's parts[]; readV2Table streams parts in order so restores are untouched — per-parent contiguity holds because the cursor resumes exactly where it stopped), and writes a checkpoint to <prefix>partial.json (completed tables + in-progress table's parts + cursor). partial.json is never a snapshot: listings only surface manifest-committed days. Guarantees ≥1 page of progress per invocation. The single-shot dumpOrgSnapshotV2 (admin "Snapshot now", clone script) is a thin wrapper — no deadline, identical behavior, no caller changed. (3) The cron route passes deadlineAt into every dump and its continuation now carries &resume=<org-id>&d=<YYYY-MM-DD> so the next hop reloads that org's checkpoint (from the ORIGINAL day's prefix — d guards midnight rollover) and continues mid-table before moving to fresh orgs; a partial org is a `partial: true` result (progress, not failure), a resume hop that advances the checkpoint ordinal by nothing gives the org up loudly (cron.orgSnapshot.stalled) instead of chaining to the hop cap, and at the cap a partial converts to an explicit error (no manifest = no backup). Accepted residual: a hop hard-killed mid-page (one 1000-row page > the ~60s headroom) dies before checkpoint+kick — that night's chain ends silently and the next night restarts the org fresh; not worth engineering around at current scale. Verified: new snapshotV2 tests drive the chain exactly like the cron (deadlineAt in the past → page-granular bails; reload checkpoint from the store each hop) and prove the chained snapshot is row-identical to a single-shot dump (incl. the U+2028 row and multi-part reassembly order), multi-part dataset_rows_flat restores with each parent deleted exactly once, alien-org checkpoints are ignored; cron tests cover the resume kick URL, same-day-prefix checkpoint load, stall giveup, and hop-cap partial→error conversion. 1393 tests green (9 new); fresh tsc clean; lint 0 delta. NO SQL, no migration — ships with the pending 24-commit push. Specs: BACKUPS.md (continuation section + offline multi-part read + failure modes) + CAPACITY.md (§4 table, §5 #2, still-open list) + PERFORMANCE_REVIEW.md (both org-snapshot findings marked FIXED). **The entire perf-review fix queue (items 1-7) is now COMPLETE.**

## Agent capability review + super-agent tier spec (Jul 14, docs-only)

WHY: Owner mandate — "we run into situations where a much more sophisticated and informed bot is necessary; should we have regular agents and super agents (more expensive) that absorb much larger knowledge bases?" Ran a Fable-level review of the full agent stack (two parallel code-mapping passes: KB ingestion→retrieval pipeline, chatCore engine + model/config surface) and wrote docs/AGENT_TIERS.md. Findings: today's agent is deliberately capped as a short-answer FAQ concierge (Sonnet 4.6 hardcoded via tier:'advanced' — a never-reverted 5/16 demo bump; maxTokens 400 + 40-160-word hard limit; last-8-message window; top-5 RAG chunks; 30-page crawl that strips every href — the Spacy-incident class), and 4 quality DEFECTS degrade all agents today: D1 the keyword-fallback RPC returns no confidence so a semantic-RPC error silently suppresses the ENTIRE knowledge base (chatCore 826-860 + sql/023), D2 chunk edits never re-embed (stale vectors forever), D3 editor save = non-atomic delete-all-then-repost of the whole KB inside a swallowed catch, D4 crawl quality (no sitemap, links stripped, nav boilerplate, no near-dup dedup). Spec: one engine, capability by config — agents.capability standard|super + CAPABILITY_DEFAULTS knob table (model→Opus 4.8 via existing modelOverride/RATES, 12 chunks, 24-turn window, multi-query retrieval, sitemap-driven resumable crawl via the D16a browser-loop pattern, PDF/DOCX ingestion via readDocument, weekly re-crawl cron, liveContext adapter registry generalizing wildfire/MCO); per-turn economics from RATES: standard ~$0.02 → super ~$0.05 (Sonnet) / ~$0.08-0.10 (Opus), 3-5×. Phasing: Phase 0 = D1-D4 quality floor for ALL agents FIRST (don't monetize fixing defects), Phase 1 = tier MVP, Phase 2 = super KB+retrieval, Phase 3 (GATED) = in-turn tool loop + streaming. Build assignment: Phases 0-2 on OPUS as executable briefs (perf-review §7 pattern); Phase 3 is the only Fable-worthy piece (shared-engine architectural change), when greenlit. Registered in docs/AUDITS.md; cross-ref added to BOTS.md §12. No code changed.

## Phase 0 D1 — fallback retrieval no longer suppresses the whole KB (Jul 14)

WHY: First quality-floor defect from docs/AGENT_TIERS.md §2, executed as a brief for ALL agents. Only `search_knowledge_semantic` returns a `confidence` column; the lexical `search_knowledge_chunks` (sql/023) returns `rank` alone. When semantic search errored, chatCore fell back to the lexical RPC, whose chunks had `confidence === undefined` → `chunks[0].confidence ?? 0` read 0 → below the 0.05 injection floor → the branch set `knowledgeInjected = true` and skipped injection, which ALSO blocked the free-text KB fallback below it: a single transient semantic-RPC error silently served the agent with ZERO knowledge. Fix is code-only (PGRST202-safe, no SQL): new pure helper `chatCore.normalizeChunkConfidence(chunks)` derives `confidence = LEAST(rank/8.5, 1)` — the exact scale the semantic RPC uses — onto any chunk lacking one, called on the RAG result before scoring. Semantic chunks (confidence already present) are untouched; a genuine low-rank keyword miss still falls under the floor honestly. Unit test `tests/unit/ragConfidence.test.ts` proves a keyword-fallback chunk (rank 3.4, no confidence) now clears the 0.05 floor so knowledge is injected, confidence is capped at 1 / floored at 0, and null/empty/rank-less input is a safe no-op. RagChunk interface exported + gained `rank?`. BOTS.md RAG section documents the normalization. 1397 tests green (4 new); fresh tsc clean; lint 0 delta. NO SQL.

## Phase 0 D2 — knowledge-chunk edits now re-embed (Jul 14)

WHY: Second quality-floor defect from docs/AGENT_TIERS.md §2. `PATCH /api/bots/[id]/knowledge/[chunkId]` updated title/content, and the `trg_knowledge_tsv` trigger refreshed the full-text `tsv` — but nothing regenerated the pgvector `embedding`. So after any edit the semantic score kept matching the ORIGINAL text forever (fix a typo, correct a fact, rewrite a chunk → semantic retrieval still ranks it by the pre-edit wording). Fix: the PATCH handler now re-embeds the merged new title+content (`generateEmbedding`, one chunk, blocking — cheap) and writes the fresh vector in the same UPDATE. On embedding failure (null result OR thrown) it writes `embedding = NULL` rather than leaving a stale vector — the chunk stays findable via the lexical tsv/trigram fallback, just without a semantic score, which is strictly better than matching old text. gateBotAccess now returns the bot's org_id (needed for the per-org embedding key mode). Verification: tests/unit/knowledgeChunkReembed.test.ts (mocked service + embeddings) proves the embed input merges new+existing fields correctly on content-only and title-only edits, and that both null-result and thrown-embed paths clear the vector while the text edit still lands. BOTS.md documents that `embedding` is not trigger-maintained. 1401 tests green (4 new); fresh tsc clean; lint 0 delta. NO SQL.

## Phase 0 D3 — editor save is an atomic diff-upsert, not a KB wipe (Jul 14)

WHY: Third quality-floor defect from docs/AGENT_TIERS.md §2. The agent editor's save did DELETE-all-chunks then re-POST (two separate client calls, inside a swallowed try/catch): any failure between the DELETE and the POST left the agent with ZERO or partial knowledge, and even on success every save re-embedded the ENTIRE knowledge base (wasteful + slow on large KBs). Fix: `POST /api/bots/[id]/knowledge` gains `replace: true`. The editor now sends its whole payload (KB + FAQ + facts + opponent research) in ONE call with the flag; the route content-dedups against stored chunks, inserts only new/changed chunks (so unchanged chunks are never re-embedded), and — only AFTER the inserts commit — deletes only the chunks whose content disappeared. A failed insert returns before any prune, leaving the previous KB fully intact; there is never a zero-knowledge window. Opponent research rides in the same payload (its "### Name (Opponent)" headings still get opponent/sentiment metadata from the server classify pass, which is fed the configured opponent names), replacing the old separate opponent POST. Without the flag POST keeps plain append semantics, so research/fetch-url/manual-add callers are unchanged; an editor save that empties all knowledge still issues a DELETE to clear the index. gateBotAccess unchanged here. Verification: tests/unit/knowledgeReplaceUpsert.test.ts (mocked service+embeddings) proves the four properties — changed-only insert + removed-only prune with unchanged chunks untouched, prune-when-nothing-new, append never prunes, and insert-failure leaves the old KB intact (prune never runs). BOTS.md knowledge section documents the replace mode. 1405 tests green (4 new); fresh tsc clean; lint 0 delta. NO SQL.

## Phase 0 D4 — deep-crawl quality + build-time link-integrity (Jul 14)

WHY: Fourth and final quality-floor defect from docs/AGENT_TIERS.md §2 — the "Spacy class." The July 2026 Spacy agent invented an entire domain because the website deep-crawl STRIPPED every href (leaving the model no real URLs), ingested repeated nav/footer chrome, seeded only from the homepage, and attached no links directory or link-integrity rule. This defect fed the whole class of agent URL hallucination, so it's fixed for ALL website-sourced agents at once. Crawl logic split into pure, unit-tested helpers: lib/crawlText.ts — (a) htmlToText now preserves in-body `<a>` as `[label](absoluteUrl)` (relatives resolved against the page; mailto/tel/# degrade to plain text); (b) parseSitemapUrls + a sitemapSeeds() step seed the queue from /sitemap.xml (sitemap-index fan-out capped at 3) before BFS fallback; (c) stripBoilerplate drops any non-blank line on >⅓ of pages (nav/header/footer chrome), ≥3-page gate. lib/agentLinks.ts — (d) build-time scaffolding: the crawl response now returns official_links (real crawled URLs), a links_directory block, and the standing link_integrity_guardrail; the agent editor auto-attaches the Official Links Directory to system_prompt (attachLinksDirectory — idempotent, a re-crawl REPLACES the block) and the link-integrity rule to guardrails (withLinkIntegrityGuardrail — added once), and each KB section carries an `Official page: <url>` line. (e) post-build 200-lint is exercised by the verify harness rather than blocking the crawl. Verification: tests/unit/deepCrawlLinks.test.ts (11 cases) covers a–d deterministically; the fresh-crawl bar is met by scripts/_verify_deep_crawl_links.mts (untracked KEEP) — a live crawl of gnu.org PASSED all four checks: 181 in-body links preserved, 6-entry directory, no invented directory hosts, 5/5 sampled directory URLs returned 200. Removed a now-unused SafeFetchError import from the route (touch-it-fix-it). BOTS.md deep-crawl section rewritten. 1416 tests green (11 new); fresh tsc clean; lint 0 delta. NO SQL. **This completes Phase 0 (D1–D4); Phases 1+ wait on the owner's §7 decisions.**

## Agent tiers §7 owner decisions resolved (Jul 14, docs-only)

WHY: Owner made the four Phase-1-blocking decisions in docs/AGENT_TIERS.md §7, threaded into §3/§3.1/§4 (TBDs removed) and the status banner. (1) User-facing name = "Super Agents" (internal capability='super'). (2) Super-tier model = per-agent choice — editor dropdown → capability_config.model, default Opus 4.8, Sonnet 4.6 available (not a single hardcoded tier model). (3) Pricing = per-agent monthly flat fee, unlimited turns; org_features quota is an abuse backstop, not a per-turn meter. (4) Weekly KB re-crawl = super-tier only (paid differentiator; standard agents stay manual-refresh — note this narrows the earlier "maybe all agents" phrasing). Decision 5 (Phase 3 in-turn tool-loop + streaming) stays GATED on a named customer need — does not block Phases 1–2. These four unblock Phase 1 (tier-split MVP) + Phase 2 (super KB/retrieval), both on Opus per §6. No code changed.

## Agent tiers Phase 1 — Standard vs Super Agents (tier-split MVP) (Jul 14)

WHY: First build phase from docs/AGENT_TIERS.md §5, executed after the §7 owner decisions. One engine, capability by config — a Super Agent is a raised knob set on chatCore, never a second code path. sql/175 adds agents.capability (standard|super, CHECK) + capability_config jsonb (TEST-applied; DEFAULT 'standard' = zero backfill, existing agents unchanged). lib/agentCapability.ts is the single source of truth: CAPABILITY_DEFAULTS + resolveCapability(agent) returning {modelOverride, maxTokens, ragChunks, historyWindow, inputCap, verbosityScale, hardLimit}. Standard values are byte-identical to the old hardcoded chatCore literals, so standard agents don't regress; super raises them (Opus 4.8 default model via callAI.modelOverride, 1200 maxTokens, 12 RAG chunks, 24-message history, 4000-char input, ×2.5 verbosity with soft framing). Wired into chatCore: resolveCapability once per turn → history-window generalized proportionally (window 8 → trigger >12, reuse-gap 8–14; scales cleanly to 24) so the delicate summary-reuse path is preserved; RAG p_limit, input cap, verbosity ladder (now a table driving both tiers — standard reproduces the four original strings exactly), and the callAI model/maxTokens all read the knobs. Super turns log event_type='chat_super' so their Opus cost is separable in /admin/usage. Editor (EditAgentClient): an "Agent Capability" section — Standard/Super toggle + a per-agent super-model dropdown (SUPER_MODEL_OPTIONS → capability_config.model). POST + PATCH /api/bots accept capability/capability_config, validated via normalizeCapability (CHECK-safe coercion) + sanitizeCapabilityConfig (only a valid super-model survives) — raw values never stored; the [id]/chat route selects the new columns. Pricing = flat per-agent monthly fee (§7), so super turns are UNLIMITED by default; assertSuperTurnAllowed (lib/featureFlags) is an abuse backstop that blocks a turn only when an org_features super_agent_turn row sets an explicit monthly ceiling this month's chat_super count has hit (fails OPEN on count error; chat route returns 429). Verification (§5 twin bar): tests/unit/agentCapability.test.ts + superTurnBackstop.test.ts (14 cases) + live-TEST scripts/_verify_super_agent.mts (untracked KEEP) — proved the CHECK constraint rejects an invalid capability, a super twin round-trips capability_config.model → Sonnet override + super knobs, and standard knobs match today. 1430 tests green (14 new); fresh tsc clean; lint 0 delta. sql/175 TEST-applied, PROD migrate on push. Phase 2 (super KB/retrieval) is next; Phase 3 stays gated.

## Agent tiers Phase 2a — multi-query retrieval for Super Agents (Jul 14)

WHY: First Phase 2 unit (AGENT_TIERS §3.1 "biggest quality lever after D4"). A single query embedding misses KB phrased differently from how the user asked. Super agents now retrieve for BOTH the raw question AND one Haiku rewrite (tier:'fast', 60 tokens, 4s timeout, logged event_type='query_rewrite'), then union the two result sets via lib/multiQueryRetrieval.mergeRankedChunks — dedup by chunk id keeping the higher-confidence hit, re-rank by confidence, take top-K (12 for super). The single-query fetch was refactored into a reusable runRetrieval(q) closure (embed → semantic RPC → keyword fallback → D1 confidence normalization) so both queries share the exact same path; standard agents call it once, unchanged. RagChunk gained `id` for dedup. mergeRankedChunks is pure + generic (no chatCore import) so it unit-tests cleanly: tests/unit/multiQueryRetrieval.test.ts proves dedup-keeps-higher-confidence + re-rank + top-K, id-less keyword-fallback chunks survive, and null/undefined sets are tolerated. Rewrite failures degrade to the raw query alone (never blocks the turn). 1435 tests green (5 new); fresh tsc clean; lint 0 delta. NO SQL.

## Agent tiers Phase 2b — near-dup dedup + chunk budget on ingest (Jul 14)

WHY: AGENT_TIERS §5 Phase 2 — "chunk budget + near-dup dedup (reuse sql/135's embedding match)." The main ingest path had only exact-string dedup, so re-crawls/research runs accumulated near-identical drift copies, and there was no ceiling on KB size. POST /api/bots/[id]/knowledge now embeds candidate chunks BEFORE inserting (vector stored in the same INSERT — removed the separate embed-after-insert update loop), which unlocks two guards: (1) near-duplicate filter — APPEND path only — drops a candidate when an existing chunk is cosine ≥ 0.95 (match_agent_knowledge_embedding, sql/135, run in parallel across candidates). Deliberately SKIPPED in D3 replace mode: there the diff-upsert already handles unchanged/edited/removed, and a near-dup match against an about-to-be-pruned old chunk would silently drop a legitimate edit. (2) per-agent chunk budget from a new CapabilityKnobs.chunkBudget (2,000 standard / 5,000 super) caps how many new chunks land (room = budget − remaining-existing-after-prune). Response carries near_dup_skipped + budget_skipped. Embedding failure still degrades to lexical-only chunks. Verified: tests/unit/knowledgeDedupBudget.test.ts — append drops near-dups (sim 0.98) / keeps below threshold (0.5), replace bypasses the guard (sim 0.99 still stored), and budget caps at 5000 (4999 existing + 5 candidates → 1 stored, 4 budget_skipped); D2/D3 route tests still green. 1439 tests (4 new); fresh tsc clean; lint 0 delta. Reuses sql/135 (no new migration).

## Agent tiers Phase 2c — PDF/DOCX/text document ingestion for KBs (Jul 14)

WHY: AGENT_TIERS §3.1/§5 Phase 2 — "PDF/DOCX ingestion via the existing readDocument vision pipeline." Documents were the one KB source the platform couldn't absorb (the owner mandate is "absorb much larger knowledge bases"), and scanned government notices / menus / whitepapers are real inputs for the civic + restaurant clients. New route POST /api/bots/knowledge-document (multipart `file`, auth-only + bot-agnostic like /deep-crawl so it works before a new agent is saved) EXTRACTS text and RETURNS it — the editor appends to the knowledge base and the normal save() runs it through the shared chunk→embed→dedup→budget ingest (P2b), so zero ingestion logic is duplicated (mirrors crawl/research/fetch-url exactly). Extraction is text-first (lib/botKnowledge/documentText.ts, pure/unit-tested): PDF via unpdf, DOCX via mammoth, both emitting one `## Page N` / `## heading` section per page so chunkText titles the chunks; plain text/markdown wrapped with a source heading. A PDF that comes back with almost no text (< 40 chars/page) is scanned/image-only and falls back to the VISION pipeline (lib/botKnowledge/documentVision.ts → renderDoc PDF→PNG in a Vercel Sandbox + visionReadPages, the reuse readDocument.ts documents; source PDF parked in the recordings bucket under bot-kb-tmp/, rendered, read, cleaned up), logged event_type='knowledge_doc_vision'. Limits: 25 MB/file, 400K chars/doc (chunk budget still caps what lands). Editor: a "Upload Document" card in Knowledge Sources with a file picker (accept pdf/docx/txt/md, 16px-safe hidden input). Deps added: unpdf@1.6.2 + mammoth@1.12.0 (npm audit still 0). Verified: tests/unit/documentText.test.ts (10 cases incl. a REAL DOCX built with the docx package through mammoth + a REAL text-PDF through unpdf — both extract locally, no sandbox needed); the vision fallback reuses the already-tested renderDoc/visionReadPages primitives. 1449 tests green (10 new); fresh tsc clean; lint 0 delta (the one EditAgentClient warning is a pre-existing Date.now()-in-render on the untouched "Last updated" display). NO SQL.

## Agent tiers Phase 2d — resumable 300-page super deep crawl (Jul 14)

WHY: AGENT_TIERS §3.1/§5 Phase 2 — "sitemap-driven resumable crawl (300+ pages) … runs as a D16a browser-driven loop; serverless 120s cannot do 300 pages in one shot." The single-shot /deep-crawl caps at 30 pages; super agents need whole-site KBs. New route POST /api/bots/[id]/crawl-job (super-only, 403 otherwise) runs the crawl as a D16a browser loop: {action:'start'} once (sitemap.xml seeds + entry), then the editor loops {action:'step', jobId}. Each step fetches a time-budgeted batch (20 pages / 45s), extracts via the shared lib/crawlText helpers (link preservation + per-batch stripBoilerplate), ingests through the SHARED ingestKnowledgeText path (P2b dedup/budget), and rewrites the cursor in agent_crawl_jobs (sql/176: queue/visited/pages_crawled, RLS org-read, TEST-applied). Page cap = new capability knob crawlPageCap (300 super / 30 std). If the tab closes the row survives at its cursor; reopening the editor ({action:'status'} → newest running job) surfaces a Resume button; the step loop is unmount-cancelled via a ref gate (the banked Stats-loop lesson — no setState/churn from a dead component). On done the link-integrity guardrail is attached (idempotent); per-chunk "Official page:" URLs carry the real links so no 300-entry directory bloats the prompt.

REFACTOR (enables P2d + P2e, guarded by the P2b/D2/D3 route tests): the knowledge-route ingest core is extracted to lib/botKnowledge/ingest.ts (ingestKnowledgeText) + lib/botKnowledge/chunkText.ts (pure splitter) so the crawl-job and the coming re-crawl cron reuse the EXACT same chunk→embed→near-dup→budget→insert→classify path with zero drift. CRITICAL interaction fixed: super-crawl chunks live outside the editor's knowledge textarea, so a D3 editor save (replace:true) would prune them — ingestKnowledgeText now NEVER prunes chunks tagged metadata.source_type='deep-crawl' (REPLACE_PROTECTED_SOURCE_TYPES); standard /deep-crawl + doc upload append to the textarea so they stay replace-managed.

VERIFIED: tests/unit/crawlQueue.test.ts (batch/merge/done math) + tests/unit/knowledgeReplaceProtect.test.ts (crawl chunks survive a replace save) + the §5 Phase 2 live bar MET via untracked scripts/_verify_super_crawl.mts — a real crawl of gnu.org on TEST reached 140 pages across 9 PERSISTED resumable step()s (cursor reloaded from agent_crawl_jobs each step), ingested 1550 chunks, stripBoilerplate removed nav chrome, job → done, temp agent/chunks/job cleaned up. 1461 tests green (12 new); fresh tsc clean; lint 0 delta (the lone EditAgentClient warning is the pre-existing Date.now()-in-render). sql/176 TEST-applied, PROD migrate on push.

## Agent tiers Phase 2e — Super-only weekly KB re-crawl cron (hash-diff) (Jul 14)

WHY: AGENT_TIERS §3.1/§5 Phase 2 + §7 #4 (owner) — "weekly re-crawl cron of training_urls with content-hash diffing (only changed pages re-chunk/re-embed) … SUPER-tier only (paid differentiator; standard agents stay manual-refresh)." Website-sourced KBs go stale silently today (no scheduled re-crawl); a super agent's whole point is a fresh, authoritative KB. New cron /api/cron/agent-recrawl (Mondays 07:00 UTC) sweeps every capability='super' agent that has training_urls, re-fetches each page, extracts text via the SAME lib/crawlText.htmlToText the deep crawl uses, and sha256-hashes it. The hash is compared to the last stored hash in agent_kb_page_hashes (sql/177, one row per (agent_id,url), RLS org-read, TEST-applied): an UNCHANGED page is skipped entirely — no re-chunk, no re-embed — so a stable site costs one cheap fetch and nothing else (that's the whole point of hash-diffing; keeps refresh near-zero cost and logs no knowledge_classify usage). A CHANGED or NEW page has its chunks replaced: delete this URL's existing chunks (metadata->>source = <url>, delete-first so the append-path near-dup guard can't reject the fresh chunks against the page's own prior copy), re-ingest through the SHARED ingestKnowledgeText (tagged source=<url>, source_type='re-crawl'), then upsert the new hash. The hash is written ONLY on a successful ingest, so a transient insert failure leaves the old hash and the page retries next week.

DESIGN: bounded per D16c cron-sweep — MAX_AGENTS_PER_RUN 25 (oldest-first), MAX_URLS_PER_AGENT 60, wall-clock deadline under maxDuration 300; any overflow rides to the next weekly run. Core logic is lib/botKnowledge/recrawl.ts (recrawlAgentPages) so it's unit-testable with the fetch/ingest mocked. ingest.ts REPLACE_PROTECTED_SOURCE_TYPES now also protects 're-crawl' (like 'deep-crawl') so a D3 editor Save never wipes cron-refreshed pages. vercel.json gains the cron entry.

VERIFIED: tests/unit/agentRecrawl.test.ts (hash determinism; unchanged→skip/touch; changed→delete-by-source+ingest+upsert; new→ingest; unreachable→no hash; ingest-throw→old hash kept; deadline stops new pages) + tests/unit/agentRecrawlCron.test.ts (fail-closed checkCronAuth 401; sweeps ONLY super agents with training_urls; clean no-op otherwise). Live bar via untracked scripts/_verify_recrawl.mts against a real gnu.org page: sql/177 insert/upsert round-trip on the (agent_id,url) unique key, refetched-hash == stored-hash (skip path), changed-hash upsert replaces in place (one row, replace path), and delete-by-metadata->>source removes only THIS page (siblings survive). 1470 tests green (9 new); fresh tsc clean; lint 0 delta. sql/177 TEST-applied, PROD migrate on push.

## Agent tiers Phase 2f — liveContext adapter registry (Jul 14)

WHY: AGENT_TIERS §3.1 (Live data row) — generalize the two hand-wired live-data injections in chatCore into "a small adapter registry: liveContext:[{source,params}] resolved from a typed adapter map; wildfire + MCO become the first two adapters (super-only knob)." The wildfire agent is LIVE ON PROD (www.sentimetrx.ai/b/wildfire), so this refactor's bar was preserving EXACT behavior. chatCore had two inline blocks: MCO (buildMcoLiveContext, gated inside by the AskAna bot id) then wildfire (buildWildfireLiveContext, gated by config.liveContext==='wildfire'). Replaced both with resolveLiveContextBlocks (lib/liveContext/registry.ts) — a typed LiveContextAdapter map whose two entries are wildfire + MCO. chatCore computes the same priorAssistant + recentUser(-8,-1) inputs and calls the registry; each returned block is pushed in the same order (MCO, then wildfire) at the same top-of-prompt position, and per-adapter errors keep the same debug labels ('mco-live-context' / 'wildfire-live-context').

PRESERVATION: the two legacy gates are unchanged and tier-agnostic — MCO fires for the AskAna bot id (ASKANA_BOT_ID now exported; the builder's internal guard stays as defense-in-depth), wildfire fires on the config flag. The only observable delta is that MCO is no longer invoked for non-Ana bots (it self-returned '' before), so the emitted system prompt is byte-identical. NEW on top: super agents opt into any registered adapter via capability_config.liveContext:[{source,params}] (validated by the client-safe lib/liveContext/types.sanitizeLiveContextRefs — unknown sources dropped, deduped, super-only; sanitizeCapabilityConfig now preserves it). A source already active via a legacy gate isn't run twice. types.ts is import-clean (no server-only) so agentCapability.ts stays client-bundle-safe for the editor.

VERIFIED: tests/unit/liveContextRegistry.test.ts (8) + liveContextTypes.test.ts (7) — legacy gate selection, super-knob activation + super-only gating, dedup, MCO-before-wildfire order, adapter-throw isolation, knob validation round-trip. LIVE bar met via untracked scripts/_verify_live_context.mts: a real "fire near 83702?" turn through resolveLiveContextBlocks produced a 4416-char live wildfire block (83702 → Boise, real NIFC/AirNow/USDM feeds) — behavior preserved through the new path. 1486 tests green (15 new); fresh tsc clean; lint 0 delta. NO SQL. This is the last Phase 2 unit — Phase 2 (super KB + retrieval) COMPLETE; Phase 3 (tool-loop + streaming) stays gated.

## Agent tiers Phase 3 — in-turn tool loop + streaming (Jul 14)

WHY: AGENT_TIERS §3.2 — the "heavy lifting" super-agent differentiator (fetch an official page live, run a second KB lookup mid-turn) plus streaming responses, owner-greenlit today ("start phase 3"). The one Fable-tier item: it changes the latency/failure model of the single engine both products share, so the design bar was zero behavior change for every existing consumer.

DESIGN: one engine, additive transport. lib/ai.ts gains callAIStream — raw-SSE twin of callAI (same per-org off/byo/platform gate via the shared resolveEffectiveProvider, same auto-logUsage from message_start/message_delta; BYO OpenAI/Azure degrades to one non-streaming call, no tools). lib/agentTools.ts owns the tool surface: search_knowledge (the IDENTICAL retrieval path as the main RAG injection, same negative-sentiment holdout) and fetch_page (hosts allowlisted from training_urls + subdomains, redirect landing host re-checked, 8s timeout, htmlToText) plus runAgentToolLoop (≤3 rounds, then budget-exhausted tool_results + tool_choice:'none' force the answer — a tool-happy model cannot loop forever). chatCore resolves the toolLoop knob (super-only, OFF in town-hall mode — live rooms are latency-sensitive) and takes an optional ctx.emit; absent emit + standard agent = the byte-identical legacy callAI path. The agent chat route answers SSE only when the client sends stream:true (delta/tool events, then done carrying the SAME object the JSON path returns; maxDuration 120; training_urls back in the select as the allowlist); every JSON client keeps working. ChatBot.tsx opts in: paints deltas live, shows tool-status lines, display-truncates at '[[' so a chips trailer never flashes, and RECONCILES the finished bubble to result.reply — the sanitizeBotReply scrub can't un-send streamed tokens, so the done event is authoritative. Non-final loop rounds log event_type='chat_tool'; the final round stays chat/chat_super so the super-quota backstop still counts 1/turn. Tool defs are stable per agent → prompt cache preserved. PulseIQ deliberately stays on blocking JSON (flip = client+route change later).

VERIFIED: 25 new tests — callAIStream.test.ts (SSE parsing incl. byte-split frames, tool_use input_json_delta assembly, in-stream error, 429 with status, OpenAI fallback strips stream/tools), agentTools.test.ts (allowlist rejects off-domain/ftp/malformed without fetching, subdomain allow, redirect-off-domain reject, negative-chunk holdout, is_error marking, round budget forces tool_choice none, per-round usage), ChatBotStreaming.test.tsx (jsdom: stream+reconcile+chips, ended locks composer, JSON fallback, mid-stream error → Retry). LIVE bar via untracked scripts/_verify_tool_loop.mts (KEEP): real Anthropic streaming (deltas assemble to final text + usage), real multi-round tool replay accepted by the API (secret from a stubbed KB reaches the answer), real fetch_page on gnu.org informing the answer, off-domain rejected. 1511 tests green; fresh tsc clean; ChatBot lint warnings 5→4 (down). NO SQL. AGENT_TIERS is now fully shipped (Phases 0–3).

## 2026-07-14 — Post-push CI fixes: sql/175 tx-wrap + Statistics/Charts navigation-wedge regression

WHY: the 40-commit production push (perf sampled/counter twins + agent tiers) went out and CI came back red on two jobs — the deploy correctly gated behind green CI, so nothing shipped. Both had to be fixed to land the release. This entry is the DD trail for why the fixes are correct and not papering over the tests.

FIX 1 — sql/175 transaction wrap. The check:sql-tx CI guard requires every migration numbered >70 to be BEGIN/COMMIT-wrapped so a partial failure rolls back cleanly; 175 shipped without the wrapper. Content-only change (idempotent ALTER … ADD COLUMN IF NOT EXISTS + guarded constraint), re-applied to prod to sync the schema_migrations ledger sha to the wrapped file. No schema change.

FIX 2 — the e2e smoke suite's FIRST-EVER prod-build CI run caught a real navigation wedge and it was NOT flaky: on the Statistics tab, EVERY outbound soft navigation (→ Schema, → TextMine) was aborted and the URL never left /stats. Reproduced deterministically against a local production build (dev mode hid it — the queue's earlier "e2e 4/4" runs were dev-mode). Root cause, localized by reverting StatsModule+ChartsModule to the last-green commit (nav worked) then bisecting the diff: the any-burn lint sweep (41c72522) INLINED `effectiveThemeModel`, deleting the `useMemo` wrapper AND its warning comment — silently reverting the exact fix 2a9782ea had landed. themeSetForField returns a fresh object every call, so the unstable identity fed the enrichedData memo → bootstrap-CI effect → setState → render chain into a perpetual update loop that starved the App Router transition (JS main thread stayed idle, no console error — a prod React build logs nothing for a caught/abandoned transition, which is why only a browser-level prod-build test sees it). Fix: restore the useMemo in BOTH modules (Charts had the identical regression → same wedge on the Charts tab) and re-add the do-not-inline warning so a future mechanical sweep can't strip it again. Verified: full smoke suite 4/4 green against a production build (the RETURN-visits hop 22s-timeout → 2.6s), fresh tsc clean, eslint 0 errors on both files. Lesson banked: annotation-only lint sweeps must not touch hook wrappers; a memoization that carries a "MUST be memoized" comment is load-bearing.

## 2026-07-14 — Substantive/"usefulness" flag, part 1: mine themes from substantive comments only

WHY: owner QC on prod flagged the Carrabba's "Liked Least" theme cards as sparse ("Theme fit Diffuse 30%"). Read-only diagnosis (scripts/_verify_least_fit.mts, real verbatims): the model is correctly tied (LEAST field has its own 6 themes, MOST has 7) — but ~45% of answers are non-substantive ("Nothing"/"N/A"/"all good"), and both the mining corpus and the fit denominator counted them. Over substantive answers the SAME themes cover ~48%. So the "sparseness" is a denominator artifact, not a bad model.

PART 1 (this commit): TextMineModule.prepareCorpus filters mining texts with scoreUsefulness (new lib/usefulness.ts, v1 = isSubstantiveText) instead of length>0 — the AI now discovers themes from real feedback, and the sample-fit denominator stops counting junk. lib/usefulness.ts is the single, VERSIONED, swappable source of truth (v2 later = an LLM "actionable feedback?" judgment); USEFULNESS_VERSION + substantiveFieldsFor() are the ingest/backfill entry points for the stored per-comment flag that parts 2-4 build on. Measured on 12K real LEAST verbatims: full isSubstantiveText = 54.6% substantive; a SQL word-count>=4 proxy lands within ~1pt (55.3%), but the decision (owner) is to STORE the JS-scored flag so the definition is canonical everywhere. Design for the remaining parts (stored flag at ingest, substantive theme-fit denominator with hover-both, dataset-level substantive % in the top bar, backfill) captured in memory project_substantive_usefulness_flag.md. TSC clean.

## 2026-07-14 — Substantive flag, part 2 (foundation): stored per-comment flag at ingest

WHY: parts 2-4 (substantive-aware theme-fit denominator, dataset-level "% substantive", clouds/entities/Ask Ana lens) all need a cheap, consistent per-comment substantive signal. Computing the word rule per query risks JS↔SQL drift; the owner-confirmed design is to STORE the scorer's verdict at load time.

WHAT: sql/178 adds `dataset_rows_flat.substantive` (JSONB `{field: true}` map) + `substantive_v` (scorer version) and `apply_substantive_flags(p_dataset_id, p_items)` — a batched writer mirroring apply_taxonomy_verdicts (service-role, id paired with dataset_id, writes only the two columns so the `UPDATE OF data` tsv trigger stays silent). `lib/usefulness.stampRowSubstantive` scores every non-`_` field of a row and stamps USEFULNESS_VERSION; it is wired into ALL 12 dataset_rows_flat insert paths (raw upload, sync, studies/bots/townhall analyze, substack/reddit/regulations/social, reviewSync/redditSync/recordings mirror). Design choice: the flag is a PURE function of `data` (no schema needed at ingest — solves the raw-upload chicken-and-egg — and robust to a field later being re-classified open-ended, since its key is already stored; consumers scope to the specific open-ended field key so extra keys are inert).

BACKFILL: `scripts/_backfill-substantive.mts` (untracked) — TEST by default, `--prod` to promote; idempotent + version-aware (only NULL/stale `substantive_v`); keysets on the unique `id` via idx_drf_id_keyset (NOT row_index, which repeats on dedup/upsert Google-review sets and skipped 35 rows at page boundaries on the first pass). TEST run: 398,234 rows scored, 0 nulls remaining, per-field maps verified (survey MOST/LEAST keyed independently; empty `{}` where the comment was blank). PROD backfill deferred — awaiting owner go-ahead (runs after `npm run migrate sql/178` at push time).

TESTS: tests/unit/usefulness.test.ts (5) — scorer, substantiveFieldsFor, stampRowSubstantive (version + row preservation incl. dedup_key). tsc clean, lint 0 delta.

## 2026-07-14 — Substantive flag, part 3: substantive theme-fit strip + header "% substantive"

WHY: the strip's "Theme fit Diffuse 30%" led with `inThemes / all-non-empty`, counting "N/A"/"Nothing" answers in the denominator; the owner-confirmed fix leads with the substantive fraction and reveals the all number on hover.

WHAT: sql/179 gives the counting RPCs a substantive twin. `sampled_signal_counts` (>50K path) keeps its signature and now also returns `records_substantive` + `union_substantive`; `count_nonempty_rows` + `count_theme_matches` (exact <=50K path) gain a trailing `p_substantive_only boolean DEFAULT false` (DROP+CREATE, but every existing 3/4-arg call still resolves via defaults — pre-179 code + post-179 db keep working). BOTH numerator and denominator gate on the stored substantive map — a denominator-only shortcut overstates fit ~6pts. signalStats returns themeFitPctSubstantive/Band + substantiveRecords/inThemesSubstantive; the cache freshness guard treats pre-179 entries (missing the field) as stale so they self-heal. DatasetMetricStrip leads with the substantive %, band, and bar; hover shows "X% of SUBSTANTIVE … Y% of ALL …". DatasetHeader shows a filter-aware "N% substantive" next to the row count, computed client-side in DatasetShell over the loaded rows (isSubstantiveText across open-ended fields).

VERIFIED end-to-end on TEST: Carrabba's GSS (origin case) all-fit 21% (7,480/35,606) → substantive-fit 34% (6,554/19,134) via the sampled path — 46% of the field's answers were non-answers, so the "Diffuse" reading was a denominator artifact; Darden 92%→94%, Ruth's Chris 70%→73% (exact path). Both RPC families spot-checked directly. Tests: signalStats + sampledSignalCounts fixtures extended (sampled twin asserted); full suite 1516 green, tsc clean, lint 0 new. sql/179 TEST-applied, PROD-migrate on push. Untracked verify: scripts/_verify_substantive_fit.mts.

## 2026-07-14 — Substantive flag, part 4: shared Dataset "About" popover

WHY: the top-bar keeps only the one headline "% substantive"; the rich per-dataset stats (fill-rate + % substantive per verbatim field, fields by type, rating fields with n, date range, last synced) needed a home. Owner idea: ONE shared popover, two triggers.

WHAT: components/analyze/DatasetAboutPopover.tsx — trigger-agnostic (caller owns open + anchor rect), fixed-position mini-table + click-away, fetches GET /api/datasets/[id]/about once per open. Wired to (a) an (i) button on each /analyze DatasetCard and (b) a hover on the row-count cluster in the analyze banner (DatasetHeader — the dataset name isn't a visible banner element, so the stats cluster is the natural anchor). The /about route (org-gated, read-only) assembles rows/source/lastSynced/fieldsByType/dateRange/ratingFields from stored schema_config + analytics.fieldSummaries, and per open-ended field its fill + substantive counts from ONE sampled_signal_counts pass with NO themes (exact <=50K, deterministic sample above — never full-scans). Verified on TEST BFG_GuestLens: MOST 91% filled/63% substantive, LEAST 66%/52% (matches the exact counts). Full suite 1516 green, tsc clean, lint 0 new. Untracked verify: scripts/_verify_about.mts.

## 2026-07-14 — Substantive flag, part 5: lens on word clouds + Ask Ana (entities/dimensions scoped)

WHY: extend the substantive default-lens beyond mining + theme-fit to the remaining text-insight surfaces.

WHAT (done — the clean client/on-the-fly surfaces): (1) WordCloud builds perRowText AND the denominator from substantive rows only (client-side isSubstantiveText over getRowText) so term frequencies/percentages read over real feedback; the cloud header notes "over N substantive of M answered" (never silently dropped). (2) loadAnaSample (Ask Ana + ad-hoc reports) partitions substantive-first before the shuffle/slice when the sample truncates, so the model reasons over real answers; a context note records the substantive share. Both mirror the stored per-comment flag semantics (isSubstantiveText over the row's non-_ fields) so no query/shape change was needed.

SCOPED (not built — server-side cached/precomputed aggregates): Entities and Dimensions. Entity mention_count is cached on entity_catalog keyed by row total (count_entity_terms/sampled_count_entity_terms); the taxonomy rollup is stored at classify time. A substantive lens needs a migration gating those RPCs on the substantive map + a mention_count cache-bust (entities), or a re-classify/re-store backfill across all datasets (dimensions) — each changes stored counts platform-wide and warrants a focused pass with owner QC, not a rushed fold-in. Captured in ANALYTICS.md.

Full suite 1516 green, tsc clean, lint 0 new. Untracked verify: scripts/_verify_substantive_fit.mts, scripts/_verify_about.mts.

## 2026-07-14 — Substantive flag, part 6: Dimensions "% tagged" substantive denominator

WHY (owner, after thinking through entities/dimensions): on these surfaces the substantive lens is a DENOMINATOR fix, not a count fix — a "N/A" answer never classifies into a dimension, so dimension counts are already clean; what's diluted is the "% tagged" rate (withSignal / rowsWithText), which counts non-answers in the denominator. Owner: "do the dimension denominator fix."

WHAT: sql/180 dataset_rows_with_substantive_count(dataset, fields) = rows with a substantive flag (sql/178) in ANY selected field — the substantive twin of dataset_rows_with_text_count. Stored as StoredTaxonomyField.rowsSubstantive beside rowsWithText at classify time (updateStoredTaxonomyRollup), returned by the taxonomy GET (live-RPC fallback for pre-fix entries). TaxonomyModule's header "% tagged" now divides by rowsSubstantive, labeled "% of substantive tagged", capped at 100% (a rare one-word answer can carry a signal yet not be substantive), with a tooltip. DENOMINATOR-ONLY — the stored rollup/counts are untouched, so NO re-classify was needed (the heavier path I'd first scoped). Entities stay scoped (lower value — raw mention tiles already clean; only prevalence % would benefit, and it needs a mention_count cache-bust).

VERIFIED TEST: Carrabba's GSS survey substantive denominator over MOST+LEAST = 37,172 of 56,117 rows (vs a larger all-non-empty denominator) → the rate rises honestly. Review datasets (Ruth's Chris) where withSignal ~ all rows pin at the 100% cap (nearly all substantive reviews are tagged — honest). Full suite 1516 green (added the new RPC to the classifyPendingRows mock), tsc clean, lint 0 new. sql/180 TEST-applied, PROD-migrate on push. Untracked verify: scripts/_verify_dim_denom.mts.

## 2026-07-14 — Substantive flag: consistent word-rule note on every substantive count

WHY (owner): every place that shows a substantive-scoped number should state the word threshold, not just "N/A"/"Nothing" examples. WHAT: one canonical string SUBSTANTIVE_RULE_NOTE in lib/usefulness.ts ("Substantive = real feedback: 5+ words, or 4+ words of natural sentence text. Shorter one-word and 'Nothing'/'N/A' answers are excluded") reused across all six substantive surfaces — TextMine Comments tile (deduped from its inline copy), theme-fit strip, header "% substantive", About popover per-field, word cloud, Dimensions "% tagged". Single source of truth so the rule can't drift. tsc clean, lint 0 new, 1516 tests green.

## 2026-07-14 — Substantive flag: two-count model (rows + substantive comments)

WHY (owner): "like we ignore blanks we should ignore non-substantive — so only two counts: total rows and total (substantive) comments." A comment IS substantive, or it's a blank; there is no separate all-non-empty count. Every %/prevalence/fit divides by the single substantive comment base.

WHAT (this commit — the headline): DatasetMetricStrip now shows ONE comment count (= substantive) and a single Theme fit (matches ÷ substantive comments); dropped the "signals" number and the dual "all vs substantive"/hover-both display + the "of substantive" qualifier. DatasetHeader dropped the separate "% substantive" chip (comments now live on the strip; the header carries the other reference count, rows) — and the now-orphaned filter-aware substantivePct plumbing (DatasetShell useMemo + prop + isSubstantiveText import) was removed. Pre-sql/179 caches fall back to the all-based numbers so nothing blanks during rollout. tsc clean, lint 0 new, 1516 green.

REMAINING (careful next step, NOT yet done): per-theme prevalence on the two-count base — TextMine overview/CompareBars, Charts theme bars, Statistics — requires flipping the theme-count NUMERATOR to substantive (count_theme_matches p_substantive_only, sql/179 — shared by the /theme-counts route + fetchServerThemeCounts) IN LOCKSTEP with the denominators (totalResp/total/groupTotal → substantive count), then re-verifying the significance tests. Comments tile (totalResp) + About popover per-field + decks/exports inherit the same base. Scoped for a focused pass with tests.

---

## 2026-07-14 — Substantive flag pt7: per-theme PREVALENCE on the two-count base (Charts/TextMine/decks) + sql/181

WHY (owner, two-count model continued): every theme prevalence % — not just the strip's single Theme fit — must divide by substantive comments, with the theme NUMERATOR gated to substantive in LOCKSTEP (a full-match numerator over a substantive denominator overstates each theme). "When we sample/report, ignore non-answers like blanks."

WHAT:
- **sql/181** — `sampled_signal_counts` now also returns `theme_counts_substantive` (per-theme hit-AND-substantive), the per-theme prevalence numerator on the >50K sampled path (sql/179 only added the union). Same signature (CREATE OR REPLACE), old callers ignore the key. TEST-applied.
- **`/theme-counts` route** (shared by Charts bars + TextMine server counts): numerator = `count_theme_matches(p_substantive_only)` (exact) / `perThemeSubstantive` (sampled); denominator = `count_nonempty_rows(...,substantive)` / `recordsSubstantivePerField`. `totalNonEmpty` is now the substantive comment total. `lib/sampledSignalCounts` gained `perThemeSubstantive`.
- **TextMine**: `totalResp` + Compare `groupTotal`/`totalRows`/`unclassified` + theme match counts → substantive (isSubstantiveText per field, matching the SQL map); CompareBars `sigTest` z-test now runs num+denom on the same base. Comments tile shows the substantive count (rule in tooltip); dropped the "% substantive" line + the orphaned `substantiveShare` memo.
- **Charts**: `__themes__` denominator = substantive comment total from `/theme-counts` (filter-aware), was `analytics.totalRows`.
- **Statistics**: NO change — themes/dimensions are only GROUPING variables for chi-square/t-test/ANOVA (group totals = classified counts, not an all-non-empty base); dimension counts already clean.
- **About popover** per open-ended field: shows the substantive **comment count**, not "% substantive".
- **Decks/exports**: `export/pptx` `computeFieldThemes`/`computeCanonicalThemes` filter rows with `isSubstantiveText` (num+denom flip together); "N comments" fallbacks + `themeSignals.buildThemeSignals` docs filter → substantive; About-This-Report methodology line states the base. Carve-outs stay full: raw verbatim quote lists, rating averages, response/completion scope counts, Survey Overview "With comments" (engagement). `projectCompare` NOT flipped (counts assembled upstream per source — a denominator-only flip would inflate; deferred).

VERIFY: fresh tsc clean, 1517 tests green (+1 themeSignals substantive-exclusion), lint 0 new. End-to-end on Carrabba's GSS (56K, both paths) via `scripts/_verify_deck_substantive.mts` (full-scan) + `_verify_theme_counts_route.mts` (sampled RPC): 35.6K non-empty → 19.1K substantive (46% non-answers); Service & Staff Issues 7%→12%, Food Quality 6%→10%, Pacing 5%→8%; numerator ⊆ denominator throughout, both paths agree within sampling noise.

OPEN QUESTIONS resolved (owner, AskUserQuestion): (a) "signals" stays dropped from the strip/overview; (b) decks use substantive for insight numbers, full denominator for raw dumps/ratings.

DEPLOY (on push): `npm run migrate sql/178` `179` `180` `181` in order, then push; after deploy run `scripts/_backfill-substantive.mts --prod`.

---

## 2026-07-14 — Prominent "Sampled view" banner across analyze tabs

WHY (owner): "we should also have a prominent indicator when a dataset being viewed is sampled not full." A >50K dataset loads a deterministic 50K sample client-side; the only cues were scattered per-surface chips, easy to miss — a sample could be mistaken for the whole dataset.

WHAT: `DatasetShell` (wraps every analyze tab, already reads RowsContext sampled/sampledCount/totalRows) renders a blue/informational banner under the header when `sampled && totalRows > sampledCount`: "Analyzing a representative N of M rows (P%). Charts, statistics and theme counts are estimates scaled to the full dataset." Distinct from the orange Filtered bar; persists on all tabs. fresh tsc clean, DatasetShell lint 0 delta. (Not browser-verified — trivial conditional JSX mirroring the existing filter-chips bar.)

---

## 2026-07-14 — Substantive: banner label + About popover shows the rate %

WHY (owner, viewing the mined banner): "should this reference substantive? where do we show the rate of substantive as a % of total comments?" The banner's count was already substantive (totalResp flipped in pt7) but labeled "open-ended"; and the two-count model had dropped the substantive % everywhere, so the "46% non-answers" signal was invisible.

WHAT: (1) TextMine AI-mined banner: "AI analysis of N **substantive** comments" (+ rule tooltip). (2) About popover per open-ended field now shows the substantive count AND "(N% of answered)" — substantive/filled — restoring the rate in the designated rich-stats home without adding a third headline number to the strip. fresh tsc clean, lint 0 delta.

---

## 2026-07-14 — Consolidate sampled/mined chrome; %-substantive in mined banner

WHY (owner, screen real estate): the full-width "Sampled view" banner + the AI-mined banner stacked two extra rows above the Themes overview. "Collapse the sample banner into the strip below it… more efficient screen real estate." Also asked to show the substantive % in the mined banner.

WHAT: (1) Sampled indicator is now a compact "◱ Sampled P% · N/M" chip leading the shared metric-strip row (was a full-width blue banner) — one fewer row on every tab, same signal + tooltip. (2) AI-mined banner slimmed to a single line and now reads "Themes AI-mined from N substantive comments (P% of answered · K sampled)". fresh tsc clean, lint 0 delta.

NOTE (flagged to owner, NOT yet fixed): the metric strip can lag the TEXT pill — screenshot showed strip "~30,308 comments" (= Liked Most's 27,004 scaled) while the pill/banner were on Liked Least (17,048). The strip follows 'dataset-active-field-changed'; suspect the per-field-swap effect early-returns before dispatching on some transitions. Needs a repro + fix before the strip's comment count can be treated as authoritative for the active question.

---

## 2026-07-14 — Fix strip-lag (wrong question's counts); mined banner = exact sample counts

WHY (owner): saw the strip at "~30,308 comments" (Liked Most's 27,004 scaled) while the pill/banner were on Liked Least (17,048), and objected to the "~" ("it is a real number on a sample"). Two root causes: (1) the strip wasn't following the pill; (2) the mined banner didn't show the total/% and its number felt approximate.

WHAT: (1) BUG FIX — TextMine dispatches 'dataset-active-field-changed' at the TOP of the per-field-swap effect now, before its early returns (`!k` / no-binding model / already-shown), so the strip can't get stuck on the previous question. Pure event emit, no setState. (2) Mined banner now reads "Themes AI-mined from N substantive (P%) of M comments · K-row sample" — EXACT sample counts (totalResp/answeredResp/substPct), no "~", tooltip notes they're exact-within-sample not extrapolated. fresh tsc clean, lint 0 delta.

STILL OPEN: the metric STRIP still shows the scaled full-dataset estimate with "~" (that's a different number from the banner's exact-sample count — deliberately, for the "dataset scale" framing on listing cards etc.). Switching the strip to exact-sample would need signalStats to expose raw sample counts (additive, but touches cached shape + tests + listing-card framing) — flagged to owner as a framing decision. Perf: cold Liked-Least load is slow because the non-active per-field signal-stats + the sampled theme-counts (10× 5K-row RPC pages over the 50K sample) compute fresh; both cache client-side after.

---

## 2026-07-14 — Model A: the 50K sample IS the view (exact sample counts, no scaling, no "~")

WHY (owner): "after sampling rows everything else should be precise numbers on the sample… at this point we're looking at the sample as our view so everything should be based on the 50K." The strip was scaling the exact sample count up to the full dataset and marking it "~", which (a) added false precision and (b) disagreed with the client's sample-based counts (mined banner/distribution). Owner confirmed Model A over Model B (exact-full headline).

WHAT: signalStats (strip) + /theme-counts route (Charts/TextMine theme bars) stop calling scaleSampledCount on the count paths — they now return the EXACT counts of the deterministic 50K sample. Theme-fit % is a ratio (unchanged). DatasetMetricStrip drops the "~" on comment/theme counts (rating avg keeps its ties-to-Google "~"). Result: strip == mined banner (both ~17,048 substantive on Carrabba's Least, vs the old scaled ~19,134/30,308). signalStats sampled test updated to exact sample numbers. fresh tsc clean, lint 0 delta, 18 targeted tests green.

NEXT (owner asked): decks/PDFs should use the same approach. The deck already counts exactly over its loaded ≤50K rows (no scaling) — but it loads the FIRST 50K (.range in id order), not the deterministic hash-based 50K the app uses, so deck numbers won't match the app. Fix = load the same deterministic sample in the deck's row loader.

---

## 2026-07-14 — Decks use the app's deterministic sample (Model A consistency)

WHY (owner): "when we generate the ppt and pdfs etc. make sure we use this new approach as well." The deck already counted exactly over its loaded rows (no scaling), but above 50K it loaded the FIRST 50K by row_index — a different sample than the app's deterministic hash 50K — so deck numbers wouldn't match what the user saw.

WHAT: export/pptx row loader now draws the SAME deterministic sample via pageSampledRows (lib/bulkRowSample, sql/160 idx_drf_sample hash order) for >50K datasets — proportional per-member shares for collections, sequential first-N fallback if the RPC is absent. ≤50K still loads every row. Verified on Carrabba's: deck deterministic sample = exactly 17,048 substantive on Liked Least, identical to the app strip/mined banner. fresh tsc clean, lint 0 delta. Untracked KEEP: scripts/_verify_deck_sample_match.mts.

---

## 2026-07-14 — HTML export + signals deck also use the app's deterministic sample

WHY (owner): "so the pdf will be exactly the same sample?" — the PPTX matched, but the HTML export (the HTML→PDF path) loaded only the first 10K/30K rows by row_index, and the reddit/substack signals deck the first 10K — different, smaller samples that disagreed with the app.

WHAT: export/html and export/signals-pptx now load the SAME deterministic 50K sample via pageSampledRows (sql/160), matching export/pptx and the app view — ≤50K loads every row, >50K the hash sample (proportional per-member shares for collections, sequential fallback). So every export format (and PDFs made from them) reports the identical numbers. The Ask-Ana ad-hoc report PDF stays on its own substantive-first loadAnaSample mechanism (narrative, not code-computed counts). fresh tsc clean, lint 0 delta.

---

## 2026-07-14 — Ask-Ana ad-hoc report on the same 50K sample (Model A denominators)

WHY (owner): "put the ana ad-hoc report on the same 50K sample too." Its rows already came from the deterministic idx_drf_sample population (sql/167), but its denominators were scaled to the full dataset (× totalDatasetRows, with "~").

WHAT: loadAnaSample now reports counts over the ≤50K sample (sampleBase = min(rowCount, 50K)), never scaled to full — totalDatasetRows stays as context. New exactSampleCounts option (ad-hoc report sets it) scans the WHOLE 50K sample so the report's counts are EXACT over the sample (no "~"); the interactive chat keeps its fast budget scan but also reports sample-based numbers (ask-ana note wording updated to say "in the analyzed 50K sample"). Verified on Carrabba's: unfiltered report totalFiltered = 50,000 exact, 200 rows drawn, 56,117 shown as context. Tests updated (2 expectations → sample base) + 1 new exactSampleCounts test; 1518 green, tsc clean, lint 0. Untracked KEEP: scripts/_verify_ana_modelA.mts.

---

## 2026-07-14 — Collapse mined banner into strip; fix stale scaled strip cache

WHY (owner, screenshot): the lower AI-mined banner is unnecessary, and the strip (30,308) disagreed with the banner (27,004 substantive). Root cause of the mismatch: 30,308 = 27,004 × 56117/50000 — the strip served a STALE v1 signal_stats cache (Model A stopped scaling, but the cache keyed on theme-hash+row_count didn't invalidate).

WHAT: (1) signalStats cache gains stats_v (2 = Model A); v1 entries recompute on next read → strip now shows the exact sample substantive count, matching the banner. (2) The strip's comment metric now carries the "% substantive" (substantive ÷ answered) the mined banner had: "N comments · P% of M answered". (3) The AI-mined banner is REMOVED from TextMine (its info folded into the strip; the "AI Mined" pill keeps provenance). signalStats test fixtures get stats_v:2; 1518 green, tsc clean, lint 0 delta.

---

## 2026-07-14 — Model A / substantive-flag: open TODOs recorded

Consolidating the remaining work (owner asked to record it) after the substantive-flag + Model A "the 50K sample is the view" pass (20 commits local, unpushed):

1. PUSH + DEPLOY (owner-gated): migrate sql/178→179→180→181, push, then scripts/_backfill-substantive.mts --prod, then re-run test:rls/test:egress.
2. Owner browser-QC on a >50K dataset: strip == every other surface (exact sample counts), collapsed layout, theme bars + PPTX + HTML export + Ask-Ana report all agree; strip signal_stats cache self-heals (stats_v=2).
3. REMAINING (NOT DONE, bigger, owner-gated): non-theme Charts aggregates still scale to full on >50K — categorical field distributions, numeric-stats counts (/aggregate sampled_* + scaleSampledCount), Dimensions sub-counts, Entity mention counts. Full Model-A consistency = drop scaling on those COUNT surfaces. Needs RPC/route edits + tests.
4. projectCompare (multi-source project-report matrix) deferred — counts assembled upstream per source; denom-only flip inflates.
5. Statistics tab: no change (grouping vars, not prevalence denominators).

Detail in memory [[project_substantive_usefulness_flag]] "OPEN TODOs".

---

## Charts calculation-correctness sweep (2026-07-14)

**WHY:** A live Average-by-category bar (Visit Type x Rating) rendered "No groups found." Root cause: the field-type classifier types a field numeric via Number(v.trim()), but the aggregate RPCs filtered values with a stricter, un-trimmed regex ^-?[0-9]+.?[0-9]*$. A numeric field whose cells carry a stray leading space (or .5/1e3/5.) passes the classifier into the VALUE slot but every value is rejected by SQL -> zero groups (and blank Statistics/Distribution too). A full sweep of all chart types (3 parallel audit agents + verification) found four more real issues; one reported issue was a false alarm.

**Fixed:**
1. Numeric parity (root cause). sql/182 centralizes numeric detection in drf_numeric_ok/drf_to_numeric (btrim + tolerant pattern + cast) and routes group_numeric_stats / numeric_field_stats / date_series metric + their sampled twins through them. Client rows-fallbacks use matching toNumericOrNull (lib/numericValue.ts) instead of parseFloat (which mis-read 1,000 -> 1). Verified end-to-end on TEST db. Needs sql/182 migrated to prod to fix the live chart.
2. Percentage bar denominator divided by the shown top-30 sum, not the true total -- inflated every % on clipped charts. Now divides by all categories.
3. Time-series TZ bucketing (bucketKey) parsed date-only ISO as UTC midnight -> prev-day misbucket in US zones, disagreeing with SQL ::date. Now local calendar parse.
4. Time-series false-dip: a metric bucket with no numeric value plotted 0 instead of a gap; now null.

**False alarm (verified, no change):** sampled crosstab space-mangling -- the join/split already use a NUL (U+0000) delimiter, which Postgres text cannot contain. The audit agents rendered the invisible NUL as a space and misread it.

**Deferred (owner-gated):** plain Count/Percentage bar + no-split Distribution read whole-dataset fieldSummaries and ignore active filters (stacked/Average bars are filter-aware via the aggregate API + rowIds). Consistency needs an async refactor of the instant-render path + a filter-aware histogram op -- surfaced, not silently changed.

Tests: tests/unit/numericValue.test.ts, tests/unit/timeBucket.test.ts. typecheck + full suite green.

---

## Charts: REAL root cause of "No groups found" + filter-aware summary charts (2026-07-14, later)

**WHY:** Verified against the REAL Carrabba's GSS dataset (56,117 rows, in TEST). The reported failure was NOT the whitespace/regex issue: "Rating" is a categorical satisfaction question with a remapping (Highly Satisfied=5 ... =1, scoreField:true), surfaced in the numeric picker as a virtual `__mapped_<field>__` field that exists only client-side (enrichRows) — not a key in the stored JSONB. BarAggInner sent group_stats with that virtual key to SQL, which read NULL for every row → zero groups. Proven in SQL: group_numeric_stats with the mapped key = 0 rows; key present in 0 rows.

**Fixed:**
1. BarAggInner detects a virtual value/category field (startsWith `__`) and routes through the enriched client rows (like BarStackedInner) instead of SQL group_stats — also makes the Average bar filter-aware. Verified over real data: Dine-In 4.63 / Carry Out 4.37 / Delivery 4.08 (full + 50K sample agree to 0.01).
2. Filter-awareness for summary charts (Count/% bar, no-split Distribution, treemap, bubbles, waterfall): recomputeFilteredSummaries recounts those fields from the filtered rows and overrides enrichedAnalytics.fieldSummaries. Reuses whole-dataset histogram bin edges; COUNT surfaces scaled by totalRows/sampledCount to match scaleSampledCount, means/extents unscaled. Closes the last logic inconsistency — whole tab agrees under a filter.

sql/182 numeric-parity fix (prior entry) stays as a real robustness fix for genuinely messy numeric columns. Test: tests/unit/filteredSummaries.test.ts. typecheck + 1525 tests + lint:ci green. Still LOCAL; sql/182 migrate on push.

---

## ChartsModule react-hooks warnings: 17 -> 0 (deliberate, verified) (2026-07-14, later)

**WHY:** Owner pushed back that touch-it-fix-it (line-scoped) never clears the react-hooks backlog since we touch files, not the stale functions. Agreed: for that class the incremental rule isn't a clearing mechanism (unlike no-explicit-any, which is runtime-invariant and got a mechanical burn-down) — it needs a deliberate, browser-verified pass because a wrong hook "fix" changes runtime (the prior Statistics-tab infinite loop). Did that pass for ChartsModule.

**Result:** 17 -> 0. Two REAL fixes: (1) PlotlyChart cleanup captures ref.current in the effect body, not the cleanup (285). (2) ScoreDriver regression effect was missing rows/schema/themeModel/hasThemes from its deps — so the regression went STALE under an active filter; added them (safe: all stable/memoized, none set by the effect, so no loop). The regression-under-filter staleness is a genuine bug the warning surfaced. The other 15 are legitimate patterns the aggressive React-19 rule over-flags — external-data-sync setState (fetch -> state), stable string-key dep proxies (cacheKey/filterKey/themesSig/_filterIdSig, where listing the churny object would infinite-loop), session restore/init, and the deliberate module-level _enrichCtx render bridge + _chartDrag drop-handler state — each cleared with a scoped disable + concrete reason (zero runtime change).

lint:ci ceiling 269 -> 252 (exactly the 17 cleared; new files add 0). Verification: new jsdom render test tests/unit/chartsEffectStability.test.tsx mounts ChartsModule with LOADED rows + an ACTIVE FILTER (the paths touched) and asserts it settles with no "Maximum update depth" loop. typecheck + 1526 tests green. (Full in-app Score-Driver QC left to the owner — a dev server was already up on :3000; didn't disrupt it / risk a cookie-mint session reset.)

---

## Statistics: Logistic Regression with auto collinearity pruning (2026-07-15)

**WHY:** Owner asked for logistic regression in the Statistics panel, done intelligently so it discards highly correlated predictors. Regression already lived in two spots (Charts Score Driver = themes->score OLS; Statistics RegressionPanel = numeric multiple-OLS); built logistic into the latter.

lib/statsUtils: logisticRegression(y 0/1, X, names) via IRLS/Newton (reuses invertMatrix) -> odds ratios + 95% CI, Wald z/p, McFadden pseudo-R2, LR chi-sq vs intercept-only, AIC, convergence/separation flags. vif() (=1/(1-R2_j)) + pruneCollinear() iteratively drops the highest-VIF predictor until all <=10.

StatsModule RegressionPanel: Linear/Logistic toggle (persisted). Logistic binarizes the outcome (auto if 2-valued, else adjustable cutoff defaulting to median; both classes need >=3), runs pruneCollinear then logisticRegression, renders odds-ratio coefficients + pseudo-R2/LR/AIC fit card + dropped-collinear note + plain-language BottomLine; OLS residual plots hidden in logistic mode.

Verified: 8 unit tests (intercept=logit(mean y), effect sign+convergence, separation flag, no-effect OR~1, VIF=1/(1-r2), pruning). End-to-end pipeline on synthetic data with a collinear twin: VIF-4890 twin auto-dropped, true driver recovered (OR 2.34, beta 0.85 vs true 0.9, p<1e-4), noise non-sig (p=0.79). typecheck + 1534 tests green; StatsModule react-hooks warnings 33->33 (no new); lint ceiling unchanged (252).

Follow-up: the Linear/Logistic toggle sat after the "need 2 numeric fields" early-return, so it was hidden on datasets with <2 numeric fields. Moved the guard into the body so the toggle + mode always render; sidebar tip updated to "Linear (OLS) & logistic regression."

---

## Logistic on categorical fields + themes (2026-07-15)

**WHY:** Owner: the panel was numeric-only, but this survey has 1 numeric field (mapped Rating) and 15 Likert categoricals + 7 themes — so logistic looked broken ("need 2 numeric fields"). Logistic (and its predictors/outcome) must work on categorical fields AND themes.

lib/regressionDesign.ts (new): buildRegVars (numeric + categorical + theme candidates) + buildDesign (one-hot dummies default w/ modal reference, per-field ordinal opt-in, theme 0/1 keyword-match column via commentMatchesTheme, complete-case rows, outcome binarization: numeric/Likert top-box threshold | category level | theme mention). Extended for a Likert categorical outcome to top-box by remap threshold.

StatsModule: new self-contained LogisticPanel (own picker grouped Numeric/Categorical/Themes + per-cat ordinal toggle + outcome selector w/ binarization) rendered in logistic mode; RegressionPanel reverted to OLS-only + delegates. Wired catFields + effectiveThemeModel + themeSourceField from the parent. Default outcome = a rating/satisfaction field, top-box.

Verified end-to-end on the REAL Carrabba GSS (471 complete cases): top-box Rating ~ sub-scores(one-hot) + themes → converged, pseudoR2 0.52, sensible ORs (mentions Service & Staff Issues -> OR 0.05x; Value for Money=Fair -> 0.05x vs Excellent baseline). Design tests tests/unit/regressionDesign.test.ts (6). typecheck + 1540 tests green; StatsModule react-hooks warnings 33->33 (2 new disables); lint ceiling unchanged. Untracked harness scripts/_verify_logit.mts (KEEP).
