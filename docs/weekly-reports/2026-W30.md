# Weekly Governance Report — 2026-W30 (Week of Jul 20 to Jul 26)

**Generated**: 2026-07-20T08:02:14Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W29-devlog.md (editorial dev log — primary source)
**Spec-drift**: 0 drifted — see 2026-W30-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 49 commits, 1 author (Sanjay Patel), top-level dirs: app/ (580 file-touches), lib/ (312), sql/ (208), docs/ (188), tests/ (174), scripts/ (147), components/ (143)

### Narrative (from devlog)

**Ops / Infrastructure (Jul 13)**
- Prod diagnosed as I/O-bound: 785K [SCALE TEST] rows = 97% of DB (3.56 GB on a Micro 1 GB instance). Deleted in 16×50K keyset batches, confirmed 0 dependents first. Settings-page queries: 54 s → 0.05 ms post-delete.
- `VACUUM (FULL, ANALYZE) dataset_rows_flat`: heap 2,332 MB → 444 MB, indexes 697 MB → 187 MB, ~2.4 GB returned to the OS. Micro instance now comfortably holds the hot working set.
- Service worker no-op fetch handler fixed: public/sw.js controlled the whole origin with a no-op listener that blocked soft-nav RSC responses on tabs open across deploys. Removed; push/installability unaffected.

**Performance scaling — Briefs A–F + C Part 3 + E (Jul 13, sql/167–174)**
- All O(N) aggregate RPCs that 57014'd at 1 M rows replaced with deterministic 50K-sample keyset-paged twins: counts family (sql/169), theme-counts (sql/170), taxonomy family (sql/171), entity counts (sql/172), theme-card extras — cooccurrence/topical/dimensions (sql/173).
- Filter compliance sweep (Brief F): fixed silent cat-exclude inversion + daterange-filter-ignored bugs in Ask Ana (sql/167 sampled_filtered_rows). Swept all applyFilters consumers; found and fixed 2 more (StatsModule dimension tests missing rowIds, share/analytics bespoke filter copy drift).
- study_response_stats MV → per-study counter table + trigger (sql/174): O(all-history) scan per refresh retired to O(1) per response.
- Org-snapshot intra-org checkpoint resume: partial.json mid-table cursors prevent same-org infinite-retry on 300s Vercel kill.
- Performance impact: 1 M-row surfaces that previously timed out (57014) now complete in ~33–43 s sampled; keyset/exact paths stay 0.1–0.7 s.

**Agent Tiers — Phases 0–3 (Jul 14, sql/175–177)**
- Phase 0: four quality-floor defects fixed for ALL agents — D1 keyword-fallback no longer silences the KB, D2 chunk edits now re-embed, D3 editor save is atomic diff-upsert (never zero-knowledge window), D4 deep-crawl preserves hrefs + sitemap seeding + link-integrity guardrail.
- Phase 1: Standard vs Super Agents tier split (sql/175, capability column). Capability knob table resolves model, maxTokens, RAG chunks, history window, input cap, verbosity. Super turns logged as `chat_super` for cost separation.
- Phase 2: multi-query retrieval + near-dup dedup (cosine ≥ 0.95) + chunk budget + PDF/DOCX ingestion + resumable 300-page super crawl (sql/176) + weekly KB re-crawl cron with hash-diffing (sql/177) + liveContext adapter registry.
- Phase 3: in-turn tool loop (≤3 rounds, search_knowledge + fetch_page) + SSE streaming. Zero behavior change for existing consumers; PulseIQ deliberately kept on blocking JSON.

**Substantive/usefulness flag — 6 parts (Jul 14, sql/178–181)**
- Stored per-row `substantive` JSONB flag at ingest (sql/178), applied across all 12 insert paths.
- Two-count model: everywhere that showed "N non-empty" now shows "N substantive comments" — theme-fit strip, mined banner, About popover, word clouds, Dimensions "% tagged" denominator (sql/180), per-theme prevalence numerator (sql/181).
- Theme-mining corpus now excludes non-answers; fit denominator matches. Root cause of "Diffuse 30%" false-alarm confirmed: 46% of Carrabba's LEAST answers were "N/A"/"Nothing" — substantive fit corrected to 34% (vs apparent 21%).
- Model A: 50K sample is THE view — exact sample counts on strip/charts/exports, no scaling/tilde.

**Charts correctness sweep (Jul 14)**
- Numeric parity: sql/182 centralizes numeric detection (`drf_numeric_ok/drf_to_numeric`) — whitespace-tolerant, btrim'd, shared by all group_numeric_stats/numeric_field_stats twins and sampled counterparts.
- Real root cause of "No groups found": virtual `__mapped_<field>__` keys don't exist in JSONB; BarAggInner now routes those through enriched client rows.
- Filter-aware summary charts: Count/% bar + Distribution now recalculate from filtered rows; whole tab agrees under a filter.
- ChartsModule react-hooks: 17→0 (deliberate, browser-verified pass). Two real bugs surfaced and fixed (PlotlyChart cleanup ref, Score-Driver regression stale under filter).

**Statistics enhancements (Jul 15)**
- Logistic regression with IRLS/Newton, VIF-pruned collinearity, odds ratios, pseudo-R², McFadden, AIC.
- Categorical fields + themes as predictors/outcomes (one-hot/ordinal toggle, binarization).
- Ranked Likert auto-quant at schema level; dual-purpose single-entry picker (Charts + Stats).
- Key-Driver auto-insight (Pearson correlation ranking vs. rating target).

**Outlet Report rebuild — Phases 1–4 (Jul 15)**
- Per-outlet absolute KPI snapshot: rating distribution, 5★/detractor share, owner-response rate, recent trend, fleet position, absolute theme table (mentions/avg★/%≤3★/READ verdict).
- AI Action Plan: LLM-narrated priorities cached in `dataset_state.outlet_action_plans` (sql/183), lazy GET endpoint, real-verbatim-validated quotes.
- Print IS the export: print CSS hides nav, forces 4-across KPIs, paginated action plan. Verified: clean 1-page snapshot + action-plan pages.

**BYOK hardening (Jul 15)**
- Agent chat now resolves `resolveOrgAiConfig` per turn: off-mode refuses, BYO+Anthropic uses customer key, BYO+OpenAI falls back to platform key.
- claude-* model ID overrides stripped when resolution lands on non-Anthropic provider (live bug in recordings analysis path).
- Legacy PulseIQ language-switch calls gated too.

**Help agent "Sherpa" 🛟 — Units 1–6 (Jul 14–18, sql/184)**
- Platform-owned Help agent (slug `help-guide`, persona "Sherpa") with 21 curated KB articles (98 chunks), seeded via idempotent script.
- Authed /api/help/chat (same-origin only, two-tier rate limit, streams via handleChatTurn).
- Global HelpWidget in TopNav (floating 🛟, streaming, pageContext injection, 16px textarea).
- scrubHelpReply strips fabricated links/emails.
- help_feedback table (sql/184, RLS org-scoped) + thumbs-up/down widget.
- Symmetric two-assistant split: Help redirects data questions to Ask Ana; Ask Ana redirects how-to/nav questions to Sherpa.

**E2E CI gate (Jul 13)**
- `tests/e2e/smoke.spec.ts` self-contained suite (throwaway login, seeded dataset, teardown). CI `e2e` job runs against a production build; deploy job gates on it.
- **First-run finding**: Statistics tab ran an infinite React update loop (max update depth exceeded, main thread pegged 20 s+ — not reproducible in jsdom). Root cause: `themeSetForField()` returned a fresh object per call; `useMemo` wrapper had been stripped by a prior lint sweep. Fixed in StatsModule + ChartsModule with a "MUST be memoized" comment. Smoke dropped from timeout → 2.6 s.

**Code quality (Jul 13–16)**
- `no-explicit-any`: burned to 0 across all 22 remaining files (annotation-only, tsc-verified per file). Promoted to `error` in eslint.config.mjs. New `any` now fails CI outright.
- Lint ceiling: 346 → 252 (-94 warnings; primarily ChartsModule react-hooks + the final any-burn).
- npm audit: 0 vulnerabilities. postcss override (`$postcss ^8.5.10`) dedupes the nested Next.js copy; exceljs scoped uuid override.
- Deck PDF fonts embedded (DM Sans + Fraunces as data-URI woff2s); zero network at render.
- sql/182 transaction wrap (CI tx-guard fix).

### Risk signals

- **Single-author velocity**: 49 commits, 1 author — highest commit volume in recorded weeks. Bus factor risk unchanged.
- **CI red on push (Jul 14)**: 40-commit push hit two CI failures — sql/175 missing BEGIN/COMMIT (tx-guard) and the Statistics infinite-loop regression (e2e caught it). Both fixed same day; the e2e gate behaved exactly as designed. No user impact (deploy-behind-CI blocked the bad build).
- **Open perf fixes**: taxonomy-family O(N) scan (Brief C Part 3) COMPLETE this week; Brief E item 4 counter-table COMPLETE. Full §7 fix-queue closed. 1M PERF TEST dataset can now be deleted + VACUUMed.
- **Prod migration queue**: sql/166–184 are TEST-applied, awaiting push + prod migrate (per CLAUDE.md push policy — not authorized in this window). Most critical: sql/182 (numeric parity, live chart bug), sql/178–181 (substantive flag + backfill).

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 13.5 |
| Security | 8/10 | 15% | 12.0 |
| Dependencies | 9/10 | 15% | 13.5 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 8/10 | 15% | 12.0 |
| Documentation | 9/10 | 10% | 9.0 |
| Maintainability | 9/10 | 20% | 18.0 |
| **Total** | | **100%** | **86.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- None. 0 npm CVEs (`npm audit` clean). No hardcoded credentials found. `no-explicit-any` is now an error — no `any` in the codebase. No eval/exec with user input.

**MEDIUM**
- `lib/chatCore.ts:1857` — `service.from('agents').update({ last_session_at }).eq('id', bot.id)` without org_id pair. `bot.id` was loaded after proper auth gating, so cross-tenant mutation is not reachable; however, this violates the CLAUDE.md multi-tenancy invariant ("Service-role queries must pair id with org_id"). Low-blast-radius MEDIUM.
- `lib/chatCore.ts:309,489,582,710,726,757,1734,1962,2028` — INSERT/UPDATE operations on `bot_conversation_turns`, `agent_session_personas`, `agent_probe_responses` without explicit `.eq('org_id', ...)` WHERE clause. These are INSERT paths where org_id is embedded in the row payload (derived from the authenticated bot's org_id), not a filter-read vulnerability; the pattern is intentional and documented. Flagged for consistency with the multi-tenancy invariant's spirit.
- 252 `react-hooks/*` lint warnings — the remaining ratchet backlog. Each is a scoped disable with a concrete reason (external-data-sync effects, stable-key dep proxies, module-level render bridges). The Statistics infinite-loop event (W29 risk signal) shows this category can mask real bugs; the behavior-sensitive nature makes bulk-burn risky without per-file browser verification.
- `app/` directory: 552 files — navigability concern as the route tree grows. No circular deps detected, but the flat file count makes orientation harder for new contributors.

**LOW / informational**
- Test ratio: 159 test files / 1026 TypeScript source files = 0.155 — below the 0.5 target. Absolute test count grew 1337→1526 (+189) but source-file growth keeps the ratio low. The suite quality is high (e2e smoke in CI, all suites green), but many UI components lack direct unit tests.
- 0 `.claude/agents`, 0 `.claude/rules` defined — governance relies on CLAUDE.md + pre-commit hooks + 4 commands. Consistent with prior weeks; not blocking.
- `dataset_rows_flat` Micro-instance capacity: post-VACUUM heap at 444 MB, healthy for current 273K rows. First >200K paid dataset triggers the documented Micro→Standard upgrade path.
- Prod migration queue (sql/166–184) is the longest pending queue in recorded history: 19 migrations awaiting a push authorization. All are TEST-applied and verified; no data is at risk, but the gap between TEST and prod state is wide.

### Progression plan (top 3 to fix this week)

1. **Org-pair `lib/chatCore.ts:1857`** — add `.eq('org_id', bot.org_id)` to the `last_session_at` update; bot.org_id is available in scope. ~10 min fix, closes the multi-tenancy invariant gap in chatCore.
2. **Push authorization + prod migrate sql/166–184** — the 19 TEST-verified migrations include the live chart bug fix (sql/182 numeric parity), the substantive-flag backfill, and the Help agent (sql/184). Owner decision, but the queue depth warrants a deliberate push window.
3. **Delete + VACUUM the 1M [PERF TEST] dataset** — Brief E is complete; the 1M Outback×8 dataset (id `d1000000-…-001000`) on TEST is no longer needed and bloats the Micro-tier TEST DB (~2.6 GB). A `DELETE FROM dataset_rows_flat WHERE dataset_id = '...'` + `VACUUM ANALYZE` will free it and unblock env-gated suite perf.

## 3. Correlation: dev work → audit findings

- **`lib/chatCore.ts:1857` org-pair MEDIUM**: not in devlog — emerged from the BYOK audit sweep but the update call was not in scope of the bots-route fix from W29. Not addressed this week.
- **react-hooks warnings (252)**: ChartsModule cleared 17 in this window (deliberate, verified). StatsModule (33) deferred — the Statistics infinite-loop event elevated caution. Partially addressed.
- **Test ratio (0.155 LOW)**: +31 test files and +189 tests this week. The ratio improves but source-file growth dilutes it. The e2e gate addition is the most meaningful quality improvement. Partially addressed.
- **app/ file count (MEDIUM)**: No structural change. Not addressed.

## 4. Trend

| Week | Score | Delta |
|------|-------|-------|
| 2026-W27 | 83.0 / 100 | baseline |
| 2026-W28 | 79.0 / 100 | −4.0 (3 drifted specs, 14 moderate CVEs) |
| 2026-W29 | 80.5 / 100 | +1.5 (CVEs 14→7; lint 3,062→346) |
| **2026-W30** | **86.0 / 100** | **+5.5** |

W30 marks a new high, recovering past the W27 peak (83.0). The +5.5 reflects closure of all four W29 MEDIUM findings (SheetJS CDN vendored, bare-id bots route org-paired, 7 moderate CVEs → 0, 4 drifted specs cured), the addition of the e2e smoke suite to CI, and the `no-explicit-any` → error promotion. Security (+1, 7→8), Dependencies (+1, 8→9), Tests (+1, 7→8), and Documentation (+1, 8→9) all improved; Secrets, Structure, and Maintainability held at 9/8/9. The open MEDIUM (chatCore last_session_at, 252 react-hooks warnings) and LOW (test ratio, app/ file count) are the primary remaining drag on approaching 90+.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Review the spec-drift companion (2026-W30-spec-drift.md) — any spec debt?
- [ ] Check correlation section — are open findings being worked on?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
