# Weekly Governance Report — 2026-W39 (Week of Sep 14 to Sep 20)

**Generated**: 2026-09-21T06:00:00Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W38-devlog.md
**Spec-drift**: 10 drifted — see 2026-W39-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 29 commits, 2 authors (Sanjay Patel × 28, Claude Governance × 1), top-level dirs: app/, components/, lib/, sql/, tests/, docs/, .github/

### Narrative (from devlog)

**Security hardening (highest-volume week to date)**
- CodeQL first-run triage (`f9ec7b9`): Substack SSRF hardened → `safeFetch`; path-segment validation; least-privilege workflow token; prototype/ excluded from analysis
- CodeQL high backlog resolved (`f898d98`): fixed-point HTML strip + correct entity-decode order in `lib/htmlStrip`; `lib/clientId` switched to `crypto.randomUUID()` (removes predictable IDs); linear regexes replacing backtracking-vulnerable patterns; scheme-validated hrefs throughout
- Admin session policy landed (`3f74162`, `c1f9b7e`): MFA required for platform admins (optional for org admins); signed activity stamp enforces 30 min idle / 24 h max; harness tracked in scripts/
- Resource gate (`4cdf8e1`): `lib/auth/gate.ts` (`gateResourceForUser` / `gateResourceAccess`) for agents, datasets, collections, studies, campaigns, PulseIQ sessions, conversations — cross-org and non-existent return identical 404; single-tenant prompt guard in `lib/aiOrgGuard.ts`; egress write-path tests added
- DR tooling (`c50b04f`): gitleaks in CI (deploy waits on it), post-mortem template, DR audit script + restore-drill runbook

**SECURITY.md open items: 8 of 9 closed (`4ced49e`)**
- Items 5, 7, 11, 12 landed as code; 9 and 13 as tooling; 10 closed as stale; 6 re-scoped; 8 prepared for ratification (ratified separately as `3f74162`)
- SECURITY item 2 closed (`fcedd33`): Dependabot weekly + CodeQL in CI; no auto-merge (ENGINEERING.md §"no auto-merge" rationale)

**Dependency hygiene**
- CI audit gate (`c82f13e`): `scripts/audit-gate.ts` — fails on any high/critical ROOT advisory unless `audit-allowlist.json` has an unexpired entry; 8 unit tests pin the roots-vs-collateral logic; live: 2 allowlisted (`image-size`), pptxgenjs collateral
- Dependabot weekly (`fcedd33`): grouped minor+patch PRs on Monday 02:00 ET, majors individually, framework pins respected, preview builds gated off Dependabot PRs
- Next 16.3.5 + sharp + js-yaml + vitest pinned (`64dd315`): audit 8 → 2; pptxgenjs untouched (verified)
- Dependabot #37–#41 merged by hand (`3165400`, `f180588`, `47d15d2`, `2454908`, `cbc92bc`): 13 prod bumps applied; isomorphic-dompurify and puppeteer-core held with documented reasons; Plotly 2.35 → 4.0 (titlefont API replaced at its one call site; browser-verified)
- e2e smoke extended (`21aabd0`): real Plotly chart with asserted axis titles (Plotly 4 drops string titles silently; no unit test renders a chart); hardNavigate document-load assertion

**Coverage week: 6 batches, floors ratcheted to 49/38/53/50**
- Batches 1–6 added 251 test cases (2057 → 2308) covering dataforseo, statsUtils narratives, mcoLiveContext, studyDesignPptx, agentStudy, agentReadout, share/analytics route, projectReportLoad, entityDiscovery, recordingDeck, reviewSync, share route, outletReportPdf, socialTagging, taxonomyMapping, agentStudyHtml, safeFetch (SSRF guard)
- `fakeSupabase.ts` helper: in-memory client honoring eq/neq/in/gte/gt/lte/lt/is, order, range, limit, single/maybeSingle, count:exact, insert/update/upsert/delete — filter-honoring tests prove query shapes
- Floors ratcheted 40/30/42/41 → 49/38/53/50 (measured 50.04/39.98/54.18/51.37 on vitest 4.1.11)
- Coverage gate LIVE in CI (e965ab6)

**Structural fixes**
- Import cycles eliminated (`4655d1e`): `serviceHealth → serviceAlerts` lazy via `await import()`; `pickBrandColor` extracted to `components/survey/brandColor.ts` — madge: 0 runtime cycles (was 2)
- 189 `console.*` calls migrated to `lib/log` (`822eea2`); `no-console` promoted to ESLint error in `app/api` + `lib`
- Governance rubric made measurable (`410b7ce`): Documentation and Maintainability bands rewritten with concrete thresholds; Structure/Security checks pointed at actual repo paths

### Risk signals
- **10 spec docs drifted** — a wide security sweep (logging migration, CodeQL hardening, resource gate, dep bumps) touched files across BOTS, TOWNHALL, DATA_SOURCES, SURVEYS, CAMPAIGNS, SOCIAL, RECORDINGS, SEARCH, USAGE_ACCOUNTING, MCO_AGENT without spec updates. The changes are infrastructure/security-origin rather than behavioral feature changes, but the specs should be reviewed for accuracy.
- **36 of 102 `eslint-disable` directives lack reasons** — SimulatorClient.tsx (7 × `no-explicit-any`) and various bot/pulseiq/recordings pages (`react-hooks/exhaustive-deps`) were not annotated per touch-it-fix-it.
- **3 open Dependabot PRs** (dev-minor-patch, prod-minor-patch, vercel/sandbox) awaiting human merge.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 10/10 | 15% | 15.0 |
| Security | 9/10 | 15% | 13.5 |
| Dependencies | 8/10 | 15% | 12.0 |
| Structure | 9/10 | 10% | 9.0 |
| Tests | 7/10 | 15% | 10.5 |
| Documentation | 5/10 | 10% | 5.0 |
| Maintainability | 9/10 | 20% | 18.0 |
| **Total** | | **100%** | **83.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- **[Documentation]** 10 spec docs drifted in range: `BOTS.md`, `TOWNHALL.md`, `DATA_SOURCES.md`, `CAMPAIGNS.md`, `SURVEYS.md`, `SOCIAL.md`, `RECORDINGS.md`, `SEARCH.md`, `USAGE_ACCOUNTING.md`, `MCO_AGENT.md`. The trigger is the security sweep (SSRF hardening in substack/reddit maps to DATA_SOURCES, resource gate touches agent/townhall routes mapped to BOTS/TOWNHALL, logging migration touched nearly every module). The behavioral specs are likely accurate but should be reviewed for any security/API-facing changes (safeFetch routing, resource gate 404 behavior, crypto session IDs). Score driver: rubric ≥4 drifted = 4-5 band; comprehensive devlog + policy doc updates place this at 5.
- **[Maintainability]** 36 of 102 `eslint-disable` directives lack `-- reason` annotation: `app/admin/simulator/SimulatorClient.tsx:115–264` (7 × `no-explicit-any`), `app/bots/[id]/readout/ReadoutClient.tsx:52`, `app/bots/[id]/report/ReportClient.tsx:98`, `app/bots/[id]/entities/EntitiesClient.tsx:122`, and 5 others (`react-hooks/exhaustive-deps`). Per touch-it-fix-it, files edited in range should have had these annotated. Unscoped disables are the only gap from a 10/10 Maintainability score.

**MEDIUM**
- **[Documentation]** `SPEC.md` and `FEATURES.md` flagged by spec-drift as likely inventory-level drift (tool cannot map module drifts to inventory docs directly — a manual sweep is warranted if the module spec updates land).
- **[Tests]** Branches coverage floor at 38% (measured 39.98%) — still in the 6-7 band. Moving Tests into the 8-9 band requires ALL floors ≥50%; branches is the lagging dimension.
- **[Dependencies]** 3 open Dependabot PRs: `dependabot/npm_and_yarn/dev-minor-patch-3ac8c389d7`, `dependabot/npm_and_yarn/prod-minor-patch-d1766ca0cc`, `dependabot/npm_and_yarn/vercel/sandbox-3.3.0`. These are fix-available updates; open PRs are not CVE findings, but aging past two weeks is a Maintainability signal.

**LOW / Informational**
- CodeQL not a deploy gate by design (first-run backlog triage phase; wire it into the required-checks list after the alert queue is cleared)
- Secret scanning + push protection off on the public repo — flagged to owner in SECURITY.md item 2(b); no change made (owner decision)
- 7 modules >1500 lines: TextMineModule.tsx (4001), ChartsModule.tsx (3476), ReportClient.tsx (3263), StatsModule.tsx (3044), useSurveyEngine.ts (2721), SessionDetailClient.tsx (2361), chatCore.ts (2216) — behavior-sensitive to split; context-load risk for future reviewers
- 0 runtime import cycles (was 2; both fixed this week)
- 2308 test cases in 237 files (3.6 per coverage-surface file) — up from 2057/215 in W38
- Lint ceiling 167 (lowered from 172 in W38; all remaining warnings are `react-hooks/*` by design)
- 1 snapshot file (minimal maintenance burden)
- `audit-allowlist.json`: 2 entries, both `image-size` (phantom pptxgenjs dep), expiry 2027-03-01

### Progression plan (top 3 to fix this week)
1. **Spec sync pass for 10 drifted docs** — review BOTS.md, TOWNHALL.md, DATA_SOURCES.md, CAMPAIGNS.md, SURVEYS.md, SOCIAL.md, RECORDINGS.md, SEARCH.md, USAGE_ACCOUNTING.md, MCO_AGENT.md for accuracy after the security sweep (safeFetch routing, resource gate 404 behavior, crypto session IDs, no-console enforcement). Update any section impacted; batch commits can use `SKIP_SPEC_CHECK` where the change is documentation-only with a justification.
2. **Annotate unscoped `eslint-disable` directives** — `SimulatorClient.tsx` (7 × `no-explicit-any` → `as unknown as T` or scoped disable with reason), bot/pulseiq/recordings pages (`react-hooks/exhaustive-deps` → add `-- reason`). Lower lint ceiling if warnings drop.
3. **Merge open Dependabot PRs** (dev-minor-patch, prod-minor-patch, vercel/sandbox) — review each diff (the `@supabase/ssr` pattern from #38 applies), verify tsc + audit gate, merge.

## 3. Correlation: dev work → audit findings

| Finding | Status |
|---|---|
| W38 CRITICAL — `next` 16.3.1 RCE (CRITICAL advisory) | **Addressed** (`64dd315`: next → 16.3.5, audit 8 → 2) |
| W38 HIGH — `js-yaml` HIGH + `sharp` HIGH | **Addressed** (`64dd315`) |
| W38 progression #1 — Run `npm audit fix` | **Addressed** (`64dd315`) |
| W38 progression #2 — CI `npm audit --audit-level=high` gate | **Addressed** (`c82f13e`: `scripts/audit-gate.ts` with allowlist + expiry) |
| W38 progression #3 — Ratchet coverage floors | **Addressed** (`e965ab6`: 40/30/42/41 → 49/38/53/50 after coverage week) |
| W39 HIGH — 10 spec docs drifted | Not mentioned in devlog — collateral from security sweep commits |
| W39 HIGH — 36 unscoped `eslint-disable` directives | Not mentioned in devlog — accumulated across multiple commits touching these files |

## 4. Trend

| Category | W38 | W39 | Delta |
|---|---|---|---|
| Secrets | 10/10 | 10/10 | = |
| Security | 9/10 | 9/10 | = |
| Dependencies | 6/10 | 8/10 | +2 |
| Structure | 8/10 | 9/10 | +1 |
| Tests | 7/10 | 7/10 | = |
| Documentation | 9/10 | 5/10 | −4 |
| Maintainability | 9/10 | 9/10 | = |
| **Total** | **83.0** | **83.0** | **0.0** |

W38 score read from `docs/weekly-reports/2026-W38.md` (Total row: **83.0 / 100**).

Net 0.0 — the +3.0 in Dependencies and Structure (critical Next advisory patched, CI audit gate live, 2 import cycles fixed) exactly offsets the −4.0 in Documentation (10 drifted specs from the widest security-sweep week to date). The Documentation drop is a process signal, not a security regression: the specs themselves are likely still accurate, but the sweep triggered the automated drift counter on 10 modules. A spec-sync pass (Progression item 1) should recover Documentation to 7-8 next week.

**METHODOLOGY NOTE**: Documentation and Maintainability now run under the written rubric bands first established in `410b7ce` (Sep 14) and scored for the first time in W38. W39 is the second run under these bands, so the Documentation drop from 9 to 5 is a real measurement difference (0 drift → 10 drift, both under the same rubric), not a methodology change.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Review the spec-drift companion (2026-W39-spec-drift.md) — any spec debt?
- [ ] Check correlation section — are open findings being worked on?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
