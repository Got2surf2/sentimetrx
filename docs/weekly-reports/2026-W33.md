# Weekly Governance Report — 2026-W33 (Week of Mon 03 Aug to Sun 09 Aug)

**Generated**: 2026-08-10T08:10:03Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W32-devlog.md
**Spec-drift**: 1 drifted (`docs/TESTING.md`) — see 2026-W33-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 8 commits, 1 author (Sanjay Patel), top-level dirs: `app/`, `lib/`, `components/`, `docs/`, `tests/`

### Narrative (from devlog)

**Statistics — Plain-English briefs overhauled (Aug 7, 4 commits)**

- Plain-English vs. Expert toggle on logistic regression was meaningless: both branches emitted identical jargon-heavy text (`logitBL` ignored the `naive` flag for the significant-terms path). Added a genuine layperson branch: names the single biggest lever in intuitive terms, phrases effect as "~X% more likely / cuts those chances by ~X%", translates pseudo-R² into plain English. Expert branch byte-identical to before.
- Owner asked: "who are the other 4 movers?" — added post-lever list of other significant factors grouped by direction (raises / lowers odds) plus the non-significant terms, all resolved through `aliases` and capped at 6 per group.
- Both regression Bottom Lines enriched to a full Gemini-style brief: target + sample size + reliability + fit%, listing directional drivers for linear, odds-ratio % for logistic.
- Visual upgrade: both regression Bottom Lines now render as colour-coded JSX blocks (logitBLNode / regrBLNode) — red/green rows, bold factor names. Linear deliberately omits per-factor %; OLS betas aren't standardized so only sign is safe to show.

**Dimensions — rating-based sentiment fallback for neutral touchpoint subs (Aug 7)**

- `touchpoint` axis entities are all neutral keyword phrases (by design). They accumulate mention counts but never pos/neg assertions → `posPct = null` → "No sentiment signal" even on large, star-rated datasets (Flemings demo issue). Fix: when a sub has zero polarised mentions, `posPct` now falls back to the star ratings of mentioning reviews (≥4★ share). New `SubStat.sentBasis` field (`'keyword' | 'rating' | null`) keeps the two denominators from being silently blended — card labels the fallback "· by rating" and the footnote stays honest.

**Outlet action plan — two correctness fixes (Aug 7, 2 commits)**

- Location naming: the KEEP DOING narrative opened with the bare brand name ("Fleming's has real momentum") instead of the specific outlet. Attempt 1 (`locationLabel = brand + city/state`) produced garbled prose on dirty demo data. Attempt 2 (owner's call): drop the naming entirely, always say "your location" — unambiguous, never wrong. `actionPlanBasis` v2 → v3 → v4.
- Credibility fix: `validateVerbatims` matched the model's quote by normalized substring then rendered the model's text (silently re-punctuated / typo-fixed / stitched) rather than the actual review text. Always render `match.raw` now — the exact candidate extracted from `extractSentence`. Prompt rule changed from "you may lightly trim" to "copy EXACTLY". New `tests/unit/outletActionPlan.test.ts` (5 cases) locks the guarantee.

**Classify — auto-retry transient statement-timeout on verdict write (Aug 7)**

- `embedVerdicts` (`lib/taxonomyClassify.ts`) was throwing a user-visible 500 on Postgres statement timeout (57014) during large-dataset Dimensions enable. Since `apply_taxonomy_verdicts` is an idempotent upsert per (row, fieldKey), retrying is safe: up to 3 attempts with 0.5s/1s backoff for transient failures (57014 / timeout / connection-reset); non-transient errors still throw immediately.

**Analyze — Manage members / Save changes stale-closure fix (Aug 7)**

- `ManageMembersModal.save()` called `onChanged()` then `onClose()` in the same tick. `onChanged` did `setManageDirty(true)` but `onClose` read the pre-update closure value (always `false`), skipping the reload. Fixed by replacing `useState` with `useRef` for the dirty flag so `onClose` reads the current value synchronously.

### Risk signals

- **chatCore.ts:2040/2159 multi-tenancy MEDIUM (carried from W31)**: `service.from('bot_conversation_turns').update({ content_flags }).eq('id', row.id)` without `.eq('org_id', ...)` — violates the invariant for a second week. Blast radius is low (rows are written by the current authenticated session; ids are not attacker-controlled), but the class violation should be closed this week.
- **Next.js CVEs (unfixable until 16.3.0 stable)**: Advisory range `14.3.0-canary.0 – 16.3.0-preview.7` covers 9+ CVEs including `GHSA-6gpp-xcg3-4w24` (proxy/middleware bypass — directly relevant to `proxy.ts` CSRF gate). No released version clears the range. Continue monitoring Next.js release channel.
- **ESLint ceiling at 252/252**: Zero headroom — any `react-hooks/*` warning in touched code fails `lint:ci` immediately. W32 did not advance this ratchet.
- **TESTING.md spec drift**: 3 new unit test files added (`outletActionPlan.test.ts`, `insightAlerts.test.ts`, `taxonomyRollup.test.ts`) without updating `docs/TESTING.md`. Minor, but the pattern should be closed.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 13.5 |
| Security | 7/10 | 15% | 10.5 |
| Dependencies | 6/10 | 15% | 9.0 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 8/10 | 15% | 12.0 |
| Documentation | 8/10 | 10% | 8.0 |
| Maintainability | 8/10 | 20% | 16.0 |
| **Total** | | **100%** | **77.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**

- **Next.js 9+ CVEs (framework-level, unfixable)** — advisory range `14.3.0-canary.0 – 16.3.0-preview.7`; installed 16.2.12 is inside it. Relevant to `proxy.ts` CSRF gate via `GHSA-6gpp-xcg3-4w24` (proxy/middleware bypass). No released version clears the range; monitor for 16.3.0 stable.
- **sharp/libvips 4 CVEs (HIGH, transitive)** — `CVE-2026-33327/33328/35590/35591` via `next → sharp`. No independent fix; clears with the Next.js upgrade.
- **npm audit: 22 high / 5 moderate total** — includes `brace-expansion` DoS, `fast-uri` host confusion, `image-size` ICNS parser DoS, and the `@workflow/*` monorepo transitive chain. Several are transitive-only with no direct fix path independent of upstream releases.

**MEDIUM**

- **`lib/chatCore.ts:2040,2159`** — `service.from('bot_conversation_turns').update({ content_flags }).eq('id', row.id)` without `.eq('org_id', ...)`. Multi-tenancy invariant violation for service-role write — carried unaddressed from W31. Blast radius is low (rows are current-session-written), but the class is the source of every CRITICAL finding to date. Close this week.
- **ESLint ceiling 252/252** — zero headroom on `lint:ci`; any new `react-hooks/*` warning in touched code fails CI outright. W32 did not burn warnings. `react-hooks/*` burndown is behavior-sensitive; use the `_wf-eslint-burndown.js` harness per `docs/ENGINEERING.md` with per-file browser verification.
- **TESTING.md spec drift** — 3 new unit test files added this week (`outletActionPlan.test.ts`, `insightAlerts.test.ts`, `taxonomyRollup.test.ts`) without a corresponding update to `docs/TESTING.md`. The spec should document what these tests cover and any new testing patterns.

**LOW / informational**

- Test ratio: 163 test files / 1035 TypeScript source files = 0.15 — below the 0.5 target. Absolute passing count 1,607 (up from 1,602); the suite grows but source file growth holds the ratio flat.
- 1 circular dependency: `lib/types.ts → lib/industryDefaults.ts`. Carried from W30/W31/W32 — low severity, no runtime impact observed.
- `app/` directory: 554 files (stable vs. W31). Navigability concern as route tree grows.
- No `.claude/agents` or `.claude/rules` defined — governance relies on `CLAUDE.md` + pre-commit hooks + 4 custom commands. Consistent with prior weeks; not a blocker.
- Minor packages behind by patch/minor: `@aws-sdk/*`, `@supabase/supabase-js` (2.105→2.110), `@playwright/test` (1.59→1.62), `@sentry/nextjs` (10.63→10.68). No CVEs.
- DOMPurify ≤3.4.11 (low, `GHSA-c2j3-45gr-mqc4`) — intentionally not patched; `isomorphic-dompurify`/`jsdom@26` cluster has a do-not-bump constraint.

### Progression plan (top 3 to fix this week)

1. **Fix `lib/chatCore.ts:2040,2159`** — add `.eq('org_id', session_org_id)` to the `content_flags` updates on `bot_conversation_turns`. `bot.org_id` is in scope throughout `handleChatTurn`. Pattern identical to the W31 `last_session_at` fix. ~10 min, tsc-clean. Closes a multi-tenancy invariant violation in its second week.
2. **Update `docs/TESTING.md`** for the three new unit test files added this week — document what `outletActionPlan.test.ts`, `insightAlerts.test.ts`, and `taxonomyRollup.test.ts` cover and note the rating-fallback + verbatim-validation testing patterns. Closes the spec-drift finding.
3. **Monitor Next.js 16.3.0 stable and upgrade deliberately** — closes 9 CVEs including the `proxy.ts`-relevant proxy/middleware bypass. Do not use `npm audit fix`; upgrade deliberately when stable lands on the Next.js release channel.

## 3. Correlation: dev work → audit findings

- **chatCore.ts:2040/2159 MEDIUM (carried)**: No devlog entry; not addressed this week. Second consecutive week unaddressed. **Not addressed**.
- **TESTING.md drift**: 3 new test files added but the spec not updated — devlog documents what was added but the spec was not co-updated. **Not addressed**.
- **Next.js CVEs**: No Next.js version change this week; advisory range unchanged. Correct posture (waiting for 16.3.0 stable). **Not addressed** (no fix available).
- **Outlet verbatim credibility (was open)**: Devlog "Outlet action plan: guarantee verbatim quotes are real" explicitly closes this — `validateVerbatims` now renders `match.raw` (exact candidate text), not the model's output. **Addressed and closed this week**.
- **Outlet location naming (product quality)**: Iterative correction from v2→v3→v4. Owner found garbled prose on v3; v4 uses "your location" which is unambiguous. **Addressed and closed this week**.
- **Dimensions neutral-sub "No sentiment signal"** (product quality, not a security finding): Rating-based fallback added with `sentBasis` field to prevent silent denominator blending. **Addressed and closed this week**.

## 4. Trend

Prior report: **2026-W31** — 81.5 / 100 (no W32 governance report exists).

Delta: **−4.5** vs. W31.

Drivers of the drop:
- **Security** 8→7: `chatCore.ts:2040/2159` multi-tenancy MEDIUM carried a second week without a fix.
- **Documentation** 9→8: `docs/TESTING.md` spec drift (1 drifted spec vs. 0 in W31).
- **Maintainability** 9→8: ESLint ceiling remains at 252/252 (zero headroom) with no burndown progress this week.

The core product quality work this week was substantive (Stats Plain-English, Dimensions sentiment, Outlet verbatim credibility), but the carried security MEDIUM and spec discipline gap dragged the score. Both are quick fixes.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Review the spec-drift companion (2026-W33-spec-drift.md) — any spec debt?
- [ ] Check correlation section — are open findings being worked on?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
