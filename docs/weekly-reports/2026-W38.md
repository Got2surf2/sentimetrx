# Weekly Governance Report — 2026-W38 (Week of Sep 7 to Sep 13)

**Generated**: 2026-09-14T08:06:20Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W37-devlog.md
**Spec-drift**: 0 drifted — see 2026-W38-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 15 commits, 2 authors (Sanjay Patel × 14, Claude Governance × 1), top-level dirs: app/, components/, lib/, sql/, docs/, .claude/, .github/

### Narrative (from devlog)

**Ask Ana intelligence**
- Dataset digest: "since you last looked" briefing leads Ana's opening summary (sql/202 applied to prod)
- Multi-subgroup comparison: Ana can now compare two subgroups in a single `query_data` call
- Panel polish: factoid scroll-follow, brand colors, status inside Thinking bubble
- Context dimensions moved server-side: client rows never carried `_tx`; chips now derived from tagged state (also resolved the CI lint ratchet overage)

**Security / multi-tenancy hardening**
- `collectionRecompute`: all three helpers now pair `org_id` on the `collections` lookup (runs first) and the `datasets` update; 4 new tests pin the pairing on real query chains. Defense-in-depth against the May-2026 CRITICAL pattern.

**Dependency hygiene**
- npm audit 8 → 2: `fast-uri` and `browserslist` patched; `qs` root cleared by scoping the `body-parser` override from exact to `@1` major. Remaining 2 are the documented `image-size` acceptance via `pptxgenjs`.

**CI / devops**
- Lint ratchet: CommentsPanel retirement freed 5 warnings (172 → 167); a coordinated ContextCloud refactor removed the new `react-hooks/set-state-in-effect` that would have pushed the ceiling back up.
- Deploy gate annotation: the gate job now emits a `::notice::` in the run summary (`TRIGGERED` vs `NO production build`) so green ≠ built is visible without reading step details.

**Schema / infra**
- sql/202 `dataset_visit_snapshots` applied to prod via Management API (pooler :5432 dead); RLS + read-only policy + unique constraint verified in the same call; schema snapshot regenerated.

**Governance tooling**
- Routine off-by-one fixed: WK (run week, names files) vs COVERED (yesterday's ISO week, names the devlog + date range); routine now `ls`es devlogs and states which it used.
- `audit-codebase.md` updated: Dependencies separates roots from collateral, honors documented acceptances, never recommends a semver-major fix; Tests notes floors are ratcheted by design.

**Analytics**
- `estimateCost`: cached calls had a sign error (cache reads subtracted instead of added tokens to the cost) — billing calculation was understating cost for heavily-cached sessions.
- CommentsPanel retired: one comments panel, two row sources.

### Risk signals
- **estimateCost sign error was in production** until `66617be` — cost calculations for cached sessions were understated. No financial impact identified (cost attribution is internal), but the bug class (sign inversion in a numeric accumulator) warrants a one-time review of similar formulas.
- **sql/202 required manual owner intervention** — the Management API path is the only DDL route from this network; if the owner is unavailable, a migration blocks. Documented but the infra constraint is unresolved.
- **New npm advisories appeared after the W37 audit fix** — `next` (critical), `js-yaml` (high), `sharp` (high) were not present when the Sep 7 fix ran. These have non-major fixes; `npm audit fix` should clear them.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 10/10 | 15% | 15.0 |
| Security | 9/10 | 15% | 13.5 |
| Dependencies | 6/10 | 15% | 9.0 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 7/10 | 15% | 10.5 |
| Documentation | 9/10 | 10% | 9.0 |
| Maintainability | 9/10 | 20% | 18.0 |
| **Total** | | **100%** | **83.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- **[Dependencies]** `next` has a CRITICAL advisory (16.0.0–16.3.2, `fix=true`, non-semver-major) — appeared after the Sep 7 audit fix. `npm audit fix` should clear it.
- **[Dependencies]** `js-yaml` HIGH (4.0.0–4.3.1) and `sharp` HIGH (<0.35.4) — both have non-major fixes available. Same `npm audit fix` pass clears these.

**MEDIUM**
- **[Dependencies]** `@vitest/mocker` and `vitest` have moderate advisories (2.1.0–4.1.10), both `fix=true`. Low-risk (dev/test scope) but tidy up in the same audit pass.
- **[Tests]** Coverage thresholds enforced at 37–39% — ratcheted correctly but still in the 25–50% band. A floor ratchet pass after the next large test addition would move this into the 8-9 band. Do not call the floors slack without a measured `coverage-summary.json`.
- **[Security]** RLS test suite skipped in this clone (env-gated) — cannot verify policy coverage without `SUPABASE_TEST_*` credentials. CI gates it; local verification requires the test project creds.

**LOW / Informational**
- 0 spec drift — 6 specs updated in sync with code changes, 11 clean (no touching code in range). Pre-commit drift guard working.
- 2,057 test cases in 215 files (3.25 per coverage-surface file) — healthy growth from ~1,646 in W36.
- Lint ratchet at 167 warnings, all `react-hooks/*` — held by design; behavior-sensitive refactor deferred.
- No `.claude/agents` or `.claude/skills` defined — functional gap in AI workflow; custom agents could accelerate recurring audit/sweep tasks.
- 1 snapshot file (minimal maintenance burden).

### Progression plan (top 3 to fix this week)
1. **Run `npm audit fix`** to clear the critical `next` advisory and high `js-yaml`/`sharp` roots — should land 8 → 2 (back to documented `image-size` acceptance only). Verify lockfile: `pptxgenjs` must not move.
2. **Add CI `npm audit --audit-level=high` gate** to catch new roots automatically before the weekly governance run (ENGINEERING.md §600 tracks this as an Open TBD item).
3. **Ratchet test coverage floors** after the next large test addition — target 50%+ to move Tests into the 8-9 band.

## 3. Correlation: dev work → audit findings

| Finding | Status |
|---|---|
| W37 CRITICAL — `collectionRecompute` bare service-role `id` lookup (May-2026 pattern) | **Addressed** (`aea98bd`) |
| W37 HIGH — npm audit 8 highs | **Addressed** (`a93caff`), then new advisories appeared post-fix (not a devlog item for W37) |
| W37 MEDIUM — governance routine devlog off-by-one | **Addressed** (`212a3aa`) |
| W38 CRITICAL — `next` critical CVE (new) | Not mentioned in devlog — appeared after W37 fix |

## 4. Trend

| Category | W37 | W38 | Delta |
|---|---|---|---|
| Secrets | 10/10 | 10/10 | = |
| Security | 8/10 | 9/10 | +1 |
| Dependencies | 7/10 | 6/10 | -1 |
| Structure | 8/10 | 8/10 | = |
| Tests | 7/10 | 7/10 | = |
| Documentation | 8/10 | 9/10 | +1 |
| Maintainability | 9/10 | 9/10 | = |
| **Total** | **82.0** | **83.0** | **+1.0** |

W37 score read from `docs/weekly-reports/2026-W37.md` table (Total row: **82.0 / 100**). Net +1.0 — Security and Documentation each gained one point (CSRF machinery well-documented, 0 spec drift confirmed, 0 tsc errors); Dependencies lost one point due to new critical/high advisories appearing after the W37 audit fix. The `npm audit fix` action in Progression item 1 should recover Dependencies to 7+ next week.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Review the spec-drift companion (2026-W38-spec-drift.md) — any spec debt?
- [ ] Check correlation section — are open findings being worked on?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
