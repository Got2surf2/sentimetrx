# 2026-W26 — Dev log (Week of Jun 22 to Jun 28)

## 2026-06-22 — Rubio's Coastal Grill pitch deck, rebuilt from real review data

**Why**: A prior session produced a Rubio's pitch deck (`~/Downloads/Rubios_Pitch_Deck.pptx`) whose numbers were generic/assumed ("4.2★", "post-bankruptcy, 3–4 silent failures dragging the brand down") — a framing that would have torched credibility against an ops team that knows its own ratings. We have the actual data loaded in Sentimetrx (10,600 Google reviews, 81 locations), so rebuild the pitch on real, auditable numbers and let the data set the story.

**What changed**:
- `docs/RUBIOS_PITCH_ANALYSIS.md` (NEW) — source-of-truth spec: dataset id, every headline metric + how it was computed, the competitive method, and the lifetime-vs-trailing caveat. Makes each deck number traceable if questioned in the meeting.
- **The data reframed the whole pitch.** Real findings vs the prior deck: overall **4.56★** (not 4.2), and a genuine turnaround **4.17→4.67 over 5 quarters**; **only 1 store below 4.0★** (not "3–4 failures"). The real problem is *variance* — a **0.88★ best-to-worst spread that is geographic**: top 10 is 9/10 California, bottom 10 is 8/10 AZ/NV. Plus **47% of complaints get no owner response**, and on live Google ratings **Rubio's (≈4.27) beats Chipotle (3.53) and Del Taco (3.79) in every market** — the only peer above is **Cava (4.69)**, the threat to watch.
- `datanautix-homepage/Rubios_Datanautix_Pitch.pptx` (NEW, 12 slides) + `scripts_build_rubio_deck.py` (python-pptx generator, datanautix.com brand palette: cream / orange `#E85A1A` / teal `#2A7A6F`; native trend/variance/competitive charts; a source line on every data slide). Deck saved to `~/Downloads/` next to the original.
- Analysis scratch (sentimetrx, **uncommitted**, kept while the opp is live): `scripts/_rubio_{discover,analyze,themes,competitors}.ts` + `data/_rubio_{locations,lowstar,highstar,competitors}.json`. `_rubio_analyze.ts` is brand-agnostic given a `dataset_id`; `_rubio_competitors.ts` benchmarks any brand/market list via live Maps.
- Spend: ~**$0.06** DataForSEO (live Maps competitor pull; balance 47.36→47.30).

**Verify**: All numbers reproduced from `dataset_id d4e53aec…` and saved to the spec doc. Deck rendered to PDF via LibreOffice and eyeballed all 12 slides — fixed two-line-title/body collisions on slides 2/4/5/6/9. Docs only in-repo; deck + scripts are artifacts, not product code (no behavior change, nothing pushed). Per the W25 scratch-script convention, the `_rubio_*` scratch is delete-after-opp, not commit.

## 2026-06-22 — Build-cost bleed + average reference line on charts

**Why**: Owner saw 5 back-to-back Vercel production builds and asked why, given they actively avoid paying for builds. Root cause was two-fold: (1) the Vercel **Ignored Build Step** was an inline one-liner that skipped previews but **always built on production**, so docs-only governance merges (`af8aa39`, spec-drift PR #20) each burned a full ~$8-10 prod build; and (2) the Monday governance automation opened **two** separate docs-only PRs (spec-drift + weekly-report), structurally 2 merges/2 builds a week.

**What changed**:
- **Vercel Ignored Build Step repointed** (via API, project `prj_c8oyt1…`) from the inline `if production exit 1` one-liner to `bash scripts/vercel-ignore-build.sh` — the repo script (present on main since Jun 16, never wired) that *also* skips production builds when a commit touches **only `docs/`**. Net: docs-only merges to main now cost $0; previews still always skipped. Fail-safe — builds whenever `HEAD^` is unreachable (shallow clone) so a deploy is never silently skipped.
- **Governance routines merged to one PR**: the cloud "Weekly governance report" routine (`trig_016j…`, Mon 08:00 UTC) now runs the spec-drift report **and** the audit, commits both files on one `governance/WK` branch, and opens **one** combined PR. The standalone "Spec drift weekly report" routine (`trig_01XyQ…`) is **disabled**. One Monday review item instead of two.
- **Charts: overall-average reference line** (`components/analyze/ChartsModule.tsx`, `BarAggInner`) — Average-mode Bar/Column charts now overlay a dashed "Avg N" line at the **count-weighted** mean across all groups (true value-field mean, not unweighted bar mean). Both orientations + taxonomy-dimension averages. Requested by owner off a Rubio's "Avg Star Rating by Location" chart.

**Verify**: `npx tsc --noEmit` clean. Ignored Build Step + routine changes confirmed via API read-back. Chart change is render-only — owner to eyeball the line in TextMine → Charts → Average. Infra changes are live (Vercel setting + cloud routines); the two repo files are committed **local only**, not pushed.

## 2026-06-22 — Outlet Report: cross-outlet "Leaderboard" (top/bottom per theme & dimension)

**Why**: The Outlet Report answered "how does THIS outlet compare across all themes." Owner wanted the inverse for a multi-location brand: for each theme and dimension, *which* locations are the top/bottom performers — so you can see at a glance who's lagging on, say, Service across the whole chain. Building block toward a laggard-improvement predictor (next).

**What changed**:
- **`lib/outletReport.ts` refactored to one-scan/two-views.** Extracted `scanDataset()` (the single pass over `dataset_rows_flat` + `dataset_row_field_taxonomy` that builds per-outlet & chain `{pos,neg,total}` accumulators). `buildReport()` (per-outlet vs peers, unchanged behavior) and the new `buildLeaderboard()` both read the same scan. New `computeOutletBundle()` returns `{report, leaderboard}` from a single scan; `computeOutletReport` kept as a thin wrapper for compat.
- **`buildLeaderboard()`** — per theme and per dimension item, ranks the outlets that clear `MIN_N_OUTLET` (≥6 mentions) by **net-positive rate** (signed pos/neg — not bare occurrence), item gated by `MIN_N_CHAIN`/`MIN_POLAR_SHARE`. Carries top-10+bottom-10 per item (`truncated` when >20 qualify). `defaultK` = 3 for ≤15 outlets else round(20%×count) capped at 7; `maxK` = min(10, ⌊count/2⌋).
- **UI**: new **Leaderboard** tab in `OutletReportTabs.tsx` (dataset-wide, ignores `?outlet=`) with a **slider** (1…maxK) to show more/less per side, defaulting to `defaultK`. Cards show Top K / Bottom K with chain baseline; collapses to a single best→worst list when `qualifying ≤ 2K`; "+N in between" note when truncated. Page now calls `computeOutletBundle`.

**Verify**: `rm tsconfig.tsbuildinfo && npx tsc --noEmit` clean. Refactor is behavior-preserving for the existing tabs (logic moved, not changed); no dedicated outletReport test exists. Owner to eyeball the Leaderboard + slider on a real multi-location dataset (e.g. Rubio's, 81 outlets). LOCAL/unpushed. **NEXT: Part 2 predictor prototype** — per-review logistic on *negative-mention* rates → per-laggard gap-to-median × severity → projected ★ lift + what-if scenarios ("lift Service laggards to median", "which units give the biggest overall lift").

**Follow-up same day (owner review):** (1) **Moved the Leaderboard out of the per-outlet tabbed report** — it's a brand-level view and shouldn't be buried inside the single-location report. Now its own route `app/analyze/[datasetId]/outlet-leaderboard/` (`+ LeaderboardClient.tsx`) reached via a new **"Leaderboard" sub-tab in TextMine** (peer to Outlets, same gate). `OutletReportTabs`/`page.tsx` reverted to `computeOutletReport`; added `computeOutletLeaderboard`, dropped the short-lived `computeOutletBundle`. (2) **Theme-label bug**: themes carry their label in `theme_model.themes[].name`, but `scanDataset` read `t.label` → every theme collapsed to one "Theme" bucket (visible on the leaderboard). Fixed to `t.name || t.label`. Fixes the per-outlet **Themes** tab too. tsc clean. (3) **Leaderboard figure clarity** — the colored % was the *absolute* net-positive rate but colored by above/below chain, so a below-chain laggard showed a confusing red "+62%". Changed the bold figure to the **gap vs chain in points** (green above / red below; e.g. "−19 pts"), with the absolute net-rate demoted to grey — matching the per-outlet report's "pts" convention.

---

### Outlet Action-Plan predictor (themes, de-biased v2) — 2026-06-22

**Why**: The Leaderboard (above) ranks outlets per theme but doesn't tell an operator *what to do*. Owner wants the per-outlet report "much richer and actionable" — the highest-impact things to work on relative to peers, with examples of what to fix and **who to learn from** — and a brand-level executive summary that becomes the "why you need us" guest-experience improvement plan. Chose **themes over dimensions** (the 7-axis keyword taxonomy folds every service/food complaint into "Experience" → can't answer "lift Service to median"; themes separate cleanly) and **de-biased v2 over naive v1** (owner call).

**What changed**:
- **`lib/outletPredictor.ts` (new, pure/unit-tested)** — `buildPredictor()`. **v2 model**: multivariate **ridge regression** of star rating on per-review theme-negative dummies; `drag(T) = −β_T` is the de-biased ★-drag. Ridge (L2, intercept unpenalized, **λ by 5-fold CV**) splits credit across **co-occurring** complaints that the naive univariate severity double-counts. **Ridge-linear not logistic on purpose** — outcome is continuous 1–5★ and the what-if is ★-denominated, so β_T *is* the ★-drag. Outputs: `themeDrag` (v1 vs v2 + shrink%), `brandLevers` (vol-weighted chain ★ if all→median), per-outlet `outletLevers` (gap×drag, exemplar peer, example quote). **Exemplar fix**: mentor = lowest neg-rate among outlets with ≥60 reviews, tie-broken by ★ then volume (a volume floor stops tiny-outlet fluke 0%s from winning).
- **`lib/outletReport.ts`** — `scanDataset` now also captures `reviewMatrix` (per rated text-review `{placeId, rating, neg[]}`) + `themeLabels` inside the *existing* flat-row loop (whole-review polarity, identical to the leaderboard → engine ≡ leaderboard). Added `computeOutletPredictor` and `computeOutletReportWithPredictor` (report + predictor from ONE scan).
- **UI** — new **Action Plan** tab in `OutletReportTabs.tsx` (now the **default** tab), `ActionPlan`/`LeverCard`: ranked levers with projected +★, guest quote ("what to fix"), and an emerald "Learn from <peer>" call-out. Empty state when the outlet is at/above peer median on every theme. Footnote explains the de-biased model + R² + the associational caveat. `page.tsx` switched to `computeOutletReportWithPredictor`.
- **`tests/unit/outletPredictor.test.ts`** — synthetic-data unit tests: recovers known per-theme drag + ranking, de-biases co-occurrence (v2 < v1, shrink > 0), attaches quotes, picks the cleaner outlet as exemplar.

**Validated on real data** (Rubio's, 5,754 rated text-reviews, 4.44★, 71 outlets ≥30): production `computeOutletPredictor` matches the standalone prototype exactly — λ=3, R² 0.225, **Staff & Customer Service** is the #1 lever and *shrinks least under de-biasing (28%)* → its drag is genuinely its own, not borrowed from globally-bad reviews; Value/Pricing shrank 70% (mostly co-occurrence illusion). Headroom is thin (chain already 4.44★) — the *ranking* is the value.

**Verify**: `npx tsc --noEmit` clean; `npx vitest run tests/unit/outletPredictor.test.ts` 4/4. Prototype scripts `scripts/_predict_lift{,_themes,_v2}.ts` are scratch (re-runnable, not part of the build). LOCAL/unpushed. Owner to eyeball the **Action Plan** tab on a real multi-location dataset. **NEXT: brand-level executive summary** — robust theme-driven guest-experience improvement plan (the "why you need us" artifact), fed by `brandLevers`.

**Phase 3a same day — brand improvement-plan page.** The brand-level "why you need us" executive summary, fed by the same predictor. New route `app/analyze/[datasetId]/improvement-plan/` reached via an **"Improvement plan →" button on the Leaderboard** header — **deliberately not a new tab** (owner: "we can't keep adding menu items or tabs — this should be a button in a logical place"; the leaderboard is already the brand-level home). Shows: an executive opportunity paragraph, brand levers ranked by `chainLift` (with `outletsAboveMedian` + `topOutlets` "start with" links), and a worst-first outlet hot-list (`outletSummaries`: rating vs brand, biggest lever, projected ★, links to each outlet report). `buildPredictor` extended with `brandLevers.{peerMedian,outletsAboveMedian,topOutlets}` + `outletSummaries[]`. Numbers stay honest (4.44★ brand → thin aggregate headroom; value is diagnostic precision). tsc clean, 4/4 predictor tests still pass; verified the new fields on Rubio's (Staff & Customer Service #1 lever, 35 outlets above median; Mesa AZ worst at 3.54★). LOCAL/unpushed. **NEXT (phase 3b): "Export deck" button → Datanautix-branded PDF of the same plan.**

**Phase 3b + REFRAME same day — deck export, then a full pivot to "recover your 1–3★ guests."** Built the Datanautix deck export (`lib/pptx/improvementPlanDeck.ts` → DeckSpec + `renderDeck`; org-scoped `GET /api/datasets/[datasetId]/improvement-plan-deck`; "Export deck" button on the plan page). **Then the owner flagged two real framing flaws in the severity model**: (1) "35 of 71 outlets below the peer median" is tautological (half always are); (2) "each negative mention costs 2.1★" is overstated/misleading (associational, reads as causal). Owner proposed reframing the whole analysis around **moving 1–3★ guests up** — and it exposed a deeper insight: raw "what's in the bad reviews" is a base-rate TRAP. Food (0.6×) and Staff (1.0×) dominate bad reviews only because they're the most-discussed topics *everywhere*; the genuine drivers (over-represented in 1–3★ vs 4–5★) are **Order Accuracy & Fulfillment (2.0×)** and **Loyalty & Brand Experience (1.5×)**. **Pivoted all four surfaces** (owner: full pivot; lift = reduction in 1–3★ rate): `lib/outletPredictor.ts` rewritten from ridge-severity to a descriptive low-rated model — brand 1–3★ rate + outlet spread (Rubio's: 1.4% Laguna Niguel → 40.5% Phoenix, target 7.1%), driver themes by over-representation lift, exemplars by lowest 1–3★ rate, per-outlet levers = driver themes the outlet's unhappy guests cite. `scanDataset` now captures bare keyword presence (`reviewMatrix.themes[]`) + `lowExamples` (1–3★ quotes) instead of neg-polarity flags. Action Plan tab, brand page, and deck all rewritten; unit tests rewritten (4/4: recovers 1–3★ rate/spread, flags over-represented theme as driver + loud-but-neutral as not, levers exclude non-drivers + attach quotes). Dropped the lift bar-chart slide (the shared `bar_chart` renderer bakes in %/n distribution columns that mislead for a multiple) → driver **table** instead. Quote→theme attribution dropped in the deck (keyword matching ties quotes to themes loosely — honesty). **Deck QC'd**: rendered all 7 slides via LibreOffice→PNG, branding/layout clean. tsc clean. LOCAL/unpushed. **NEXT (owner): eyeball Action Plan tab + Improvement plan page + Export deck on a real multi-location dataset.** Known follow-up: in-product LeverCard still shows a per-theme quote (keyword-loose); sentence-level theme attribution is a future precision upgrade.

**Same-day refinement — actionable/outcome split + PEER QUARTILES per theme (owner-driven).** Owner flagged two more: (1) "Loyalty & Brand Experience" is a lagging OUTCOME, not a manager lever — so it's now classified via `OUTCOME_RE`, excluded from drivers/weaknesses, and reported only as a brand-health *signal*. (2) Over-representation-vs-happy-guests left only ONE brand driver (Order Accuracy) and marked 37/71 outlets — incl. the two worst — as "diffuse", which is useless guidance; and "one top issue ≠ no other issues". **Pivoted the per-outlet/per-theme layer to peer quartiles** (owner's idea): for each actionable theme, rank every outlet by its **problem rate** (share of its reviews that are 1–3★ and cite the theme); bottom quartile = weakness, top = strength. Now every outlet shows ALL its bottom-quartile weaknesses (Phoenix → 6: Cleanliness/Food/Wait/Value/Order/Staff, no more "diffuse"), plus strengths. New predictor outputs: `outletStrengths`, `themeFocus` (per theme → bottom-quartile outlets to coach), `actionableThemes`, `outletSummaries.{topIssue,weaknessCount}`. Brand over-rep driver kept as the *strategic* layer ("Order Accuracy is the one SYSTEMIC issue; everything else is location-specific"). UI: Action Plan tab shows weaknesses (peer-rank badge) + strengths; brand page adds a "by issue — which outlets to focus on" section; **deck restructured to one "Where to Focus" slide per actionable theme** (owner: "exec summary should have a slide per theme and who to focus on"). Deck re-QC'd via LibreOffice (12 slides, clean). Tests rewritten 5/5, 510 unit pass, tsc clean. LOCAL/unpushed.
