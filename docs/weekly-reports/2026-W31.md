# Weekly Governance Report — 2026-W31 (Week of Mon 27 Jul to Sun 02 Aug)

**Generated**: 2026-08-03T08:07:54Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W31-devlog.md
**Spec-drift**: 0 drifted — see 2026-W31-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 31 commits, 1 author (Sanjay Patel), top-level dirs: `app/`, `lib/`, `components/`, `sql/`, `docs/`, `scripts/`

### Narrative (from devlog)

**Town Hall — Public Sharing Overhaul** (Jul 30, multiple commits)
- Share panel visibility extended to platform admins (`isOwner || isAdmin`), closing a UI/API gap.
- `/th/[token]` now renders the full `ReportClient` in `publicMode` instead of a bespoke parallel implementation — Q&A, Action items, Participation, Transcript tabs, no edit controls.
- Three mutation controls (re-extract, scope, regenerate/mark-reviewed) gated on `canEdit = !publicMode && (isOwner || isAdmin)` — had no gate at all while report was login-only, became a hole on the public route.
- RSC payload leak closed: switched from `select('*')` to an explicit allowlist, dropping sign-off reviewer notes, `confidentiality_class: client_confidential`, `cost_cents`, raw ASR payload, and share token from the serialized page source. Page weight: ~2.3 MB → ~207 KB.
- Audio opt-in per meeting (sql/185, `recordings.share_audio` default false); new `GET /api/th/[token]/audio` authorises by share token and fails closed on 6 conditions. Verified both directions against TEST.

**Town Hall — Audio Player** (Jul 30, multiple commits)
- Player now tracks the active speaker in real time, resolving speaker names from the diarization label map.
- Speaker-change headers + alternating banded blocks (banded by run, not identity, to avoid same-shade adjacency).
- Corrected transcript applied everywhere text is displayed (player follow-along, action-item hovers, coverage drill-downs, participation ribbon tooltip) — raw ASR was the only view that showed transcription errors to authenticated users; corrected transcript was only applied to PDF/deck exports.
- Per-speaker reassignment from the player (block-level "move →" select on speaker-change headers + per-line select for stray segments). Gated on `canEdit`; verified public report renders zero `<select>` elements.

**Town Hall — Transcript Editing** (prior to Jul 30, committed in W31 range)
- Structural segment editing: nudge / split / merge on the Transcript tab.
- Speaker-merge deduplication in Participation, clickable floor ribbon with hover tooltips.
- "Heard as…" comma-separated variant field fixed — `onChange`/`onBlur` draft pattern replaces split-in-onChange, which silently prevented entering more than one variant per term.

**Agents — Embed-URL Attribution** (Jul 30)
- `lib/attribution.ts`: shared sanitizer (trim, 80-char cap, reject empties, utm_* aliases).
- `BotClient` and `ChatBot` beacon now read and pass `source`/`medium`/`campaign` from URL params; `chatCore` threads attribution through all 8 `mirrorTurns` call sites.
- Provenance breakdown in Agent Study (v8): "Where They Came From" table (opens, conversations, conversion rate per tuple) in report, HTML export, and deck. `STUDY_SCHEMA_VERSION` v7→v8 to flush stale caches.
- 14 + 9 = 23 new unit tests; real-schema verification in `_verify_attribution.mts`.

**Security fixes** (Jul 27)
- `chatCore.ts:1857` (`last_session_at` update): added `.eq('org_id', bot.org_id)` — closes the multi-tenancy invariant violation carried from W29/W30.
- Targeted `overrides` for `fast-uri` 3.1.3→3.1.4 (HIGH CVE) and `body-parser` 1.20.5→1.20.6 (LOW CVE). `npm audit fix` was evaluated and rejected (inflated vuln count 6→27; Next.js CVEs still within advisory range). Net: 6→4 vulnerabilities, 4 high→3 high.

**Service Health** (Jul 30)
- Deepgram billing probe fixed (`billing:read` scope required); `DEEPGRAM_BILLING_KEY` minted and configured in Vercel production.
- Twilio credentials wired from owner's existing account ($28.58 balance confirmed). SMS sending deliberately NOT enabled (number carries MovieRex brand); `TWILIO_PHONE_NUMBER` withheld pending a Sentimetrx/Datanautix number.
- SPEC.md env table corrected: `TWILIO_FROM` → `TWILIO_PHONE_NUMBER`.

**Repo hygiene** (Jul 27–28)
- `.gitignore` gap: `scripts/_*.mts` added so 35 untracked harnesses no longer appear as `??`.
- 4 delivered oneoff generators archived to `scripts/oneoff/` (committed) with secret scan.
- `docs/ENGINEERING.md` §6: pptxgenjs `shadow` corruption gotcha documented.
- HelpWidget lint ratchet CI fix: scoped disable for one-shot SSR-safe `sessionStorage` hydration effect (253→252, ceiling holds).

**NOWOCATS deck corrections** (Aug 2)
- Client-confirmed road name corrections (Big Road→Vick Road, Plymouth-Cemento→Plymouth-Sorrento) and meeting renumber (#3→#2) in archived deck generator source.

### Risk signals

- **Next.js CVEs unfixable until 16.3.0 stable**: The advisory range `14.3.0-canary.0 – 16.3.0-preview.7` now covers 9 CVEs (up from 4 in W31) including `GHSA-6gpp-xcg3-4w24` (proxy/middleware bypass — directly relevant to `proxy.ts` CSRF gate). No released version clears the range; `npm audit fix` bumps to 16.2.12 which is still inside it. Monitor the Next.js release channel.
- **sql/185 on TEST only**: `recordings.share_audio` migration was applied to TEST during W31. Production push has not been authorized yet; the share audio feature is in prod code but `share_audio` column doesn't exist in prod DB. Next authorized push must include `npm run migrate sql/185_recordings_share_audio.sql`.
- **Presentation tab unverified in browser**: Both shared TEST meetings have `proceedings_summary = NULL`, so the public Presentation tab has been typecheck/code-verified but never rendered. If any meeting with a summary is shared, this tab surfaces untested.
- **ESLint ceiling at 252/252**: Zero headroom — any hooks-touching change that adds a `react-hooks/*` warning fails `lint:ci` immediately. Ratchet is working as designed but the fragility is worth tracking.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 13.5 |
| Security | 8/10 | 15% | 12.0 |
| Dependencies | 6/10 | 15% | 9.0 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 8/10 | 15% | 12.0 |
| Documentation | 9/10 | 10% | 9.0 |
| Maintainability | 9/10 | 20% | 18.0 |
| **Total** | | **100%** | **81.5 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**

- **Next.js 9 CVEs (framework-level, unfixable)** — advisory range `14.3.0-canary.0 – 16.3.0-preview.7`; installed 16.2.12 is inside it and is `latest` on npm. New CVEs surfaced since W31 (4→9):
  - `GHSA-6gpp-xcg3-4w24`: Middleware/proxy bypass with Turbopack + single locale — directly relevant to `proxy.ts` CSRF gate.
  - `GHSA-4633-3j49-mh5q`: Cache confusion for invalid UTF-8 byte sequences.
  - `GHSA-4c39-4ccg-62r3`: Unbounded Server Action payload in Edge runtime.
  - `GHSA-p9j2-gv94-2wf4`: SSRF via attacker-controlled destination in rewrites.
  - `GHSA-q8wf-6r8g-63ch`: DoS in Image Optimization API via SVGs.
  - `GHSA-955p-x3mx-jcvp`: Unauthenticated disclosure of internal Server Function endpoints.
  - Plus 3 from W31: `GHSA-m99w-x7hq-7vfj` (DoS/Server Actions), `GHSA-89xv-2m56-2m9x` (SSRF/custom servers), `GHSA-68g3-v927-f742` (cache confusion).
  - Action: wait for Next.js 16.3.0 stable release, then upgrade deliberately.
- **sharp <0.35.0 (4 libvips CVEs, HIGH)** — `CVE-2026-33327`, `CVE-2026-33328`, `CVE-2026-35590`, `CVE-2026-35591` via `next` → `sharp`. No released fix independent of Next.js upgrade.

**MEDIUM**

- `lib/chatCore.ts:2040` and `2159` — `service.from('bot_conversation_turns').update({ content_flags: ... }).eq('id', assistantRow.id / userRow.id)` without `.eq('org_id', ...)`. Violates the multi-tenancy invariant for service-role writes. Blast radius is low (rows are just written by the current authenticated session; `assistantRow`/`userRow` ids are not attacker-controlled), but the class violation persists after the W31 `last_session_at` fix.
- **ESLint ceiling at 252/252 warnings** — zero headroom on `lint:ci`. Any new `react-hooks/*` warning in touched code fails CI before a fix is applied. Ratchet is working but fragility is a weekly friction cost.
- `app/` directory: 554 files — navigability concern as route tree grows (+2 from W31).

**LOW / informational**

- Test ratio: 171 test files / 1035 TypeScript source files = 0.165 — below the 0.5 target. Absolute count 1,599 tests passing (all green, 158 files); attribution work added 23 new tests this week, but source file growth (138 files changed) offsets ratio gains.
- **DOMPurify ≤3.4.11 (low, GHSA-c2j3-45gr-mqc4)** — `CUSTOM_ELEMENT_HANDLING` bypass; intentionally not patched (pinned `isomorphic-dompurify`/`jsdom@26` cluster with a do-not-bump constraint; 3.4.12 available but low-severity gain not worth the risk).
- **brace-expansion DoS** — likely still present as a transitive dep; npm audit may be consolidating it under the Next.js/sharp count.
- 1 circular dependency: `lib/types.ts → lib/industryDefaults.ts`. No change from W30/W31.
- Minor packages behind by patch/minor: `@aws-sdk/*` (3.1079→3.1095), `@supabase/supabase-js` (2.105→2.110), `@playwright/test` (1.59→1.62), `@sentry/nextjs` (10.63→10.68), `@vercel/sandbox` (2.0→2.9). No CVEs on these.
- `sql/185` (`recordings.share_audio`) applied to TEST only; prod migration pending an authorized push.
- No `.claude/agents` or `.claude/rules` defined — governance relies on `CLAUDE.md` + pre-commit hooks + 4 custom commands. Consistent with prior weeks.

### Progression plan (top 3 to fix this week)

1. **Monitor Next.js 16.3.0 stable release and upgrade** — closes 9 CVEs including the proxy/middleware bypass (`GHSA-6gpp-xcg3-4w24`) directly relevant to `proxy.ts`. This is the single highest-impact dependency action. Check https://github.com/vercel/next.js/releases; upgrade deliberately (not via `npm audit fix`) when stable lands.
2. **Fix `lib/chatCore.ts:2040` and `2159`** — add `.eq('org_id', session_org_id)` to the `content_flags` updates on `bot_conversation_turns`. The `bot.org_id` is in scope throughout `handleChatTurn`; same pattern as the W31 `last_session_at` fix. ~10 min, tsc-clean.
3. **Browser-verify the public Presentation tab** — share a recording with a non-null `proceedings_summary` on TEST and confirm the tab renders without leaking internal fields. One-time QC; unblocks confident use of the full public share feature.

## 3. Correlation: dev work → audit findings

- **Next.js 9 CVEs (HIGH)**: New advisories published upstream this week (4→9). No local code change caused them. Devlog documents the rejected `npm audit fix` and explains why waiting for 16.3.0 is the correct posture. **Not addressed** (no fix available).
- **sharp 4 CVEs (HIGH)**: Transitive via Next.js; same fix path as Next.js upgrade. **Not addressed** (no fix available).
- **RSC payload leak (CRITICAL, now closed)**: Devlog "Town Hall: the public share link IS the report now" documents the allowlist fix explicitly. **Addressed and closed this week.**
- **chatCore.ts:1857 org_id pairing (MEDIUM from W29–W31)**: Devlog "chatCore: pair org_id on the last_session_at update" documents fix with rationale. **Addressed and closed this week.** New MEDIUM at chatCore.ts:2040/2159 opened (same class, different path).
- **ESLint 252/252 ceiling**: Devlog "CI fix: HelpWidget lint ratchet 253→252" maintains the ceiling without lowering it. **Partially addressed** (ceiling held; headroom gap persists).
- **sql/185 prod migration pending**: Devlog footnote "⏭ ON PUSH: `npm run migrate sql/185_recordings_share_audio.sql`" flags it. **Not yet addressed** (awaiting authorized push).

## 4. Trend

| Week | Score | Delta |
|------|-------|-------|
| 2026-W27 | 83.0 / 100 | baseline |
| 2026-W28 | 79.0 / 100 | −4.0 (3 drifted specs, 14 moderate CVEs) |
| 2026-W29 | 80.5 / 100 | +1.5 (CVEs 14→7; lint 3,062→346) |
| 2026-W30 | 86.0 / 100 | +5.5 (0 CVEs, no-explicit-any → error, e2e gate added) |
| 2026-W31 | 80.0 / 100 | −6.0 (4 high CVEs published upstream; no code commits) |
| **2026-W32** | **81.5 / 100** | **+1.5 (RSC payload leak closed, chatCore org_id fix; offset by 5 new Next.js CVEs)** |

The +1.5 recovery is driven by the Security category improvement (RSC payload leak fix, chatCore multi-tenancy fix) from a highly productive week (31 commits) partially offset by 5 additional Next.js CVEs materializing upstream. Dependencies category remains capped at 6/10 until Next.js 16.3.0 stable is released.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — Next.js 16.3.0 timeline to monitor?
- [ ] Review the spec-drift companion (2026-W31-spec-drift.md) — any spec debt?
- [ ] Confirm `sql/185` migration will be included in the next authorized production push.
- [ ] Check correlation section — open findings being worked on?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
