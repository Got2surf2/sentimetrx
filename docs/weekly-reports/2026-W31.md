# Weekly Governance Report — 2026-W31 (Week of Mon 27 Jul to Sun 02 Aug)

**Generated**: 2026-07-27T08:10:48Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: git log fallback (no 2026-W31-devlog.md)
**Spec-drift**: 0 drifted — see 2026-W31-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 1 commit, 1 author (Sanjay Patel), top-level dirs touched: `docs/`

### Narrative (from git log)

- **Governance housekeeping**: Merged the W30 weekly governance + spec-drift PR (#25). Docs-only: `2026-W30.md` (health 86.0/100, +5.5 vs W29) and `2026-W30-spec-drift.md` (0 drifted, 6 updated, 49 commits in range). No application code committed this week.

### Risk signals

- **npm advisory database added 4 high-severity CVEs against packages in the lock file** — no package versions changed locally; new advisories were published upstream. Affects Next.js (middleware bypass, Server Actions DoS, SSRF, cache confusion), DOMPurify (XSS bypass for custom elements), fast-uri (host confusion), and brace-expansion (DoS). All fixable via `npm audit fix`. This is the dominant risk signal for the week.
- Quiet week with zero application commits. Prod migration queue (sql/166–184, 19 migrations) from W30 remains unpushed.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 13.5 |
| Security | 7/10 | 15% | 10.5 |
| Dependencies | 6/10 | 15% | 9.0 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 8/10 | 15% | 12.0 |
| Documentation | 9/10 | 10% | 9.0 |
| Maintainability | 9/10 | 20% | 18.0 |
| **Total** | | **100%** | **80.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**

- **Next.js 4 CVEs (framework-level)** — affects versions in range 14.3.0-canary.0–16.3.0-preview.7, which includes the installed version:
  - `GHSA-6gpp-xcg3-4w24`: Middleware/proxy bypass in App Router with Turbopack + single locale — directly relevant to `proxy.ts` CSRF protection.
  - `GHSA-m99w-x7hq-7vfj`: Denial of Service in App Router via Server Actions.
  - `GHSA-89xv-2m56-2m9x`: Server-Side Request Forgery in Server Actions on custom servers.
  - `GHSA-68g3-v927-f742`: Cache confusion for response bodies in requests with bodies.
  - Fix: `npm audit fix` (upgrade available).
- **DOMPurify ≤3.4.11 (XSS bypass, GHSA-c2j3-45gr-mqc4)** — `CUSTOM_ELEMENT_HANDLING` config bypasses `afterSanitizeElements` for allowed custom elements. Fix: `npm audit fix`.
- **fast-uri 3.0.0–3.1.3 (host confusion, GHSA-v2hh-gcrm-f6hx)** — literal backslash authority delimiter allows host confusion. Fix: `npm audit fix`.
- **brace-expansion ≤5.0.7 (DoS, GHSA-3jxr-9vmj-r5cp / GHSA-mh99-v99m-4gvg)** — exponential-time and OOM expansion via crafted patterns. Affects `brace-expansion` in 7 dependency paths (eslint, sentry, @oclif/core, @vercel/queue, etc.). Fix: `npm audit fix`.

**MEDIUM**

- `lib/chatCore.ts:1857` — `service.from('agents').update({ last_session_at }).eq('id', bot.id)` without `.eq('org_id', bot.org_id)`. Violates the multi-tenancy invariant (service-role queries must pair `id` with `org_id`). Carried from W30; not addressed. Low blast radius (bot.id loaded after auth gating) but a policy violation.
- ESLint ceiling at 252/252 warnings — zero headroom. Any new code that touches hooks and adds a `react-hooks/*` warning will fail `lint:ci` on the next CI run. The ratchet cannot creep up, but the zero-headroom state makes any hooks-touching change fragile.
- `app/` directory: 552 files — navigability concern as route tree grows. No structural change from W30.

**LOW / informational**

- Test ratio: 159 test files / 897 TypeScript source files = 0.17 — below the 0.5 target. Absolute count 1,563 tests passing; suite quality high (all green, 150 test files passing). Ratio is a chronic low finding.
- Multiple packages behind by minor/patch versions: `@aws-sdk/*` (3.1079 → 3.1095), `@supabase/supabase-js` (2.105 → 2.110), `@playwright/test` (1.59 → 1.62), `@sentry/nextjs` (10.63 → 10.68), `@vercel/sandbox` (2.0 → 2.9), `autoprefixer` (10.4 → 10.5). Minor security-risk surface; no CVEs on these.
- 1 circular dependency: `lib/types.ts → lib/industryDefaults.ts` (detected by madge). Not new; no change from W30.
- No `.claude/agents` or `.claude/rules` defined — governance relies on `CLAUDE.md` + pre-commit hooks + 4 custom commands. Consistent with prior weeks.
- Prod migration queue remains: sql/166–184 (19 migrations) TEST-verified but unpushed to production.

### Progression plan (top 3 to fix this week)

1. **Run `npm audit fix`** — clears all 4 high CVEs (Next.js, DOMPurify, fast-uri, brace-expansion) and 2 low CVEs in one command. Estimated time: 5 minutes. Commit and push in the next authorized push window.
2. **Fix `lib/chatCore.ts:1857`** — add `.eq('org_id', bot.org_id)` to the `last_session_at` update call. `bot.org_id` is in scope. Closes the multi-tenancy invariant gap that has been open since W29. ~10 min fix.
3. **Schedule a push window for sql/166–184** — 19 TEST-verified migrations including the live chart numeric-parity fix (sql/182) and the Help agent (sql/184). The queue depth is the widest prod/test gap in recorded history.

## 3. Correlation: dev work → audit findings

- **Next.js 4 CVEs (HIGH)**: New advisories published upstream this week — no local code change caused them. Not mentioned in devlog (no devlog this week). Requires `npm audit fix` response.
- **DOMPurify XSS bypass (HIGH)**: Same — upstream advisory. Not mentioned in devlog.
- **fast-uri / brace-expansion DoS (HIGH)**: Upstream advisories. Not mentioned in devlog.
- **`lib/chatCore.ts:1857` org-pair MEDIUM**: Carried from W30. Not in devlog. Not addressed this week (no code commits).
- **ESLint 252/252 ceiling**: No change. Not addressed (no code commits).

## 4. Trend

| Week | Score | Delta |
|------|-------|-------|
| 2026-W27 | 83.0 / 100 | baseline |
| 2026-W28 | 79.0 / 100 | −4.0 (3 drifted specs, 14 moderate CVEs) |
| 2026-W29 | 80.5 / 100 | +1.5 (CVEs 14→7; lint 3,062→346) |
| 2026-W30 | 86.0 / 100 | +5.5 (0 CVEs, no-explicit-any → error, e2e gate added) |
| **2026-W31** | **80.0 / 100** | **−6.0 (4 high CVEs published upstream; no code commits)** |

The −6.0 regression is driven entirely by 4 new high-severity advisories published to the npm registry against packages already in the lock file. No application code changed this week. Running `npm audit fix` should recover most of this drop in W32.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — `npm audit fix` authorized before next push?
- [ ] Review the spec-drift companion (2026-W31-spec-drift.md) — any spec debt?
- [ ] Check correlation section — open findings being worked on?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
