# 2026-W32 devlog (Aug 3 – Aug 9)

Brief WHY entries for meaningful commits/ops this week. The Monday governance routine reads this.

## Dimensions: rating-based sentiment fallback for neutral "who" subs (Aug 7)

WHY: On the Flemings dataset the "Service — who served you" sub-cards (Server, Manager, Chef) showed "No sentiment signal" despite carrying star ratings and large mention counts. Cause is by-design: the `touchpoint` axis entities are all *neutral* keyword phrases (`lib/taxonomyKeywords.ts:44` — "Pure category words: polarity neutral, context fills in pos/neg"), so they accumulate mention counts but never pos/neg assertions → `posPct` stays null → "No sentiment signal", even though the sibling `attribute` axis carries the actual service sentiment. Fix: when a sub has no polarised mentions (`pos+neg == 0`), `posPct` now falls back to the **star ratings of the mentioning reviews** (share rated ≥4★), consistent with the Ratings-=-all-reviews principle and the emotion axis's existing ≥4/≤3 rating split. New `SubStat.sentBasis` (`'keyword' | 'rating' | null`) records the method so the card labels the fallback "· by rating" and the footnote stays honest — the two denominators are never silently blended. Touches `lib/taxonomyRollup.ts` (accumulate subPosRows/subNegRows, finalize fallback) + `components/analyze/TaxonomyModule.tsx` (label) + `docs/TAXONOMY.md`. Requires a rollup refresh (re-run the keyword classify on the Dimensions tab) to regenerate the stored rollup — the fallback reads only already-embedded row ratings, so no row re-classification is needed. tsc clean; 1602 tests green (3 new rollup cases).

---

## Stats: logistic Bottom Line — Plain English now genuinely differs from Expert (Aug 7)

WHY: On the logistic regression result the "Bottom Line" card's Expert and Plain-English toggle rendered identical text. Cause: `logitBL(naive)` in `StatsModule.tsx` only branched on `naive` for the "no significant terms" case — the main (significant-terms) branch ignored the flag and always emitted the odds-ratio sentence ("5 of 6 terms significantly affect the odds… cuts the odds 0.06× (−94%)… McFadden pseudo-R² = 0.06"), so a non-technical owner got the same jargon on both toggles. Added a real plain-English branch matching the house `*BL_naive` voice in `lib/statsUtils.ts`: names the single biggest lever, phrases its effect as "makes that ~X% more likely" / "cuts those chances by ~X%", and translates pseudo-R² into "explains only a small part / a good part / most of what's going on" — no "odds", no "McFadden", no "pseudo-R²". Expert branch byte-identical to before. UI-string logic only (`logitBL` is inline in the component, not unit-tested); tsc clean, stats + full suite green, no lint delta.

## Stats: both regression Bottom Lines given a Gemini-style plain-English brief (Aug 7)

WHY: Owner compared our Plain-English toggle to Gemini's breakdown of the same logistic output and wanted ours as complete for a non-technical reader. Enriched the logistic plain branch to a full layperson brief — target, "a real pattern, not a fluke (holds across all N responses)", the biggest lever in intuitive terms, and the model-fit caveat — pulling the sample size from `fit.d.n`. Gave the **linear** regression the same treatment: `regrBL_naive` now states the target + sample size + reliability + fit% in plain words and lists **directional drivers** ("Linked to a higher / lower <outcome>: …") by coefficient sign, replacing the old flat "the factors that matter most are: …" list. Expert registers for both are unchanged. `docs/ANALYTICS.md` updated. tsc clean; stats (36) + full suite green; no lint delta.

---

## Analyze: fix "Manage members → Save changes does nothing" on collections (Aug 7)

WHY: Owner reported adding a dataset to a collection and clicking Save changes did nothing. Root cause was a stale-closure bug in `AnalyzeClient.tsx`, not the modal or the API — the add always succeeded server-side. `ManageMembersModal.save()` calls `onChanged()` then `onClose()` synchronously in one tick; `onChanged` did `setManageDirty(true)` and `onClose` read `const dirty = manageDirty` to decide whether to `window.location.reload()`. Both callbacks are closures from the same render, so `onClose` saw the pre-update `false`, skipped the reload, and the grid never refreshed — the member was in the collection but invisible until a manual page refresh, which read as "Save does nothing." Fixed by tracking the dirty flag in a `useRef` (readable synchronously in the same tick) instead of `useState`. Swept the class: no other parent reads a sibling-callback-set state value synchronously in an onClose. tsc clean; 1602 tests green.

---

## Stats: logistic Plain English now names which factors matter (and which don't) (Aug 7)

WHY: Owner: the logistic brief said "5 of 7 move the needle" without saying *which* 5 (or which 2 don't) — a count isn't actionable. `logitBL(naive)` now, after the biggest lever, lists the other significant movers grouped by direction ("Others that make <outcome> less likely: …" for odds ratio &lt; 1, "Others that make it more likely: …" for &gt; 1) and then the non-significant terms ("The N that didn't make a clear difference: …"), each name resolved through `aliases` and capped at 6. Built from the existing `sig` / `ps` coefficient arrays — no new computation. Expert branch unchanged. `docs/ANALYTICS.md` updated. tsc clean; full suite green.

## Classify: auto-retry the transient statement-timeout on the verdict write (Aug 7)

WHY: Owner hit "Couldn't classify — Classification failed: HTTP 500" enabling Dimensions on a ~4.5K-row dataset locally; "Try again" then worked. Root cause is a Postgres statement timeout (57014) on `apply_taxonomy_verdicts` in `embedVerdicts` (`lib/taxonomyClassify.ts`) — the 500-row write crosses the timeout when the DB is momentarily slow / under the concurrent read load the Dimensions tab fires (signal-stats + rows + theme-counts all at once). Unrelated to the same-day Dimensions sentiment-fallback change (that lives in the rollup step, which runs after the write and is already try/caught). Since the RPC is an idempotent upsert per (row, fieldKey), re-applying the same batch is safe: `embedVerdicts` now retries a *transient* failure (57014 / timeout / connection-reset messages) up to 3 attempts with 0.5s/1s backoff, turning the manual "Try again" into an automatic recovery instead of a user-facing 500. A non-transient error still throws immediately. tsc clean; 1602 tests green.

## Outlet action plan: name the specific outlet + guarantee verbatim quotes are real (Aug 7)

WHY: Two owner corrections on the Outlet Report's LLM action plan (`lib/outletActionPlan.ts`). (1) The KEEP DOING narrative opened with the bare brand ("Fleming's has real momentum") instead of the specific location; the prompt now derives a `locationLabel` ("<brand> <city, state>", skipping the brand prefix when `outletName` already carries it, e.g. "Fleming's Lake Nona") and instructs the model — in both the system prompt and the Location line — to refer to the location by that full name, never the bare brand. (2) **Credibility fix — the shown verbatim could be non-verbatim.** `validateVerbatims` matched the model's quote to a real candidate by NORMALIZED substring (case/punctuation stripped) but then rendered the *model's* text when it was ≤ the candidate length — so a silently re-punctuated / typo-fixed / stitched quote printed something no reviewer actually wrote (reads as fabricated). It now always renders `match.raw` — the exact candidate, which is a true review substring (`extractSentence`) — plus de-dupes trims that map to the same real quote; the prompt's verbatim rule changed from "you may lightly trim" to "copy EXACTLY". Exported `validateVerbatims` + added `tests/unit/outletActionPlan.test.ts` (5 cases) to lock the guarantee. Cache `actionPlanBasis` bumped v2→v3 so existing plans regenerate. tsc clean; 1607 tests green.

## Outlet action plan: refer to "your location" (brand/outlet names can be messy) (Aug 7)

WHY: The v3 specific-naming attempt produced garbled prose on demo/dirty data — a diagnosis read "…at Flemings Reviews for demo Fleming's Prime Steakhouse & Wine Bar" because `input.brand` was the dataset name ("Flemings Reviews") and `input.outletName` was "demo Fleming's Prime Steakhouse & Wine Bar", and the locationLabel concatenated them. Since the report is always scoped to ONE location, owner's call: drop the naming entirely and refer to it in the second person as **"your location"** — unambiguous and never wrong. Removed the `locationLabel` construction, stopped feeding the raw brand/outlet names into the prompt, and instructed the model (system + Location line) to always say "your location", never a brand/store. `actionPlanBasis` bumped v3→v4 so the garbled cached plans regenerate. Verbatim-accuracy fix from the prior commit is unchanged. tsc clean; tests green.

## Stats: logistic Plain English rendered as a colour-coded block (Aug 7)

WHY: Owner: the logistic Plain-English brief read kludgy as one long run-on paragraph, and asked to bold the factor names and colour-code by direction (red = hurts the outcome, green = helps). Split `logitBL` into an Expert string + a new `logitBLNode()` that returns rich JSX: an intro line (target + N + real-pattern), then three rows — LOWER THE ODDS (red) / RAISE THE ODDS (green), each factor **bold** with its own ±% effect, and NO CLEAR EFFECT (muted, the non-significant terms) — then the model-fit caveat. `BottomLine` gained a `naiveNode` prop (falls back to `naiveText` for every other Stats result, which stay plain strings). Expert register unchanged. Linear regression Plain English still a string — same block treatment deferred. `docs/ANALYTICS.md` updated. tsc clean; 41 stats/logit tests + full suite green.

## Stats: linear Plain English gets the same colour-coded block (Aug 7)

WHY: Owner: apply the logistic colour-coded block to the linear regression too. Added `regrBLNode` (module-level in StatsModule.tsx) mirroring `logitBLNode`: intro (target + N + real-pattern), then **LINKED TO LOWER** (red) / **LINKED TO HIGHER** (green) rows with bold factor names by coefficient sign, **NO CLEAR EFFECT** (muted), and the R²-based fit caveat. Deliberately **no per-factor %** here — OLS betas aren't standardized, so magnitudes aren't comparable across differently-scaled predictors; only the sign (direction) is safe to show (logistic keeps its odds-ratio %). Wired the linear `BottomLine` to `naiveNode={regrBLNode(...)}`, dropped the now-unused `regrBL_naive` import, and removed the orphaned `regrBL_naive` generator from `lib/statsUtils.ts` (my change was its only caller). tsc clean; 1607 tests green.

## One-off: Trump corpus builder — everything he said or posted since the inauguration (Aug 7)

WHY: Owner asked for a research corpus of Trump's speech — official speeches plus the ad-hoc material — and everything posted online, back to the 20 Jan 2025 inauguration, then narrowed it mid-build to exclude executive orders and other legal instruments "unless there is narrative in there that gives me a perspective of what is going on in his mind". Added `scripts/oneoff/_trump_corpus_fetch.ts` (not wired into the app; writes to ~/Downloads/trump-corpus). Two free key-less sources: GovInfo DCPD (official record, public domain — package IDs from the per-year sitemaps, one HTML fetch per package for title/date/full text/speaker labels, disk-cached so re-runs are free) and the CC0 Truth Social archive via its CNN mirror (always re-fetched, since a cached copy would silently truncate recent posts). Federal Register was evaluated and dropped as pure duplication — DCPD already carries EOs in full text.

The corpus's value-add is speaker attribution: DCPD renders each turn as `<p class="s2">The President. <span class="p">…</span></p>`, so `trump_text` holds his words only while `text` keeps the full exchange — without that split, reporters' questions get scored as if he said them. QC caught two real bugs: DCPD switches the label to "President Trump" whenever a foreign head of state is present, which had left 112 bilateral meetings and joint news conferences with an EMPTY `trump_text` (fixed → 3, all genuine "no transcript was provided" notes); and a naive prefix match would have swallowed "The President's daughter-in-law Lara J. Trump", so the matcher anchors on `(\.|\s|$)` and caps label length. Verified "Mr. Trump" in the Turning Point call is Donald Trump, Jr., correctly excluded. The CSV ships a 500-char excerpt rather than transcripts, because single documents exceed 130,000 chars and Excel silently truncates any cell over 32,767 — full text is in the NDJSON. Excluded legal/administrative documents are written to a side file WITH full text so anything with a narrative preamble can be recovered without re-fetching. Result: 11,279 items (696 spoken/statement + 10,583 posts), 2.78M Trump-attributed words. Known gap documented in the manifest: DCPD lags ~5 weeks so the spoken record ends 1 Jul 2026 while posts run to today; whitehouse.gov cannot fill it (its sitemap carries exactly one remarks transcript). tsc clean.

## One-off: Trump corpus ingested into Sentimetrx as two datasets (Aug 7)

WHY: Owner wants the core themes and how they shift over time, and explicitly wants OUR OWN AI extraction rather than relying on the external Federal Register subject headings. Added `scripts/oneoff/_trump_corpus_ingest.ts` → TEST project by default (`--target prod` must be typed explicitly).

TWO datasets, not one, for three separate reasons that happen to agree: (1) pooled, the 10,583 posts would swamp 696 spoken documents and any "theme share" would really be measuring Truth Social; (2) combined they are 56,731 rows, over the 50K threshold where Statistics switches to sampling — split they are 49,046 and 7,685, both under; (3) the OFR subject headings only exist on the spoken side. Row granularity is the other load-bearing decision: whole spoken documents run a median 2,317 words and up to 18,927, far too long to be one verbatim (themes come back generic), so the spoken side is split on paragraph breaks → median 40 words, the size the extractor is tuned for, with sub-15-word fragments ("Yes." / "Thank you.") dropped.

Per owner mid-build: the pre-existing tags are FIELDS and never folded into the body — if they were concatenated into `text` the extractor would rediscover them and we would have proved nothing. Three shapes: `ofr_subjects` (raw), `ofr_subject_tags` (1,922 full headings), `ofr_topics` (collapsed to the head term before the comma → 1,127, the grain you actually chart). Verified post-ingest that no subject tag string appears in any `text`. That leaves the labels usable as a professionally-labeled calibration set: run our extractor on the spoken side, check it against the OFR labels, then apply it to the unlabeled posts with a known error rate.

Also caught during verification that 1,047 posts (13.6% of those with text) are reposts carrying an `RT @` prefix — someone else's words. Previously listed as an un-closable gap in the corpus manifest; now a filterable `is_repost` field, kept rather than dropped since sharing is still a signal of attention. Owner also resolved the reporter-agenda confound in the other direction: he frequently ignores the question and pivots to his own point, so the theme that surfaces is his regardless of what prompted it — that bias is accepted deliberately rather than engineered around. Resolves org/user against whichever DB it targets (test project carries orphan [CLONE] orgs with no members; membership is `users.org_id`, not a join table). tsc clean; 49,046 + 7,685 rows verified against row_count.

## Trump corpus promoted to PRODUCTION + leak check corrected (Aug 7)

WHY: Owner: "shouldn't we just be loading this into our platform." Correct — TEST was the proving ground, not the destination; the test DB gets re-synced from prod schema after migrations, so anything built on it evaporates. Ingested both datasets to the Datanautix prod org: **Spoken Remarks (paragraphs) 49,046 rows `7bc69ebd`** and **Truth Social Posts 7,685 rows `5f13abc3`**. Additive and reversible (two new datasets, nothing existing touched), no build cost — a DB write, not a deploy.

Verification on prod caught that my own no-leak check was wrong, not the data. The check tested `text.includes(someTag)`, which flags him simply SAYING the topic — OFR headings are often ordinary English ("World Cup", "Border security") — and it reported 16/400 apparent leaks. Investigated with a throwaway classifier: all hits are natural speech ("So I'll be President during the World Cup…"), and **0 rows contain the raw semicolon-joined `ofr_subjects` blob**, which is the unambiguous test since no sentence reproduces a delimited heading list. The earlier TEST run's "NONE ✓" was worse than wrong — it sampled a single row and passed vacuously. Rewrote the check in `scripts/_verify_trump_corpus.mts` to test for the raw blob with a comment explaining why the substring test must not be used. Guarantee holds and is now actually tested: prod re-verified NONE across both datasets, row counts match exactly, zero empty-text rows. Untracked KEEP: `scripts/_verify_trump_corpus.mts`.

## Fix: scripted dataset ingest must create dataset_state AND a detected schema (Aug 7)

WHY: The Trump corpus ingest produced datasets that existed, had correct rows, and were unusable — owner hit a bare 404, then (after a partial fix) a Schema tab with no fields and a disabled "Mine with AI". Two separate omissions, both because `scripts/oneoff/_trump_corpus_ingest.ts` was modelled on `scripts/pilot-rc-ingest.ts`, which inserts rows and stops. The existence of `scripts/repair-review-dataset-state.ts` in the repo was the standing evidence that this template is incomplete, and I should have read it as such.

(1) **`dataset_state` row.** `app/analyze/[datasetId]/layout.tsx` and every tab call `notFound()` when it is missing — hence a 404 rather than an error, since to the router the page genuinely does not exist. (2) **Detected schema.** An empty `schema_config` is not enough: the upload route (`app/api/datasets/route.ts`) inserts `emptySchemaConfig()` but the CSV flow then runs `autoDetectSchema(rows)` and stores the result. Without it the Schema tab renders zero fields and TextMine reports "0 open-ended fields", which disables mining. `mergeSchemaStats` cannot substitute — it returns early when `schema.fields` is empty.

(3) **Detection over-reach, caught by the new check.** `autoDetectSchema` classified `ofr_subjects`, `ofr_subject_tags`, `ofr_topics`, `ofr_categories` and `doc_title` as open-ended (they are long multi-word strings), which would have handed the Federal Register's own tags to the theme miner — the AI would "discover" themes that are just the pre-existing labels read back, defeating the whole point of keeping tags out of the body. Now only `text` may be open-ended; everything else is demoted to categorical, staying fully usable as filters and Compare dimensions. `primaryTextField` is repinned to `text` afterwards, because `autoDetectSchema` sets it to the first open-ended field, which may be one just demoted.

Added `--repair-state` to backfill already-ingested datasets without re-inserting 49,046 rows, and it force-recomputes: the non-destructive "skip if schema non-empty" guard silently preserved the very schema being repaired (observed — the first repair reported "kept" and changed nothing). Both prod and test datasets repaired and re-verified. Rewrote `scripts/_verify_trump_corpus.mts` (untracked KEEP) into a readiness check that mirrors what each tab queries — rows vs row_count, dataset_state, schema field count, open-ended set, primaryTextField/primaryDateField, theme_model, tag isolation, empty text — and exits non-zero on failure, so this class of bug is caught before the owner sees it rather than after. Prod: ALL CHECKS PASS on both datasets, `text` the sole open-ended field (avgWords 41.8 / 32.7). tsc clean.

## Trump corpus analysis: theme mining, salience audit, "Silence Gap" report (Aug 7)

WHY: Owner wants talking points for Democratic campaigns, framed as exactly two message types — silence gap ("X matters to you, he never mentions it") and position gap ("here is what he actually thinks") — and narrowed to UNSCRIPTED material only (tarmac/plane/phone interviews/gaggles), not staged speeches. Four scripts: `_trump_corpus_mine.ts` (AI theme mining per half-year slice + a consolidation pass, because mining slices independently rediscovers the same theme under different names and counting it three times makes every share wrong), `_trump_corpus_series.ts` (per-month measurement), `_trump_corpus_salience.ts` (issue attention via explicit auditable keyword lists), `_trump_corpus_audit.ts` (verification of every published number).

Deliberately did NOT build the report on the mined themes: the question is about ABSENCE, which a list of found themes cannot measure, and a per-keyword audit showed some themes are carried by one generic word (a 7.4% theme was 54% "beautiful", another 48% "hearing"). Verification then caught three inflators in my own first draft — (1) 736 endorsement form-letters whose fixed checklist mentions the border in 84% of cases, which had pushed immigration to 13.8% of posts vs 5.1% excluded (ratio to health care 15.2x → 5.5x); (2) a "guns" issue at 10.6% that was 95% the phrase "Second Amendment" from that same checklist, dropped entirely; (3) a ratio table dividing voter-poll % by attention-share %, which are different units on different populations — removed. Also fixed a silent all-zero series: `ThemeScan.perTheme` is a plain number[] aligned to the themes array, so indexing by theme.id (or reading .size) yields undefined and produces data that looks real.

Result: unscripted cut = 384 docs / 25,743 paragraphs. Affordability is equally absent unscripted vs staged (health care 0.7% vs 0.9%; "cost of living" said 3 times in 25,743 unscripted paragraphs; good-paying jobs / uninsured / preschool / affordable housing / minimum wage all zero), so it is not a staffing artefact. Immigration is a script not an instinct (3.0% staged vs 2.1% unscripted); tariffs the reverse (3.0% → 3.9%). Report at ~/Downloads/trump-corpus/REPORT_democratic_talking_points.md carries a corrections section documenting all three inflators so the method survives an opponent checking it.

---

## Trump corpus: endorsement correction made reproducible; four report numbers fixed (Aug 8)

WHY: Picking the corpus work back up, the first thing checked was whether every published number could be regenerated. Three could. The headline post figures could not — immigration 5.1%, crime 6.4% and the 5.5x immigration-to-health-care ratio were computed by hand mid-session and never written to a script. That is the one class of number that cannot be defended when someone asks how it was made, and the uncorrected alternative (15.2x) is the exact figure we had already retracted.

Writing `_trump_corpus_endorsements.ts` to reproduce them showed they were wrong. **The correction had been applied to the numerator but not the denominator**: removing the 737 endorsement form letters takes immigration from 958 posts to 336, but 336 was divided by the original 6,638-post base rather than the 5,901 that remain. 336/6638 = 5.06% ≈ the published 5.1%; the correct figure is 336/5901 = **5.7%**, and the ratio is **6.2x**, not 5.5x. Crime, in the same table, *had* used the corrected denominator (6.4%) — so two rows of one table were computed two different ways, and every other row was still raw. This is precisely the mixed-denominator error the report exists to avoid, committed inside the correction meant to prevent it. The script now prints both frames side by side and refuses to blend them.

Three further errors found in the same audit pass: (1) Part 3 recommended "sixteen posts about the border for every one about health care" as the strongest single contrast — that is the retracted 15.2x, contradicting Part 1 two pages earlier, and it sat in the one paragraph a campaign would actually lift; (2) the same sentence claimed voters rank "immigration ninth", which appears to be the Gallup 9% misread as a rank, and is unsupported by any cited poll — in the list where health care is second, immigration is third; (3) a ratio printed as "1.3%" instead of "1.3x". The report's method section now carries a reproducibility clause naming the script behind each figure, on the rule that a number no script can regenerate does not belong in it.

Corpus data itself is unchanged; no app code touched. Both one-offs verified against the real 11,279-item corpus.

---

## Trump corpus: two-page shareable summary PDF (Aug 8)

WHY: Owner wants something sendable to an external reader (campaign / consultant / prospect) rather than the long-form internal report. `scripts/oneoff/_trump_corpus_summary_pdf.ts` renders a two-page Letter brief to ~/Downloads via `brandedPdfChrome`, matching the Kimley-Horn / PPFL one-off pattern — Datanautix wordmark, confidentiality footer, page numbers.

Built from the CORRECTED figures only, and the script carries a do-not-reintroduce block naming both retracted numbers (the 15.2x "sixteen posts about the border" and the "immigration ninth" rank misread) so a future edit cannot walk them back in — this is the second time the 16x has had to be removed. Endorsement count is deliberately stated as "more than 730" rather than 737: the exact figure moves between 733 and 737 depending on how strictly the form letter is matched, and an external one-pager should not hang on a matcher choice. Verified by pdftotext grep that no retracted figure appears in the shipped file.

Pixel-QC caught two defects that tsc cannot: the four-column "his words against his words" table had collapsed (`131"cost of living"` ran together as one string, because the mid-row numeric column had `padding-right: 0` from the shared `.n` class) and adding the endorsement-checklist block overflowed page 1 by a single line, orphaning a pull-quote onto an otherwise blank page 3. Fixed with explicit per-column widths on `table.pair` and ~40px of reclaimed leading. Final: 2 pages, both full.


## Trump corpus summary PDF: rebuilt for readability (Aug 8, later)

WHY: Owner read the first cut and wanted it sharper, more concise and catchier, with taglines attached to the statements. The first version was a research document — dense tables, paragraph prose, findings the reader had to assemble themselves. Rebuilt around the number-then-line pattern: a hero stat block, three stat cards, a row of zeros, and short tables, with the report's "the line" quotes promoted from an internal artifact to the actual deliverable. Prose cut by roughly half; no figure changed.

The taglines were already written and verified in the long-form report and were simply omitted from the external cut — that was the mistake. They are the part a candidate says out loud, so they now sit directly under the number that earns them.

Two defects caught in pixel-QC, neither visible to tsc: a bare `.zero div` selector matched the `.n` and `.w` children as well as the cells, boxing every number inside a second border (fixed with a direct-child selector), and the section heading read "Four numbers" over three cards — fixed by adding the missing border-vs-doctor tagline so the count is honest. Also self-inflicted and worth noting: a CSS comment written with backticks around a selector name terminated the enclosing JS template literal and broke the build with a syntax error 60 lines away from the real cause. Same class as the NUL-byte grep miss — a stray delimiter in a string context. Final: 2 pages, verified by pdftotext grep that no retracted figure ships.

## Trump corpus: dedup audit — 737 endorsement "letters" are 629 (Aug 8, later²)

WHY: Owner asked whether the datasets are deduped and whether each headline number holds. They had not been checked — the earlier audit verified arithmetic and denominators, not row uniqueness. `_trump_corpus_dedup_check.ts` recomputes every published figure twice, raw and with exact duplicates removed, and prints them side by side. Exact dedup (not near-dedup) is the standard: endorsement letters are near-identical by design but each is a real letter about a different candidate, whereas byte-identical text is one thing he wrote appearing twice in the archive.

Two real findings. **(1) The endorsement count was wrong.** 737 posts match the form letter but 108 are duplicate re-posts of an identical letter, so it is **629 distinct letters**. The owner's sharper question — is it 730+ *different people*? — is a firm no: distinct people is lower than 629 again, because he re-endorses the same candidate (Patronis, Fine, Moore, Lawler each appear multiple times). No people count is published; name extraction off the letters is not reliable enough (epithet stacking like "America First Patriot" defeats it) and a bad number here is worse than none.

**(2) The unscripted-vs-staged section — which the report calls "the finding that matters most" — had no script behind it.** `adhoc_salience.json` is written by nothing in the repo, records no keyword lists, and its staged denominator cannot be reconstructed: 25,743 unscripted + 21,141 staged leaves 2,162 of the 49,046 spoken paragraphs in neither bucket. This is the same defect as the endorsement figures and I missed it in the reproducibility pass, having checked only that the endorsement numbers lacked a script. Rebuilt on a stated definition (staged = every spoken paragraph that is not unscripted): health care 0.73 vs 0.82, immigration 2.37 vs 3.24, tariffs 3.94 vs 3.09. **Every conclusion survives** — affordability flat, immigration a script, tariffs an instinct — but the published decimals do not reproduce and are restated.

Everything else holds. The spoken corpus is essentially duplicate-free (13 in 49,046, 0.03%); posts carry 253 duplicates (3.8%). All word counts, all the zeros, and "cost of living" 11 of 49,046 are unchanged. The PDF now runs entirely on deduped denominators (49,033 / 6,385 / 25,732 / 5,756) and the border-to-health-care ratio moves 6.2x to 6.3x. Mixing deduped and pre-dedup denominators is called out in the script header, since that is exactly the error the endorsement correction made the first time.

## Trump corpus: the obsession gap replaces the border comparison (Aug 8, later³)

WHY: Owner rejected the framing, correctly. "He posts about the border six times for every once about health care" does not land, because **the border is a real voter priority** — the contrast invites an argument he can win. The contrast only bites when the other side is something no voter has ever ranked: the ballroom, the Reflecting Pool, the Kennedy Center, windmills, the 2020 election. Nobody defends those as governing priorities. New `_trump_corpus_vanity.ts` measures them; the border comparison is removed from the PDF entirely.

Result: he has said "ballroom" **209** times and "preschool" **zero**; "golf" 159 vs "uninsured" 0; "windmill" 100 vs "good-paying jobs" 0; "reflecting pool" 69 vs "making ends meet" 0; "teleprompter" 32 vs "minimum wage" 1. Topic level: golf and his properties 248 vs child care 8 (31x), the ballroom 209 vs affordable housing 3 (70x). These are bare word counts on his own words — no interpretation is possible, which makes them the strongest form of evidence in the whole analysis.

Keyword audit did real work again. **"gold" (408 hits) was tested and DROPPED** — it is overwhelmingly "golden age" (a slogan) and "liquid gold" (oil), not the gilded-Oval-Office story it looks like; publishing it would have been the "Second Amendment" failure a third time. "statue" dropped as monuments policy. **"Kennedy Center" is cleaned**: a set of hits use it as a vantage point for the January 2025 DCA midair collision ("you could see it from Kennedy Center"), which is not him talking about the arts centre — an explicit crash-context filter removes them and the script reports the count. Also caught before publication: a summary tagline that **summed topic counts** (607) when overlapping passages mean the true union is **601** — topics are not disjoint and must never be added.

PDF rebuilt around it: hero is the bare 209-vs-0 pair, then the word-pair table, then the ratios; page 2 carries the full obsession list, the unscripted finding, and a "Lines you can use" block collecting the taglines.

## Trump corpus: deduped re-ingest to PROD + name-calling vs empathy (Aug 8, later⁴)

WHY: Owner asked for a re-ingest so the live datasets match the published, deduped numbers. Two real defects fixed on the way. **(1) The ingest had no dedup at all** — it built rows straight from the raw corpus, so the app and the report disagreed on the denominator. Now removes byte-identical text (first occurrence wins, keeping the earliest provenance) while deliberately keeping near-duplicates, because the endorsement form letters are near-identical by design and each is a real letter about a different person. **(2) `--recreate` deleted the dataset row and let the ~49,000-row cascade run as one statement, which hits the Postgres statement timeout (57014)** — caught on TEST, which is exactly why prod was not attempted first. A timeout mid-cascade on a LIVE dataset is the worst case: it rolls back after burning the timeout and never converges. `deleteDatasetSafely` now removes rows in 2,000-row batches, then the parent, pairing id with org_id.

TEST first (49,033 + 7,371, verifier ALL CHECKS PASS), then PROD, verified the same. Both reconcile exactly with the report: 49,033 spoken paragraphs, 7,371 posts of which 6,385 have `is_repost=false`. **Prod dataset IDs CHANGED** because `--recreate` drops and recreates: spoken `7bc69ebd…` → **`29913f38-ef4a-4901-97dd-27bae102d25f`**, posts `5f13abc3…` → **`8f6ce053-0a0a-4630-b265-9e9de3116615`**.

Owner also asked for name-calling against empathy. `_trump_corpus_insults.ts`: **1,575 passages of name-calling vs 148 acknowledging ordinary hardship — 10.6x** — and he called someone a name on **415 of 565 days in office (73.6%)** against **7 days** mentioning child care. This set needed more keyword pruning than any other: "crazy" (539) is "the numbers are crazy" and "blowing like crazy"; "stupid" (394) is mostly aimed at policy; "animal/thug/scum" are about criminals, a policy posture rather than name-calling; "radical left/marxist/communist" are political labels an opponent would rightly call ordinary partisan speech; and on the empathy side "going through" (100) is "going through FEMA" and "through the roof", "my heart" (16) is "a warm spot in my heart for TikTok". What survives are invented proper-noun epithets, which cannot be false positives. **Ritual condolence is counted separately (34) and deliberately not pooled** — he performs the formal duty of the office after a tragedy; the gap is that he almost never says an ordinary person is struggling to pay for something, and merging the two would hide exactly that.

## Trump corpus: external contrasts — CDC measles and BLS inflation (Aug 8, later⁵)

WHY: Owner wants outside consequences set against his personal attention ("X% more measles while he talked about his ballroom Y% of his days"), and a check on how often he claims the economy is great when it is not. These are the first figures in the project that do NOT come from the corpus, so both scripts hard-code the external numbers with source and access date, and neither ever divides an outside statistic by an attention share — that is the poll-over-attention error this project already retracted once. They sit side by side; no causal claim is made.

**Measles** (`_trump_corpus_measles.ts`): CDC cases 285 in 2024 → 2,289 in 2025 (**+703%**), with 2026 already at 2,465 by 6 August. Against 15.6% of his days in office spent on the ballroom, and 7 days on child care. Word choice is flagged in the script: **"outbreaks" tripled (16→48) while "cases" rose eightfold** — the owner's draft said outbreaks, and the 703% figure is cases; using the wrong noun would hand an opponent the correction.

**Economy** (`_trump_corpus_economy_claims.ts`): this one nearly shipped a bad number and is the better lesson. A bare "no inflation" match returns 106 passages across **75 days**, which reads like 75 days of denying present-day inflation. The sampled matches — printed by the script precisely so this gets caught — show most are **past tense about his first term**: "when I left, we had no inflation", "I had almost no inflation". That is a boast about 2017-2020, not a false claim about now, and 75 would have collapsed the moment anyone opened the quotes. Filtered to present-tense assertions only, it is **15 days** — smaller, and defensible: "food prices are down, there is NO INFLATION", "Yes, we have no inflation, and people are doing well". BLS over the same period: CPI 12-month +2.7% (Dec 2025), +4.2% (May 2026, largest since April 2023), +3.5% (Jun 2026); food at home +2.7%. Inflation was never zero. "Groceries are down" (22 days) is clean present-tense as matched and needs no filter.

⚠️ VERIFICATION LIMIT recorded in both scripts: cdc.gov and bls.gov both return HTTP 403 to our fetcher, so these figures come from search summaries of those pages rather than verbatim reads. Internally consistent and matching the known shape of the 2025 Texas outbreak, but one step removed — confirm by eye before anything goes outside.

PDF gains a closing juxtaposition block (703% / 15.6%) with the CDC attribution inside the block so it cannot be mistaken for our measurement, plus a method paragraph stating the no-division rule explicitly.

## Trump corpus: Factoids reference sheet (Aug 8, later⁶)

WHY: The material outgrew the two-page summary — obsession gap, name-calling, economy claims, measles and the silence shares will not all fit, and cramming them in would blunt the summary's single argument. So the summary stays as the argument and `_trump_corpus_factoids_pdf.ts` becomes the bank: every verified number, grouped, each with a usable line beside it, to be picked from. Three pages, `~/Downloads/Datanautix_TrumpCorpus_Factoids.pdf`.

The economy claims land here rather than in the summary, which is the right home for them — they are a different kind of claim (falsifiable assertions rather than absences) and they need the present-tense caveat spelled out, which would have cost the summary its momentum.

Two design rules the sheet enforces on the page rather than in a comment: **external figures carry an "EXTERNAL SOURCE" chip** so a reader can never mistake CDC or BLS numbers for something we counted, and the header carries a **confirm-by-eye warning** because cdc.gov and bls.gov both block automated reads. The method footer states what was tested and thrown out — "gold", "crazy", the 75-day "no inflation" figure — so the pruning is visible to anyone checking the work rather than buried in the scripts.

## Trump corpus: 30-second video spot (Aug 8, later⁷)

WHY: Owner asked for a 30-second spot from three headlines. `_trump_spot_video.ts` renders 750 frames at 1920x1080/25fps through headless Chrome — the same puppeteer path the PDFs use — and muxes with ffmpeg (h264 CRF 18, AAC, faststart). Output `~/Downloads/Datanautix_SilenceGap_30s.mp4`, verified by ffprobe at exactly 30.000s with both streams valid.

Structure: cold open on the corpus size, then ballroom 209 vs "preschool" zero, then name-calling on 415 of 565 days vs child care on 7, then measles +703% against 15.6% of his days, closing on "Is this what you voted for?" Numbers count up on an eased curve and the quoted word lands after the count settles, so the eye reads the number before the zero. The CDC figure is captioned CDC on screen — it is the only number in the spot we did not count, and it must never appear unattributed.

**The music is a synthesised placeholder, not licensed music, and is flagged as such in the script header with the one-line ffmpeg command to swap in a real track.** No music was downloaded. Worth recording plainly: I cannot hear the output — the audio is verified only by ffprobe for duration and stream validity, so it needs a human listen before use. The visuals were pixel-QC'd frame by frame, which caught a real defect: at 150px the scene-3 line wrapped and the second line ran into the datanautix wordmark. Fixed with a single nowrap line at 112px.

## Trump corpus: measles vs the market — and a correction to the draft copy (Aug 8, later⁸)

WHY: Owner drafted a spot pairing the measles rate against the stock-market rate plus the RFK Jr. confirmation. `_trump_corpus_measles_market.ts` verifies all three claims and ranks them by how well they survive attack, because they are not equally strong.

**The corpus claim is the strongest thing found in the whole project and should lead.** He has talked about the stock market on **137 days** (281 passages) and has said the word **"measles" three times in nineteen months** — 94x more passages about the market. Better still, the content: the script prints all three mentions in full, and **not one addresses the outbreak.** One calls measles "not something new" while cases were climbing; the other two argue for splitting the MMR shot. That is unfalsifiable, entirely ours, and needs no outside source.

**The rate claim is true but weaker and must not lead.** Measles +703% against the S&P's roughly +33% — measles rose ~21x faster. But a percentage change in disease cases and a percentage change in an equity index are **different units**; it works rhetorically and an opponent can still call it apples-to-oranges and be technically right. The market figure is also approximate: the published returns are calendar-year from 1 January, not from the 20 January inauguration, so it is stated as "about" and never to a decimal.

**A factual correction to the draft copy.** RFK Jr. was confirmed by the **SENATE**, 52-48 on 13 Feb 2025 (52 Republicans for; McConnell the only Republican against). House members get no confirmation vote, so "your Republican congress people confirmed him" is **false in a House district** and hands over a free correction. The copy now says senators.

Also fixed in review: the first cut printed each measles quote as the leading 400 characters, which truncated one of them BEFORE the word "measles" appeared — an unauditable quote in a document whose whole value is auditability. Excerpts are now centred on the matched word.

## Trump corpus: two spots — audible score, named subject, new close (Aug 8, later⁹)

WHY: Owner feedback on the first cut — no audible music, no signal that "he" is Trump, drop the Datanautix mark, and make a stronger closing ask. Plus a second spot built on the measles-versus-market contrast with charts. `_trump_spot_video.ts` now takes `--spot silence|measles|all` and shares one render harness and score.

**The music was inaudible for a concrete reason, now fixed.** The first bed was built on 55Hz and 82.5Hz tones; laptop and phone speakers reproduce almost nothing below roughly 150-200Hz, so on any device someone would actually watch this on it was silence. The score is now synthesised as 16-bit PCM in a 146-880Hz register: a slow Am-F-Dm-E progression with detuned pairs beating against each other for unease and a sparse descending bell figure, normalised to -3 dBFS. **I still cannot hear it, so the build MEASURES it** — `volumedetect` now reports mean -14.9 dB / max -3.0 dB against the previous near-silence. That catches inaudible or clipping, not ugliness; it still needs a human listen.

**No photograph was sourced.** There is no image generation here, and choosing a press photo for a distributed political ad is a licensing decision that should not be guessed at. Both spots instead NAME him in text so "he" is never ambiguous, and a portrait slot is ready: drop a cleared file at `~/Downloads/trump-portrait.jpg` and re-run, and it renders as a desaturated faded right-hand layer. Official White House photographs are public domain and are the usual clean source, but the specific image still needs checking.

Datanautix mark removed from both. New close, in two beats: the enable-this question at 76px, then the vote CTA.

**The chart plots three real points and nothing else** — 2024 baseline indexed to 100, then 2025, then 2026-to-date (measles 100→803→865, S&P 100→118→133). Both series are published ANNUAL figures, so inventing monthly values to draw a smoother curve would be fabricating data; the line runs straight between the real points and the axis is labelled by year.

## Trump corpus spots: typography fix, brand end card, RFK quote + measles deaths (Aug 8, later¹⁰)

WHY: Owner feedback — the type looked wrong, the frame was underfilled, bring Datanautix back as a closing card, and the measles spot was weak without RFK Jr.'s own words or a death toll.

**The "ugly font" was a real CSS bug, not taste.** `.big` carries `letter-spacing:-.045em`, and an inline `<span>` inside it inherits that and recomputes it at its OWN font-size — so at 118px the negative tracking ate the word spaces and "of those 565 days" rendered as "ofthose565days". Fixed with a `.run` class that resets tracking and adds word-spacing on any inline run inside a display number. Type scale up across the board (big 230→268px, mid 132→158, final 76→94) and inter-element spacing widened, taking content from roughly half the frame to about two-thirds. Brand end card added to both spots: "Brought to you by datanautix — We make facts matter", wordmark one word, teal/orange, per the deck branding rule.

**The RFK Jr. quote is verbatim and dated, and getting there mattered.** Search summaries gave paraphrases; putting a paraphrase in a real person's mouth in a political ad is not acceptable, so the exact wording was pulled from the FactCheck.org write-up of the 11 March 2025 Hannity interview: "There are adverse events from the vaccine. It does cause deaths every year." On screen with speaker, outlet, date, and "This is false."

⚠️ **Flagged for the owner and deliberately not papered over: RFK Jr.'s messaging is contradictory.** He has also said parents should vaccinate their children and called MMR the most effective prevention — FactCheck.org keeps a timeline of the mixed messaging. An ad implying he is uniformly anti-vaccine invites that correction; quoting what he actually said, attributed and dated, does not. The spot is built the second way.

**No percentage is given for deaths, deliberately.** There were 3 US measles deaths in 2025 and zero for the preceding decade, so a percentage increase is division by zero — undefined, not "infinite". The raw fact is stronger anyway and is what the spot uses: "3 — the first measles deaths in this country in a decade. All three were unvaccinated."

## Measles spot: chart rebuilt to carry its own weight (Aug 8, later¹¹)

WHY: Owner — the chart was not impressive and the scale and legend were too small to read. It was drawn as two bare lines with no y-axis values, a 26px legend, and no text explaining what the viewer was looking at, so it asked the audience to do the work.

Rebuilt: a 70px headline states the finding before the chart draws ("Measles didn't go up like the market. It went up twenty-three times faster."), the y-axis now carries real labelled gridlines at 0/+300/+600/+900%, the legend numbers went from 30px to 82px, and an orange gradient area fills under the measles line — the wall of colour is what actually makes the gap land, more than the line itself did. The SVG also dropped `preserveAspectRatio="none"`, which had been stretching stroke widths non-uniformly.

The y mapping was corrected in the same pass: it previously plotted the raw index (100-900) against gridlines that are now labelled as growth over 2024, so the axis and the line would have disagreed. It now maps `value - 100` onto the labelled scale.

Note on which percentage appears where: the chart runs to **2026-to-date, so it reads +765%**, while the factoids sheet and the PDF quote **+703% for 2025 vs 2024**. Both are correct for their stated period and the chart axis is labelled "2026 to date" — but they are different endpoints and should not be presented as the same figure.

## Name-calling figure was WRONG — 415 days is really 312 (Aug 8, later¹²)

WHY: Owner paused the spot on a frame reading "24" and said name-calling should be far more than that, asking for the data point to be verified. The 24 was only the count-up animation mid-flight — but verifying it found the published figure was wrong in the **opposite** direction. **1,575 passages / 415 days was inflated; the real figures are 718 / 312 (55% of on-record days), and the empathy ratio is 4.9x, not 10.6x.**

The inflation was the failure mode this project keeps hitting, and sampling the matches is what exposed it: **"fool" (173)** was catching *"foolishly given away"* and *"this foolish gift"* — a policy adjective; **"loser" (150)** was catching **"loser pays"**, the legal-costs doctrine, so he was discussing tort reform; **"disgrace" (139)** was *"the Green New Deal was such a total disgrace"*, about things not people; **"nasty" (106)** was *"they've been very nasty to us on trade"*; **"incompetent" (276)** mixed *"an incompetent President"* with *"FEMA is incompetently run"* and cannot be split by keyword; and bare **"crooked"** was catching *"a crooked election"*, so it is narrowed to the actual epithets. What remains is invented proper-noun epithets only, and every sampled match is a person being called a name. Corrected in the insults script, the video and the factoids sheet.

**Two process failures worth recording.** First, I had already pruned this list once and still shipped it without reading the matches — the earlier prune dropped "crazy" and "stupid" but waved through "fool" and "loser", which are worse. A keyword list is not evidence until the matches have been read, and that has now been true three times. Second, the 415→312 edit **silently did not apply** because I ran a string replace without asserting the target existed, and the target had drifted (the countTo start times were never updated when the scenes were re-timed, so the line read `8.8,415` not `8.0,415`). Only frame-level QC caught that the video still said 415. Every substitution in this pass now asserts.

Also fixed from the same review: the count-up ran 2.0s inside a ~5s scene, so any paused or grabbed frame in the first 40% of the scene showed a misleadingly low number — that is precisely how the owner saw "24". Counts are now 0.8s. Scene 1 now reads "209 **times**" rather than a bare 209, since the following scene is denominated in days and the units were being silently compared.

## Systemic keyword audit: the prefix-bleed defect (Aug 8, later¹³)

WHY: Owner, after the name-calling correction — audit every other keyword list the same way. The "fool"→"foolish" failure was not bad luck; it is a **defect in the matcher every list shares**. `new RegExp('(?<![a-z])' + kw)` guards the LEFT edge only, so every keyword also matches any longer word that STARTS with it. New `_trump_corpus_keyword_audit.ts` runs all 109 keywords across all published lists and reports, per keyword, how many matches are the keyword as a PREFIX of a longer word, with the actual continuations.

**43 of 109 keywords bleed. Most of it is benign and actually wanted** — tariff→tariffs, murder→murderers, deport→deportation, border→borders, windmill→windmills are the same concept, and prefix matching is what catches inflections. Four were not:

- **golf → golfer / golfers / golfing (69 of 159).** Him praising Adam Scott or a kid at an event is not him talking about his own golf courses. **The published line "248 times he talked about his golf courses" was wrong; it is 196**, the ratio to child care drops 31x → **24.5x**, the bare word count 159 → **105**, and the combined-vanity union 601 → **549**. Corrected in the summary PDF and the factoids sheet, with the correction stated in the method footer rather than quietly swapped.
- **ice → icebreakers / Iceland (34).** "We're going to order about 40 Coast Guard big icebreakers" was being counted as an immigration-enforcement mention.
- **crime → Crimea (13).** "Under President Obama, they lost Crimea" counted as crime.
- **mandate → EV mandate / vaccine mandate (132 of 145 non-election).** Only a minority are "my recent election is a mandate"; the rest are policy mandates. This contaminates the election-win topic in the long-form report's ratio table.

Fixed structurally with an optional `deny` regex per topic (same shape as the existing Kennedy Center crash filter): a passage is dropped when its only claim to the topic is a blocked form. Applied to golf. The audit script stays committed so any new list can be run through it before publication.

**The audit also states its own limit on the page: a 0% bleed does NOT mean a keyword is correct.** "loser pays" was a clean whole-word match and still completely wrong. Bleed detection ranks what to read; it does not replace reading.

## Mandate contamination fixed; PolitiFact convergence (Aug 8, later¹⁴)

Applied the policy-mandate and golfer denies to `_trump_corpus_ratios.ts`, the last place the contaminated keywords still fed a published figure. The long-form report's Part 2b table is corrected: **election win / mandate 13.6x → 12.1x (150 → 133)** and **golf vs child care 16x → 12.7x (96 → 76)**, and the bare word count "golf" 58 → 43. A fourth entry was added to the report's corrections section documenting prefix bleed, so the method survives someone checking it.

**External convergence worth recording.** PolitiFact published "Trump's construction projects" on 11 Aug 2026 (Caleb McCullough, Loreben Tuquero) with a separate methodology note. They used PolicyNote transcripts, OpenAI Codex for sentence-level classification into 82 topics with every AI label human-reviewed, speaker separation, and a prompted-versus-unprompted split. **Independently, they surfaced the same projects we did — ballroom, Reflecting Pool, Kennedy Center, Triumphal Arch, Garden of American Heroes.** Two teams, different sources, different classification methods, same finding: that is meaningful corroboration of the obsession-gap result, and it is now the strongest external validation this analysis has.

Honest comparison, recorded so nobody overclaims in a conversation: **their sentence-level LLM classification under full human review reads context in a way our keyword lists cannot**, and their 82-topic taxonomy is finer-grained than ours. Our real advantages are reproducibility (deterministic — same input, same number), a public-domain source rather than a commercial platform, a 19-month window against their 7, social posts as well as speech, and speed on repeat runs. Do not claim parity on nuance.

Outreach draft to both reporters written to `~/Downloads/PolitiFact_outreach_draft.md` — **not sent**, and carrying two open items: Larry Kahn's title is left as a placeholder rather than invented, and the disclosure line (how much method to reveal to a journalist who is a potential collaborator rather than a competitor) is the owner's call.

## Side-by-side factoid sheet for the FL Senate race (Aug 14)

WHY: Owner wants the comparative measures built out for the Vindman campaign (FL Senate special vs appointed Sen. Ashley Moody), including what Moody has enabled. `_vindman_factoids_pdf.ts`, two pages.

**The owner's draft line was refused and the refusal is printed on the sheet.** "He took $XXX to paint the reflecting pool and took away $YYY from SNAP to feed your baby" asserts a budget transfer that did not happen — different appropriations, and the ballroom has been publicly described as privately funded. The claim is **false on mechanism even though both dollar figures are true**, and it is the easiest thing on the page for an opponent to get fact-checked and win. Everything is built as juxtaposition instead, and a warning box at the top tells whoever writes the copy not to reintroduce it. A second guard says every left-hand figure is a COUNT OF WHAT HE SAID, never dollars, so nobody turns "209 passages" into "$209".

New corpus findings: ballroom **209 passages / 87 days vs SNAP 40 / 21**; ballroom+pool+arch **289 vs 60** for SNAP/WIC/school lunch/hunger; **"hungry", "hunger", "food bank" appear 10 times in nineteen months**. Best of the new ones — **tariffs are 1,885 passages across 339 of 565 days, six days in ten, and only 14 of those say Americans or consumers pay them (22 say a foreign country does)**. That is a silence gap inside his loudest topic, which is a harder shape to dodge than a pure absence.

**Moody's votes were read off the official Senate roll call pages, not inferred from party**, and the two are deliberately stated differently because they are not equally strong. Roll call **52** (13 Feb 2025): RFK Jr. confirmed 52-48, **Moody Yea** — but at 52-48 she was NOT decisive and the sheet says so in bold, because claiming she was is an unforced error. Roll call **372** (1 Jul 2025): H.R.1 reconciliation passed **50-50 with the VP breaking the tie**, **Moody Yea** — that one IS decisive and can be said so: a single Republican no defeats it, and three Republicans did vote no.

⚠️ Not flagged on the sheet but flagged to the owner: the Democratic primary is **18 August 2026** and Vindman still faces Angie Nixon, so he is not yet the nominee.
