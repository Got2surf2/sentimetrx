# Weekly Governance Report — 2026-W29 (Week of Jul 13 to Jul 19)

**Generated**: 2026-07-13T08:10:00Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W29-devlog.md (single ops entry); git log fallback for Jul 10–12 commits
**Spec-drift**: 4 drifted — see 2026-W29-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 50 commits, 1 author (Sanjay Patel). Top-level dirs touched: app/ (610), lib/ (273), sql/ (190), docs/ (155), scripts/ (151), components/ (144), tests/ (142). 2,051 file-touch events; 707,549 insertions, 1,079 deletions (high insertion count reflects sql/160–165 schema snapshots and the lint burn-down).

### Narrative (from devlog + git log)

**Ops: prod slowness diagnosed, 785K scale-test rows deleted (Jul 13 — devlog)**
- `dataset_rows_flat` was 3.56GB = 97% of the database; a single [SCALE TEST] dataset (785K rows) on a ~1GB Micro instance caused cache-hit ratio to drop to 84.6% (healthy ≥99%), making every auth/org lookup pay disk I/O. Jul 12's sql/165 index build amplified the drain. Owner-approved deletion: 785,638 rows removed in 16×50K batches; table now 273,795 rows. Settings-page queries measured 0.05ms post-delete. Nightly org-snapshot cron had been timing out (300s Vercel limit) paging the bloated table — should self-resolve.
- Lesson surfaced: "schema won't load" after a deploy is often a stale browser tab, not a server fault — hard refresh first.
- TODO logged: filterable-value list in Filters modal is sample-derived and misses values on high-cardinality fields; exact distinct-values pass needed; workaround already shipped (Schema tab → "↻ Refresh").

**Per-field theme sets — multi-question support (Jul 10–11)**
- Each open-ended prompt now keeps its own theme set; combined sets retired. `text toggle` single-selects with auto-save; already-mined questions start unchecked in the setup checklist with a badge. Charts/Stats source picker and Ana follow the active question pill. Reports surface one Theme Analysis block per open-ended prompt. Metric strip refetches when themes are saved in-session.

**Per-question dimensions and taxonomy (Jul 12, sql/164)**
- Tax_* chart aggregates now take the source field, so Dimensions charts correctly follow whichever open-ended field is active. Per-question themes extend into the Dimensions layer.

**Analytics at scale — bulk loading and sampling hardening (Jul 10–11, sql/160–163)**
- Bulk row load now streams the payload with MB progress, then switches to "N of M rows · %" display after load. Proportional O(sample) collection via shared sql/160 pager. Sampled avg rating above 50K-row cap (sql/163). First-open efficiency audit: sampled signal counts (sql/162) + cache fixes. Trim: deletes ALL matching rows in rounds; merges analytics, never replaces. Fixed schema-detection stack overflow on large numeric columns, broken bulk-row sampling above 50K cap, and comma-safe non-empty counts (sql/161).

**Ember wildfire agent — live data injection (Jul 11)**
- New agent wired to NIFC/WFIGS fire perimeters + AirNow air quality + U.S. Drought Monitor fuel-dryness context via `config.liveContext`. Three shipped versions (v1 NIFC baseline, v2 perimeters + AirNow, v3 Drought Monitor). Blue Mountains agent KB deepened (wildfire, wildlife, Tribal, grazing, access then-vs-now, economics).

**NEPA vertical — answerability lift + PII triage (Jul 10–12)**
- DEIS ingestion script + measured answerability lift. PII-triage capability + deck slide. "If We Built CARA Today" deck + open-questions funnel layer. Shareable "generate a response" review demo. Dynamic funnel total + as-of date for daily dashboard refresh. NEPA deck updated with post-DEIS numbers and tuning-to-80% roadmap.

**Health monitoring + credit alerts (Jul 12)**
- Live Claude credit probe in the System Health dashboard with re-up pills. Quiet survey traffic now shows neutral instead of red. Real-time email alert when org hits credit limit, plus low-balance heads-up email.

**Breadcrumbs UX (Jul 12)**
- Breadcrumbs across every internal page; admin section rooted at Settings & Admin. This single commit is the source of all 4 spec drifts this week (touched Social, Campaigns, Surveys, Recordings pages without updating their specs — breadcrumbs are pure navigation chrome, behavior unchanged).

**Lint burn-down (Jul 7 — within reporting window)**
- 16-wave sweep cut lint warnings 3,062 → 346 (−97%): `no-explicit-any` 2,787 → 83. `lint:ci` ceiling in `package.json` reflects the new floor at 346. No-explicit-any and react-hooks/* warnings now at manageable levels.

**Schema snapshots (Jul 12)**
- sql/160–164 and sql/165 applied to prod; schema snapshots committed and docs/db/schema.sql refreshed. sql/165 adds `(dataset_id, id)` index on `dataset_rows_flat` backing the classify id-keyset.

### Risk signals
- 4 drifted specs (SOCIAL, CAMPAIGNS, SURVEYS, RECORDINGS) — one commit (`726ae30` breadcrumbs) bypassed SKIP_SPEC_CHECK or the spec check was not triggered. +1 spec vs W28 (3). All 4 are low-behavioral-risk (navigation chrome) but the drift itself is a process gap.
- SheetJS CDN 403 (`npm install` blocked in clean remote/CI checkout) — unresolved from W28. CI runs on cached node_modules so tests still pass there, but fresh remote sessions can't install.
- 5 bare-id service-role lookups in non-admin code (`app/api/bots/[id]/route.ts` lines 89, 118, 127, 162) remain unresolved from W28 MEDIUM finding — context-safe but violates CLAUDE.md multi-tenancy invariant.
- Single-author velocity (50 commits, 1 author) — bus factor risk; no regression from prior weeks.
- `dataset_rows_flat` capacity: post-deletion table is 273K rows on a Micro instance. No hard ceiling, but the [SCALE TEST] event revealed the instance is undersized for large-dataset testing. Owner-decided for now.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 13.5 |
| Security | 7/10 | 15% | 10.5 |
| Dependencies | 8/10 | 15% | 12.0 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 7/10 | 15% | 10.5 |
| Documentation | 8/10 | 10% | 8.0 |
| Maintainability | 9/10 | 20% | 18.0 |
| **Total** | | **100%** | **80.5 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- None. 0 critical / 0 high CVEs. No hardcoded secrets detected. No eval/exec with user input.

**MEDIUM**
- `app/api/bots/[id]/route.ts:89,118,127,162` — four `service.from('agents')...eq('id', params.id)` calls without `org_id` pairing, violating CLAUDE.md multi-tenancy invariant ("Service-role queries must pair id with org_id"). Pattern is context-safe (auth middleware validates session first) but the invariant exists as a class-level safety net. Carried from W28; not addressed this week.
- SheetJS sourced from `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` — `npm install` returns 403 Forbidden in clean remote checkouts, blocking CI-less environments and this governance run's install step. Supply-chain reliance on a proprietary CDN. Carried from W28, unresolved.
- 7 moderate npm CVEs (down from 14 in W28 — the 7 `npm audit fix`-fixable ones were cleared; remaining 7 are the deliberate `isomorphic-dompurify`/`jsdom` pin requiring a breaking `@sentry/nextjs` version bump for resolution).
- 4 drifted specs: SOCIAL, CAMPAIGNS, SURVEYS, RECORDINGS — all caused by single breadcrumbs commit `726ae30`; behavioral impact is zero (navigation chrome only), but the hook should have flagged it.

**LOW / informational**
- Test ratio ~0.13 (128 test files / 986 TypeScript source files) — below the 0.5 target for higher tier. Absolute test count grew from 108 (W28) to 128, and 1,200+ tests pass on CI, but ratio diluted by source file growth. e2e suite still at 0 tests.
- Lint ceiling at 346 warnings — major improvement from 3,062 (W28). `no-explicit-any` is down to ~83; `react-hooks/*` ~236 warnings remain as a behavior-sensitive backlog. Neither category has been promoted to `error` yet.
- 0 `.claude/agents`, 0 `.claude/rules` defined — governance relies on CLAUDE.md + 3 hooks + 4 commands only. Consistent with W28.
- Google Fonts dependency in PDF deck routes: no bundled fallback; render fails silently if fonts unreachable. Documented, unresolved from W27.
- `dataset_rows_flat` Micro-instance capacity: 273K rows post-cleanup is healthy, but the [SCALE TEST] event exposed that the shared-buffers envelope (256MB) is undersized for production-scale testing against the live database.

### Progression plan (top 3 to fix this week)

1. **Cure 4 drifted specs** — add breadcrumb navigation notes to SOCIAL.md, CAMPAIGNS.md, SURVEYS.md, and RECORDINGS.md; also sweep SPEC.md per the spec-drift flag. Low effort (~30 min).
2. **Fix SheetJS CDN dependency** — replace `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` with the npm-registry package (`xlsx@0.20.3`) in `package.json`; restores `npm install` in clean environments and removes the proprietary-CDN supply-chain risk.
3. **Pair `org_id` on the 4 bare-id bots route lookups** — `app/api/bots/[id]/route.ts` lines 89, 118, 127, 162: add `.eq('org_id', session.org_id)` after `.eq('id', params.id)` to comply with CLAUDE.md invariant and close the potential cross-tenant class risk.

## 3. Correlation: dev work → audit findings

- **SheetJS CDN block (MEDIUM)**: not mentioned in W29 devlog or commits. Unaddressed this week.
- **Bare-id service-role lookups — bots route (MEDIUM)**: not addressed. The W29 commits focused on analytics, NEPA, and UX — the bots route was not in scope.
- **7 moderate CVEs (MEDIUM)**: not addressed this week. The remaining 7 require a `@sentry/nextjs` version bump which is a more involved change.
- **4 drifted specs (MEDIUM)**: `726ae30` (breadcrumbs) bypassed the pre-commit spec check. The commit message does not include `SKIP_SPEC_CHECK`, suggesting the hook may not have been active in the session that created it. Not addressed post-commit.

## 4. Trend

| Week | Score | Delta |
|------|-------|-------|
| 2026-W27 | 83.0 / 100 | baseline |
| 2026-W28 | 79.0 / 100 | −4.0 (3 drifted specs, 14 moderate CVEs) |
| 2026-W29 | 80.5 / 100 | **+1.5** (CVEs 14→7; lint 3,062→346) |

W29 recovers from the W28 dip. The +1.5 reflects: Dependencies improved 7→8 (CVE count halved from the W28 `npm audit fix` pass that landed in this window), and Maintainability quality signal strengthened via the lint burn-down (3,062→346 warnings, `no-explicit-any` −97%). The open MEDIUM findings (SheetJS, bare-id lookups, drifted specs) are the primary drag on the score; resolving all three would move the score to ~83+.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Review the spec-drift companion (2026-W29-spec-drift.md) — any spec debt?
- [ ] Check correlation section — are open findings being worked on?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
