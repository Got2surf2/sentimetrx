# Weekly Governance Report — 2026-W22 (Week of May 25 to May 31)

**Generated**: 2026-06-01T12:22:54Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W22-devlog.md

## 1. Development Summary

**Quantitative**: 50 commits, 2 authors (Sanjay Patel, Claude Spec Drift), top-level dirs: app/ (420 file-touches), lib/ (161), components/ (101), sql/ (99), scripts/ (87), docs/ (78), tests/ (32), middleware (1)

### Narrative (from devlog)

**W21 audit score-lift — all 6 items + bonus closed (May 25)**
- Sentry `beforeSend` PII scrub shipped: `lib/sentryScrub.ts` with 7 tests, wired into all three Sentry configs. SECURITY.md §5 TBD closed. (W20/W21 MEDIUM → resolved)
- Datanautix → Sentimetrx branding in customer-visible surfaces: SurveyWidget, StepBasics, PPTX export metadata. ChatBot widget partially reverted same day for UCF Business Incubator demo (parent-brand credibility during pitch).
- `send_at` phantom campaign field removed from UI, types, and PATCH body.
- `tsconfig.tsbuildinfo` .gitignore note added; `mco_live_ratings.json` disposition documented with refresh-policy block.
- MCO parallel-session WIP verified clean: `tsc --noEmit` clean + prod smoke test across all four MCO API routes.
- brace-expansion DoS CVE (`GHSA-jxxr-4gwj-5jf2`) patched via `npm audit fix`.
- Bonus: 42 new unit/integration tests across `nameExtractor`, `probeFocusClassifier`, `admin-usage-detail`, `sentryScrub` (suite: 319 passed / 54 skipped, up from 277).

**Decision Study agent — protocol hardening through multiple passes (May 25–26)**
- Six protocol bugs fixed from Sunil's + Sanjay's test transcripts: generic-pronoun ban, drill-example-as-script fix, recovery-from-confusion rule, silence-probe template bug, non-answer escalation fix, counterfactual drill route.
- Three-way A/B test (V1+patches vs V2 goal-tracker vs V2+state-injection): state-injection wins (13 clean turns vs 18 with one re-ask for V1). Architectural lesson: deterministic code-side state beats instructing the model to scan context.
- Phase 5 deeper-why + Phase 7 imagined-replay probe added (WHY: proximate vs root cause + forward-looking without priming change-comparison frame).

**NOWOCATS / PulseIQ — sim infra + adapter fixes + activation gate (May 25–27)**
- NOWOCATS 18-persona sim pack + sim-clone town hall (no production data pollution). First sim run: 5 personas × 6 turns, language switch fired, clean.
- PulseIQ adapter: response_count now `DISTINCT conversation_id` (correct); mention_count tracks raw turn tags (separate concept). Per-conversation turn/timestamp/topicCount overlaid live.
- Town hall activation gate: `lib/townhallActivationGate.ts` — ≥1 enabled topic with opening question AND event description grade ≥3 required before `setup → active`. Grader snapshot re-used only when text matches verbatim (edit forces re-grade). Optional agent linkage (import focuses) added to new-session wizard.
- Export route phase-3 fallback: magnifying-glass 404 on pure phase-3 sessions fixed.

**Ruth's Chris taxonomy pilot — full infra shipped (May 27)**
- `sql/088_dataset_row_taxonomy.sql` — per-row 7-axis taxonomy table with org-scoped RLS and GIN indexes. Applied to prod.
- Closed vocabulary (`taxonomyVocabulary.ts`), canonicalizer (`taxonomyMapping.ts`), LLM extractor v1–v4 (`taxonomyExtractor.ts`), keyword tier v1 (`taxonomyKeywords.ts`, ~250 phrases), hybrid merger (`taxonomyKeywordMatcher.ts`).
- Evidence spans (≤12-word verbatim quotes) added at v3; cross-highlight on chip hover in viewer. Chip provenance styling: filled=keyword, inverted-with-AI-badge=LLM-only, filled+emerald-ring+✓=both-tiers.
- Regression: 7/7 pass across all three tiers. 50-row ingest + classify ran in ~10s.
- Prompt v4 explicitly bans `product:<sub>` inference from steakhouse context (background-knowledge hallucination caught by user).
- Path B (AI-generated keyword library from 5K reviews, ~$8.50 Haiku) scoped and queued — not built.

**MCO Concierge agent hardening (May 26–28)**
- Rebranded "AskAna" → "MCO Concierge"; Ana stays her name internally.
- Live flight/security/parking data injected into system prompt context before each chat call.
- FlightPrepPanel: gate + security wait + walking time + spare-time hero. FlightListCard for plural flight queries.
- Multiple follow-up and parsing fixes: 4h→12h flight search window, bare-digit follow-up resolution, hydration mismatch fix, double-prefix bug, dead link removal.
- Meridian indoor maps: 32 floors + 2,313 real pins loaded.
- NOWOCATS approach deck: 4 substance slides added for VHB/Orange County review (Babuji, Hatem). First Sarina entity extraction run: 202 entities (71 visible, 131 hidden) at $0.03. Spec drift check on 128 commits: **zero drift**.
- Cubie UCF Incubator agent: KB 52 KB → 270 KB, chunks 126 → 390, system prompt 3.5 KB → 12 KB, guardrails 9 → 12. Anti-hallucination: zero-fabrication rules + link-formatting + list-vs-recommendation query distinction.

**Recordings module — Phases 0, 1a, 1b, 2 (May 25–31) — major new feature**
- Phase 0: `sql/089_org_user_features.sql` (generic feature-gate), `sql/090_recordings.sql` (4 recording tables, all with org-scoped RLS + denormalized `org_id` from day 1), `sql/091_recordings_storage_bucket.sql` (private bucket, 4 org-scoped storage RLS policies). `lib/featureFlags.ts` — `assertFeatureAllowed()` with user/org/default-false resolution order.
- Phase 1a: Pure logic libs: `lib/recordings/types.ts`, ASR adapters (Whisper + Deepgram + hybrid), `lib/recordings/analyze.ts` (Opus 4.7 extraction + Sonnet 4.6 curator, two-pass), `lib/recordings/mirror.ts`, `lib/recordings/coverage.ts`. `lib/ai.ts` gains `modelOverride` field. `lib/usageRates.ts` gains `claude-opus-4-7` pricing.
- Phase 1b: `lib/recordings/extract.ts` (ffmpeg in Vercel Sandbox), `lib/recordings/transcribe.ts`, `workflows/recordings.ts` (Workflow DevKit five-step orchestration). New packages: `@vercel/sandbox`, `workflow`, `@workflow/next`.
- API routes: `POST /recordings` (§ 4.1 create + TUS upload setup), `GET/POST /recordings` (§ 4.8 list), `GET /recordings/[id]` (§ 4.3 status), `POST /recordings/[id]/files/[fileId]/uploaded` (§ 4.1a ack), `POST /recordings/[id]/process` (§ 4.2 WDK trigger), `POST /recordings/[id]/extractions/[extractionId]/regenerate` (§ 4.10), `POST /recordings/[id]/reanalyze` (§ 4.11).
- UI: Upload wizard (§ 5.2 — tus-js-client), status surface with vertical step list + sub-details (§ 5.3), four-tab report page with Q&A summary / coverage / transcript search / stubs (§ 5.4), org-admin recordings list (§ 5.5), admin monitor section in Downloads (§ 5.6).
- `pm1-smoke.ts` calibration harness: Jaccard-on-question F1 ≥ 0.90 acceptance criterion.
- Critical path closed: tile → wizard → upload → ack → process → WDK → status poll → report. Every link exists in code. **SQL migrations not yet applied to prod; Deepgram key and Sandbox not yet provisioned.**

**Vercel file-tracing fix (May 25)**
- `next.config.js` `outputFileTracingIncludes` added for admin report pages — ensures `docs/weekly-reports/*.md` ships in the Vercel serverless bundle reliably.

### Risk signals

- **Recordings pipeline not smoke-tested end-to-end**: `sql/090` + `sql/091` not applied to prod; `DEEPGRAM_API_KEY` not in Vercel envs; Sandbox not provisioned. NOWOCATS PM-2 pilot deadline is 2026-06-16 — 15 days.
- **50 commits accumulated locally on `main`**: No CI signal until a push is authorized. Push policy is deliberate but means the entire week's output (including new route handlers and recording pipeline) has not been CI-validated.
- **ChatBot "Powered by" partial regression**: Reverted to Datanautix for UCF demo (business decision). W21 progression item 2 is now partially open again for agents specifically.
- **npm CVE count increased**: 8 HIGH (up from 1 HIGH post-brace-expansion-fix in W21). New packages from recordings module likely introduced transitive CVEs.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 8/10 | 15% | 1.20 |
| Security | 7/10 | 15% | 1.05 |
| Dependencies | 5/10 | 15% | 0.75 |
| Structure | 8/10 | 10% | 0.80 |
| Tests | 4/10 | 15% | 0.60 |
| Documentation | 9/10 | 10% | 0.90 |
| Maintainability | 9/10 | 20% | 1.80 |
| **Total** | | **100%** | **71.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- None confirmed.

**MEDIUM**
- **[NEW] npm CVE count increased to 8 HIGH**: `npm audit` shows `{high: 8, moderate: 1, low: 9}`, up from 1 HIGH + 1 MODERATE post-brace-expansion-fix. The increase is consistent with transitive dependencies pulled in by `@vercel/sandbox`, `workflow`, `@workflow/next`, and `tus-js-client` added for the recordings module. Root fix for the pre-existing Next.js HIGH (`GHSA-9g9p-9gw9-jx7f`) still requires a Next 14 → 15/16 upgrade (deferred).
- **[PRE-EXISTING] Bare service-role agent ID lookups without org_id**: `app/bots/[id]/history/page.tsx:28`, `app/bots/[id]/entities/page.tsx:30`, `app/bots/[id]/questions/page.tsx:30` — `service.from('agents').select(...).eq('id', params.id)` without `.eq('org_id', ...)`. A logged-in user who guesses another org's bot UUID can load that bot's admin page. Not new in W22 but not yet addressed.
- **[PRE-EXISTING] Test coverage widening gap**: Test ratio 0.041 (29 test files / 696 source files). W22 added 10+ new lib files and 6+ new API routes in the recordings module with **zero tests**. The 42 new tests from W21 score-lift items were offset by proportionally larger source growth. Route handlers and React components remain largely untested.
- **[NEW] Recordings pipeline pre-flight blockers**: Prod SQL migrations (`sql/090`, `sql/091`) not applied; `DEEPGRAM_API_KEY` not set; Vercel Sandbox not provisioned; `recordings` storage bucket not created. End-to-end pipeline has not run against real audio. NOWOCATS PM-2 pilot is 15 days away.

**LOW / informational**
- `lib/meridian.ts:26` — `PUBLIC_MERIDIAN_TOKEN` JWT hardcoded (labeled PUBLIC; this is the publicly accessible MCO Meridian indoor maps token, same risk class as the GOAA API key flagged in W21).
- `.gitignore` missing `*.pem`, `*.key`, `*.p12` patterns.
- `execSync` in `next.config.js` and spec-check scripts — build-time only, not on the request path.
- ChatBot.tsx "Powered by Datanautix" deliberate revert for UCF demo — W21 item 2 partially open for agents.
- Several Decision Study one-off SQL files applied directly to prod (system_prompt updates) without code commits — audit trail lives in devlog SQL file names and `sql/one-off/` directory.

### Progression plan (top 3 to fix this week)

1. **Apply prod SQL migrations + provision Vercel Sandbox + set DEEPGRAM_API_KEY** — pre-flight for NOWOCATS PM-2 pilot (June 16 deadline). `sql/090_recordings.sql`, `sql/091_recordings_storage_bucket.sql`. Non-code; requires explicit user sign-off per CLAUDE.md. Then run `scripts/pm1-smoke.ts` against real PM-1 audio.
2. **Audit and `npm audit fix` for new HIGH CVEs** — identify which of the 8 HIGH CVEs are from newly added packages (vs pre-existing Next.js). Fix the fixable ones; document the deferred ones. Prevents CVE drag from compounding with each feature sprint.
3. **Add org_id guard to bare agent page lookups** — `app/bots/[id]/history/page.tsx:28`, `app/bots/[id]/entities/page.tsx:30`, `app/bots/[id]/questions/page.tsx:30`. Three-line fix each: add `.eq('org_id', userOrgId)` to the agent select + 404 on mismatch. Closes pre-existing cross-tenant page access risk.

## 3. Correlation: dev work → audit findings

- **[W21-MEDIUM] Sentry `beforeSend` scrub** — **fully addressed** (shipped `lib/sentryScrub.ts`, 7 tests, wired into all three Sentry configs; SECURITY.md TBD closed).
- **[W21-MEDIUM] npm CVEs** — **partially addressed** (brace-expansion CVE fixed); but CVE count regressed to 8 HIGH from new package additions. Net negative.
- **[W21-MEDIUM] PPTX Datanautix branding** — **partially addressed** (PPTX metadata + SurveyWidget + StepBasics fixed; ChatBot widget reverted to Datanautix for UCF demo — business decision, not a regression in kind).
- **[W21-MEDIUM] `send_at` phantom campaign field** — **fully addressed** (removed from UI, types, PATCH body; CAMPAIGNS.md updated).
- **[W21-MEDIUM] Coordination commit MCO WIP unverified** — **fully addressed** (tsc clean + prod smoke on all 4 MCO routes + documented review).
- **[W21-LOW] Test ratio** — **not addressed**; ratio dropped from 0.044 to 0.041 as source base grew faster than tests.
- **[W21-LOW] `tsconfig.tsbuildinfo` committed** — **already resolved** (pre-existing commit; auditor noted it was stale finding).
- **[W21-PROGRESSION] Bare agent ID lookups** — **not in W21 top-3 progression plan**; not addressed in W22.
- **[NEW-W22-MEDIUM] CVE regression from new packages** — **not mentioned in devlog** as a focus; packages added without CVE pre-screen.
- **[NEW-W22-MEDIUM] Recordings pre-flight blockers** — **identified in devlog** (explicitly listed as pre-flight blockers in `2026-05-31` entry); not yet resolved by week end.

## 4. Trend

Previous report (W21): **72.5 / 100** (generated 2026-05-25)

This report (W22): **71.0 / 100** — **−1.5 points**

W22 delivered the most feature volume of any week to date (recordings module Phases 0–2, taxonomy pilot, Decision Study hardening, NOWOCATS sim infra, MCO Concierge hardening) and paid off all five W21 progression items. However the score slipped because: (a) new packages added for the recordings module brought the npm CVE count from 1 HIGH to 8 HIGH (Dependencies: 7 → 5); (b) test ratio declined as the large recordings code surface shipped without tests (Tests: 4 → 4, but lower raw ratio); offset partially by Maintainability improvement (8 → 9) from W21 debt payoff and clean architectural decisions throughout.

Primary drags remain: test ratio (4/10, 15% weight) and Dependencies (5/10, 15% weight).

## 5. Human review checklist

- [ ] Skim development summary — recordings pre-flight blockers: `sql/090`, `sql/091`, `DEEPGRAM_API_KEY`, Sandbox. PM-2 pilot June 16.
- [ ] Review CVE regression — 8 HIGH CVEs up from 1. Is this acceptable before the recordings module goes live?
- [ ] Check bare agent ID lookups — `app/bots/[id]/history`, `/entities`, `/questions` — confirm risk tolerance or schedule fix.
- [ ] ChatBot "Powered by Datanautix" revert — intentional for UCF demo. When should it flip back to Sentimetrx (or is it permanent for agents)?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine. Requires human review before being recorded.*
