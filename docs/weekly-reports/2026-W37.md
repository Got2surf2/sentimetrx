# Weekly Governance Report — 2026-W37 (Week of Aug 31 to Sep 6)

**Generated**: 2026-09-07T08:09:54Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: git log fallback (no 2026-W37-devlog.md committed for this 49-commit week)
**Spec-drift**: 2 drifted — see 2026-W37-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 49 commits, 1 author (Sanjay Patel), top-level dirs: app/ (609 file-touches), lib/ (345), tests/ (246), docs/ (231), sql/ (224), components/ (159), scripts/ (158)

### Narrative (from git log)

**Data Story — major feature sprint**
- Arguments-led narrative: findings-led headings, timeline, bands, drift explorer, score drivers with plain-English explanations, what-if modeler (`18e0895`, `12c1fcc`, `b53b02f`, `beaac9b`)
- Short links: `/story/<slug>` with editable expiry and revocation (`6b5f568`); share modal with list/copy/extend/revoke (`144d2f4`)
- PDF export with interactive machinery stripped (`495785a`); collapsible sections, denominator-ladder tiles, % signs in AI prose (`beaac9b`)
- Data Story for collections and on per-selected-verbatim focus (`bd9a580`, `e2f1aa8`)

**Ask Ana — query composition enhancements**
- Self-composed subgroup scoping (WHERE clause) + active-field recovery on mount (`48ec731`)
- Fuzzy value matching; every mapping lands in provenance (`99a64b9`)
- 300s budget for long tool loops; honest stream-death message (`9c14b52`)
- Coexists with nav chrome: nav reachable, header collapses, Sherpa visible (`5c4369a`)
- Font size: 14px on desktop (16px touch floor enforced) (`2cfffc4`)

**Emotion and Signal analysis (ANES-validated constructs)**
- Anger + threat-ascribed emotion flags (keyword-tier) (`1f6e586`)
- Emotion in Charts: picker shows only axes that fired; five-flag label (`90bfea7`)
- Signals = themes per comment with ratio and coverage trio (`bd4db4db`)
- Signal tile pinned to COMPLAINT signals to prevent drift with theme model (`a016926`)

**Charts improvements**
- % modes across chart types + guided drag-and-drop (`70a6493`)
- Compare-periods on Time Series (prev period / same period last year) (`ece2de1`)
- Honest truncation, one canonical theme matcher, real distributions (`99c1426`)
- Compute analytics recovery CTA (`cb47bc7`)

**Collections**
- Collections answer Ana's queries: aggregate ops + where-subgroups fan out over members (`ede0c6a`)
- Manage-members candidates scoped to collection's org; errors in footer (`3307c4d`)

**Performance**
- Subgroup matching as one SQL (migrations 200+201): minutes → 9s on owner scenario (`f4517c9`, `4b7cd13`)
- Sampled twins above 50K; fail-loud RPC errors (`17e72ad`)
- Sentry triage: entity-count sampled fallback on timeout; verdict batches halve on error 57014 (`2a72f31`)

**American English enforcement**
- AI client (`lib/ai.ts`) enforces American English via `AMERICAN_ENGLISH_RULE` appended to every system prompt (`91232eb`)
- Americanize backstop for AI prose; muted consultant tone for story headlines (`53ae031`)

**UX polish and infra**
- Building screen fun-fact rotation (138-fact pool, 8s interval); matches Ask Ana rhythm (`b6c3076`, `2b7b4c3`, `4ee0cb4`, `f3b6e77`, `9e167b3`, `5a232b8`)
- Provenance steps carry duration (query time vs thinking time) (`6fa52ad`)
- Help KB Dimensions how-to updated; Americanize help articles (`98ebed1`)
- ANES finding-brief regen scripts (`e542de6`)

### Risk signals

- **Single-author, high-velocity week** — 49 commits from one author with no review before merge; no second pair of eyes on AI-guardrail changes to `lib/ai.ts`.
- **lib/ai.ts touched** — American English rule appended centrally; any inadvertent prompt-injection surface change would be in every AI call. Worth a careful read.
- **W37 devlog absent** — for a 49-commit week with significant feature work, no devlog was committed; the WHY rationale lives only in commit messages.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 10/10 | 15% | 15.0 |
| Security | 8/10 | 15% | 12.0 |
| Dependencies | 7/10 | 15% | 10.5 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 7/10 | 15% | 10.5 |
| Documentation | 8/10 | 10% | 8.0 |
| Maintainability | 9/10 | 20% | 18.0 |
| **Total** | | **100%** | **82.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- **4 high-severity npm CVEs** — `npm audit` reports 1 low, 9 moderate, 4 high, 0 critical in 14 total vulnerabilities. Exact packages not enumerated here but blocking `npm audit fix` is the first remediation step. (Dependencies category)

**MEDIUM**
- **`lib/collectionRecompute.ts:82` bare-id UPDATE** — `service.from('datasets').update({ row_count: rowCount }).eq('id', collectionDatasetId)` uses only `.eq('id')` without `.eq('org_id', ...)`. CLAUDE.md multi-tenancy invariant §: "Service-role queries must pair `id` with `org_id`." The dataset ID is derived from internal processing (not direct user input), making exploitation difficult, but the pattern should be corrected for consistency and defence in depth.
- **Spec drift: `docs/ENGINEERING.md` not updated** — 3 commits touched `next.config.js` (PDF export) and `CLAUDE.md` (American English rule) without updating the engineering spec. The Story PDF export and new AI client rule affect documented engineering practices.
- **Spec drift: `docs/USAGE_ACCOUNTING.md` not updated** — `lib/ai.ts` was modified to enforce American English; USAGE_ACCOUNTING.md tracks AI client usage accounting and should be reviewed/updated.
- **W37 devlog missing** — A 49-commit week with significant feature work has no devlog entry. The pre-commit devlog hook was bypassed (or commits used `SKIP_DEVLOG_CHECK=1`). The Monday governance review cannot reconstruct the WHY from commit messages alone for complex features.

**LOW / informational**
- **Coverage thresholds conservative** — Enforced floors: statements 37%, branches 28%, functions 39%, lines 38%. For 2,027 test cases across 211 files (3.21 cases/source file) and CI-gated, the floors likely sit well below actual coverage. Raising the floor toward actual would make the gate meaningful.
- **Ask Ana font size compliance** — `2cfffc4` fixed the composer to 14px desktop / 16px touch; monitoring advised to ensure no new `<16px` inputs were introduced in the new Charts/Story UI.
- **No .claude/agents or .claude/skills** — Agent definitions and skills directories are empty. The 4 custom commands and 7 hooks are solid, but agent definitions (e.g., for the audit routines themselves) would improve the AI configuration maturity.

### Progression plan (top 3 to fix this week)

1. **Run `npm audit fix`** — address the 4 high-severity CVEs; if breaking changes required, pin with `--legacy-peer-deps` and note in ENGINEERING.md. Target: Dependencies 7→9.
2. **Update `docs/ENGINEERING.md` and `docs/USAGE_ACCOUNTING.md`** — close the two spec drifts by documenting the Story PDF export pattern and the new `AMERICAN_ENGLISH_RULE` AI client enforcement in the respective specs. Quick desk task, ~30 min.
3. **Add `.eq('org_id', orgId)` guard to `lib/collectionRecompute.ts:82`** — thread org_id through `recomputeCollectionAnalytics` from its callers and pair the datasets UPDATE. Aligns with the six May-2026 CRITICAL findings pattern documented in CLAUDE.md.

## 3. Correlation: dev work → audit findings

- **American English enforcement in `lib/ai.ts` (91232eb)** → `docs/USAGE_ACCOUNTING.md` spec drift — **not mentioned in devlog** (no devlog this week). The change is in CLAUDE.md §Content rules but not captured in USAGE_ACCOUNTING.
- **Story PDF export / `next.config.js` (495785a)** → `docs/ENGINEERING.md` spec drift — **not mentioned in devlog**. Engineering spec was not updated alongside.
- **Subgroup SQL rewrite (4b7cd13)** → performance risk partially addressed; sampled-twins safety net added in same week — **addressed**.
- **High npm CVEs** → **not mentioned in devlog** (pre-existing; no remediation this week).

## 4. Trend

Previous report: **2026-W36 — 83.0 / 100**

| Category | W36 | W37 | Delta |
|---|---|---|---|
| Secrets | 9/10 | 10/10 | +1 |
| Security | 8/10 | 8/10 | 0 |
| Dependencies | 8/10 | 7/10 | -1 |
| Structure | 8/10 | 8/10 | 0 |
| Tests | 7/10 | 7/10 | 0 |
| Documentation | 9/10 | 8/10 | -1 |
| Maintainability | 9/10 | 9/10 | 0 |
| **Total** | **83.0** | **82.0** | **-1.0** |

Small regression (-1.0). Secrets improved (no hardcoded credentials found, full gitignore coverage, .env.example present). Dependencies dipped due to 4 high CVEs visible in audit output (may be pre-existing). Documentation dipped due to 2 spec drifts (ENGINEERING.md, USAGE_ACCOUNTING.md) in a high-volume week.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Review the spec-drift companion (2026-W37-spec-drift.md) — any spec debt?
- [ ] Check correlation section — are open findings being worked on?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
