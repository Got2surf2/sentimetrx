# Weekly Governance Report — 2026-W21 (Week of May 18 to May 24)

**Generated**: 2026-05-25T08:09:30Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W21-devlog.md

## 1. Development Summary

**Quantitative**: 50 commits, 1 author (Sanjay Patel), top-level dirs: app/ (473 file-touches), lib/ (130), docs/ (123), sql/ (101), components/ (106), scripts/ (66), tests/ (31), .claude/ (8)

### Narrative (from devlog)

**MCO Airport agent — 4-commit prototype build (Mon–Tue, May 18–21)**
- Canvas shell + chat wired to AskAna bot; 40/60 landscape layout, three deployment modes (home / in-venue / kiosk), keyboard shortcuts, demo strip.
- Sibling `/api/bots/[id]/ui-hints` extractor endpoint (Haiku 4.5, ~$0.0003/call); 31 unit tests; open-redirect guard in link_card hint.
- Frontend wired: `ChatPane` fires extractor fire-and-forget after each reply; `CanvasShell` `liveHint` state overrides demo strip; shimmer bar during extraction.
- Live data: GOAA public parking API (`api.goaa.aero`) with per-lot availability + rates, 60s in-memory cache; Google Places (New) for restaurant cards; `lib/parking.ts`, `lib/places.ts`, `app/api/mco/parking`, `app/api/mco/places`.
- May 23 follow-up: TSA wait times (SeatGuru/TSA Checkpoint API) + surface parking open/full status surfaced in parking card.
- May 22 bug fixes: GOAA endpoint requires `api-version: 140` + `Referer: https://flymco.com/` headers (missed in original Playwright sniff); parking was returning empty arrays in dev. QR "Continue on phone" modal for kiosk handoff; `z-index` fix; "Send to phone" button hidden on mobile.
- "Common Searches" header above chip suggestions; markdown links in chat; clear-conversation button; 60s auto-reset in kiosk mode.
- MCO_AGENT.md rolled up into a full two-day session spec.

**Decision Study / regret-framework agent for Dr. Contractor (Wed–Thu, May 22–23)**
- Pilot agent seeded via SQL-only to `/b/decision-study` (no code deploy needed): SQL + `agents` row, Sarina persona (name "Sarina", no "regret"/"study" in chrome).
- Bug shipped and caught same day: `agents.name = 'Decision Study (regret framework pilot)'` rendered in widget header — respondent saw "regret", "research", "study" before typing. Fixed immediately via SQL override of `config.name`.
- May 23 full protocol redesign: opener changed to IMPORTANT + UNANTICIPATED-OUTCOME decision filter; Phases 3–6 reframed from cognitive structure → emotional state (feel of it / persistence band / attribution pattern / irreversibility). Banned-word list extended (disappointment, disappointed, guilt, guilty). Live audit confirms zero banned-word matches in page HTML.

**Entity-from-KB MVP — spec + build (Wed, May 22)**
- Spec drafted first (BOTS.md § 9.y), then built same day: `entity_catalog` CHECK widening, `lib/entityVariants.ts` plural/singular aware, `lib/flagStyles.ts`, 3 API routes, admin tab, pill style, `sql/087`.
- 26 unit tests (12 + 14). Full suite 277/277 green post-build.
- On-demand extraction, `entity:<slug>` prefix pills (distinct from `topic:` amber and `focus:` teal).

**NOWOCATS / PulseIQ Phase 5 convergence complete (Mon–Wed, May 18–22)**
- Phase 5 commit 6: `lib/townHallAdapter.ts` projects new-substrate `town_halls` rows into legacy JSON shape so `SessionDetailClient.tsx` (1900 lines) needed no changes.
- NOWOCATS readiness gaps #1–4 closed: `join`, `live`, `themes`, `responses`, `sessions PATCH/DELETE` all wired for phase-3 substrate.
- Gap #5 (analyze sync) and Gap #5b (post-session responses) and Gap #6 (bot-level analyze town-hall attribution) all shipped in same day.
- `town_hall_conversations.json` export made substrate-aware; per-turn `content_flags` + per-participant persona/name now surfaced in three render surfaces (admin modal, public shared HTML, PulseIQ facilitator modal).
- `lib/flagStyles.ts` extracted as shared source of truth for pill rendering.
- Prod env flipped: `TOWNHALL_VIA_AGENT_HANDLER=true` + `DUAL_WRITE_PHASE3=true`. Phase-3 substrate is live.

**Probe-focus tagging + name capture (Wed, May 22)**
- `agents.probe_focus_enabled` toggle + `lib/probeFocusClassifier.ts` — per-user-turn `topic:<slug>` flags unlock matched/mismatched bot-topic vs user-topic diff analysis.
- `lib/nameExtractor.ts` (Haiku, 80 tokens, 5s timeout): fires at user_turn_count == 2 and 5, idempotent. Fixes ~88% "Anonymous" rate in admin conversation views.

**NOWOCATS demo data + annotated transcripts (EOD Wed, May 22)**
- 14-turn demo conversation for "Maria" (Apopka resident) seeded to prod; 7 Sarina focuses authored; retroactive `content_flags` with intentional mismatched pair at T9→T10 for demo credibility.
- Annotated transcripts extended to public shared HTML and PulseIQ facilitator dashboard modal.
- Hope conversation bugs: T0/T0 turn-number collision fixed; widget-collected `user_name` not persisted to `agent_session_personas` — fixed; 3 historical sessions backfilled.

**Admin usage date-range + drill-in detail (Fri, May 22)**
- `/admin/usage` gains custom From/To date-range pickers.
- Per-resource drill-in detail page: `by_event`, `by_model`, `daily_trend`, resource and org name resolved via service role (all admin-gated with `requireAdmin`).

**Hope donor agent (Thu–Fri, May 21–22)**
- Donor agent for Foundations Project + Coalition KB seeded; `?site=` deployment context param for white-labeling across different coalition properties.

**CI infrastructure (Wed, May 22)**
- `sql/080` + `sql/081` wrapped in `BEGIN/COMMIT` (tx-wrap guard failure).
- `vi.mock('server-only', ...)` in `tests/setup.ts` to fix `server-only` leaking through unmocked libs in integration tests.
- Coordination commit absorbed parallel-session MCO WIP that had leaked into a NOWOCATS commit.

### Risk signals

- **NOWOCATS at launch gate**: prod env has `TOWNHALL_VIA_AGENT_HANDLER=true` + `DUAL_WRITE_PHASE3=true` confirmed set, but NOWOCATS `town_halls` row is in `draft` status — one manual "Start" click required before participants can connect. The provisioning SQL (`sql/one-off/2026-05-22-nowocats-town-hall.sql`) requires explicit user approval before run; devlog explicitly flags this.
- **Coordination commit with unreviewed WIP**: commit `49e7b9b` absorbed parallel-session MCO code (MCO canvas components, `lib/places.ts`, scripts) that was not typechecked or smoke-tested by the absorbing session. Verification explicitly deferred in devlog.
- **50 commits, 1 author, push freeze for entire week**: all work accumulated locally on `main` behind a push gate; no incremental CI signal until the batch push. Tight single-author dependency.
- **W20 progression items not addressed**: Sentry `beforeSend` scrub, PPTX Datanautix branding, and `send_at` phantom campaign field all carried forward unresolved.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 1.35 |
| Security | 7/10 | 15% | 1.05 |
| Dependencies | 7/10 | 15% | 1.05 |
| Structure | 7/10 | 10% | 0.70 |
| Tests | 4/10 | 15% | 0.60 |
| Documentation | 9/10 | 10% | 0.90 |
| Maintainability | 8/10 | 20% | 1.60 |
| **Total** | | **100%** | **72.5 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- None confirmed. No new bare-id service-role lookups found in non-admin routes this week. The new admin usage drill-in routes (`app/api/admin/usage/[type]/[id]/route.ts`) use bare `.eq('id', params.id)` service-role queries but are all gated with `requireAdmin` — correct posture.

**MEDIUM**
- **[MEDIUM — OPEN FROM W20] Sentry `beforeSend` scrub not implemented** — `sentry.{client,edge,server}.config.ts` still have no `beforeSend` hook. PII (email, phone, survey payloads) can reach external Sentry on unhandled exceptions in `/api/respond`, `/api/campaigns/send`, `/api/b/[slug]`. SECURITY.md §5 mandates this. Not addressed in W21.
- **[MEDIUM — OPEN FROM W20] npm: 1 HIGH CVE + 2 MODERATE** — Next.js DoS via Image Optimizer `remotePatterns` (HIGH), PostCSS XSS via unescaped `</style>` (MODERATE), brace-expansion DoS defeating `max` protection (MODERATE). Root fix is Next.js 14→15/16 upgrade (deferred; two-major-version jump). Vercel managed runtime partially mitigates the self-hosted paths.
- **[MEDIUM — OPEN FROM W20] PPTX decks branded "Datanautix"** — `components/ui/DatanautixAttribution.tsx` + `components/ui/ChatBot.tsx` ship "Powered by Datanautix" + `datanautix.com` links in customer-visible widget chrome. `app/api/rollup-deck/route.ts` PowerPoint generation likely also uses "Datanautix" in metadata. Product spec is "Sentimetrx". Not fixed in W21.
- **[MEDIUM — OPEN FROM W20] `send_at` phantom campaign field** — `CampaignDetailClient.tsx` submits `send_at` in PATCH body; no database column exists; API silently drops it. Users believe they're scheduling a specific-time send that never fires. Not fixed in W21.
- **[MEDIUM — NEW] Coordination commit with unverified MCO WIP** — commit `49e7b9b` (`chore(coordination): absorb parallel-session MCO_AGENT WIP`) includes `lib/places.ts`, 5 scripts, MCO canvas components, and `data/mco_live_ratings.json` absorbed from a parallel session. Devlog explicitly states: "this session didn't review the code, didn't typecheck against the changes, didn't smoke-test." This WIP includes Google Places API integration and new public routes.
- **[MEDIUM] Test ratio 0.044** — 28 test files / 631 source files. Marginal improvement from W20's 0.041 despite significant new feature surface (MCO agent, entity extraction, phase-3 convergence routes, Decision Study agent, Hope donor agent). Route handlers and React components remain largely untested.

**LOW / informational**
- `execSync` in `next.config.js:6` — build-time `git rev-list` call, not a runtime risk; flagged by automated scans.
- `data/mco_live_ratings.json` (388-line snapshot) committed — includes live business names and ratings from Google Places; Google TOS prohibits caching rating data (only `place_id` is cacheable). Low severity since it's a prototype, but a cleanup step before production MCO launch.
- No `.claude/rules`, `.claude/agents` defined — next maturity tier for AI governance.
- `tsconfig.tsbuildinfo` committed (build artifact; bloats diffs).
- GOAA public API key hardcoded as fallback in `lib/parking.ts` — devlog acknowledges this is a public site key visible in flymco's frontend JS, but should be env-var-only before kiosk production.

### Progression plan (top 3 to fix this week)

1. **Add Sentry `beforeSend` scrub** — create `lib/sentryScrub.ts`, strip email/phone/`req.body`/auth headers, wire into all three Sentry configs. ~30–60 min. Closes SECURITY.md §5 PII-in-error-reports mandate. Carried from W20 — third week open.
2. **Fix PPTX / widget Datanautix branding** — replace "Datanautix"/"datanautix.com" in `DatanautixAttribution.tsx`, `ChatBot.tsx`, and deck-generation metadata with "Sentimetrx" / correct domain. Customer-visible, surgical fix. ~30 min. Carried from W20.
3. **Typecheck + smoke-test the absorbed MCO parallel-session WIP** — run `rm tsconfig.tsbuildinfo && npx tsc --noEmit`; manually verify `lib/places.ts` routes; confirm Google Places TOS compliance (no caching of ratings in `mco_live_ratings.json` snapshot). Before any MCO demo deployment.

## 3. Correlation: dev work → audit findings

- **[W20-MEDIUM] Sentry `beforeSend` scrub** — **not mentioned in devlog**; W21 work did not address this.
- **[W20-MEDIUM] npm CVEs (1 HIGH + 2 MODERATE)** — **not addressed**; Next.js 14→15/16 upgrade not attempted.
- **[W20-MEDIUM] PPTX Datanautix branding** — **not mentioned in devlog**; still present in code.
- **[W20-MEDIUM] `send_at` phantom campaign field** — **not mentioned in devlog**; still present.
- **[W20-MEDIUM] Test ratio** — **partially addressed**: W21 added entity detection tests (26 tests), ui-hints tests (31 tests), CI infrastructure fixes, auth-flows and egress integration tests already present. Test file count up to 28 from ~24. Ratio still 0.044 — source base grew proportionally.
- **[NEW-W21-MEDIUM] Coordination commit with unverified WIP** — **identified in this report, not yet addressed**.

## 4. Trend

Previous report (W20): **72.5 / 100** (generated 2026-05-18)

This report (W21): **72.5 / 100** — **+0.0 points (flat)**

Score unchanged: W21 delivered substantial new feature work (MCO agent, entity extraction, NOWOCATS convergence, Decision Study) but none of the three W20 progression items were addressed. Test ratio improvement is marginal. Security and Dependencies hold steady with no new HIGH/CRITICAL findings but no CVE remediation either. Documentation and Maintainability remain at W20 levels.

Primary drags: test ratio (4/10, 15% weight) and three carried-forward MEDIUMs (Sentry scrub, PPTX branding, CVEs).

## 5. Human review checklist

- [ ] Skim development summary — NOWOCATS launch-readiness: is the `town_halls` provisioning SQL ready to run?
- [ ] Review MEDIUM findings — Sentry scrub is now 2 weeks overdue; is it a pre-NOWOCATS blocker?
- [ ] Check correlation — three W20 progression items still open; prioritize before W22 or explicitly defer.
- [ ] Coordination commit `49e7b9b`: review `lib/places.ts` + MCO scripts for correctness before MCO demo.
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine. Requires human review before being recorded.*
