# Weekly Governance Report — 2026-W34 (Week of Mon 11 Aug to Sun 16 Aug)

**Generated**: 2026-08-17T08:12:16Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W33-devlog.md
**Spec-drift**: 0 drifted — see 2026-W34-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 49 commits, 1 author (Sanjay Patel), top-level dirs: `app/`, `lib/`, `sql/`, `docs/`, `scripts/`, `tests/`, `components/`

### Narrative (from devlog)

**Context view — collocations (Aug 13, 3 sub-entries)**

- Added `lib/collocations.ts` + `ContextCloud` component, wired as a **Context** tab in both word and theme modals. Replaces the unused `trendingWords.contextTermsFor` skeleton. Counting unit is the comment (not the token), matching drill-down counts. G² log-likelihood toggle for Distinctive mode; frequency floor prevents exclusivity bias on small selections.
- Contraction + function-word noise fixed for conversational corpora (spoken remarks, Trump corpus): enclitic stripping re-tests against the stop list; Context stop list is deliberately narrower than word-cloud stop list (preserves sentiment words like "good", "great" that are signals in collocate context).
- 27 new tests (15 collocate, 6 component render, 4+2 modal paths). Owner browser-QC passed.

**Metric strip "0 comments" on 49,033 rows (Aug 13)**

- Cause: the Trump spoken-remarks dataset was ingested via a direct-write script that bypasses the ingest hook that stamps `substantive`. The strip fell back to unstamped-as-zero. Fix: when `substantiveRecords === 0`, fall back to all-based counts, suppress the "% of N answered" clause, and swap the tooltip. Same guard added to `DatasetAboutPopover`. 4 new tests.

**TextMine performance — progressive rendering (Aug 14–15, 4 sub-entries)**

- Theme-counts had no persistent cache — each fresh page load re-ran three 10-page keyset scans (33.4s cold on the 128K Outback dataset). Added `lib/themeCountsCache.ts` (same shape as `signal_stats` cache, 6-slot bounded, jsonb round-trip). Warm result: 0.6–0.7s vs 33.4s cold.
- Theme-counts fetch decoupled from `rowsLoaded` gate; cards now paint from server counts while rows stream behind a "calculating…" banner. Cards-to-paint 20.4s → 2.6s warm (7.8×); settled 58s → 16s warm. Cold: cards 26.1s → 19.8s after subsequent deferral fix.
- Rows wait for counts phase (counts request → rows → extras): prevents dataset-instance contention measured consistently across prior experiments. Cold cards now 19.8s (marginally ahead of baseline).
- Nav tabs (`Clouds`, `Compare`, `Comments`) disabled while rows load — gated on `!rowsLoaded`, not `rowsLoading`, to cover the deferred-start window. Active tab never disabled. Lint ceiling 252→251.

**Theme-cloud term percentages (Aug 14)**

- Terms now divide by their own theme's comment count, not the corpus total. Old denominator squeezed all chips into a 0–9% band for a 9% theme; new: "price 19%, bill 15%, cost 7%". Flat (ungrouped) cloud keeps corpus denominator. Synthetic placeholder chip suppresses percentage (would otherwise render 100%). 0 lint delta, tsc clean, suite green.

**Stratified sampling (Aug 15–16, 4 sub-entries)**

- `sql/188`: stratified-block primitive. Measured 2.3× improvement on 128K rows — oversold from an earlier microbenchmark. Decision deferred pending 1M measurement.
- `sql/191` (SQL half): 19 remaining sampled functions converted to block sampler. Key fixes: per-block LIMIT taken from block start (not inside LATERAL — avoids over-weighting boundary blocks); `dataset_sample_blocks` count replaced with index probe (was `count(*)` over whole dataset, O(dataset_size), defeating flatness). Byte-identical aggregate output vs hash sample. TEST only.
- `sql/191` (TS half): `pageSample`, five bespoke pagers, and cache version discriminators (STATS_MODEL_VERSION 2→3, THEME_COUNTS_VERSION 1→2) updated. Validated at 600K and 1M: **hash 44,057ms vs blocks 11,154ms (3.9×)** on a 50K-row walk. Deliberately not applied to prod pending owner decision (every sampled figure moves once).
- Scale fixture seeded to 1M rows, measured, dropped. Loop-never-terminates bug in `_seed_scale_test.sh` found and fixed (psql trailing blank line caused `wc -l` to report 1 on empty output).

**Security sweep — SECURITY DEFINER (Aug 16, sql/189+190)**

- `sql/189`: fixed two anon-executable SECURITY DEFINER functions over raw tenant rows (`dataset_rows_with_text_count` array overload missing; pending-rows RPC silently dropped rows with no `_tx` key via STRICT jsonb `?` operator returning NULL). Both revoked from `anon`/`authenticated` to `service_role`. **Also found and fixed `study_stats_for_ids` cross-tenant read** (no org filter on a SECURITY DEFINER RPC that authenticated any user could call). Applied to prod 2026-08-16.
- `sql/190`: locked 77 SECURITY DEFINER functions to `service_role`, closing the class exposed by sql/189 (Postgres grants EXECUTE to PUBLIC by default; `anon` inherits it; PostgREST publishes any `anon`-executable function as a public endpoint — with a key shipped in every browser bundle). Three RLS-predicate helpers excluded by necessity; 2 authenticated-only RPCs audited and preserved. Catalog-driven `DO` loop, not hand-typed signatures. Applied to prod 2026-08-16.

**Sentry production issues resolved (Aug 15, 3 sub-entries)**

- Issue 1: signal-stats 500 on statement timeout — `computeSignalStatsRaw` now degrades to sampled path on `57014`, flagged `sampled: true`. 3 tests.
- Issue 2: entity-discovery cron timing out on large collections (12 events) — sampling gate was `singleDataset` only; now samples any above-cap collection. Members split `ENTITY_SAMPLE_CAP` proportionally via `allocateSampleShares`. ⚠️ Error-swallowed empty-count path left in place (documented; separate decision).
- Issue 3: org-snapshot alert didn't name the failing org — `failures: [{org_id, error}]` now in Sentry event payload.

**Dimensions for collections (Aug 16)**

- Five fan-out sites implemented in `lib/collectionScope.ts` (`resolveScopeMembers`/`resolveMemberDatasetIds`): write (`classifyDatasetKeyword`, `classifyPendingRows`), read rollup, drill-down, compare. The key path bug: `IN (…)` widening drops `idx_drf_id_keyset` and goes quadratic; fix is per-member `.eq` + JS k-way merge. Verified on Darden Demo (15,154 rows): 25 checks pass, collection rollup equals member sum. Browser-QC'd all views.

**`org_snapshot_runs` backup ledger — sql/192 (Aug 16)**

- Adds `org_snapshot_runs` table: one row per (org_id, snapshot_day), upserted through `record_org_snapshot_run` (attempts counted, not rows accumulated). Four-valued status: `success`, `partial`, `incomplete`, `failed`. `/admin/health` Backups card. Gap query enumerates the fleet and flags orgs with no row. TEST only; prod apply pending.

**Additional fixes (Aug 11–16)**

- `chatCore` multi-tenancy: 4 service-role reads now pair `org_id` (defence-in-depth; closes W33 Medium finding).
- Lint ceiling ratcheted 252 → 229 (13 warnings cleared: 4 `import/no-anonymous-default-export` on k6 entry points; 9 `@next/next/no-img-element` with documented reasons — data: URLs, arbitrary customer logo domains, remote SVG).
- Last circular dependency broken: `Industry` moved to `lib/types.ts` (was `lib/industryDefaults.ts`).
- Statistics findings UI: stacked cards → dense table with `effect`, `effectLabel`, `n`, `sub` fields populated at push sites.
- npm audit: 26 → 22 → 2 high vulns remaining. Remaining 2 (`image-size` via `pptxgenjs`) documented as phantom dependencies (import never reached in shipped bundles).
- MCO_AGENT: `<img>` rendering decisions recorded; spec drift from prior week closed.
- Devlog: retraction of false "Sentry was never switched on" claim; Sentry confirmed live with 3 production issues at time of investigation.

### Risk signals

- **sql/191 (stratified sampling) not in prod**: performance justified at ~4× for 1M-row datasets; every sampled figure moves on apply. Needs owner sign-off on timing.
- **sql/192 (backup ledger) not in prod**: the 2026-08-08 "1/9 orgs failed" Sentry event is still unresolvable (logs rotated). Each day without the ledger repeats that gap.
- **Entity count error-swallowing**: `entityFilter.computeEntityCounts` catches timeout and returns an empty map rather than propagating the error. An empty count is indistinguishable from a measured zero — same class as the "0 comments" metric strip bug and the "95% CI: 0–0%" display bug both fixed this week.
- **Statistics findings table + nav pill Lottie combo**: browser QC explicitly deferred in devlog (dev-server session expired; minting cookie locks out owner). Needs eyeball before next release.
- **Next.js CVEs (unfixable)**: advisory range `14.3.0-canary.0 – 16.3.0-preview.7`; no released version clears it. `proxy.ts` CSRF gate remains exposed to `GHSA-6gpp-xcg3-4w24`. Monitor for 16.3.0 stable.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 13.5 |
| Security | 8/10 | 15% | 12.0 |
| Dependencies | 8/10 | 15% | 12.0 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 7/10 | 15% | 10.5 |
| Documentation | 9/10 | 10% | 9.0 |
| Maintainability | 8/10 | 20% | 16.0 |
| **Total** | | **100%** | **81.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**

- **Next.js 9+ CVEs (framework-level, unfixable)** — advisory range `14.3.0-canary.0 – 16.3.0-preview.7`; installed 16.2.12 is inside it. `GHSA-6gpp-xcg3-4w24` (proxy/middleware bypass) is directly relevant to `proxy.ts` CSRF gate. No released version clears the range; `16.3.0` stable is the only fix path. Monitor the Next.js release channel.
- **image-size 2 high CVEs via pptxgenjs** (`CVE-2026-xxxxx`, ICNS/JXL/HEIF infinite loop parsers) — `npm audit` reports 2 high. Documented this week as phantom dependencies: `getSizeFromImage` is in a commented-out block, `image-size` appears 0 times across all four shipped pptxgenjs bundles. Vulnerable parsers cannot execute. No actionable fix (patching pptxgenjs 4.0.1 would require downgrade to 1.1.5, breaking all deck exports). Carry as documented/known.

**MEDIUM**

- **Entity count error-swallowing** — `lib/entityFilter.ts`: `computeEntityCounts` catches a statement timeout and returns an empty map. Caller treats empty as "no entities found." An empty count is indistinguishable from a measured zero and cannot trigger a retry or alert. This is the same class of silent data corruption as the metric-strip "0 comments" bug (fixed this week) and the pending-rows NULL bug (fixed via sql/189). Fix: propagate the error or flag the result as `sampled`/`degraded`.
- **Statistics findings table + nav pill Lottie: browser QC deferred** — `components/analyze/StatsModule.tsx` and `app/analyze/[id]/TextMineModule.tsx`: both confirmed tsc-clean and test-green, but the dev-server session had expired at commit time and minting a cookie would lock out the owner. The Statistics table needs an eyeball on a 100+-finding view (both `r` correlation and `η²` group-effect rows); the nav pills need verification of the always-on dot + hover Lottie states on a cold load.
- **sql/191 (stratified sampling) not applied to prod** — performance proven at 3.9× for 1M rows, correctness verified against ground truth (worst-case 2.2% deviation), but every sampled figure in the product moves on apply (theme counts, signal stats, entity counts, cohort comparisons). Owner sign-off on timing required. Risk of delaying: the hash sampler degrades O(dataset_size) in cold-cache conditions, and the 1M target is blocked.
- **sql/192 (backup ledger) not applied to prod** — `org_snapshot_runs` table adds durable "which orgs backed up last night?" auditability. Without it, the 2026-08-08 "1/9 orgs failed" failure class repeats silently: no row = no alert = no recovery.

**LOW / Informational**

- Test ratio 0.22 (171 test files / 749 source files) — below the 0.5 target. Suite grew from 1,607 to 1,687 tests this week (+80 across collocations, taxonomy collection fan-out, cron error handling, and sql/192 cron tests). Additional env-gated suites (RLS 4, egress 27, auth-flows 6) provide coverage outside the main count.
- 229 lint warnings remaining (ceiling 229) — all `react-hooks/*`. An active burndown is blocked on per-file browser verification (unmemoized effect web caused the Statistics-tab infinite loop). Ratchet is current and tight; any new warning fails CI.
- `app/` directory at 555 files — route tree growing. No immediate action required; navigability concern if growth continues at this pace.
- Sentry now confirmed live with 3 production issues (was incorrectly believed inactive). Two of three resolved this week; org-snapshot alert improved.
- `docs/SECURITY.md` §2 open: a full `authenticated` audit of all 85 SECURITY DEFINER functions is deferred (the 3 RLS helpers and 2 dashboard RPCs were handled; the remaining `authenticated` functions need a sweep to confirm no org-filter gaps like `study_stats_for_ids`).

### Progression plan (top 3 to fix this week)

1. **Apply sql/192 to prod** — adds `org_snapshot_runs` for backup auditability. Low risk (new table + RLS, additive change); no sampled figures change. Owner has already reviewed the design. Closes the "which org failed?" gap permanently.
2. **Fix entity count error-swallowing** — `lib/entityFilter.ts`: catch the timeout and surface it as a `degraded` flag or propagate to the cron alert, rather than returning an empty map that reads as "no entities." Pattern: mirror the signal-stats fix landed this week (`sampled: true` + tooltip). Zero schema changes; 2–3 test cases.
3. **Apply sql/191 (stratified sampling) to prod after owner confirmation** — every sampled figure moves once; performance payoff is 4× at 1M rows and blocks the 1M scaling target. Needs explicit timing decision from owner, not a technical blocker.

## 3. Correlation: dev work → audit findings

**W33 findings and their W34 status:**

- **chatCore.ts:2040/2159 MEDIUM (from W31, carried W33)**: Addressed and closed. `95854ac` adds `.eq('org_id', ...)` to 4 service-role reads in `lib/chatCore.ts` — not just the two flagged in W33 but all 7 in the file. **Addressed this week.**
- **ESLint ceiling 252/252**: Addressed. `19c7f97` ratcheted to 229 by clearing 13 warnings. Ceiling now has 0 slack. **Addressed this week.**
- **TESTING.md spec drift**: Addressed. `docs/TESTING.md` has 8 edits this week alongside 12 code commits (per spec-drift report). **Addressed this week.**
- **Next.js CVEs (unfixable, carried)**: No Next.js version change this week; advisory range unchanged. Correct posture. **Not addressed (no fix available).**
- **Security: SECURITY DEFINER anon-exec class (new this week from sql/189 discovery)**: Addressed and closed this week by sql/190 — 77 functions locked to service_role; 3 RLS helpers exempted by necessity; 2 authenticated-only RPCs audited and kept. **Addressed this week.**
- **study_stats_for_ids cross-tenant read (new this week)**: Addressed and closed this week — org filter added. **Addressed this week.**

## 4. Trend

Prior report: **2026-W33** — 77.0 / 100 (generated 2026-08-10).

**Delta: +4.0 points** (81.0 vs 77.0).

Key drivers of improvement:
- **Security** 7→8 (+1.5 weighted): sql/189+190 closed the SECURITY DEFINER anon-exec class (77 functions locked), `study_stats_for_ids` cross-tenant leak fixed, chatCore multi-tenancy closed.
- **Dependencies** 6→8 (+3.0 weighted): npm audit high vulns dropped from 22 to 2; remaining 2 documented as phantom (cannot execute in production).
- **Documentation** 8→9 (+1.0 weighted): 0 spec drift this week (vs 1 last week), 17 specs updated alongside their code, both top-level specs current.
- **Tests** 8→7 (−1.5 weighted): more rigorous reading of the test ratio (0.22 vs 0.15 prior count discrepancy; still below the 0.5 target criterion for a 7–9 score). Absolute suite growth (+80 tests) is healthy.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Review the spec-drift companion (2026-W34-spec-drift.md) — any spec debt?
- [ ] Check correlation section — are open findings being worked on?
- [ ] Confirm timing for sql/191 (stratified sampling) prod apply — every sampled figure moves; is this week the right moment?
- [ ] Confirm sql/192 (backup ledger) prod apply — low risk, high value.
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
