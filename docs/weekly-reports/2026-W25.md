# Weekly Governance Report — 2026-W25 (Week of Jun 15 to Jun 21)

**Generated**: 2026-06-22T00:00:00Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W25-devlog.md

## 1. Development Summary

**Quantitative**: 50 commits, 1 author (Sanjay Patel), top-level dirs touched: app/ (520 file-touches), lib/ (218), sql/ (157), docs/ (134), components/ (119), scripts/ (108), tests/ (85), public/ (18), .claude/ (11)

### Narrative (from devlog)

**Saved Views / Snapshots / Periods — full feature (Jun 21–22, 11 commits)**
- Complete relative-date analysis feature: views (live named filter configs), snapshots (frozen aggregates, drift-immune), periods (relative date ranges resolved at read time — "this quarter", "same period last year"). Designed, specced (`docs/SAVED_VIEWS.md`), and built in a single week, Phases 1–4.
- Key engineering decisions: calendar arithmetic with half-open `[start,end)` boundaries; to-date alignment for in-progress periods (kills "−90% on day 8" phantom); `serializedFiltersEqual` for dirty-flag detection; snapshot content is immutable (only `expires_at` mutable); comparison delta guards (deferred prior, "new" on zero base, "—" before data exists).
- `sql/130_saved_views.sql` applied to prod with RLS (private-by-default + org-share toggle). `test:rls` green.
- E2e Playwright suite run green against the live linked DB: auth → CSRF → route → service-role + RLS. 929 tests passing by week-end (up from 875).

**Town Hall sweep — transcript review, PDF polish, speaker tooling (Jun 16–18, ~25 commits)**
- Transcript review: shared `<TranscriptReview>` component available at both the gate (before first AI pass) and the report; inline verbatim edit + per-segment speaker reassignment + auto-fill same-speaker gaps; entity-map corrections applied on-read across report/PDF/deck.
- Re-transcription: full re-transcribe action + targeted span re-transcription for quiet stretches (ffmpeg slice → ASR → merge, no Q&A cleared); `qa_stale` flag + amber banner.
- Speaker tooling: `speaker_names` (sql/128), editable speaker roster, self-intro auto-suggest, diarized-label reassignment. Consolidated to one place (transcript panel).
- PDF: running "powered by datanautix" header, per-page footer (timestamp / meeting name / page N of M), separator rules, Action Items section, US Letter with 1" margins, entity corrections applied to all AI text.
- Agent cross-org bug fix: Town Hall agent picker and PATCH guard now admit cross-org agents for platform admins; `transfer_agent_org` RPC (sql/127) cascades the move across all related tables (was stranding conversations in the wrong org).

**Analytics: per-outlet peer-group report (Jun 17)**
- New Outlets tab gated on `sources:['google_reviews']` + `minOutlets:5`. `computeOutletReport` joins taxonomy assertions to flat rows on `flat.id === taxonomy.row_id`, ranks outlets by net-positive rate per sub-theme vs chain, guards stability floors; `isNoiseAssertion()` drops keyword false-positives. Verified against live BareBurger 29-outlet / 14.7k-review dataset.

**Survey: kiosk mode (Jun 21)**
- `?kiosk=1`: attract screen → session → auto-reset (~7s after close card); idle-abandon (90s → attract); per-guest fresh session_id; per-device lock bypassed. Publish-page enablement card with kiosk QR + attract-copy inputs.

**Pitch decks + investor materials (Jun 16–19, ~15 commits)**
- Anthology full (17 slides) + short (12 slides) PDF variants; warm-editorial HTML-to-PDF pipeline (Fraunces + DM Sans, paper canvas, datanautix.com tokens).
- Community-engagement deck rebuilt 5 times iterating toward "five practitioner pains as the spine." Final: 13 slides, pain-led (representativeness → access → heard-enough → keep-up-without-email-flood → closing-the-loop), consistency reassurance slide.
- Engineering Reality Check deck updated with current metrics (2,428 commits, 903 TS files, 875 tests, CI on every push).
- Admin decks: grouped by audience (Investor / Technical / Client), sorted most-recently-updated first.

**Infrastructure / ops (Jun 16)**
- Vercel build savings: docs-only production deploys now skip (`git diff HEAD^ HEAD -- . ':(exclude)docs'`); weekly governance PRs no longer cost a build.
- Dependency remediation: esbuild/vite HIGH CVEs 14 → 0 (pinned via `package.json` overrides; `npm audit fix --force` deliberately avoided). 2 residual HIGH CVEs in `@vercel/sandbox` build-time chain (not runtime).
- Service credit monitor: `service_health` table (sql/126), Tier-1 balance API probes (DataForSEO / Deepgram / Twilio), Tier-2 capture-on-402 wired into all paid integrations, 6-hour cron, admin health panel. 11 new tests.

**CI fix (Jun 17)**
- `sql/126`, `127`, `128`, `130` wrapped in `BEGIN/COMMIT` for `check:sql-tx` guard (>migration #70 requirement).

### Risk signals
- Saved Views Phase 4 comparison (per-chart two-series rendering) intentionally deferred — documented in SAVED_VIEWS.md.
- PDF render depends on Google Fonts reachability from Vercel serverless — no bundled fallback. Noted in devlog but not resolved.
- ~18 commits ahead of remote, unpushed (multiple feature branches of work pending authorization to push).
- Town Hall speaker/transcript features rely on Sandbox (ffmpeg) for span re-transcription — only exercisable post-deploy; local test coverage is compile/type-only for that path.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 1.35 |
| Security | 8/10 | 15% | 1.20 |
| Dependencies | 7/10 | 15% | 1.05 |
| Structure | 8/10 | 10% | 0.80 |
| Tests | 7/10 | 15% | 1.05 |
| Documentation | 9/10 | 10% | 0.90 |
| Maintainability | 9/10 | 20% | 1.80 |
| **Total** | | **100%** | **81.5 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- None this week. No hardcoded secrets found; no eval/exec with user input; no new cross-tenant leaks detected.

**MEDIUM**
- `app/api/bots/[id]/route.ts:73,102` — service-role bare `eq('id')` reads (two-step read-to-get-org_id pattern). CLAUDE.md invariant is "pair id + org_id"; these reads retrieve org_id as the first step, which is acceptable, but defense-in-depth would pair them from call-site. Non-admin route; the subsequent write operations should verify they pair id + org_id (spot-check recommended).
- 2 HIGH npm CVEs remain: `@vercel/sandbox` + the full `@workflow/*` build chain — build-time only, no runtime path. No available non-breaking fix (same constraint as the resolved esbuild/vite batch).
- `app/api/admin/users/[id]/route.ts:26,29` and `app/api/admin/usage/[type]/[id]/route.ts:96–107` — service-role bare id lookups. Protected by `requireAdmin` (53 requireAdmin calls across 22 admin routes = fully gated), so cross-tenant access requires a compromised platform admin. Low practical risk; worth pairing for consistency with the invariant.
- Test ratio 0.098 (83 test files / 845 source files) — below the 0.5 target. CI ratcheting thresholds exist (15% stmts) but remain low. Integration and RLS suites are strong; unit coverage of route handlers is the main gap.

**LOW / informational**
- 0 `.claude/rules` and 0 `.claude/agents` — governance and automation rely entirely on CLAUDE.md + hooks + commands. Rules/agents would formalize per-task constraints.
- `scripts/` contains 108 files; bulk appears to be one-off tooling. W25 devlog deleted 8 scratch scripts — good hygiene signal; systematic audit of the directory not yet done.
- Google Fonts dependency in PDF routes (`pitch-deck-v2`, `pitch-deck-v3`, `community-feedback-deck`) — serverless fn renders fail silently if fonts aren't reachable; no bundled fallback. Documented but unresolved.
- `lib/filterUtils.ts` period resolver uses UTC for v1 (`ResolvePeriodOpts` seam reserved for org-TZ) — periods near midnight in non-UTC orgs will be slightly off. Deliberate; documented in SAVED_VIEWS.md §3.

### Progression plan (top 3 to fix this week)

1. **Audit `app/api/bots/[id]/route.ts` write paths** — confirm every service-role UPDATE/DELETE after the two-step read pairs both `id` and `org_id` in the actual mutating call (or confirm the read + RLS combination makes it safe). If write paths are bare, add `.eq('org_id', orgId)` to match the CLAUDE.md invariant. Estimated: 30 min.
2. **Pin @vercel/sandbox / @workflow CVEs or document accepted advisory** — check if a non-breaking pin exists; if not, document in SECURITY.md §9 (same treatment as the accepted esbuild prior). Keeps audit findings honest. Estimated: 30 min.
3. **Raise CI coverage ratchet** — now that Saved Views (+54 tests) landed, bump `vitest.config.ts` statement threshold from 15% toward 20% (measure current % first). Prevents future regressions eroding the gains made this week. Estimated: 15 min.

## 3. Correlation: dev work → audit findings

- **2 HIGH npm CVEs (@vercel/sandbox / @workflow)** — partially addressed. W25 devlog explicitly fixed 14 HIGH CVEs (esbuild/vite); 2 remaining are a separate advisory in the same build-time chain. Not mentioned as resolved.
- **Bare service-role id lookups (`bots/[id]`, admin routes)** — not mentioned in devlog. W25 fixed the cross-org agent-picker and transfer bugs but did not sweep the service-role query pattern in these files.
- **Test ratio below target** — incrementally addressed. Saved Views added 54 tests (875→929); ratio ticked up from ~0.09 to ~0.098. Not a focus this week; major feature shipping took priority.

## 4. Trend

Previous week (2026-W24): **78.5 / 100**
This week (2026-W25): **81.5 / 100**
Delta: **+3.0 points**

Driver: Dependencies improved 5→7 (+0.30 weighted) as the 14 HIGH esbuild/vite CVEs were remediated. Secrets, Security, Structure, Documentation, and Maintainability held steady. Tests ticked up slightly (ratio 0.089→0.098) but not enough to lift the score.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review MEDIUM findings — `bots/[id]` write-path spot-check; HIGH CVE posture
- [ ] Check correlation section — bare id lookup + CVE open items being tracked?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine. Requires human review before being recorded.*

---

## Reviewer notes — actions taken during review (2026-06-22, Sanjay Patel + Claude Code)

This report was recorded by direct commit (the 04:00 Monday routine did not land a `2026-W25.md`; only its spec-drift companion was generated). The three progression items were actioned during review rather than deferred:

1. **`bots/[id]/route.ts` write-path audit — DONE.** Confirmed the *mutating* calls already pair `id` + `org_id` for non-admins (PATCH `update(...).eq('id').eq('org_id', auth.orgId)`; DELETE likewise). No cross-tenant write hole existed. Added defense-in-depth: PATCH/DELETE now also guard the pre-mutation snapshot read (return 404 when a non-admin targets another org's agent), which closes a minor spurious-`success`/spurious-audit-log path. Two regression tests added to `tests/integration/bot-routes-gate.test.ts`.

2. **HIGH npm CVEs — RESOLVED (supersedes the "no non-breaking fix" finding).** The 2 HIGH were `undici` and `piscina` (the `@vercel/sandbox` + `@workflow/*` build chain). A non-breaking fix *was* available: the `undici` override was pinned to `7.27.0` (inside the vulnerable ≤7.27.2 range) → bumped to `7.28.0` (patched, still 7.x, keeps consumer compat); `piscina` (proto-pollution→RCE, ≤4.9.2, via an unused `@workflow/nest`→`@swc/cli` build path) overridden to `5.2.0`. `npm audit`: **2 HIGH → 0 HIGH / 0 critical**. No `npm audit fix --force`.

3. **CI coverage ratchet — deferred (recommendation stands).** Not auto-bumped: raising `vitest` thresholds without first measuring current coverage risks breaking CI on the next push. Measure `npm run test:coverage` actual %, then bump toward 20% in a dedicated change.

Net: progression items #1 and #2 closed in-review; #3 documented as a measured follow-up. Verified locally — typecheck clean, full suite green (931 tests after the 2 new gate tests). Changes committed locally, pending an authorized push.
