# Weekly Governance Report — 2026-W24 (Week of Jun 8 to Jun 14)

**Generated**: 2026-06-15T00:00:00Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W24-devlog.md

## 1. Development Summary

**Quantitative**: 49 commits, 1 author (Sanjay Patel), top-level dirs: app/ (537 file-touches), lib/ (199), sql/ (152), docs/ (136), components/ (112), scripts/ (108), tests/ (76), public/ (19), .claude/ (12)

### Narrative (from devlog)

**Security — Phase 1 route-gate campaign (all 13 batches, June 8)**

- Systematic org-scoping gate tests across ~90 of ~140 service-role API routes in 11 batches; Area 2 (admin/* gate, 46 tests); Area 3 (public surface no-leak catalogue + 7 tests).
- **6 cross-org vulnerabilities found and fixed** — 3 write holes + 3 read leaks:
  - WRITE: `POST /api/townhall/themes/[id]` (any authed user could approve/dismiss another tenant's town-hall topics), `POST /api/townhall/sessions/[id]/duplicate` (could inject a row into another tenant's org), `POST /api/townhall/themes/custom` (custom question injection into any live town hall).
  - READ: `theme-counts` (exposed theme co-occurrence + topical review words cross-org), `signal-stats-batch` (exposed analytics signals for arbitrary dataset ids), `theme-impact` (latent; removed table but gated for defense-in-depth).
  - All fixes owner-authorized; all carry regression tests.
- Admin surface fully gated: 23 admin routes × 2 auth states = 46 tests asserting every non-admin caller gets 401/403/404.
- Public surface catalogued in SECURITY.md §3 (12 routes, guard mechanism documented for each).

**Tests — systematic lift (Batches 1–4, June 8)**

- Coverage now runs and gates in CI: `npm run test:coverage` replaces bare `npm test`; ratcheting thresholds (stmts 15 / br 10 / fn 17 / lines 15 — bumped from baseline 12.78/8.42/13.18/13.68).
- +92 unit/integration tests: `statsUtils`, `themeUtils`, `datasetUtils`, `contentGuard`, `entityFilter` lib suites; bot-routes-gate (13 tests); 5 component suites (DatanautixAttribution, ModulePlaceholder, HelpHint, FavoriteStar, LottieLoader).
- **normCDF correctness bug found and fixed**: `lib/statsUtils.ts normCDF()` was computing Φ(z·√2) instead of Φ(z) — Φ(1.96) returned 0.997 instead of 0.975. Affected `mannWhitneyU`, `tDist2p` (df>100), `shapiroWilk` p-values (understated). One-line fix; statsUtils test now pins reference z-table values.
- Test files 52 → 73; ratio 0.061 → 0.089; full suite 575 → 864 pass.

**Town Hall / Recordings — feature sprint (June 8–9)**

- **Stereo per-mic speaker separation (RØDE)**: channel-aware extract path (ffprobe L−R energy, dual-mono guard), `multichannel=true` Deepgram path (channel = speaker), `recordings.audio_channels` (sql/122 applied), live recorder requests `channelCount:ideal:2`.
- **Mic check pre-flight panel**: self-contained device test, L/R level meters, AGC/echo/noise toggles, software gain, stereo dual-mono live detection (Chrome AGC collapses to mono → warns + one-click fix), AGC defaults OFF (stereo-safe), Auto-tune sweep (records 4 combos, scores SNR vs clipping, suggests settings), live karaoke reading progress, test clip playback (stereo/mono), per-channel color-coded live captions (single multichannel Deepgram socket), name L/R channels via speak-to-identify.
- **Draft reports**: `recordings.draft` (sql/125), watermark + banner, default ON; sign-off is the sole finalize path (captures reviewer name/timestamp; revoke returns to draft).
- **Other**: Q&A extraction optional (skip_qa flag, "Finish without Q&A" path); duplicate project (inherit setup, not media); full-width live record-start layout; Export tab renamed "Reports"; deck pre-fill bypasses Vercel 4.5 MB body limit via TUS; sql/123+124 applied.
- **Town Hall bugs fixed**: upload-stuck brick (status=uploading recovery); org-wide recording visibility (transferred recordings were invisible to recipient org); cross-org transfer RPC fix.

**Agents — Agent workbook (June 8–9)**

- Combined 4-tab Excel workbook (Summary + Public Comments + Q&A Pairs + Low-Confidence Answers + Transcript); exceljs client-grade styling (bold teal headers, zebra rows, frozen+filterable); personified column headers (agent name, not "agent"); lead-in context column; low-confidence tab excludes deflects + shows agent reply; Lottie loader overlay; `lib/agentExport.ts` extracted.
- **Public Comments record**: `classifyExchanges` extended to extract substantive resident feedback (zero added AI calls); cached study schema v4→v5; surfaced as 5th workbook tab + Summary count.

**REO taxonomy robustness track (June 8–9)**

- `lib/reoVocabulary.ts` lean closed vocab + inline `REO_ASPECT_DEF` definitions; `lib/reoExtractor.ts` Haiku-4.5 LLM classifier with validated rules; `sql/121_reo_gold_set.sql` (applied); `/admin/reo-gold-set` tap-to-judge UI (completion screen, no double-count); seed 30 owner-validated + 490 LLM-drafted reviews (2,352 obs). 10/10 domains covered. Finding: LLM drafts skew Positive (~72%), over-calling neutral — calibration pass next.

**Infrastructure / DevOps (June 8)**

- Vercel preview builds killed: `scripts/vercel-ignore-build.sh` skips every preview, builds only `main` (requires one-time dashboard activation by owner).
- CLAUDE.md push policy hardened: covers all branches, not just `main`.
- Claude Code agent-hardening hooks: `PreToolUse` (blocks reverse shells, remote-exec pipes, fork bombs, recursive `/`/`~` deletes, private-key reads); `PostToolUse` (scans for 8+ secret token shapes in Bash/Read output). 11 dangerous commands + 6 secret patterns tested.

**MCO deck (June 9)**

- `lib/pptx/mcoListeningDeck.ts` + admin route — 14 slides + appendix; history recap + 4 channels + 5-phase methodology + agentic leap + MCO Concierge. Illustrative-labeled sample outputs (no fabricated real-MCO data).

**Bug fixes**

- Town Hall PDF 500 in prod: `next.config.js outputFileTracingIncludes` now includes `@sparticuz/chromium/bin/**` for all 3 headless-Chrome routes (also fixes Agent Study PDF latent bug).
- Survey clarifier: `isDecline` no longer blanket-rejects inputs <5 chars; `shouldClarify` relaxed for negative/low-rating; AI SKIP path now respected (was ignored).
- Survey creator flow: Basics gating hint; adaptive follow-up Tab-to-accept; `🔘` emoji JSX render fix.

### Risk signals

- **13 HIGH npm CVEs** (all `esbuild` via `@workflow/*` dev toolchain — build-time, not runtime; but audit flags them as HIGH). Up from 0 HIGH in W23.
- **49 commits local on `main`, unvalidated by CI.** Town Hall stereo, mic-check, draft-reports, agent workbook, 6 security fixes all untested in CI. typecheck + suite pass locally (exit 0 locally throughout); no CI signal until push authorized.
- **Browser/hardware exercising not complete**: Town Hall stereo split, mic check (Auto-tune, karaoke, per-channel captions, L/R naming, speak-to-identify), live captions in mic check, draft watermark PDF/deck — all require an in-browser owner test with the RØDE hardware.
- **normCDF-affected p-values**: mannWhitneyU, tDist2p (df>100), and shapiroWilk previously returned incorrect (understated) p-values. Any persisted/displayed stat outputs for those tests should be recalculated on next dataset analysis run.
- **Vercel preview-kill requires one-time dashboard activation** (owner: Settings → Git → Ignored Build Step → `bash scripts/vercel-ignore-build.sh`). Until set, pushes to any branch still trigger preview builds.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 1.35 |
| Security | 8/10 | 15% | 1.20 |
| Dependencies | 5/10 | 15% | 0.75 |
| Structure | 8/10 | 10% | 0.80 |
| Tests | 7/10 | 15% | 1.05 |
| Documentation | 9/10 | 10% | 0.90 |
| Maintainability | 9/10 | 20% | 1.80 |
| **Total** | | **100%** | **78.5 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**

- **13 HIGH npm CVEs** (`npm audit`: 13 high, 5 moderate, 0 critical) — all traced to `esbuild` vulnerability via `@workflow/builders`, `tsx`, and their consumers (`@workflow/astro`, `@workflow/cli`, `@workflow/next`, `@workflow/rollup`, `@workflow/vite`, `@workflow/sveltekit`, `@workflow/nuxt`, `@workflow/nest`). Build-time-only exposure (no runtime web-server path), but audit severity is HIGH and count jumped from 0 HIGH in W23. Run `npm audit` for full CVE details; consider pinning `esbuild >=0.25.0` or upgrading `@workflow/*` if a non-breaking version exists.

**MEDIUM**

- **`app/bots/[id]/entities/page.tsx` and `app/bots/[id]/questions/page.tsx` bare service-role bot lookups (W23 carry-over)**: Phase 1 campaign covered API routes; these page-level server components were not confirmed fixed. Apply the same `agentQuery.eq('org_id', userData.org_id)` pairing introduced in `history/page.tsx` in W23.
- **Coverage floor at ~15.9% lines** — ratcheting thresholds set, but the realistic target is >50%. The Phase 1 campaign route tests and lib unit tests are a foundation; significant growth needed across the remaining 700+ source files.
- **`lib/statsUtils.ts` normCDF fix has user-visible stat impact**: stored/cached p-values from mannWhitneyU, tDist2p (df>100), and shapiroWilk before the fix are incorrect (understated). Old results remain in any cached TextMine/analytics output until the next recompute.

**LOW / informational**

- `lib/meridian.ts` — `PUBLIC_MERIDIAN_TOKEN` hard-coded API key for Meridian indoor maps (GOAA / MCO). Labeled public, read-only, for floor-map reads; risk class is low, but a credential in code. Carry-over from W23.
- `execSync` in 3 build-time files (`next.config.js`, `lib/deckLastModified.ts`, `scripts/check-devlog-drift-staged.ts`) — static git CLI commands, no user-controlled input; acceptable.
- Vercel preview-kill (`scripts/vercel-ignore-build.sh`) requires one-time dashboard activation before taking effect.
- Test ratio 0.089 (73/819) — improved from 0.061 in W23; well below 0.5 target but trajectory is now positive.

### Progression plan (top 3 to fix this week)

1. **Audit and remediate the 13 HIGH npm CVEs** — run `npm audit --json | jq '.vulnerabilities'` for the full chain; check if upgrading `esbuild` to ≥0.25.0 (or the `@workflow/*` suite) resolves without breaking the build. Do NOT `npm audit fix --force` (CLAUDE.md: causes absurd next@9.3.3 downgrade). Manual pin or selective upgrade only.
2. **Fix `app/bots/[id]/entities/page.tsx` and `questions/page.tsx` bare service-role lookups** — two-line fix each, same pattern as `history/page.tsx`. Closes the W23 MEDIUM that was not explicitly carried through the Phase 1 campaign.
3. **Raise coverage thresholds and add targeted API-route tests** — bump `vitest.config.ts` thresholds to 20/14/22/20 as the next ratchet; target the highest-blast-radius dataset read endpoints and recordings workflow functions that Phase 1 didn't yet unit-test.

## 3. Correlation: dev work → audit findings

- **[W23-MEDIUM] `townhall/themes/[id]` and `townhall/sessions/[id]/duplicate` cross-org write holes** — **fully addressed**. Both fixed in W24 (batch 1 + batch 4); regression tests added.
- **[W23-MEDIUM] `theme-counts`, `signal-stats-batch`, `theme-impact` cross-org read leaks** — **fully addressed**. All three fixed in W24 batch 8; regression tests added.
- **[W23-MEDIUM] `app/bots/[id]/entities/page.tsx` and `questions/page.tsx` bare lookups** — **not mentioned in devlog**. Phase 1 covered API routes; page components are carry-overs.
- **[W23-LOW] Test ratio and coverage not visible in CI** — **fully addressed**. Coverage now runs in CI; thresholds gating; ratio 0.061→0.089; 20+ new test files including route gate tests.
- **[W23-LOW] Agent Study PDF Vercel path unverified** — **partially addressed**. `next.config.js outputFileTracingIncludes` now ships `@sparticuz/chromium/bin/**` (fix for the prod 500); the fix only takes effect post-deploy. First verification is owner clicking Download PDF after next authorized push.
- **[W23-RISK] sql/111, sql/112, sql/102 not applied to prod** — status not mentioned in W24 devlog. Unknown whether applied.
- **[NEW-HIGH] 13 HIGH npm CVEs** — **not mentioned in devlog**. Regression identified in this audit; not addressed in W24.

## 4. Trend

Previous report (W23): **77.0 / 100** (generated 2026-06-08)

This report (W24): **78.5 / 100** — **+1.5 points**

Security gained +0.15 weighted (7→8, +1 point) driven by the Phase 1 route-gate campaign finding and fixing 6 cross-org vulnerabilities and systematically auditing ~90/140 service-role routes. Tests gained +0.30 weighted (5→7, +2 points) from CI coverage gating, +21 test files, and the normCDF correctness fix. These gains were partially offset by Dependencies dropping −0.30 weighted (7→5, −2 points) as 13 HIGH CVEs (esbuild via @workflow/*) appeared in the audit — a regression from W23's 0 HIGH state. Secrets, Structure, Documentation, and Maintainability held steady at W23 scores.

## 5. Human review checklist

- [ ] Skim development summary — 6 cross-org vulns fixed, stereo Town Hall shipped, agent workbook delivered. Anything unexpected?
- [ ] Review HIGH finding: 13 HIGH npm CVEs (esbuild chain). Build-time-only? Authorize a targeted upgrade pass?
- [ ] MEDIUM carry-over: bots/entities/page.tsx + questions/page.tsx bare lookups — schedule fix now or next week?
- [ ] normCDF impact: are any persisted p-value outputs (TextMine/analytics) stale? Should a batch recompute be run?
- [ ] Vercel preview-kill: has the Ignored Build Step been activated in the dashboard yet?
- [ ] 49 commits local, awaiting authorized push. CI is the next gate.
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine. Requires human review before being recorded.*
