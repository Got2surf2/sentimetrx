# Weekly Governance Report — 2026-W23 (Week of Jun 1 to Jun 7)

**Generated**: 2026-06-08T17:00:00Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W23-devlog.md

## 1. Development Summary

**Quantitative**: 53 commits, 2 authors (Sanjay Patel 48, Claude 5), top-level dirs: app/ (recordings routes, live capture, bots, surveys, textmine), lib/ (recordings, asr, liveRecovery, brandGlossary), sql/ (6 migrations applied: 101, 103, 104, 120, + 111/112/113 pending), tests/ (19 new test files → 48 total), docs/ (devlog, RECORDINGS.md, BOTS.md, ANALYTICS.md, SURVEYS.md, FEATURES.md)

### Narrative (from devlog)

**Town Hall / Recordings — primary sprint (June 1–8)**

- **Live capture: all 5 pieces + hardening shipped.** Piece 1: short-lived Deepgram grant token minted server-side (DEEPGRAM_GRANT_KEY → browser, raw key never exposed). Piece 2: in-browser capture backbone (getUserMedia → MediaRecorder → Blob → TUS → batch pipeline). Piece 2b: AudioWorklet PCM → Deepgram live WS for on-screen captions. Piece 3: rolling summary panel (Haiku pass every 45s on transcript growth). Piece 4: waveform + room-tuned audio (AGC on, echo/noise off). Piece 5: mic device picker + AGC toggle for pro mic (RØDE Wireless PRO). Hardening: crash recovery via IndexedDB (resume on tab reload) + provisional recap at Stop (live summary persisted to `recordings.live_summary` via sql/120 applied to prod). DEEPGRAM_GRANT_KEY set + live WS auth verified end-to-end; in-browser real-mic run still needed.
- **Permissions-Policy security fix**: `microphone=()` → `microphone=(self)` in `next.config.js` (first-party self may request mic; cross-origin iframes stay blocked). One-line fix that was blocking the entire live capture feature locally.
- **Report UX overhaul**: Coverage-page overhaul (live timeline, confidence histogram, transcript modal); Q&A unification (Appendix tab removed, all typologies shown with filter chip); Action items tab added; human edit layer (Verbatim/AI/Human three layers, PATCH endpoint, revert path); vendor-grade Q&A polish (road designators as numerals, synthesized questions); In-order chronological view; sticky header + tabs; segment trim ungated in Play modal.
- **Auditability**: `lib/recordings/timeline.ts` shared geometry drives Coverage, `/th`, PDF, and PPTX. `tightenSpansFromTranscript` re-anchors spans to real ASR timestamps (zero overlap after backfill on Meeting 2). `traceActionItem` content-word overlap links action items to transcript segments.
- **Card UX**: favorite star + ⋯ menu (rename/delete), progress lifecycle popover (✓ per step), cross-org transfer (sql/102 — RPC not yet applied to prod).
- **Misc**: brand-entity convergence for recordings (brandGlossary.ts merges agent catalog + brand collection into extraction prompt); share-verbatim toggle (sql/104, applied); live-vs-final transcript comparison tab; worklet prefetch to shrink opening loss.

**TextMine/Analytics — incremental polish (June 3–5)**

- Unified Comments filter (sql/113 applied to prod) — AND across theme + entity + dimension facets, `FilteredCommentsPanel` with server RPC.
- Drillable pills sweep: theme card Co-occurs + Items chips, Theme Clouds Items and Dimensions chips — all drill into the Comments filter.
- Dimensions rows on theme cards and clouds (sql/111 theme_dimension_counts RPC — **not yet applied to prod**).
- Dynamic rating field + aliases in Dimensions rollup (sql/112 dataset_field_values RPC — **not yet applied to prod**).
- Compare bar toggle (% ↔ ★ Rating) on Group Comparison chart.
- Auto-classify on Dimensions field selection (drops manual Classify button).
- Multi-field concat selection for Dimensions (parity with Themes).
- Filter-aware dimension aggregates (view-level); chart bar tooltip fix for comma-containing field names.

**Surveys — feature (June 5)**

- Per-prompt clarifier depth dial (Q3/Q4): `q3ClarifyDepth`/`q4ClarifyDepth` in StudyConfig; engine re-probes up to the configured depth, gated by session-wide `maxClarifierCount`. Backward-compatible (absent → 1).

**Agents/Bots — incremental (June 3–5)**

- Manual entity CRUD on `/bots/[id]/entities`: POST (create/rescue hidden), DELETE (manual-only, hard-delete). Full CRUD now: Create · Read · Update · Delete; Hide/Unhide for discovered rows.
- Q&A pairs export (`?shape=pairs`) — review-gated (auto-flagged/excluded sessions dropped), one row per Q+A.
- Agent Study server-side PDF (puppeteer-core + @sparticuz/chromium); Activity-bar counts; Datanautix wordmark.
- Agent Study health dot hover tooltip; "needs review" pending-work pill on Town Hall cards.

**SQL migrations applied to prod this week**: sql/101 (favorites recording type), sql/103 (recordings brand_tag + underlying_agent_id), sql/104 (share_verbatim), sql/113 (get_rows_by_filters RPC), sql/120 (live_summary + live_transcript columns).

**SQL migrations written but NOT yet applied**: sql/102 (cross-org recording transfer RPC), sql/111 (theme_dimension_counts RPC), sql/112 (dataset_field_values RPC).

### Risk signals

- **53 commits locally on `main`, never CI-validated.** Includes the full live-capture module, 6 new API routes, new Deepgram WS path, PDF puppeteer route. No typecheck or test CI signal until a push is authorized. Owner has verified tsc clean + 472 tests locally throughout, but no CI confirmation.
- **sql/111 + sql/112 not applied to prod.** Dimensions theme rows and aliased-field rating rollup silently degrade (empty results, no error) until applied. Code already deployed references these RPCs.
- **sql/102 not applied to prod.** Cross-org recording transfer returns 500 until the RPC lands.
- **Live capture not end-to-end browser-tested with a real mic.** Multiple devlog entries note "needs an in-browser mic run" for waveform, AGC, captions render, and crash-recovery paths. Captions WS auth was verified server-side; the browser subprotocol path is unconfirmed in a real browser.
- **Agent Study prod PDF path (@sparticuz/chromium on Vercel) not verified.** Local path works (macOS Chrome); Vercel Linux path may need a function memory bump. Noted in devlog.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 1.35 |
| Security | 7/10 | 15% | 1.05 |
| Dependencies | 7/10 | 15% | 1.05 |
| Structure | 8/10 | 10% | 0.80 |
| Tests | 5/10 | 15% | 0.75 |
| Documentation | 9/10 | 10% | 0.90 |
| Maintainability | 9/10 | 20% | 1.80 |
| **Total** | | **100%** | **77.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- None confirmed.

**MEDIUM**

- **Bare service-role lookups in `app/api/bots/[id]/route.ts:70,94,96,114,156,158`**: `service.from('agents')...eq('id', params.id)` without a paired `.eq('org_id', ...)` in those specific query chains. The PATCH/DELETE handlers for the bot resource itself. Downstream of an auth check, but a guessable UUID still reaches a service-role write. The history/page.tsx fix this week showed the pattern; the API route handler needs the same pairing.
- **`app/bots/[id]/entities/page.tsx` and `app/bots/[id]/questions/page.tsx` bare lookups**: W22 named all three page-level lookups. History page was fixed this week; entities/questions status unconfirmed.
- **sql/111 + sql/112 not applied to prod**: Dimensions theme-card rows and aliased-rating rollup are code-complete and spec'd but silently return empty until the RPCs land. References in `app/api/datasets/[datasetId]/theme-counts/route.ts` and `lib/taxonomyRollup.ts`.
- **sql/102 (cross-org transfer RPC) not applied to prod**: Transfer path 500s. Admin-only feature, not a security gap, but broken until applied.
- **5 moderate npm CVEs**: `npm audit` shows `{moderate: 5, high: 0, critical: 0}`. Traces to puppeteer-core transitive deps added for the PDF route. No high/critical.

**LOW / informational**

- Test ratio 0.061 (48 test files / 790 source files) — improved from 0.041 in W22 but still well below the 0.5 target. Route handlers and React components remain largely untested.
- `lib/meridian.ts` — `PUBLIC_MERIDIAN_TOKEN` JWT hardcoded (labeled PUBLIC; same risk class as the GOAA key). No change from W22.
- Live capture path not end-to-end browser-tested (waveform, captions, crash-recovery).
- Agent Study PDF Vercel path unverified.
- No code coverage report generated (no `coverage/` directory); vitest config present but `--coverage` not in CI script.

### Progression plan (top 3 to fix this week)

1. **Apply `sql/111`, `sql/112`, `sql/102` to prod** — three pending migrations, owner-authorized. sql/111 and sql/112 unblock the Dimensions feature (theme-card rows + aliased-rating rollup are code-shipped but silently degrade). sql/102 unblocks cross-org recording transfer. Command: `supabase db query --linked --file sql/NNN_name.sql` for each.
2. **Fix entities/page.tsx:30 and questions/page.tsx:30 bare service-role agent lookups** — two-line fix each: apply the same `!isAdmin && userData?.org_id ? agentQuery.eq('org_id', userData.org_id) : agentQuery` pattern introduced in history/page.tsx this week. Closes the W22 MEDIUM that was partially addressed.
3. **Authorize push and run CI** — 53 local commits (live-capture module, PDF route, 6 new API routes) with no CI validation is the highest concentration of unvalidated output to date. typecheck + test pass locally (owner-verified), but CI on the branch would surface any import/config issues before the next production deploy.

## 3. Correlation: dev work → audit findings

- **[W22-MEDIUM] 8 HIGH npm CVEs** — **fully addressed**. npm audit now shows 0 HIGH, 5 MODERATE. W23 delivered no additional packages that introduced new high CVEs.
- **[W22-MEDIUM] Recordings pre-flight blockers** — **partially addressed**. DEEPGRAM_GRANT_KEY is set and live WS auth verified end-to-end (piece-1 blocker closed). sql/120 applied (live_summary/live_transcript). sql/090/sql/091 from W22 are no longer mentioned in blockers (implied applied); Vercel Sandbox provisioning status unclear. Live capture code-complete; in-browser real-mic path still unconfirmed.
- **[W22-MEDIUM] Bare bot page lookups (history/entities/questions)** — **partially addressed**. `app/bots/[id]/history/page.tsx` fixed with org_id pairing for non-admins + cross-org redirect. entities/page.tsx and questions/page.tsx not confirmed.
- **[W22-LOW] Test ratio declining** — **partially addressed**. Ratio improved 0.041→0.061; 19 new test files. Rate of test addition outpaced source growth this week (first time in 3 weeks).
- **[W22-LOW] .gitignore missing *.pem, *.key, *.p12** — **fully addressed**. All three patterns now confirmed present in .gitignore.

## 4. Trend

Previous report (W22): **71.0 / 100** (generated 2026-06-01)

This report (W23): **77.0 / 100** — **+6.0 points**

The gain is the largest week-over-week improvement since the baseline. Three categories drove it: Dependencies (5→7, +0.30 weighted) as the W22 CVE regression was reversed (8 HIGH → 0 HIGH); Secrets (8→9, +0.15) with .gitignore gaps closed; Tests (4→5, +0.15) with 19 new test files and the first week where test growth rate exceeded source growth. Maintainability and Documentation held at 9; Security held at 7. The primary remaining drags are Tests (0.061 ratio, 5/10, 15% weight) and the three pending SQL migrations blocking feature correctness.

## 5. Human review checklist

- [ ] Skim development summary — live capture shipped (pieces 1–5 + hardening); in-browser real-mic test still pending. Acceptable to ship?
- [ ] Review sql/111 + sql/112 + sql/102 — three pending migrations. Authorize application to prod (supabase CLI, linked)?
- [ ] Bare bot API route lookups (`app/api/bots/[id]/route.ts:70,94,96`) — accept current state or schedule fix alongside entities/questions pages?
- [ ] Agent Study PDF Vercel path — should this be verified with a deploy before the feature is user-facing? (Noted risk: may need memory bump.)
- [ ] 53 commits on local main — when to push/release? CI pass is the next gate.
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine. Requires human review before being recorded.*
