# Weekly Governance Report — 2026-W36 (Week of Mon 25 Aug to Sun 30 Aug)

**Generated**: 2026-08-31T22:27:05Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: git log fallback (no 2026-W36-devlog.md — zero development commits this week)
**Spec-drift**: 0 drifted — see 2026-W36-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 0 development commits, 0 authors. Top-level dirs: none touched. The sole commit in range (`0919565`, 2026-08-24) is the W35 governance report itself (docs-only, Claude Governance automation).

### Narrative (from devlog)

No development activity in W36. The week is a clean holding period following the W35 refactor-and-burn-down sprint (useSurveyEngine, lint ratchet, Outlet PDF, entity-count reliability). All pending W35 progression items carry forward.

### Risk signals

- **W35 progression items not yet addressed**: three items from last week remain open — (1) `docs/MCO_AGENT.md` spec drift closure, (2) outlet-reporting grandfather backfill → numbered `sql/` migration, (3) broader survey-payload end-to-end test coverage. None were touched this week.
- **No activity for 13 days** (last dev commit: 2026-08-18). Not inherently risky, but if the pause extends beyond two weeks, consider a dependency sweep before the next dev sprint — npm packages age and CVE surface grows silently.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 13.5 |
| Security | 8/10 | 15% | 12.0 |
| Dependencies | 8/10 | 15% | 12.0 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 7/10 | 15% | 10.5 |
| Documentation | 8/10 | 10% | 8.0 |
| Maintainability | 8/10 | 20% | 16.0 |
| **Total** | | **100%** | **80.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**

- **`image-size` 2 high CVEs (via `pptxgenjs`)** — `npm audit` reports 2 high (ICNS/HEIF/JXL infinite-loop DoS parsers, `GHSA-w3rx-r6r6-pgpr`, `GHSA-5p2g-fcmc-qvqq`). Carried from W34/W35: `getSizeFromImage` is in a commented-out block in pptxgenjs; `image-size` appears 0 times in the four shipped bundles. Documented phantom dependency. No upstream fix without downgrading pptxgenjs 4.0.1 → 1.1.5 (breaks all deck exports). Reconfirm quarterly.
- **Next.js CVE range (still unresolved)** — `GHSA-6gpp-xcg3-4w24` (proxy/middleware bypass) covers 14.3.0-canary.0 through 16.3.0-preview.7; installed 16.2.12 is inside the range. Directly relevant to `proxy.ts`. No stable Next.js 16.3.0 release yet. Monitor the Next.js release channel; upgrade is the only path.

**MEDIUM**

- **`docs/MCO_AGENT.md` spec drift (carry-forward from W35)** — commit `8767a91` (Aug 18) converted MCO logo `<img>` to `next/image` without updating the spec. Not a security or correctness issue; one commit closes it. No W36 activity touched this.
- **Outlet-reporting grandfather backfill not in `sql/` ledger** — the backfill applied in `cd84ded` (Aug 18) is not recorded as a numbered migration; `schema_migrations` and `docs/db/schema.sql` don't reflect it. The audit trail is partially reconstructable from git but not from the ledger. Carry-forward from W35.

**LOW / informational**

- Test-to-source ratio: 187 test files / 1,052 source files = **0.18** (unchanged from W35 effective baseline; minor counting variance from find path differences). Well below the 0.5 target; held at 7/10 pending deliberate coverage push.
- Lint ceiling: 172 max-warnings. No ratchet movement (0 commits). `no-explicit-any` holds at 0 errors. Remaining warnings are `react-hooks/*`, behavior-sensitive, unchanged.
- TypeScript strict mode on; no typecheck regressions (no changes landed).
- No `.env` files committed; `.gitignore` covers `.env`, `.env*.local`, `*.pem`, `*.key`. No hardcoded API-key patterns found.
- No SQL migrations added this week. `sql/191` (stratified sampling) and `sql/192` (backup ledger) remain queued pending owner decision.

### Progression plan (top 3 to fix this week)

1. **Close `docs/MCO_AGENT.md` drift** — edit the spec to note the `next/image` rendering choice for MCO logos (or tighten `scripts/specMap.ts` if the mapping over-includes). One commit, ~15 min. Now two weeks overdue.
2. **Record the outlet-reporting backfill in `sql/`** as a numbered migration so `schema_migrations` and `docs/db/schema.sql` reflect the grant-widening. If it was code-only, document the targeted org list in a devlog entry. One commit.
3. **Pre-sprint dependency sweep** before the next dev sprint: run `npm audit` and `npm outdated`, and consider upgrading Next.js to 16.3.0 stable once it ships to clear the active CVE range. Low effort to check; high value for the security posture.

## 3. Correlation: dev work → audit findings

No development commits in W36 — no new work to correlate. Carrying W35 open items:

- **`image-size` phantom CVEs** — *not addressed* (no actionable path; carry-forward documented).
- **Next.js CVE range** — *not addressed* (no stable fix available; monitoring posture unchanged).
- **`docs/MCO_AGENT.md` spec drift** — *not addressed*. Now two weeks open (W35 + W36). Progression item #1.
- **Outlet backfill → numbered migration** — *not addressed*. Two weeks open. Progression item #2.
- **Survey-payload coverage broadening** — *not addressed*. Carry-forward from W35 progression #3.

## 4. Trend

- W33: 86.0 / 100 (baseline).
- W34: 81.0 / 100 (−5.0).
- W35: 80.0 / 100 (−1.0).
- W36 (this report): **80.0 / 100 (0.0)**. Score is flat — no code changes, no regressions, no improvements. All open findings carry unchanged from W35.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected about the zero-activity week?
- [ ] Review CRITICAL/HIGH audit findings — Next.js CVE still unresolved; any timeline on 16.3.0?
- [ ] Review the spec-drift companion (2026-W36-spec-drift.md) — 0 drifted this week; `docs/MCO_AGENT.md` drift from W35 still open
- [ ] Check correlation section — W35 progression items are now two weeks old; assign or defer each
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
