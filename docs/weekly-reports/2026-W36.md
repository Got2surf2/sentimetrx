# Weekly Governance Report — 2026-W36 (Week of Aug 24 to Aug 30)

**Generated**: 2026-08-31T08:07:39Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W35-devlog.md (covers Aug 24–30 commits; no W36 devlog file)
**Spec-drift**: 0 drifted — see 2026-W36-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 16 commits, 1 author (Sanjay Patel), top-level dirs: docs/ (20 touches), lib/ (4), app/analyze/ (6), app/api/ (2), sql/ (3), tests/unit/ (5), scripts/ (3), components/ (1), .github/workflows/ (2)

### Narrative (from devlog)

**Upload reliability overhaul (core focus):**
- Fixed the "dropped socket deletes entire dataset" bug — `lib/retryTransient.ts` now distinguishes transport failures (retry) from application errors (never); the supabase-js call site was wrapping the wrong interface (returns `{data,error}`, not throws)
- Upload batch stride corrected 50→200 rows/POST, cutting ANES round trips from 2,518 to 630
- Upload progress modal rewritten — percentage, measured ETA (withheld until 3 batches), non-dismissable by design; StrictMode `mountedRef` bug fixed (effect must set ref `true` in body, not just clear it in cleanup)
- Upload page jank eliminated — O(rows) render-body computation was executing 4,000× during upload, blocking main thread for ~312s at 100K rows

**ANES 1984–2024 production rescue:**
- Server-side loader (`scripts/oneoff/_load_anes_dataset.ts`) rescued a 66,850/125,897 row truncated upload (browser tab closed after ~3.2h); full 125,897 rows in 4m39s with RFC4180 splitter
- `parseCSV` interior-quote-dropping bug identified but NOT yet fixed: 2,796/125,897 rows are mangled in prod ANES data; verbatim quote marks stripped silently
- Year field promoted to Single Select; 2008 file deliberately not loaded (would conflate `Top Problem` with `Top Problem - Political`)

**Performance — filters modal 41.5s → instant:**
- `sql/194`: cells CTE rewritten from per-field jsonb parse to single `jsonb_each_text` pass; route caches computed options in `dataset_state.filter_options` with fingerprint; `DatasetShell` prefetches on mount
- Applied to prod via Supabase Management API (direct DB host is IPv6-only; pooler-only workaround documented in DATABASE.md)
- `scripts/apply-migration.ts` updated to honour `PROD_DB_URL`/`SUPABASE_DB_URL`

**Statistics / export:**
- Driver Simulator JSON export from both regression panels (`lib/simulatorExport.ts`); 11 new unit tests; verified log-odds, vcov, sigma, dummy sd, separation guard

**CI / infra fixes:**
- Added `workflow_dispatch` to `ci.yml` — a push landed with 0 CI runs (GitHub quirk); production silently did not deploy; `workflow_dispatch` enables manual recovery
- `sql/193` outlet-reporting grandfather migration added — prior state-change had been script-only, now in ledger and idempotent

**W35 audit follow-up:**
- Closed both open W34 audit items: MCO_AGENT.md spec drift corrected, numbered migration for outlet backfill added
- Tests metric rewritten in audit-codebase.md: exclude build dirs, count cases not files, scope denominator to vitest `include`

### Risk signals

- **parseCSV interior-quote-dropping bug is live in production** — affects all uploads with quoted verbatim fields; ANES prod data has 2,796 mangled rows. No ticket, no ETA.
- **Upload resume not implemented** — a dataset stranded mid-load shows "active" with empty schema; no user-visible signal. Flagged as follow-up in devlog.
- **`sql/191` (stratified sampling) and `sql/192` (backup ledger) remain unapplied to prod** — both were MEDIUM findings in W34; not mentioned in W36 devlog.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 13.5 |
| Security | 8/10 | 15% | 12.0 |
| Dependencies | 8/10 | 15% | 12.0 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 7/10 | 15% | 10.5 |
| Documentation | 9/10 | 10% | 9.0 |
| Maintainability | 9/10 | 20% | 18.0 |
| **Total** | | **100%** | **83.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**

- **Next.js advisory range (GHSA-6gpp-xcg3-4w24 et al.)** — installed 16.2.12 is inside `14.3.0-canary.0 – 16.3.0-preview.7`. Proxy/middleware CSRF bypass is directly relevant to `proxy.ts`. No released stable fix yet; `16.3.0` stable is the only exit. Carried from W34.
- **image-size 2 high CVEs via pptxgenjs** — `getSizeFromImage` appears in a commented-out block only; the vulnerable parsers (ICNS/JXL/HEIF) cannot execute in the shipped bundle. Documented/known; no actionable fix without downgrading pptxgenjs. Carried from W34.

**MEDIUM**

- **parseCSV interior-quote-dropping bug (live in production)** — `app/analyze/new/UploadClient.tsx`: parser toggles on every `"` and never emits one, mangling fields that contain interior quote marks. Confirmed 2,796/125,897 ANES rows affected. Any upload with quoted verbatim text is silently corrupted. Not fixed this week.
- **Upload resume absent** — a dataset truncated mid-upload shows status "active" with `schema_config: {fields:[]}`. `canMine` is false, Mine button greyed, schema tab blank — three-symptom presentation of one root cause. No recovery path without manual operator intervention.
- **Entity count error-swallowing** — `lib/entityFilter.ts:computeEntityCounts` catches a statement timeout and returns an empty map. Caller cannot distinguish "zero entities" from "timed out." Empty is indistinguishable from a real zero; no degraded-result flag. Carried from W34.
- **sql/191 (stratified sampling) not applied to prod** — 3.9× perf gain at 1M rows proven, correctness verified. Blocked on owner timing sign-off since W34.
- **sql/192 (backup ledger) not applied to prod** — `org_snapshot_runs` table adds durable per-org backup auditability. Without it, silent backup failures recur. Blocked since W34.

**LOW / informational**

- **ESLint ceiling at 172** — `lint:ci` allows up to 172 warnings; actual count likely lower after the July burndown but ceiling has not been tightened to match. Run `npm run lint:ci` after touching any file and lower the ceiling if actual < 172.
- **No agent definitions or skills in `.claude/agents/`** — 4 custom commands, 7 hook configs, strong CLAUDE.md; would score 10/10 on AI Patterns with ≥1 agent definition.
- **Route auth coverage is 67%** — 400 route handlers, 268 have `requireAdmin`/`requireAuth`/`getCallerOrgContext`/`getSession` references. The 132 without may be intentional (public endpoints), but no systematic audit of unauthenticated routes was done this week.

### Progression plan (top 3 to fix this week)

1. **Fix parseCSV quote-dropping bug** — `app/analyze/new/UploadClient.tsx`: replace the toggle parser with RFC4180-compliant splitting (already proven in the server-side loader). Protects all future uploads; current prod ANES data needs a re-run of `_load_anes_dataset.ts` after the fix.
2. **Raise the enforced test coverage floor** — run `npm run coverage` locally to capture actual stmt/branch/func/line percentages; set each floor in `vitest.config.ts` within 2pp of actual (currently 30/23/33/30, likely much higher post-W36 additions). A floor that mirrors real coverage actually gates regressions.
3. **Apply sql/191 and sql/192 to prod** — both were MEDIUM findings as of W34. The stratified-sampling rewrite (sql/191) unlocks the 1M-row performance target; the backup ledger (sql/192) closes a silent-failure class that bit in August.

## 3. Correlation: dev work → audit findings

W34 CRITICAL/HIGH findings:

- **Next.js advisory range** — not mentioned in W36 devlog → **not addressed**
- **image-size CVEs via pptxgenjs** — not mentioned in W36 devlog → **not addressed** (documented/known; acceptable carry)

W34 MEDIUM findings:

- **Entity count error-swallowing** — not mentioned in W36 devlog → **not addressed**
- **Statistics findings / nav pill Lottie QC deferred** — not mentioned in W36 devlog → **status unclear**
- **sql/191 stratified sampling not applied** — not mentioned in W36 devlog → **not addressed**
- **sql/192 backup ledger not applied** — not mentioned in W36 devlog → **not addressed**

New W36 finding:

- **parseCSV quote-dropping bug** — identified and explicitly flagged as "not yet fixed" in W35 devlog (2026-08-27) → **partially addressed** (identified, rescue script uses RFC4180, fix not shipped)

## 4. Trend

Most recent prior governance report: **docs/weekly-reports/2026-W34.md** (W35 governance report was not produced; only the devlog exists).

W34 total: **81.0 / 100**

| Category | W34 | W36 | Δ |
|---|---|---|---|
| Secrets | 9/10 | 9/10 | 0 |
| Security | 8/10 | 8/10 | 0 |
| Dependencies | 8/10 | 8/10 | 0 |
| Structure | 8/10 | 8/10 | 0 |
| Tests | 7/10 | 7/10 | 0 |
| Documentation | 9/10 | 9/10 | 0 |
| Maintainability | 8/10 | 9/10 | **+1** |
| **Total** | **81.0** | **83.0** | **+2.0** |

W36 total: **83.0 / 100** (+2.0 from W34). Maintainability gain reflects the CLAUDE.md hook-configuration depth (7 hooks, 4 commands) scoring higher on re-measurement with the corrected rubric. All other categories unchanged.

Note: W35 governance report was not generated; if it had been, the trend would show a continuous series. The W35 devlog is present and covers Aug 24 work; the audit itself did not run that week.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Review the spec-drift companion (2026-W36-spec-drift.md) — any spec debt?
- [ ] Check correlation section — are open findings being worked on?
- [ ] Confirm status of sql/191 and sql/192 prod applies
- [ ] Confirm parseCSV fix is prioritised this week
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
