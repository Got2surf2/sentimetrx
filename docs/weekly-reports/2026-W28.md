# Weekly Governance Report — 2026-W28 (Week of Jul 6 to Jul 12)

**Generated**: 2026-07-06T08:04:54Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W28-devlog.md
**Spec-drift**: 3 drifted — see 2026-W28-spec-drift.md in this PR

## 1. Development Summary

**Quantitative**: 49 commits (7-day window Jun 29–Jul 6), 1 author (Sanjay Patel). Top-level dirs touched: app/, lib/, sql/, components/, scripts/, tests/, docs/, .claude/. Note: this window overlaps W27 (Jun 29–Jul 5 covered by 2026-W27.md already merged); the W28-devlog below covers the Jul 6 work specifically.

### Narrative (from devlog)

**Deploy-behind-CI: gate proven in both directions (Jul 6)**
- First real push since ENGINEERING §12 armed. The 16-commit batch auto-built NOTHING on push (deploymentEnabled main:false working); CI run 1 failed the `check:sql-tx` guard on sql/158 (missing BEGIN/COMMIT), correctly blocking deploy; fix applied and re-pushed; CI run 2 green in 4m02s → exactly one production build of the right sha.
- Resolved owner alarm about "all these deployments": confirmed every deployment on the Vercel dashboard matched a GitHub push-event timestamp, zero unauthorized builds.
- Housekeeping: `.claude/scheduled_tasks.lock` untracked (machine-local state accidentally committed); `scripts/_*.json` added to gitignore.

**Emotion-language flags: disappointment/blame/churn as the embed's 8th axis (Jul 6)**
- `lib/emotionFlags.ts` ports validated probe lexicons with negation guard on disappointment and regret kept dark (structurally excluded from the closed-vocab extractor AXES; LLM tier required for regret).
- Architecture decision: 8th axis inside `data._tx` rather than a sidecar key — every existing surface (Dimensions tab, TextMine, Charts/Stats, crosstabs) inherits the feature without additional wiring.
- Rollup grows an optional `emotion` block omitted at zero (genre gate — no "0% emotion" on ideation prompts). Operational Review deck gets a `kpi_grid` tile for co-occurrence.
- sql/158 widens five taxonomy RPC axis guards + adds `p_sub_emotion`; deploy-order safe (client omits param until migration applied). TAXONOMY_VERSION v3→v4. 1,208 unit tests pass (12 new lexicon cases).

**LLM-tier prototype: regret validation + cost model (Jul 6)**
- 300-review gold sample via the dormant `lib/taxonomyExtractor`; LLM recalls 90–100% of keyword hits and finds 2–5× more. Regret ~75–80% precise at LLM tier vs ~50–60% at keyword tier.
- Cost: $0.0047/review → RC full ≈$97 live / ≈$48 Batch API. EA decision: membership vertical runs keyword-tier always-on with a non-renewal overlay; LLM as per-wave pass.
- Research only (no wiring to prod). Findings in TAXONOMY.md §2a.1–2a.2.

**DD efficiency audit + infrastructure hardening (Jun 29–Jul 5, in 7-day window)**
- Multi-agent efficiency audit (94 agents, 33 confirmed findings fixed-or-documented); CAPACITY.md created with live-measured provider envelopes; k6 load suite ×4.
- ARCHITECTURE.md D15–D19: five implicit decisions surfaced and documented.
- Legacy PulseIQ substrate deleted; /pulseiq URL retired (tranche-2 finale, sql/153).
- Restore fidelity: dependency order + FK retry, debt-row bisection, self-verifying restore; S3 DR re-drill PASSED; multipart abort permission + 7d lifecycle backstop (BACKUPS.md TBD cleared).
- Backup coverage: recordings module + 10 metadata tables were unbacked, now covered.
- Snapshot v2: streamed, uncapped backups (5 latent spec bugs found live).
- Ceiling-fix blitz: 6 CAPACITY fix-larger packages (sql/155–157).
- Response counters restored O(1) (sql/154); runaway-topic controls (bounded engine pool, pending cap).
- Admin-org feature auto-grant: sweep of 17 gate sites for owner-found bug.
- Taxonomy embed: verdicts move into the row blob (sql/151; sidecar retired).
- Dev-mode split: `npm run dev` → TEST project; `npm run dev:prod` → prod + red banner.
- Public privacy notice + trigger-mapped compliance checklist (COMPLIANCE.md).

### Risk signals
- 3 drifted specs (SEARCH, SOCIAL, CAMPAIGNS) — `SKIP_SPEC_CHECK` was used on commits that touched those surfaces without updating the specs. Regression from 0 drift in W27.
- sql/158 applied to prod (schema snapshot refreshed) but the five RPC axis guards make this a prod-schema-dependent deploy. The pre-condition (sql/158 applied) was met before the code landed.
- LLM-tier prototype untracked results file (`scripts/_llm-tier-prototype-results.json`) is gitignored by design; owner spot-check is the ship gate before wiring. No functional risk, but the decision evidence lives only locally.
- SheetJS CDN install failure (403 on clean checkout) continues to block `npm install` in remote/CI environments — this run confirmed it.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 13.5 |
| Security | 7/10 | 15% | 10.5 |
| Dependencies | 7/10 | 15% | 10.5 |
| Structure | 8/10 | 10% | 8.0 |
| Tests | 7/10 | 15% | 10.5 |
| Documentation | 8/10 | 10% | 8.0 |
| Maintainability | 9/10 | 20% | 18.0 |
| **Total** | | **100%** | **79.0 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- None. 0 critical / 0 high CVEs. No hardcoded secrets detected. No eval/exec with user input. RLS enabled on 69 tables; `question_batches` has RLS + org-scoped SELECT policy (sql/142).

**MEDIUM**
- `lib/chatCore.ts:1408,1561,1655` — three `service.from(...).update({...}).eq('id', x)` without `org_id` pairing, violating the CLAUDE.md multi-tenancy invariant. Context-aware (rows derived from prior org-scoped queries), but the invariant exists to prevent class-level bugs. Same pattern at `lib/collectionRecompute.ts:81` and `lib/orgTransfer.ts:59` (organizations select by id only — low risk, no user data). The DD audit fixed 33 other findings; these 5 were not in scope.
- SheetJS sourced from `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` — `npm install` returns 403 Forbidden in clean remote checkouts. Blocked this governance run's `npm install`. Supply-chain reliance on a proprietary CDN. (Flagged W27, unresolved.)
- 14 moderate npm CVEs: `@babel/core` arbitrary file read (fixable with `npm audit fix`); `@opentelemetry` chain through `@sentry/nextjs` (requires breaking `@sentry/nextjs` version bump to fix — needs evaluation).
- 3 drifted specs: `docs/SEARCH.md` (taxonomy embed changed SearchPanel.tsx), `docs/SOCIAL.md` (admin feature auto-grant + DD audit hit social-sync and auto-config routes), `docs/CAMPAIGNS.md` (same two commits hit campaign-scheduler and campaigns page). Top-level `SPEC.md` and `FEATURES.md` flagged as likely also needing a sweep.

**LOW / informational**
- Test ratio 0.20 (108 test files / 539 source files) — below the 0.5 target for 7+ score tier. 1,208 tests pass across unit/integration; e2e remains 0 tests in suite.
- lint ceiling at 3,062 warnings (same as W27) — the ratchet prevents regression but progress toward 0 is stalled. No warnings moved to `error` this week.
- `vitest` not available on clean checkout (npm install fails) — test suite cannot run locally in this environment until SheetJS is resolved. CI has a cached node_modules so tests still run there.
- 0 `.claude/agents`, 0 `.claude/rules` defined — governance relies on CLAUDE.md + hooks + commands only. Consistent with W27; not a regression.
- Google Fonts dependency in PDF deck routes: no bundled fallback; render fails silently if fonts unreachable. Documented, unresolved from W27.

### Progression plan (top 3 to fix this week)

1. **Fix the 3 drifted specs** — update `docs/SEARCH.md` (taxonomy verdict-in-blob change), `docs/SOCIAL.md` (admin feature auto-grant behavior + DD audit findings), `docs/CAMPAIGNS.md` (scheduler and campaigns page changes). Sweep `SPEC.md` and `FEATURES.md` for any downstream references. Estimated: 30–45 min.
2. **Resolve SheetJS CDN dependency** — evaluate replacing `xlsx@0.20.3` (SheetJS Pro CDN) with a public npm alternative (`exceljs` or public SheetJS community edition) or document the credential requirement in `.npmrc.example`. This unblocks `npm install` in all remote environments including this governance runner. Estimated: 1–2 hr (depends on surface area of xlsx API usage).
3. **Fix `@babel/core` CVE with `npm audit fix`** — the fix is non-breaking and available. Run `npm audit fix` to resolve the arbitrary file read. The `@sentry/nextjs` chain (12 of the 14 moderate CVEs) requires a separate evaluation of the breaking Sentry upgrade. Estimated: 15 min for babel, 1–2 hr for Sentry evaluation.

## 3. Correlation: dev work → audit findings

- **Security (bare-id service-role patterns)**: DD efficiency audit (bf90a85) fixed 33 verified findings from a 94-agent review, but these 5 bare-id patterns were not in scope — **not addressed in devlog**.
- **Dependencies (SheetJS CDN / moderate CVEs)**: no commits touched `package.json` dependency versions this week — **not addressed in devlog**.
- **Documentation (3 drifted specs)**: the commits that caused drift (11ef822, bf90a85, 2f222eb) do not include spec updates for SEARCH/SOCIAL/CAMPAIGNS — **not mentioned in devlog**. The drift guard was bypassed (`SKIP_SPEC_CHECK`) on these commits.
- **Maintainability (deploy-behind-CI)**: fully addressed this week — the deploy-behind-CI exercise (781cf58) proved the gate in both directions, a significant infrastructure milestone — **directly addressed in W28 devlog**.
- **Tests (1,208 tests, 12 new emotion lexicon cases)**: incremental improvement — **addressed in devlog** (emotion flags commit added unit test coverage).

## 4. Trend

Prior week: **83.0 / 100** (2026-W27, 0 drifted specs)
This week: **79.0 / 100** (2026-W28, 3 drifted specs)
**Delta: −4.0**

The drop is driven by three categories: Security (8→7, bare-id patterns persist post-DD-audit), Dependencies (8→7, SheetJS CDN unresolved, @babel/core fixable CVE still open), and Documentation (9→8, 3 drifted specs vs 0 last week). Maintainability held at 9 (deploy-behind-CI is a meaningful infrastructure gain). The core product work this week is high-quality — 33 verified findings fixed, emotion axis shipped with solid test coverage, LLM-tier prototype validated. The score dip is concentrated in hygiene/housekeeping gaps, not architectural issues.

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review CRITICAL/HIGH audit findings — any blockers?
- [ ] Review the spec-drift companion (2026-W28-spec-drift.md) — any spec debt?
- [ ] Check correlation section — are open findings being worked on?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine (spec-drift + governance combined). Requires human review before being recorded.*
