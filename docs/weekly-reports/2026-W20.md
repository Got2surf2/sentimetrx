# Weekly Governance Report — 2026-W20 (Week of May 11 to May 17)

**Generated**: 2026-05-18T08:18:26Z
**Repository**: Got2surf2/sentimetrx
**Status**: Pending human review (merge this PR to record the review)
**Dev log source**: docs/weekly-reports/2026-W20-devlog.md

## 1. Development Summary

**Quantitative**: 49 commits, 1 author (Sanjay Patel), top-level dirs: app/ (390 changes), components/ (106), lib/ (85), sql/ (73), docs/ (66), scripts/ (39), tests/ (21), .claude/ (8)

### Narrative (from devlog)

**Security follow-through from W19 audit (Mon–Tue)**
- Completed the W19 bare-id sweep: 154 static candidates triaged, 65 already-gated or intentional (admin/cron), 2 real bugs fixed (`/api/datasets/[datasetId]/sync` and `/api/regulations-sources/download-comments` both lacked org_id checks). New `tests/integration/dataset-routes-egress.test.ts` locks the fixes.
- All 5 W19 HIGH admin routes now have `requireAdmin` — confirmed in code. `/api/org/users` and `/api/questions/library` split out so every remaining `/api/admin/*` path is uniformly admin-gated. Combined with the bare-id sweep, Security 5/10 → 7/10 is now well-evidenced.
- Three-state AI mode (`off/platform/byo`) + multi-provider BYOK — org gate is now unconditional in `callAI`, closing a bypass where export routes passed `apiKey` directly. `lib/embeddings.ts` and `lib/moderation.ts` now require `orgId` so AI-off orgs can't leak embeddings/moderation calls.
- Meta-prompt leak in bot deflection (`lib/guardrails.ts`) patched with expanded regex + 11 unit tests locking the fix; `sanitizeBotReply` added as defense-in-depth on the main bot reply path.
- `admin_action_log` table added (`sql/048`) and wired into cross-org sync/trim operations for SOC 2 audit trail.
- `*.pem`, `*.key`, `*.p12`, `*.pfx` added to `.gitignore` (W19 LOW finding closed).

**Entity extraction rebuild — major feature (Tue–Thu)**
- Full rewrite of entity discovery: `lib/entityDiscovery.ts` (scope resolution, Haiku NER, catalog upsert), `lib/entityFilter.ts` (scope-aware counts via FTS), migrations 060–070 applied to prod.
- Phase 5: app-code rewrite (discovery, catalog, scope-resolving routes). Phase 6: automatic incremental discovery triggered from compute route + weekly cron (`/api/cron/entity-discovery`). Entity field-selection fix to stop structured location columns polluting NER. Context-aware NER prompt to avoid brand/city noise. Open-ended-only counting fix via migration 070 (`count_entity_terms` recheck against text fields — verified "Tampa" 780→46).
- Brand cards: distinct UI treatment on /analyze, member drill-in, ≥2-member lifecycle. Merged schema for brand-collections. Brand-tag autocomplete on 5 of 6 dataset-create forms.
- Canonicalisation pass: `canonicaliseDiscovered` merges cross-batch near-duplicates under a canonical.
- EntitiesCard: category tabs, 30-pill cap, comment modal with inline entity highlights, plural/singular variant matching.

**Tripadvisor reviews + review download accounting (Wed–Thu)**
- Added Tripadvisor as a second review source alongside Google (`sql/065`). Single dispatcher pattern in `lib/reviewSync.ts`.
- Review-download accounting: `review_downloads` ledger + `organizations.limits.review_records_monthly` cap (`sql/067`). Admin UI for cap management + live usage readout. Monthly-cap logic gates Phase 2/3 task submission.
- Brand-match scoring: `lib/brandMatch.ts` — deterministic token similarity + chain consensus, zero API cost, scores 0–100. Wizard pre-selects only `'strong'` matches; `'weak'` rows get amber badge. 7 unit tests.

**AI completions (GhostTextarea) + misc UX (Tue)**
- `GhostTextarea` component: mirror-div ghost text, 500ms debounce, in-memory cache, Tab-to-accept, hidden on coarse-pointer. Surfaces: Export modal instructions, bot system prompt, personality, deflection message.
- Gen-Z slang terms added to AFINN-165 via `extras` option (6 unit tests).
- Resend invite: per-row inline status replacing off-screen flash; `PendingInvitesList` shared component.
- Survey `completed_at` always stamped on every save; partial-save proxying fixed.
- DataForSEO cost correction; all review_sources set to manual refresh.
- Invite flow: auto-login after accept, warm theme, email field locked, org name visible, resend + revoke buttons, owner-can-invite path fixed.

**Spec audit — 14-doc pass (Thu–Fri)**
- 14 of 14 docs audited and corrected in one day: SECURITY, TOWNHALL, BOTS, SURVEYS, CAMPAIGNS, ANALYTICS, SOCIAL, SEARCH, DATA_SOURCES, USAGE_ACCOUNTING, ENGINEERING, TESTING, FEATURES, SPEC.md.
- Notable real-eng findings surfaced: `send_at` phantom field in campaigns (UI silently discards it), PPTX decks still brand as "Datanautix", `auto_delete_enabled` dead config in social cron, PulseIQ simulator `switch_language: 'ht'` targets an unsupported lang code.
- Buyer DD bundle: SECURITY_OVERVIEW + DATA_FLOW + CAIQ_LITE added.

**Governance infrastructure (Mon)**
- In-app governance trend page (`/admin/governance`) — SVG chart + per-category bars + report list, reads `docs/weekly-reports/*.md` at request time.
- Admin Hub (`/admin/hub`) — tabbed cards replacing 11-item gear dropdown.
- ESLint rules enabled (`no-floating-promises`, `no-misused-promises`, `no-explicit-any`, `consistent-type-imports`) at warn level; 374 errors + 1801 warnings triaged, all at warn to avoid breaking Vercel builds; promotion to error level queued.
- Engineering TBDs #20–#24 shipped: console.error single-object form, x-request-id stamping, Resend webhook dedup via svix-id ledger, inline button-busy spinner exception, BEGIN/COMMIT enforcement on migrations.

### Risk signals
- **Sentry `beforeSend` scrub still not implemented** — all 3 Sentry configs pass no `beforeSend` hook; PII (email, phone, survey payloads) can leak to the external Sentry service on unhandled exceptions in high-risk routes (`/api/respond`, `/api/campaigns/send`, `/api/b/[slug]`). SECURITY.md §5 mandates this.
- **374 ESLint errors at warn level** (`any` types, floating promises) across 349 files — logged but not enforced; promise-handling bugs could be silently masked.
- **`send_at` campaign field is a phantom** — users can configure a specific-datetime schedule in the UI that is silently discarded; no column in any migration, silently dropped by the API.
- 49 commits in 7 days, single author; changes touch auth, multi-tenancy, AI routing, and all 14 spec docs — human review at each PR merge is the only control.

## 2. Codebase Health Audit

| Category | Score | Weight | Weighted |
|---|---|---|---|
| Secrets | 9/10 | 15% | 1.35 |
| Security | 7/10 | 15% | 1.05 |
| Dependencies | 7/10 | 15% | 1.05 |
| Structure | 7/10 | 10% | 0.70 |
| Tests | 4/10 | 15% | 0.60 |
| Documentation | 9/10 | 10% | 0.90 |
| Maintainability | 8/10 | 20% | 1.60 |
| **Total** | | **100%** | **72.5 / 100** |

### Findings (grouped by severity)

**CRITICAL / HIGH**
- None. W19's two HIGH findings (bare-id service-role lookups and admin routes missing `requireAdmin`) are both confirmed closed in code this week. See §3.

**MEDIUM**
- **[MEDIUM] Sentry `beforeSend` scrub not implemented** — `sentry.{client,edge,server}.config.ts` each pass only `dsn`/`integrations`/`tracesSampleRate`/`environment`; no `beforeSend` hook anywhere in the codebase. SECURITY.md §5 mandates dropping email/phone/password/`req.body` on survey and campaigns endpoints. Devlog explicitly calls this out as unresolved. Concrete leak surfaces: unhandled exceptions in `/api/respond/route.ts`, `/api/campaigns/[id]/send/route.ts`, `/api/b/[slug]/route.ts`.
- **[MEDIUM] npm audit: 1 high + 1 moderate CVE unresolved** — same 2 CVEs as W19; root is Next.js 14 transitive chain. Fix requires Next 14→16 upgrade (two-major-version jump). Vercel managed runtime mitigates several self-hosted-only paths, but not all.
- **[MEDIUM] `send_at` phantom field in campaigns** — `CampaignDetailClient.tsx` submits `send_at` from a "datetime" schedule mode; the PATCH allowlist drops it silently and no migration has ever added the column. Users believe they've scheduled a specific-time send; nothing fires. Identified in spec audit; flagged as TBD in `CAMPAIGNS.md`.
- **[MEDIUM] PPTX decks branded "Datanautix"** — every shipped deck still uses "Datanautix" / "datanautix.com" in title-page logo, slide footers, `pptx.author`, `pptx.company`, and "Prepared by Datanautix" line. Per CLAUDE.md user-facing brand is Sentimetrx. Customer-visible artifact. Identified in spec audit; queued as real-eng followup.
- **[MEDIUM] Test ratio 0.041 unchanged** (21 test files / 514 source files) — 4 new test files added this week but source base also grew proportionally. Route handlers and React components remain largely untested.
- **[MEDIUM] 374 ESLint errors at warn level** — `no-explicit-any` (1594 violations), `no-misused-promises` (212), `no-floating-promises` (162), `consistent-type-imports` (123). Demoted to warn to avoid breaking Vercel builds; enforce-later path is queued but not yet scheduled.

**LOW / informational**
- `auto_delete_enabled` is dead config in the social cron — exposed in admin UI and defaults, but `social-sync` cron never reads or acts on it. Identified in spec audit, queued in open-work-list.
- PulseIQ simulator persona `switch_language: 'ht'` targets Haitian Creole which is not in `LANG_CODES`; the persona flag is a no-op. Flagged as product call (add lang or drop flag).
- `execSync` in `lib/deckLastModified.ts:23` — build-time git call, not a runtime risk; appears in automated security scans.
- `tsconfig.tsbuildinfo` committed — build artifact that bloats diffs.
- No `.claude/rules`, `.claude/agents`, or `.claude/skills` defined — next AI governance maturity tier.
- tsc verification: `node_modules` incomplete in this audit container (missing `@types/react`). Devlog confirms "typecheck clean" on multiple commits during the week; this is an environment artifact, not a code regression. CI continues to run `npm run typecheck` on every push.

### Progression plan (top 3 to fix this week)

1. **Add Sentry `beforeSend` scrub** — create `lib/sentryScrub.ts` exporting a `scrubEvent(event)` function that strips email, phone, `req.body`, and auth headers; wire into all three Sentry configs. ~30–60 min. Closes the PII-in-error-reports risk that SECURITY.md §5 mandates. Security finding.
2. **Fix PPTX branding** — replace "Datanautix" with "Sentimetrx" in deck generation (title-page, footer, `pptx.author`, `pptx.company`). Customer-visible artifact, surgical fix. ~30 min. Documentation/branding.
3. **Fix or remove `send_at` phantom campaign field** — either (a) add `send_at` column to `campaign_emails` and plumb it through the scheduler, or (b) remove the "datetime" branch from the campaign UI. Option (b) is faster. Either way stops users from configuring a schedule that silently does nothing. ~1–2h. Product quality.

## 3. Correlation: dev work → audit findings

- **[W19-HIGH] Bare service-role `id`-only lookups** — **fully addressed**: triage completed (154 candidates → 2 real bugs), both bugs fixed, egress tests locking the fixes, W19 finding closed in devlog.
- **[W19-HIGH] 5 admin routes missing `requireAdmin`** — **fully addressed**: requireAdmin confirmed in code on all 5 routes, non-admin reads split to honest paths, devlog explicitly closes the W19 finding.
- **[W19-MEDIUM] npm CVEs (1 high + 1 moderate)** — **not addressed**: devlog acknowledges Next 14→16 upgrade is needed but not attempted this week (two-major-version jump, dedicated session required).
- **[W19-MEDIUM] Test ratio 0.04** — **partially addressed**: 4 new test files added (brandMatch, sentiment-slang, dataset-routes-egress, guardrails expansion); ratio essentially unchanged because source base grew equally.
- **[W19-MEDIUM] tsc vitest errors** — **not confirmed resolved**: audit environment has incomplete node_modules; devlog claims typecheck clean throughout the week.
- **[NEW] Sentry scrub** — **not mentioned for fix**: surfaced in devlog as an open item (Tier-1 P2 audit, 2026-05-12) but no fix committed this week.
- **[NEW] PPTX branding** — **not mentioned for fix**: surfaced in spec audit pass 6 (ANALYTICS), queued in open-work-list.
- **[NEW] `send_at` phantom** — **not mentioned for fix**: surfaced in spec audit pass 5 (CAMPAIGNS), logged as TBD.

## 4. Trend

Previous report (W19): **65.0 / 100** (generated 2026-05-11)

This report (W20): **72.5 / 100** — **+7.5 points**

Score delta driven by:
- Security 5→7 (+2): bare-id sweep completed with zero remaining confirmed leaks, requireAdmin on all admin routes, AI-mode org gate unconditional, meta-prompt scrubber added.
- Secrets 8→9 (+1): `*.pem/*.key/*.p12/*.pfx` now in .gitignore.
- Documentation 8→9 (+1): 14-doc spec audit completed (all major docs corrected), buyer DD bundle added.
- Maintainability 7→8 (+1): pre-commit spec-drift hook, governance trend page, Engineering TBDs #20–24 shipped.

Primary drags holding score below 80: test ratio (4/10, 15% weight) and unresolved CVEs (2 remaining).

## 5. Human review checklist

- [ ] Skim development summary — anything unexpected?
- [ ] Review MEDIUM audit findings — any blockers for the Darden pilot?
- [ ] Check correlation section — Sentry scrub and PPTX branding are open; are they pre-ship blockers?
- [ ] Merge this PR to record the review

---
*Generated automatically by the Weekly Governance routine. Requires human review before being recorded.*
