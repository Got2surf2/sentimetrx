# 2026-W27 — Dev log (Week of Jun 29 to Jul 5)

## 2026-06-29 — PulseIQ round-based pacing — Phase 3: moderator control + engine gate

Made round-based tasting mode actually gate the room live (Phases 1+2 were model layer + creator UI). Three pieces. (1) New `POST /api/townhall/sessions/[id]/round` body `{round: N}` — completes any still-active earlier-round guide themes (round_number < N) and activates round N's themes from their paused seed; org-gated inline through the parent townhall_sessions row (mirrors themes/custom, since legacy townhall_themes has no own org_id). (2) Round-aware standby gate in chat/route.ts: the existing "all topics covered" branch only entered standby when theme_detection_mode==='auto' (else it wrapped up with the closing message) — rounds mode runs detection OFF but still needs the hold between items, so when pacing_mode==='rounds' it now checks for any paused round-tagged guide theme and emits a round-hold standby regardless of detection mode. Open mode behavior unchanged. On the participant's next turn after the moderator advances, pickNextTopic routes them into the now-active round (round gating is free — picker only serves 'active'). (3) Moderator console: a "Tasting Rounds" strip at the top of the Topics tab (active sessions) showing each round's status (done/live/pending) + item name + response count, with a primary "Start Round N — {item}" button. round_number added to TownHallTheme type (GET already select('*')). WHY: a menu-research client runs synchronous tastings and needs the moderator to hold everyone between items so nobody races ahead. tsc clean (incl. clean-cache rebuild), 1059 tests, eslint warnings-only (pre-existing patterns). sql/140 already applied to prod. Next: Phase 4 participant hold screen (render a branded hold card on the round-hold turn + poll for the next round).

## 2026-06-29 — PulseIQ round-based pacing — Phase 4: participant hold screen (pilot complete)

Closes the pilot (P1–P4). The chat response gains a `round_hold` flag (set only on the round-hold standby branch, so it's distinguishable from the disengagement "chill" standby that shares source='standby'). In the participant widget (pi/[id]/TownHallChat.tsx), when round_hold arrives the bot bubble is suppressed and a branded hold card renders in place of the input ("⏸️ … Waiting for the next item…"), while a useEffect polls POST /api/townhall/resume/[sessionId] every 4s. The resume endpoint is deliberately isolated from the 800-line shared chat handler (which serves every PulseIQ session) — no AI/clarifier/deflection, just the shared pickNextTopic over the participant's undiscussed active topics: while the moderator hasn't advanced (round still active, all discussed) it returns {holding:true}; once the moderator's Start-Round-N has completed the prior round and activated the next, pickNextTopic finds the new round's topic, the endpoint inserts it as a real bot turn (user_message null → not response-counted until answered) and returns it; the widget drops the hold card, appends the question, and the participant's next answer threads onto that turn via the normal chat flow. Turn-budget guarded; final round (no paused rounds left) falls through to the engine wrap-up/closing. Open mode untouched (round_hold never set). WHY: "participants placed on hold between items" is the core of the client's ask — a real hold card + auto-resume beats a bare standby bubble. Pilot note: resumed question served verbatim (no AI "next item" transition). tsc clean (clean-cache), 1059 tests, eslint warnings-only. Next: Phase 5 round-grouped exports.

## 2026-06-29 — PulseIQ round-based pacing — Phase 5: round-grouped exports

Final phase. Exports now group by tasting round/item. export/route.ts: themes select gains round_number; build a themeRound map + roundItems (from config.rounds). In rounds mode ONLY (open-mode exports stay byte-identical — columns added conditionally): the Responses CSV/XLSX gains `round` + `item` columns; the Themes sheet gains round/item and is sorted by round_number (groups the per-item roll-up); the JSON carries `round` per turn and round/item per theme. export/pptx/route.ts: extracted the 2-per-slide theme-card loop into a renderThemeCards(list, headerTitle) helper; in rounds mode iterate rounds in order, emitting a navy section-divider slide per round (ROUND N + item name + theme count, mirrors the cover-slide pattern) followed by that round's theme cards with a "Round N — item" header. WHY: the deliverable the client pays for is "what the room said about each item, item-by-item" — flat theme slides don't convey that; per-round sections do. tsc clean (clean-cache), 1059 tests, eslint warnings-only. QC note: the PPTX divider needs a visual pass against a real rounds session (auth-gated route, couldn't headless-render) per the deck-credibility rule. Pilot P1–P5 now COMPLETE.

## 2026-06-29 — PulseIQ round-based pacing — polish (cross-round move, generate-append, item lead-in)

Cleared the three pilot limitations. (1) Cross-round move: each question card in the creator's rounds view now has a "Move to round" dropdown (shown when >1 round) that reassigns topic.round via updateTopic — so questions can be redistributed across rounds, not just authored in place. (2) AI Generate-from-Description in rounds mode no longer wipes the round structure: generated topics get round:1 and APPEND to the existing guide (open mode still replaces, as before), so the user keeps their rounds and distributes the generated questions with the move control. (3) Resume lead-in: the /resume endpoint now selects round_number and, for the freshly-served round-entry question, prefixes a deterministic item-aware lead-in ("Now for {item} — {question}") when the round has an item name — no AI call (keeps the poll fast/cheap), falls back to verbatim when no item. tsc clean (clean-cache), 1059 tests, eslint warnings-only. Round-based tasting mode pilot + polish COMPLETE; still LOCAL/unpushed, owner visual QC pending.

## 2026-06-29 — PulseIQ round-based pacing — test coverage + audit verification

Added gate tests for the new mutating round route (multi-tenancy invariant: route-handler org filters need their own tests, not covered by RLS). Extended tests/integration/townhall-mutation-gate.test.ts: POST /sessions/[id]/round → 401 unauth, 401 no-org-non-admin, 400 invalid round, 404 cross-org non-admin, 200 owning org, 200 admin bypass; plus participant-facing /resume guard tests (400 missing participant_id, 404 unknown session, holds when not active). Mock builder extended with not/lt/gt/gte/lte chain methods. Verified the full CI-equivalent locally: `npm run check:sql-tx` OK (sql/140 tx-wrapped), clean-cache `tsc --noEmit` clean, `npm run test:coverage` PASSES the gate (statements 23.35% / branches 15.99% / functions 23.06% / lines 23.91% — all above the 15/10/17/15 thresholds), 1068 tests pass (+9). Round-based tasting mode (6 feature commits 8f8d6006→cabcf448 + this test commit) is CI-green and ready for the Monday governance audit. Still LOCAL/unpushed; owner visual QC of the auth-gated UI surfaces (creator/console/participant/PPTX) is the only remaining item.

## 2026-06-29 — Fix: collection "Members updated — refresh" button silently did nothing

Bug report: clicking the amber refresh badge on a collection card appeared to do nothing. Root cause was NOT server-side — a read-only prod diagnostic confirmed POST /api/collections/[id]/refresh recomputes correctly (bumps the collection dataset's updated_at; members' max(updated,synced) is older → staleness=false server-side). The bug was client state: app/analyze/AnalyzeClient.tsx seeds `const [datasets] = useState(initialDatasets)` and never re-syncs when the prop changes. handleRefreshCollection calls router.refresh(), which re-runs the server component with members_updated:false / fresh row_count — but the seeded useState kept the stale rows, so the badge never cleared (classic derived-state-from-props-not-synced). Fix: `useEffect(() => setDatasets(initialDatasets), [initialDatasets])` — initialDatasets only changes identity on a server re-render (router.refresh / navigation), so local archive/delete optimistic updates between refreshes aren't clobbered. Also clears stale row_count/updated_at after any recompute. tsc clean, 1068 tests, eslint warnings-only.

## 2026-06-29 — Fix: collection project-report PDF 500s in prod (missing chromium trace) + sweep

Bug report: building the competitive report (Darden) and the community report (NOWOCATS) from a collection card both failed. Diagnosis (read-only prod repro): the HTML builds fine (buildProjectReportForCollection → 42KB / 135KB), so generation is healthy — but the card calls POST /api/collections/[id]/project-report/pdf, and that NEW route (added 2026-06-28) was missing from next.config.js outputFileTracingIncludes. Without the trace, @sparticuz/chromium's bin/ payload isn't shipped to the serverless function → it dies with `input directory ".../@sparticuz/chromium/bin" does not exist` (the exact failure documented in the server-PDF reference). Works locally (chromium resolves locally) so it slipped through the push. Fix: added the project-report/pdf route to outputFileTracingIncludes. SWEEP (sweep-the-class): grepped every route using htmlToPdfBuffer/puppeteer/@sparticuz/chromium and cross-checked the trace list — also added the three admin deck PDF routes that were likewise untraced (/api/community-feedback-deck, /api/pitch-deck-v2, /api/pitch-deck-v3). NOTE: build-time tracing config — only takes effect on a prod deploy, cannot be verified locally (this is one of the rare push-to-verify cases).

## 2026-06-29 — Fix: community/project report clipped agent responses mid-word

Owner reported the RESPONSE text in a collection community report was truncated mid-word ("Want me to walk thro"). Root cause: lib/agentStudy.ts line 613 capped focus sample question/answer at .slice(0,300)/.slice(0,320) when building the agent study. Those samples feed the community report's Q&A cards (projectReportLoad loadAgentInput → qa.answer = s.answer) AND the Agent Study HTML report — both full-width wrapping HTML, so the cap only served to clip verbatims. Removed both slices (store full text); the agent-study PPTX doesn't render these samples and the readout deck uses a separate pipeline, so no slide-overflow risk. Bumped STUDY_SCHEMA_VERSION v6→v7 so cached studies regenerate with full samples (otherwise the NOWOCATS/Sarina cache keeps the truncated text). Verified read-only: NOWOCATS community report max answer length 320→866 chars, ends on a complete sentence. tsc clean, tests pass.

## 2026-06-29 — StoryTime: Dimensions section + "Heads-Up" exception alerts (Feature A, static tier)

Now that datasets carry the ABSA Dimensions layer, the per-dataset StoryTime PPTX (POST /api/datasets/[id]/export/pptx) gains a Dimensions section after "About This Report" — gated on taxonomy_enabled (or the google_reviews proxy unless suppressed) AND withSignal>0. Three slides: Aspect Coverage (column_chart, mirrors the Operational Review deck) + What Stands Out (table) + a NEW Heads-Up watch list. The watch list is a pure, unit-tested engine lib/dimensionAlerts.ts (computeDimensionAlerts) that turns the rollup into ranked exception alerts: 🔴 pain points (high volume + low %-positive or rating drag), 🟢 bright spots, ⚠️ safety flags (always surfaced first), and — when a recent-vs-prior window is passed — 📉 deteriorating / 📈 heating-up / ✨ improving trends. Adaptive min-mention floor (max(8, 2% of classified rows)) so thin data doesn't over-flag; per-aspect dedupe (most-urgent framing wins); every line carries its mention count n (no fabrication). ExportModal gets an includeDimensions toggle (default ON) under a new "Analysis Sections" group. 10 unit tests. QC'd by rendering the 3 slides via renderDeck → LibreOffice → image: clean layout, color-coded icons; tightened the safety detail string after spotting a table-cell clip. tsc clean, 1078 tests. NEXT: wire the TREND tier (attach review_date in computeTaxonomyRollup, compute recent/prior windows, pass opts.trend) — the engine already supports it. Then Feature B (rating-over-time on the competitive collection report).

## 2026-06-29 — Heads-Up alerts generalized to a lens-agnostic engine (themes + dimensions + quant)

Owner direction: the Heads-Up watch list shouldn't be Dimensions-only — exception conditions and trends apply to themes, dimensions, AND quant variables (rating minimally, plus any other numerics), merged into ONE list. Renamed lib/dimensionAlerts.ts → lib/insightAlerts.ts and rebuilt it as a lens-agnostic engine: computeInsightAlerts({signals, quant, safety, ...}) where signals are a neutral AlertSignal (lens 'theme'|'dimension', count/rate/posPct/avgRating + optional prior-window snapshot) and quant are QuantSignal (a numeric variable's avg + optional prior + low-score tail). Producers: count/sentiment lens → 🔴 pain / 🟢 bright / 📉 deteriorating / 📈 heating / ✨ improving; quant lens → avg-drift (★4.5→★4.2 for ratings, relative-change for other numerics) + low-score-tail surge; safety stays Dimensions-only. Everything merges, ranks by severity, dedupes one-per-title, caps (default 8), keeps all safety. Adapters dimensionsToSignals/themesToSignals. Kept a computeDimensionAlerts back-compat wrapper so the StoryTime route (Dimensions-only today) is unchanged — no regression. 13 unit tests (added quant + cross-lens-merge + theme-signal cases). tsc clean, 1081 tests. NEXT: wire the theme + quant adapters and the shared windowed recent-vs-prior read into the StoryTime route so the merged Heads-Up actually spans all three lenses with trends; then Feature B.

## 2026-06-29 — Dynamic, data-driven time-framing for trends + charts (lib/trendWindows.ts)

Owner direction: trend windows and chart timeframes must NOT be hard-coded — derive them from the date range of the data in the current view, and let the user optionally pick a reference window. New pure helper deriveTrendWindows(minMs, maxMs, reference): computes an adaptive bucket unit (week ≤90d / month ≤420d / quarter beyond, so any span yields ~6–12 readable buckets) + the chart domain + the recent/prior comparison windows. Reference modes: 'auto' (most-recent third vs prior third — volume-balanced, the default), 'yoy' (recent window vs the SAME window 12 months earlier — controls for seasonality, falls back when <~13mo history), and { recentDays } (explicit trailing window). Too-short spans (<45d) return a chart domain but no comparison windows (trend skipped, not faked). humanDur labels ("last 4 months vs prior 4 months"); bucketKey buckets a timestamp by unit. Shared by BOTH Feature A (Dimensions/theme/quant trend alerts) and Feature B (competitive rating-over-time charts). 7 unit tests, tsc clean. NEXT: wire it into the StoryTime route's windowed read (recent/prior rollups for dimensions + themes + quant) feeding the merged Heads-Up, and into the competitive report's rating-over-time chart.

## 2026-06-29 — Rename user-facing "StoryTime" → "Report"

Owner: "StoryTime" is too cutesy — users have no idea what it means; "Report" is clearer. Renamed every user-facing occurrence: the analyze-view More-menu button (label 🎬 StoryTime → 📊 Report, tooltip "Build a report from this view"), the HTML export <title> ("… — Report"), the custom-decks upsell slide line, the entity-analysis-deck subtitle, and the admin AI-uses list. Internal route/code names (export/pptx, renderDeck) unchanged. First step toward a unified "Reports" picker (deck + collection community/competitive/brand-360 + Operational Review + an ad-hoc Ask-Ana report) with scope semantics: run from a card → full dataset; run from the analyze view → honors current filters/saved-view. ANALYTICS.md naming note added. tsc clean, 1088 tests.

## 2026-06-29 — Unified Reports: the catalog (source of truth) — lib/reportCatalog.ts

First build of the unified "Reports" picker (replacing the scattered StoryTime/Operational-Review/collection-PDF/agent entry points). Started with the pure, testable backbone — same pattern as lib/textmineNav.ts — so the card menu, the analyze-view menu, and any future surface never drift. availableReports(ctx: ReportContext) returns the report TYPES valid for a context (single dataset → Full report [ex-StoryTime] + Operational Review for restaurant/Dimensions data; collection → community/competitive/brand-360 filtered by purpose + member-count ≥2 for competitive; ad-hoc Ask-Ana report everywhere when AI is on), each carrying its currently-wired FORMATS (deck pptx/html, collection pdf/html, ops-review pptx — formats expand as renderers land so the picker never offers a broken file), supported SCOPES (full from a card; in-view/filtered from the analyze view), and a launch(id, format)→{url,method,body} builder. 13 unit tests. tsc clean. NEXT: the ReportsMenu UI consuming the catalog (card + analyze header), then the ad-hoc Ask-Ana report endpoint + deck PDF + collection PPTX as the formats/types fill in. Honesty note (per owner): bespoke one-off cuts route to the ad-hoc report, not new structured-report code.

## 2026-06-29 — Unified Reports: ReportsMenu wired into the collection card

First UI consumer of lib/reportCatalog. New presentational components/analyze/ReportsMenu.tsx renders availableReports(ctx) — each report type as a row with its format pills (PDF/HTML), calling an onLaunch(type, format) the caller provides. Wired into DatasetCard's collection ⋯ menu under a "Reports" header, replacing the three hardcoded inline buttons (Community/Competitive/Brand-360). All working launch logic stays in the card via handleReportLaunch: HTML opens the GET project-report?purpose=… in a new tab (NEW capability — collection reports were PDF-only in the UI), PDF routes to the existing handlers (competitive still triggers the focus-picker modal, the rest download the blob). Removed the now-orphaned show*Report vars. adHocEnabled stays false (gated in the catalog) so the menu never shows the not-yet-built ad-hoc option; the header+divider are gated on hasAnyReport so a <2-member competitive collection shows nothing rather than an empty section. +14 catalog tests (incl. the ad-hoc gate). tsc clean, 1102 tests, eslint warnings-only. NEXT: mirror ReportsMenu in the analyze-view header (deck + ops-review there), then the ad-hoc Ask-Ana endpoint, then fill in formats (deck PDF, collection PPTX).

## 2026-06-29 — Unified Reports: analyze-view header picker

Second surface for the catalog. The analyze-view header "Report" button (the renamed StoryTime) is now catalog-driven: for a single configurable report (the deck) it opens the ExportModal directly as before; for a restaurant dataset (deck + Operational Review) it opens a small popover (ReportsMenu) so the Operational Review — previously buried on the Brand Health page — is reachable from the main header. ReportsMenu now branches on configurable: configurable types (deck/ad-hoc) render as a single "Label →" row (format picked inside their own modal), one-click types render with format pills. The header's handleReportLaunch is method-aware (GET→window.open, POST→fetch+blob download) so it also handles collection synthesis reports if a collection is opened in the analyze view. tsc clean, 1102 tests, eslint warnings-only. NEXT: ad-hoc Ask-Ana report endpoint (then flip adHocEnabled), then fill formats (deck PDF, collection PPTX).

## 2026-06-29 — Ad-hoc Ask-Ana report endpoint (+ shared Ana data layer)

The "release valve" report type: the user describes exactly what they want and Ana writes a focused report over only that data. Extracted the Ana data pipeline (collection resolution, sampled+filtered row fetch, reddit signal selection, formatting) verbatim from app/api/ask-ana into a shared lib/anaReportContext.ts (loadAnaSample/resolveCollectionMembers/fetchDatasetRows/computeMemberBudgets); refactored ask-ana to call it (removed ~120 inline lines, behavior preserved, tsc clean). New POST /api/datasets/[datasetId]/ad-hoc-report {prompt, format, filters?}: org-gated, content-safety-checked, loadAnaSample → one callAI('advanced', timeout 110s) producing a clean HTML fragment (h2/h3/p/lists/blockquote/table only, grounded-in-data system prompt) → pdfDoc-wrapped → HTML or PDF (htmlToPdfBuffer + brandedPdfChrome). Collections route through the same endpoint (source='collection' dataset_id). Added the route to next.config outputFileTracingIncludes + ENGINEERING.md (chromium PDF route — else prod 500). Catalog ad-hoc launch now always hits the datasets route. VERIFIED end-to-end via read-only repro: a real google_reviews dataset → 6KB HTML report, h2 + tables + verbatim, grounded (pulled the real street address from the reviews), stop=end_turn. tsc clean, 1102 tests. NEXT: a prompt modal to collect the request + wire ad-hoc into the pickers (flip adHocEnabled) so it's user-reachable.

## 2026-06-29 — Ad-hoc report: prompt modal + wired into both pickers (now live)

Made the ad-hoc report user-reachable. New components/analyze/AdHocReportModal.tsx: a prompt textarea (16px, iOS-safe) + PDF/HTML toggle + Generate → POSTs the ad-hoc endpoint, opens the HTML in a new tab or downloads the PDF, with a LottieLoader while Ana writes. Wired the "Ad-hoc report" picker row (configurable type → single "Label →" row) to open the modal from both surfaces — the collection card menu (handleReportLaunch ad-hoc branch) and the analyze-view header — and flipped adHocEnabled:true in both reportCtx. Full chain now live: reportCatalog → ReportsMenu → AdHocReportModal → /api/datasets/[id]/ad-hoc-report → loadAnaSample → callAI → HTML/PDF. (Header ad-hoc currently runs full-dataset; passing the in-view filters is a later refinement since DatasetHeader only receives filterCount, not the raw filters.) tsc clean, 1102 tests, eslint warnings-only.

## 2026-06-29 — EA membership-survey capability deck (PPTX, /admin/decks)

New Sentimetrx-forward capability deck for the EA (gaming) membership-survey meeting Wed. lib/pptx/eaMembershipDeck.ts (exported builder, pixel-QC'd standalone via scripts/_ea_membership_deck_qc.ts) + admin-gated route app/api/ea-membership-deck/route.ts + catalog entry in DecksClient (client group, badge EA). 11 slides: title → who-we-are (40-yr arc) → the 40–50% unusable-open-ends problem (owner's own client data, attributed) → the monologue insight → conversational surveys → side-by-side conversational advantage (membership-flavored: vault/early-access/renewal) → collection↔analysis pipeline → analysis layer (themes/drivers/feature sentiment/significance/cohorts) → why-now (gaming chat-native angle) → proof (Globetrotters/JWM/UCF/Orlando, verbatim Okumus quote, reused from shipped pitch deck) → tailored membership-study close + CTA. Reused real on-brand copy from app/api/pitch-deck; no fabricated stats. Membership example labeled illustrative in talk track. CUSTOMIZE-before-send notes baked into title/close speaker notes (program + perk names, contact). QC: all 11 slides rendered via soffice+pdftoppm, fixed 2 issues (slide 10 "Seconds" wrap, slide 11 orphaned-bullet/CTA-collision). tsc clean. LOCAL/unpushed.

## 2026-06-29 — Feature B: rating-over-time on the competitive report

Owner's ask: ground the competitive report in trajectory — is each brand going up or down, and how far apart are they. New multi-line rating-over-time chart, rendered after the competitors manifest. Data layer: readMemberRows now also extracts review_date; loadGenericInput computes a compact monthlyRatings aggregate ({ym,sum,n}) for free from the rows the shared-theme path already reads (gated to review inputs with dates) → ProjectInputModel.monthlyRatings. New pure lib/ratingTrend.ts: buildRatingTrend re-buckets each competitor to a DYNAMIC cadence via lib/trendWindows (month/quarter by span), clamps the y-axis to the actual star range, computes a recent-vs-prior Δ per competitor (≥10-review min-bucket guard); renderRatingTrendSvg emits a PDF-safe inline SVG (focus brand thick/dark + bold legend, ▲/▼ deltas). buildCompareModel computes competitorTrends (competitive only, ≥2 dated review inputs); renderCompareReportHtml drops in a "Rating trajectory" pdfSection. 4 unit tests. VERIFIED end-to-end on the real Darden CG-vs-Fleming's collection (17 months each → quarterly, 6 buckets, y=[4,5], window "last 5 months vs prior 5 months", real deltas CG 4.65→4.61 / Fleming's 4.55→4.50, SVG with 2 polylines) + visual QC of a synthetic 3-brand chart (fixed an edge x-label clip → start/end anchors). tsc clean, 1106 tests. The dynamic-window helper is now shared by Feature A (Heads-Up trends) and Feature B (this chart).

## 2026-06-29 — Heads-Up: merged Dimensions + quant trends (Feature A trend tier, part 1)

Extended the StoryTime Heads-Up from Dimensions-only to MERGED across Dimensions + quant variables with real trends. New pure lib/quantSignals.ts (buildQuantSignals): for the rating + every numeric schema field, averages the recent window vs the prior window (dynamic cadence via trendWindows, derived from the in-view dated rows) and computes a low-score-tail surge for ratings → QuantSignal[] feeding the lens-agnostic computeInsightAlerts. The export/pptx Heads-Up now calls computeInsightAlerts({ signals: dimensionsToSignals(dim), quant, safety, ... }) instead of the dimensions-only wrapper; slide retitled "Heads-Up" (Signal · What · Detail). Field/date keys resolved through the route's rowKeyMap; dateField = schema.primaryDateField or review_date for google_reviews. 5 unit tests. VERIFIED on Ruth's Chris (27,234 rows): review_date parses, dynamic window "last 6 months vs prior 6 months", recent 4.53 vs prior 4.56 → ZERO quant alerts (correct — −0.03 is below the 0.2 threshold, no false alarm), while the unit test proves a real 4.6→4.1 shift fires a deteriorating alert. tsc clean (widened insightAlerts windowLabel to string|null), 1111 tests. Remaining for the full "trends on everything": theme signals (weak posPct + heavy per-theme windowed re-matching), dimension trends (need date-aware taxonomy rollup), and making the Heads-Up render for non-Dimensions datasets too.

## 2026-06-29 — EA deck: realign slide 11 to value-discovery / longitudinal

After researching the actual program (EA SPORTS MVP+ Membership — bundles College Football 27 + Madden NFL 27, $149.99/yr, US+CA console-only) and getting the real ask from Seann Graddy (VP/EP, owns both football franchises) — "more data from players that are members and those that aren't around what would make the membership valuable to them… grow its value over time" — reframed the deck's close slide from a churn/renewal angle to forward value-discovery. Slide 11 now: title "Growing the Value of MVP+", two population columns (Your members / The players who haven't joined), a "walk the journey" longitudinal band (baseline now → re-ask as the perk mix grows → compounding time-series), CTA "Let's baseline a first wave" + sanjay@datanautix.com. Speaker notes carry the two-population + longitudinal talk track and a contact-swap note (larry@ if Larry fronts the relationship). QC'd (slide renders clean). tsc clean. NOTE: rest of deck (title/footer say generic "membership program", slide 6 example still generic gaming perks) intentionally left — only slide 11 realigned per owner. The deck is for a LATER meeting; Wednesday's artifact is the separate ~/Downloads warm brief (uncommitted). All LOCAL/unpushed.

## 2026-06-29 — Unified Reports loose ends: header ad-hoc honors in-view filters (+ format-honesty)

Finished the small Reports loose ends. (1) The analyze-header ad-hoc report now respects the current filters: DatasetShell serializes the active filters (the wire shape the export/ask-ana/ad-hoc routes apply — extracted the Sets→arrays serialization, reused by save-session) and passes them as inViewFilters → DatasetHeader → AdHocReportModal → the endpoint's loadAnaSample. So a report run from a card = full dataset; from the analyze view = the in-view/filtered scope (the scope semantics now hold end-to-end). Empty filters → undefined so the modal copy reads "this data" not "currently in view". (2) Keep-me-honest on the two remaining "format" items: deck PDF is NOT a clean small win (the deck's HTML form is a Reveal.js presentation that won't print cleanly, and PPTX→PDF needs LibreOffice which isn't on Vercel) — left unbuilt deliberately; collection PPTX is a genuine deck-builder lift, deferred (not small). tsc clean, 1111 tests, eslint warnings-only.

## 2026-06-29 — EA deck now Wednesday-ready: MVP+ accuracy sweep

Owner clarified the deck IS for Wednesday (leave-behind / present-if-Graddy-brings-people). Swept the whole deck for MVP+ accuracy + consistency: named "EA SPORTS MVP+ Membership" on the cover; fixed two factually WRONG perks ("vault"/"cloud" are EA Play, not MVP+) on slides 6 + 8 → real MVP+ perks (7-day early access, monthly Ultimate Team packs, two-game CFB+Madden bundle, mode progression); reworked the slide-6 conversational example to the real Franchise-vs-Ultimate-Team value-mismatch friction (Graddy's world: he owns both football franchises); dropped "tier" everywhere (MVP+ is single-tier, not EA Play) → segment by game/mode/platform/region. Title-slide speaker note now frames it as a warm-meeting leave-behind, not a script. tsc clean, changed slides QC'd. LOCAL/unpushed.

## 2026-06-29 — Heads-Up: three full lenses + universal placement (theme signals, dimension trends)

Knocked out three of the queued "big items" on the StoryTime/Report Heads-Up. (1) **Theme lens** — new `lib/themeSignals.ts` (`buildThemeSignals`, pure + tested): re-matches each theme's keywords over the actual rows to compute **real per-theme pos/neg + avg ★** from the rated rows (rating ≥4 pos / ≤2 neg / 3 neutral; posPct stays null with no rating field — we don't fabricate precision from a one-word sentiment label) AND **recent-vs-prior windowed re-matching** so an organic theme fires 📉/📈/✨, same contract as quant (recent window is the "now" when datable). (2) **Dimension trends** — `computeTaxonomyRollup` gained an optional `dateField`: it attaches each taxonomy row's timestamp (direct select for simple field names, `dataset_field_values` RPC otherwise, best-effort) and returns `recent`/`prior`-window rollups (`TaxonomyTrendRollup`); the route feeds `dimensionsToSignals(dim.recent || dim, dim.prior)` so sub-aspects get 📉📈, not just static pain/bright/safety. (3) **Universal** — lifted the Heads-Up out of the Dimensions-gated block into its own up-front block (right after "About This Report"), fed by all three lenses (theme + quant always; dimensions when enabled). It now renders for ANY dataset that surfaces ≥1 alert — e.g. a quant rating trend on a non-restaurant survey. The Dimensions section reuses the single rollup. Why: the queue's items #1–#3 — make Heads-Up a true 3-lens exception list, not Dimensions-only. tsc clean; insightAlerts/quantSignals/themeSignals/taxonomyRollup/trendWindows suites green (34 tests, +5 new). ANALYTICS.md synced. LOCAL/unpushed.

## 2026-06-29 — Collection reports gain PPTX (queued item #4)

The "deck-builder lift" deferred earlier. Refactored `buildProjectReportForCollection` to split out `buildProjectModelForCollection` (gate + load + purpose-dispatch → a discriminated `ProjectBuilt`: `kind:'community'`→`ProjectReportModel`, `kind:'compare'`→`CompareReportModel`); the HTML/PDF routes keep rendering to HTML, and a new `POST /api/collections/[id]/project-report/pptx` route renders the model via `lib/pptx/projectReportDeck.ts` (`renderProjectReportDeck` → shared cream `renderDeck`). Community deck = at-a-glance KPIs + exec bullets + themes table + top-theme quote slides + entity grid; comparison deck = at-a-glance + exec + themes matrix (focus brand ★, ▲▼ significance vs focus) + Dimensions matrix (when ≥1 input classified) + rating-over-time recent-vs-prior delta table. Catalog: `projectType.formats` → `['pdf','pptx','html']`, with a `project-report/pptx` launch; both ReportsMenu surfaces (collection card ⋯, analyze-view header) now show a PPT pill. Card's `handleProjectReport` is format-aware (endpoint + extension); the competitive PPT path reuses the focus picker (format stashed in `projectFormat` until the focus is chosen); the header launcher was already format-agnostic. Visual-QC'd both decks headless (LibreOffice + pdftoppm) — matrix, quotes, branding all clean. Why: queue item #4 — collections were PDF/HTML-only; a Datanautix PPT is what clients actually present. tsc clean, reportCatalog test updated (pptx launch). ANALYTICS synced. LOCAL/unpushed.

## 2026-06-29 — Report button: deck-direct + caret picker (queued item #5)

The owner's UX call: the analyze-header "Report" button always opened the picker, costing one extra click for the deck (the common case). Resolved deck-direct: when a deck report is available the button runs it directly (opens ExportModal), and a small caret ▾ beside it opens the full ReportsMenu picker for the other types (Operational Review on restaurants, ad-hoc). A collection (no deck) keeps the button as the picker toggle; a lone non-deck report opens directly. Why: queue item #5. tsc clean, 1116 tests. ANALYTICS synced. LOCAL/unpushed.

## 2026-06-30 — Owner QC fixes: Reports label/caret, exec-vs-about, TextMine section nav

Five fixes off the owner's live testing of the 6/29 reports work. (1) **Report → Reports** button label (plural). (2) **Reports caret was clipped** — root cause: the analyze header's middle tab strip is `overflow: hidden`, which silently clipped the `position: absolute` dropdown (it opened but was invisible → "the caret does nothing"; the deck-direct modal worked because it's a full-screen fixed modal). Fixed: render the picker `position: fixed` anchored to the button's `getBoundingClientRect`, and show the caret on collections too (was gated on a deck existing) so it's consistent. (3) **Exec Summary vs About repetition** — they repeated scope metrics; Exec now carries *findings* KPIs (overall ★, top theme + share, themes count) above the AI narrative, About owns scope/method and gains a **Data Range** KPI (min–max of the date field) distinct from the generation date. (4) **TextMine Entities "does nothing"** — repro: on the Comments view, clicking a section (Entities) preserved the Comments view, which renders the same comment list across every lens, so it looked inert; `selectSection` now lands on the section's Overview when leaving Comments (where the entity cloud/list actually lives). Verified the MVP+ dataset has 12 catalog entities (9 matching) + 6 themes, so the data path was fine — it was the nav. Why: owner-flagged bugs in the just-shipped reports/TextMine work. tsc clean, textmineNav + reportCatalog suites green. ANALYTICS synced. LOCAL/unpushed.

## 2026-06-30 — External questions: paste a community list, answer with the agent KB

New agent feature (owner request, greenlit with decisions). A client gives us a list of community questions; we answer each with the agent's KB and send responses back. Built on the existing 6/27 Questions→draft→answer→KB loop — external questions just needed a front door + to flow into the corpus. `sql/141` (APPLIED) adds `logged_questions.{source,external_contact,answer_text,batch_label}`. New: `POST /questions/import` (paste box → bulk insert, classification='external', synthetic session_id, contact parsed from tab/comma cells), opt-in KB write on `/answer` (`writeKb` flag — decision 2), and on accept of an external question `/answer` materializes a `conversations`+`conversation_turns` pair (source='external', idempotent) so it shows in **What We Heard** via loadTurns (decision 4; live-perf metrics unaffected since they're computed at chat time). `GET /questions/export-responses.csv` downloads Question·Response·Name·Email·Phone (contact NOT redacted — the point is to reply; decision 3), personified filename `{Agent}_Responses_<date>.csv`. UI: "+ Add questions" modal + "Download responses" + KB opt-in checkbox + inline contact + "External" badge on the Questions page; reuses guided Review + /draft (source-agnostic). Decision 1: agent-level (answers with one agent's KB), not collection. tsc clean, 1116 tests, eslint warnings-only. BOTS.md synced. sql/141 APPLIED to prod. LOCAL/unpushed.

## 2026-06-30 — External questions: CSV comment-log import + AI question-extraction (Phase 1)

For a real client file (NOWOCATS June 2026 comment log — 39 rows, ~10 long mixed comments + contact columns), the flat paste box isn't enough: each comment mixes a question with commentary. Added "Import CSV" → `CommentImportModal` (parses CSV/Excel client-side via SheetJS/xlsx — handles quoted/multiline cells — auto-guesses a column map) → `POST /api/bots/[id]/batches`, which runs one AI pass (`extractQuestions`) to distil each comment's answerable question into `user_message` while keeping the full text as `original_comment` (commentary) — owner's "extract the question, store the rest." sql/142 (APPLIED) adds `question_batches` (with share_token cols, mirroring recordings) + `logged_questions.{batch_id, original_comment}`. Review rows show the extracted question + collapsible Full comment + contact; accept materializes the full comment as the What-We-Heard turn; the responses export gains Full comment/Address/Agency. `GET /batches` lists batches w/ counts. Why: lets us add a client comment log to an existing agent and answer with its KB. Phase 2 (the shareable client review link `/review/<token>`) is next. tsc clean. BOTS.md synced. LOCAL/unpushed.

## 2026-06-30 — External questions: shareable client review link (Phase 2)

The client experiences the accept/edit of the AI-drafted responses themselves (owner's chosen option). "Share with client" on a batch → `POST /api/bots/[id]/batches/[batchId]/share` mints a share_token (mirrors recordings/share) and copies the link. Public `/review/<token>` (`ReviewClient`) is no-auth, fail-closed (unknown/disabled/expired → 404, mirrors /th/<token>), exposes only the batch's questions + the agent name. The client walks the queue: "Draft from knowledge" + edit + Accept, all via token-gated `POST /api/review/<token>` ({action:draft|accept}). Accept records the answer + materializes the What-We-Heard turn but NEVER writes the KB (client can't teach the agent). Same-origin → proxy CSRF passes, no bypass. Extracted the draft prompt to `lib/agentDraft.ts` (shared by admin /draft + public review) and the corpus materialization to `lib/externalExchange.ts` (shared by admin answer + public accept) so the two paths can't drift. Admin Questions page gains an Imported-batches panel (share toggle, copy link, per-batch download). Why: owner wants clients to do the accept/edit hands-on. tsc clean, 1116 tests, eslint 0 errors. BOTS.md synced. LOCAL/unpushed.

## 2026-06-30 — External questions: auto-draft responses at import (not on demand)

Owner: an empty answer box has zero value — these are website questions the agent KB can answer, so the draft must be automatic. `POST /batches` now pre-drafts each response from the agent's KB during import (draftAnswerFromKB, runConcurrent limit 6, cap 80) and stores it on sql/143 `logged_questions.draft_response` — distinct from suggested_kb_addition (the accepted answer / "in knowledge base" flag) so an un-answered import isn't mis-shown as KB content. Admin answer box + client review page both open pre-filled (admin shows a "✨ Suggested from {Agent}'s knowledge" label); the review "Draft from knowledge" button is now a re-draft. Also deleted the owner's first NOWOCATS test batch (10 Qs) from prod so they can restart the flow clean. tsc clean. sql/143 APPLIED. BOTS.md synced. LOCAL/unpushed.

## 2026-06-30 — Shared FileDrop component (drag-and-drop), adopted in comment import

Owner: standardize file uploads into one shared piece (we have drag-and-drop in some places, plain inputs in others). Created `components/ui/FileDrop.tsx` — drag-and-drop OR click-to-browse, matching the dataset uploader's UX (dashed dropzone, orange drag highlight, "Browse files"); props: accept, onFile/onFiles, multiple, title/hint/icon, compact. Adopted it in `CommentImportModal` (was a plain file input → now drag-and-drop). The ~11 other upload sites (dataset UploadClient, KB upload, recordings, campaigns, creator steps, team avatar, etc.) are candidates to migrate onto it next — left for a focused follow-up sweep rather than tangling it with the external-questions feature. tsc clean, eslint 0 errors. LOCAL/unpushed.

## 2026-06-30 — External questions: async-aware drafts + preserved suggestion on review page

Two owner refinements for the website-submission case. (1) These are one-way submissions, so the AI reply shouldn't ask the submitter a follow-up question: draftAnswerFromKB gained opts.asyncReply (set by the batch import + public review draft) adding a "complete, self-contained, no follow-up questions" rule. (2) The client review page no longer edits the AI draft in place — it shows the AI suggestion READ-ONLY (preserved, with a ↻ Re-draft) above a separate "Your response" field that starts empty; the client writes their own wording, or clicks "↧ Use this as my response (then edit)" to copy the suggestion in. Accept saves the client's response (answer_text); the AI suggestion (draft_response) is never overwritten. Admin box unchanged (team edits-to-teach there). Also tabled the FileDrop full-migration as a to-do (owner). tsc clean, eslint 0 errors. BOTS.md synced. LOCAL/unpushed.

## 2026-06-30 — External questions: curate live questions into a client-review batch

Owner chose: include live "needs clarification" questions in the client review link, but curated (not auto — live captures can hold sensitive visitor messages). Each admin Questions row gets a "Client review:" dropdown (existing batches + "＋ New review batch…") → PATCH .../questions/[id] {batch_id} validates same-agent/org, stamps batch_id, and drafts a response (asyncReply) if none, so it opens with a suggestion. POST /batches {createEmpty:true} makes an empty source_kind='curated' container. The review page shows a per-item origin badge ("From the comment log" vs "Asked on the website — agent had no answer/off-topic/unsure" from source+classification) so a reviewer sees where each came from in a mixed batch. tsc clean, eslint 0 errors. BOTS.md synced. LOCAL/unpushed.

## 2026-06-30 — Client review UI: sharper, more sophisticated redesign

Owner: make the client response-editing surface look much sharper. Full visual overhaul of ReviewClient (the public /review/<token> page): sticky datanautix header with a live progress bar + "N of M responded"; orange eyebrow + bold display title; refined elevated card (18px radius, layered shadow); origin + asker chips + a "Responded / Needs a response" status pill; the AI suggestion in an elegant teal gradient accent card (✦ Suggested by {agent} + Re-draft + "Use this as my response"); a focused "Your response" area with a char count and a teal focus ring; a strong orange "Accept & continue" primary with hover lift; clickable progress dots; ghost prev/skip; and an "All responses reviewed" completion banner. Hover/focus/transition states via a scoped <style> block; responsive. Rendered headless (puppeteer-core + local Chrome) to verify. Logic unchanged. tsc clean, eslint 0 errors. LOCAL/unpushed.

## 2026-06-30 — External questions: export shows AI drafts + firmer no-questions rule

Two fixes from owner testing. (1) "No export ability" — the batch had 0 accepted answers, so Download responses produced empty Response cells (looked broken). The responses export now has BOTH an "AI suggested response" (draft_response) and "Final response" (answer_text) column, so it's useful before the client accepts anything; per-batch button relabeled "⬇ Export responses". (2) Drafts still asked the submitter questions on pure commentary — strengthened the asyncReply rule: a one-way submission must never contain ANY question to the submitter, and commentary should be acknowledged ("recorded for the team") not turned into a Q&A. tsc clean. LOCAL/unpushed.

## 2026-06-30 — External questions: concise reply mode, warm email drafts, KB training loop

Off the Sarina eval (agent answers vs the consultant's real answers, blind). (1) Concise mode: extractQuestions now classifies a reply tier (answer/contact_list/acknowledge) — simple contact-list/acknowledgement comments get the agency's terse canned line (per-agent config.replies, toggle config.conciseAcknowledgements, default on), only real questions hit the KB; Re-draft upgrades a canned reply. Matches the consultant house style (one line for simple asks). (2) Warm email drafts: export-responses.csv?emails=1 → copy-paste emails, one per contact WITH an email + a response, warm "Hi {first name}, … Warm regards, {signoff}" wrapper (config.emailSubject/emailSignoff); "📧 Email drafts" button per batch. (3) KB training loop: the eval surfaced 5 gaps (traffic-study jurisdiction, trail alignment/Parks&Rec, Apopka Blvd curve treatments, Sadler/US-441 widening + MetroPlan 2050, Jespersen not a study intersection); loaded 5 consultant-sourced chunks into Sarina's KB (tagged consultant_eval, removable in KB Health) and re-ran — all five flipped miss/contradiction → match. tsc clean, eslint 0 errors. BOTS.md synced. LOCAL/unpushed (the 5 KB chunks are prod DATA, not code).

## 2026-06-30 — External questions: stop fabricating questions + fix concise mis-tiering

Owner caught two extraction bugs in the comment-import. (1) A complaint with no question (Peaden's Binion Rd commentary) was shown as a fabricated question ("What plans does the study have…?"). Fixed: extraction's `question` is empty unless one was actually asked (never invented), the review title falls back to a neutral `topic` label, and the reply is drafted from the FULL original comment (not the title) — so a concern still gets a relevant, on-point reply. (2) An HOA point-of-contact (Angela) got down-tiered to contact_list → terse "we'll add you to the list." Fixed: a stated role/representative disqualifies contact_list → answer, and the form-intent checkbox is ignored when the text carries substance. Re-draft (public + admin) now also drafts from original_comment. Verified on both cases: Peaden → answer, no fabricated question, empathetic on-point draft; Angela → answer; bare "keep me updated" still contact_list; "lived 37 yrs" → acknowledge. tsc clean. BOTS.md synced. LOCAL/unpushed.

## 2026-06-30 — Draft token cap 500→1200 (multi-question comments were truncated)

Reviewing the re-imported NOWOCATS drafts: the two multi-question civic comments (Fred/Sierra Club with 5 asks, Sara with ~7) had drafts cut off mid-sentence — draftAnswerFromKB capped at maxTokens 500. Bumped to 1200 (~900 words) + timeout 30s→45s so a resident asking six questions at once gets a complete reply. Rest of the batch reviewed clean: no fabricated questions (honest topic labels for commentary; real questions only where asked), HOA/reps now get substantive responses, bare "add me / project updates" still concise, all grounded + no follow-up questions. tsc clean. LOCAL/unpushed.

## 2026-07-01 — Lock the chat input when a session is terminated

Owner: when an agent session is terminated (profanity/abuse strike-out or other reasons), lock the text input so no further input can be provided. The 3-strike shutdown already existed in contentGuard (checkMessage returns shutdown:true) but only sent a warning — the input stayed open. Now handleChatTurn propagates it as `ended:true`, and all four widget-backed chat endpoints (bots/[id]/chat, bot-chat, clara-chat, nora-chat) return it. components/ui/ChatBot sets sessionEnded → disables the textarea + send button, guards sendMessage (covers chips/retry/Enter), stops the silence-probe, drops follow-up chips, and shows "This conversation has ended." `ended` is generic so any future termination reason locks the same way. tsc clean, contentGuard tests green, eslint 0 errors. BOTS.md synced.

## 2026-07-01 — EA MVP+ demo kit (agent LIVE in prod; deck local)

For the warm EA / Seann Graddy meeting (VP/EP, owns Madden + College Football; his ask: what would make MVP+ valuable to members AND non-members, grown over time → value-discovery, not churn). Built a demo kit — NOTE most of this is PROD DATA + ~/Downloads artifacts via throwaway uncommitted scripts, NOT committed app/lib code: (1) **MVP+ conversational agent LIVE in prod** at `/b/mvp-plus` (slug mvp-plus, Datanautix org, 16-chunk KB crawled from ea.com MVP+ pages, gamer tone, EA-matched colors dark #161616 / red #ff3f3f / gold #f7c64d + 🏈 avatar) — and it doubles as a passive listening instrument: soft interest probes ("what would make it a no-brainer to sign up", barriers, perk preference, Madden/CFB + mode) tagged via focuses interest_signal/no_brainer/barrier/perk_preference for review in the agent Conversations/Readout, pipeable to TextMine. (2) A 28-comment public-listening TextMine dataset (EA official forums + MUT.GG, 6 themes / 12 entities, dataset a40ddc3d…; labeled vocal-minority public sample — Reddit was not retrievable). (3) Branded summary-report PDF (every quote verified-verbatim), warm meeting-brief PDF, QR code + "Scan to meet MVP+ 🏈" card. The EA value-discovery deck (lib/pptx/eaMembershipDeck.ts, slide 11 realigned to member+non-member value-discovery + "walk the journey") stays 3 commits LOCAL/unpushed. WHY: turns a capability pitch into a live value-discovery instrument that answers Graddy's exact question in one motion. Detail in memory project_ea_mvp_demo. Product observation (not a change): agents have no built-in QR button — that lives on surveys/townhall/pulseiq via components/ui/QRCode.tsx; a QR+Download button on the agent page would be a small clean add.

## 2026-07-02 — Fix cross-tenant signal-stats leak + wire isolation tests into CI (audit remediation)

Two fixes off the full-system audit. (1) **Cross-tenant leak (HIGH):** `GET /api/datasets/[datasetId]/signal-stats` authenticated the caller but never checked their org before calling `computeSignalStats(service, datasetId)` with the RLS-bypassing service-role client — any authed user could pull another tenant's signal stats by dataset UUID. Its siblings (`theme-counts`, `signal-stats-batch`) already had the gate; this single-dataset variant was the miss. Added the standard `getCallerOrgContext` → resolve `datasets.org_id` → 404 on mismatch (admin-org bypass preserved). Added 4 gate tests to `dataset-query-routes-gate.test.ts` (the cross-org case returns 404 where the old code returned 200); full suite 1120 green. (2) **Isolation tests never gated CI:** the RLS/cross-org-egress/auth-flow suites are env-gated and self-skip, and CI ran only `test:coverage` without the flags — so the most important test class for a multi-tenant product never ran on a PR. Added an `isolation` job to ci.yml running all five suites (`test:rls`, `test:egress`, `test:auth-flows`, `test:campaign-egress`, `test:dataset-egress`) against real Supabase creds (`SUPABASE_TEST_*` repo secrets), with a fail-loud preflight: because the suites skip silently on missing/placeholder creds, the job hard-fails if the secrets aren't set — a green check now genuinely means isolation was tested, not skipped. Ran `test:rls` (4) + `test:egress` (27) locally against the prod project — both green. WHY: closes the one confirmed data-leak and the CI gap that would let the next one through. NEXT (owner): populate the three `SUPABASE_TEST_*` GitHub secrets and mark the isolation job a required check; pre-launch they hold prod values, repoint to a test project once customers exist. tsc clean. LOCAL/unpushed.

## 2026-07-02 — Audit remediation batch 2 (scalability + enterprise-readiness backlog)

Worked the rest of the full-system-audit backlog. All commit-only; three prod-touching steps were correctly blocked by the auto-mode classifier and are left for the owner (below).

**Scalability (correctness under load):**
- `datasets/[id]/rows` `all=true` + collection-union paths now **reservoir-sample while paging** (`makeReservoir`, Algorithm R) bounded by `BULK_ROWS_HARD_CAP=50000`, so the endpoint can't buffer a whole 500K-row dataset into heap when `sampleMax` is omitted (the safety limit lived client-side only). Deterministic (seeded by dataset_id) so Stats don't drift; removed the now-unused `sampleInPlace`.
- `townhall/chat` `response_counter` was a racy read-modify-write (live town hall = peak concurrency → lost updates). New `sql/144` atomic increment RPC returning the new value (feeds the every-N theme-detect trigger); code falls back to the old +1 if the RPC isn't applied yet.
- `dataset_state.analytics` JSONB was RMW'd by 3 sites (signalStats compute + invalidate, compute route) → cross-key clobber. New `sql/145` `merge_dataset_analytics` (jsonb `||`) + `delete_dataset_analytics_key`, wrapped in `lib/datasetAnalytics.ts` with a pre-migration RMW fallback; all 3 sites now touch only their own keys.
- `townhall/sessions` per-session counts were tallied from a `.in()` turn fetch capped at 1000 rows (silent undercount past 1000 turns). New `sql/146` `townhall_session_counts_for_ids` aggregates in Postgres (mirrors sql/038); route falls back to the old JS tally if the RPC is absent.
- `townhall/responses` (public unauthenticated write) now rate-limited (per-participant 10/min + per-IP 600/min), matching `townhall/chat`.

**Enterprise readiness:**
- **BYOK AI-key at-rest encryption:** new `lib/secretbox.ts` (AES-256-GCM, `enc:v1:` envelope) keyed by `AI_KEY_ENC_SECRET`. Encrypt-on-write (ai-key PUT), decrypt-on-read (`resolveOrgAiConfig`). Backward-compatible both ways: legacy plaintext still reads; if the env secret is unset it stores plaintext (== today) so deploying pre-provisioning breaks nothing. 7 unit tests. Secret generated + added to `.env.local`; **Vercel prod secret is an owner step** (classifier blocked the write — safe, encryption is inert until it's set).
- **admin_action_log coverage:** added general `recordAdminAction` (always logs, free-text resource) and wired the highest-value destructive admin ops — AI-key set/delete + org-snapshot restore (overwrites a tenant's data). Previously only org-transfer was logged.
- **serverError helper (`lib/apiError.ts`):** captures the real error to Sentry (with a `where` tag) + returns a generic `{ error: 'Internal server error' }` instead of raw Postgres/driver strings. Adopted in the `bots/*` + `ana/render-deck` routes; ~90 other routes still leak `error.message` (documented as an opportunistic migration in ENGINEERING §4). First concrete step on the structured-logging TBD.
- **Doc honesty:** SECURITY_OVERVIEW + CAIQ_LITE claimed SAML SSO "available on request" — no code exists (auth is Supabase magic-link). Reworded IAM-06 / IPY-03 / the SSO bullet to "roadmap, not implemented."

tsc clean; 1127 tests pass (added secretbox + updated signalStats persist-path assertion); sql-tx guard OK.

**⚠️ OWNER prod steps (classifier-gated, not done autonomously):** apply `sql/144`, `sql/145`, `sql/146` to prod (`supabase db query --linked --file …`), and set `AI_KEY_ENC_SECRET` in Vercel prod env. All code degrades safely until then. Plus the batch-1 items: push + make the CI `isolation` job required.

## 2026-07-02 — Fix 3 verified prod data-integrity bugs (from the Fable role-audit)

Confirmed against the live DB before fixing. All commit-only.

- **#1 Nightly per-tenant backup silently dropped ALL customer content.** `lib/orgSnapshot.ts` filtered `dataset_rows_flat` by `org_id` — a column that doesn't exist on that table (it's keyed by `dataset_id`) — so the query errored and the snapshot shipped 0 rows for it while reporting success (verified: 188,328 flat rows for one org → backed up as 0). Repointed to `parent_via dataset_id` (verified the new path reaches all 188,328). Also made `fetchTable` surface per-table fetch errors into `meta.fetch_errors`, and the cron now returns HTTP 500 (not silent `ok`) when any org's snapshot is incomplete — so a dropped table can never again pass as a green backup.
- **#2 `theme-impact` read the dead legacy `dataset_rows` table.** CLAUDE.md claims that table was removed in May 2026; it still exists with 1,582 stale rows while the real data (212,807 rows) lives in `dataset_rows_flat`, and the route discarded the query error — so key-driver regression returned empty/stale silently. Ported the read loop to `dataset_rows_flat` (with error handling), and removed the same dead `dataset_rows` fallback branches from `theme-counts`, `export/html`, `export/pptx`. Added `scripts/check-no-legacy-dataset-rows.ts` + `npm run check:no-legacy-rows` in CI to ban future legacy-table READS (deletes still allowed for teardown).
- **#3 Org hard-delete orphaned tenant data.** The route deleted only the `organizations` row assuming FK cascade. Verified the schema: ~30 org_id tables (users, conversations, townhall_sessions, town_halls, logged_questions, collections, …) have NO cascading org_id FK, so they orphaned (the audit's "no FK at all" was overstated — many DO cascade — but the orphan set is real). New `lib/orgDelete.ts` explicitly deletes every org_id table via a retry-until-fixpoint sweep (handles FK ordering without hand-sorting) and is **fail-closed**: if any table can't be cleared, the org row is NOT deleted (no silent half-erasure). Table list derived from `information_schema` (46 tables); 3 unit tests. Fixed the mislabeled audit `resourceType: 'dataset'` → `'org'`. Remaining gaps (noted, not fixed): Storage audio, S3 snapshots, and auth.users still need their own purge.

tsc clean; 1130 tests (added orgDelete ×3); check:no-legacy-rows green. No migrations (all app-level).

## 2026-07-02 — First-tier role-audit fixes: subprocessor disclosure, AI-cost logging, test-DB prep

- **#2 Subprocessor inventory corrected (doc-only).** Verified in code that **OpenAI** receives town-hall **meeting audio** (Whisper), customer text (embeddings), and social comments (Moderation), and **Deepgram** receives meeting audio (Nova-3) — neither appeared in any of the three subprocessor tables buyers cite (Anthropic was also missing from SECURITY_OVERVIEW §2, and Azure OpenAI is a configured provider). Added all of them to SECURITY.md §8, SECURITY_OVERVIEW.md §2, and DATA_FLOW.md §4 (+ the Mermaid diagram) with data types + retention/DPA status, plus a gov-deployment gate note (constituent audio → undisclosed processors). Filled out the key-rotation table (OpenAI/Deepgram/Azure/CRON_SECRET/META_APP_SECRET/AI_KEY_ENC_SECRET/webhook secret were missing).
- **#3 AI usage logging — drop + unlogged holes.** `logUsage`/`logFlatCost` inserts now go through `waitUntil` (`lib/usageLog.ts`) so they don't get dropped on Fluid Compute's response-flush (was systematically under-counting spend under load — the biggest COGS-accuracy lever since it covers all ~92 logged call sites). Added `usage:` to the ~7 helper `callAI()` sites that had none (projectReport, projectCompare, entityAnalysis, nameExtractor, reoExtractor, recordings/setupExtract) so their spend is captured — closing the "unlogged AI spend" finding. Documented what's still open: those helpers log with `org_id = null` (no org in scope) so per-org attribution + off-mode gating for them still needs org threading; `embeddings.ts` still bypasses callAI; and the RATES table is a 14-month-old May-2025 snapshot (refresh before quoting COGS). USAGE_ACCOUNTING.md §9 corrected (it claimed only entity-analysis-deck was unlogged).
- **#1 (CI/tests write to prod DB) — decision + prep.** Owner chose a paid Supabase test project. Can't create it (billing = owner dashboard action), so shipped the bootstrap: `scripts/bootstrap-test-db.sh` (schema-only dump of prod → apply to the test project's connection string) + a runbook in TESTING.md. Owner steps: create project → run bootstrap → repoint the 3 `SUPABASE_TEST_*` secrets from prod to the test project.

tsc clean; 1130 tests (usageLog suite green). No migrations.

## 2026-07-02 — #3 follow-through: org attribution/gating on helper AI calls + RATES refresh

- **3a — org threading.** The ~7 helper `callAI()` sites that were logging without `org_id` (so they attributed to null and bypassed off-mode/BYOK gating) now thread `org_id` from their calling routes: projectReport + projectCompare (from the collection owner via `projectReportLoad`), entityAnalysis (`canonicalize`/`categorize` from the export route; the admin entity-analysis-deck stays org-less by design), nameExtractor (`bot.org_id` via chatCore), recordings/setupExtract (from the extract-setup route). Left intentionally org-less: reoExtractor (admin-only internal tool). `embeddings.ts` still bypasses callAI entirely (separate follow-up).
- **3b — RATES refreshed (`lib/usageRates.ts`).** Verified Anthropic pricing against the claude-api reference. Prior "May 2025" table had **Haiku under-priced** ($0.80→$1.00) and **Opus at $15/$75** (old Opus-3-era) when current Opus 4.x is **$5/$25** — so recording-analysis cost (hardcoded Opus) had been logged **~3× too high**; since cost is derived from stored tokens at display time, the fix corrects historical rows too. Added Opus 4.8 + Fable 5; changed the `estimateCost` unknown-model fallback from Haiku (cheapest) to Sonnet (standard) so new models aren't silently under-priced.

tsc clean; 1130 tests. USAGE_ACCOUNTING synced. No migrations.

## 2026-07-02 — Observability foundation + silent-failure captures (high-list #1/#2)

- **`lib/log.ts` structured logger.** `logError(where, err, {orgId, …})` emits a structured console line + captures to Sentry tagged with `where` / `request_id` / `org_id`, so a tenant's `x-request-id` (already stamped on every request by proxy.ts — the earlier "proxy only does CSRF" note was wrong) joins to both logs and the Sentry event. Best-effort, never throws, safe un-awaited. `serverError` (lib/apiError) now routes through it and is `async` — transparent to `return serverError(...)` call sites — so every serverError site gets request/org tags for free. Closes the observability half of Open TBD #12/#21. 6 unit tests.
- **Silent-failure reads — worst sites.** The audit found ~500 Supabase calls that discard `error` and proceed with `||[]`, returning HTTP 200 with wrong numbers and no trace (exactly how theme-impact/signal-stats hid). Wired `logError` into the highest-value swallows: agent-card counts (`bots.list.*` — a failure would paint every card 0), study-stats refresh (`studies.responses.refresh_stats` — the "my report is wrong" case), and dataset-teardown `dataset_rows_flat` deletes (orphan risk in the sole-source table). Documented in ENGINEERING §4 as the standard; remaining ~500 `{data}`-only reads are an opportunistic migration.

tsc clean; 1135 tests (+ log.test ×6). ENGINEERING + TESTING synced. No migrations.

## 2026-07-02 — Formal freeze on the legacy PulseIQ orchestrator (high-list #3)

The audit's structural finding: the ~1075-line legacy town-hall chat route keeps gaining features (round-based tasting mode was built only there) while the `chatCore` convergence (Phases 4–6) is stalled behind a dark flag — every legacy-only feature widens the parity gap. *Finishing* the migration is a separate multi-week project; what's actionable now is stopping the gap from widening. Added a formal **freeze policy**: docs/CONVERGENCE.md §4.1 (no new features on the legacy orchestrator — land them in chatCore or defer; round-based pacing must be ported before the next feature), a ⚠️ header warning on `app/api/townhall/chat/route.ts` where an editor will see it, and a line in CLAUDE.md so future sessions honor it. Bug/security fixes to the legacy path stay allowed. Doc/comment-only, no behavior change.

## 2026-07-02 — Medium tier: embeddings COGS + org-delete Storage/auth purge

- **embeddings.ts now logged (last AI-cost blind spot).** `lib/embeddings.ts` makes its own OpenAI call (bypasses callAI), so it was invisible to accounting. It now `logUsage`s the response `prompt_tokens` (`resource_type:'system'`, `event_type:'embedding'`, model `text-embedding-3-small`, provider `openai`) with the caller's org_id. Added the rate to `RATES` ($0.02/1M input). USAGE_ACCOUNTING synced.
- **Org-delete extended beyond Postgres.** `deleteOrgStorage` removes Supabase Storage objects under the `<org_id>/` prefix (`org-logos` + the `recordings` media bucket, recursive list→remove), and `purgeOrgAuthUsers` deletes the org's `auth.users` (ids collected before the sweep clears `public.users`). Both best-effort — DB data is already erased, so a partial failure surfaces as `warnings` rather than stranding the org row. S3 org-snapshots stay a documented break-glass purge (backup IAM has no s3:DeleteObject by design). SECURITY.md §7 TBD-5 updated.

tsc clean; 1135 tests (one flaky one-off, stable on re-run). No migrations.

## 2026-07-02 — Migration ledger (schema_migrations)

The repo had ~146 hand-applied SQL files with no record of what's applied where — "applied to prod but not committed" was undetectable. Built the tooling: `sql/147` creates a `schema_migrations` ledger table (RLS: platform-admin read, service-role write, mirrors admin_action_log); `scripts/apply-migration.ts` (`npm run migrate <file>`) applies a migration AND records filename/sha256/applied_by; `scripts/migrations-status.ts` (`npm run migrate:status`) diffs sql/ files vs the ledger and reports the three drift classes (committed-not-applied, sha-changed, applied-but-gone), with `--backfill` to seed the ledger once. ENGINEERING §3 updated. **Owner bootstrap:** apply sql/147 → `tsx scripts/migrations-status.ts --backfill`; then migrate:status is a CI candidate. tsc clean.

## 2026-07-02 — eslint 9 flat config live in CI (medium-tier #4, was blocked)

Unblocked the npm cache (root-owned `~/.npm` → pointed npm at a fresh user-owned `~/.npm-cache`, no sudo needed). Then migrated: removed the peer-conflicting `@typescript-eslint/eslint-plugin@7`, installed eslint 9 + typescript-eslint 8 + eslint-config-next 16 + @eslint/eslintrc 3. Replaced `.eslintrc.json` with `eslint.config.mjs` (native next flat config + typescript-eslint type-aware `projectService`, same rules at warn). `"lint"` → `eslint .`. Baseline over the repo: **0 errors, 3870 warnings** (2864 no-explicit-any, 329 no-misused-promises, 204 no-floating-promises, 187 consistent-type-imports, + react-hooks). eslint-plugin-react-hooks@6 (bundled with next 16) added 165 ERROR-level violations (set-state-in-effect etc.) over the god-components — demoted to warn so they ride the ratchet. **CI now runs `npm run lint:ci` (`eslint . --max-warnings 3900`)** — green today, fails only if the count climbs; ratchet the ceiling down as warnings are burned off, promote a rule to error at 0. Verified: exit 0 at 3900, exit 1 at 3800 (ratchet bites). tsc clean, 1135 tests. ENGINEERING §1 TBD-10 closed.

## 2026-07-02 — Deferred-list marathon 1/4: error.message leak sweep (all of app/api)

The audit left "~90 routes returning raw error.message" as opportunistic; actual count at sweep start was worse (165 files matching 5xx + .message). Swept ALL of app/api with six parallel agents on disjoint directory slices: every 5xx JSON body that embedded a caught exception's / Supabase error's message now goes through `serverError(err, 'scope.action', {orgId})` — Sentry capture (where/request_id/org_id tags) + generic client body. **284 conversions across ~150 route files**; 4xx bodies and static-literal 500s untouched. Deliberate survivors, each a real product decision: BYO-API-key routes forward upstream Anthropic auth/quota detail (insights/expand-keywords/merge-themes/mine-themes), `bots/fetch-url` SSRF-guard copy, substack fetch-posts wizard feedback, cron org-snapshot diagnostic body, admin erasure-incomplete table list, SSE errors written into streams. One inline special case: the public ChatBot rehydrate route (`bots/[id]/session/[sessionId]/turns`) needs CORS headers serverError can't carry — logError + generic body inline. Known status-shape change: a handful of former 502s now return 500 (serverError is fixed at 500); consumers are cron/internal. ENGINEERING §4 rewritten to match reality (serverError adopted repo-wide; lib/log.ts exists — stale "no logger exists" text corrected). WHY: raw Postgres/driver strings to clients are an info leak, and handler-caught errors never reached Sentry — this closes both, everywhere, instead of "when touching a route". tsc clean (fresh), 1135 tests, lint 0 err/3880 warn (≤3900), zero residual dynamic-message 5xx by grep.

## 2026-07-02 — Deferred-list marathon 2/4: silent error-swallow capture (all of lib/)

Second deferred item: ~500 `{data}`-only Supabase reads discard `error` and proceed — wrong counts/missing report sections with zero trace. Principled cut: **all of lib/ (166 sites, 46 files)** — shared logic feeding reports/chat/sync where silent failure becomes wrong customer output — leaving route-level reads (~600) opportunistic per the original audit judgment. Every fix is log-only: destructure `error` (aliased), `if (err) void logError('file.fn', err, {orgId})` — ZERO control-flow change, so behavior is identical except failures now land in Sentry with tenant tags. Server-only import safety verified per file (importer trace; no client-bundle exposure). Noise engineering: expected-miss `.single()` sites (chatCore persona/demographics, projectReportLoad dataset_state, agentReadout cache) guard `code !== 'PGRST116'`; `auth.getUser()` sites skipped entirely (AuthSessionMissing fires on every anonymous request). One caveat: legacy `scripts/verify-phase5c6.ts` (self-described deletable) now needs `node --conditions=react-server` to run since townHallAdapter transitively imports server-only lib/log. WHY: the audit's three verified prod data bugs (backup shipping 0 rows for months!) were all silent-swallow class — this makes the next one visible in Sentry instead of invisible for months.

## 2026-07-02 — Deferred-list marathon 3/4: data-room docs honesty pass

Third deferred item: re-read SECURITY_OVERVIEW / CAIQ_LITE / DATA_FLOW for overclaims, verifying each claim against code. Fixed (all were stated as live capability): (1) "quarterly DR restore drill, result logged" — no drill has ever run (and the 0-row backup bug proves it); now states capability + first-drill-scheduled honestly, CAIQ BC-04 Yes→Roadmap. (2) "guest emails stored encrypted with tenant-scoped key" — campaign_respondents.email is plaintext TEXT; claim removed, replaced with the real app-layer encryption example (BYOK ai_api_key AES-256-GCM). (3) "protected-branch settings prevent direct push" — false (solo operator commits to main); CCC-02 now Compensating with the honest control set. (4) admin_action_log coverage list claimed exports/route-access logging it doesn't do — scoped to the real coverage (org lifecycle, AI-key, snapshot restore, transfers). (5) DATA_FLOW diagram + actors still said SSO — removed (roadmap). Also two UNDERclaims corrected upward: Sentry PII scrubbing is real (lib/sentryScrub.ts wired in all three configs, unit-tested) — docs said "in progress/roadmap"; DR scratch project now actually exists (today's Sentimetrx-Test). CAIQ scoring updated 68/9 → 67/10. WHY: buyer DD that catches one fabricated claim discounts the whole data room.

## 2026-07-02 — Deferred-list marathon 4/4: W27 governance mediums closed

(1) bots/[id] write-path audit (flagged W25+W27): verified PATCH/DELETE pair id+org_id for non-admins with defense-in-depth org pre-checks; admin bypass is the intentional cross-org capability. No change needed — finding closed as verified-by-design. (2) Coverage thresholds ratcheted 15/10/17/15 → 20/15/20/20 (actual post-sweep: 23.6/16.6/23.5/24.2; gate verified green). (3) README note on the xlsx SheetJS-CDN dependency (403-on-clean-install risk + local-tarball workaround + exceljs as tracked long-term option). WHY: closes the W27 progression plan without waiting for Monday's cycle.

## 2026-07-02 — Lint burn-down batch 1: consistent-type-imports 187→0, rule promoted to error

`eslint . --fix` auto-fixed 172 sites (imports split into `import type`); the 15 non-fixable inline `import('module').Type` annotations hand-converted to proper top-of-file type imports (mostly `ModuleFeatures` in client components). Rule promoted `warn`→`error` in eslint.config.mjs per the ratchet doctrine (0 warnings → error, can never regress). Ceiling ratcheted 3900→3700 (actual 3678). Also codified the **touch-it-fix-it convention** for the ~2,900 `no-explicit-any` backlog in CLAUDE.md + ENGINEERING §1: no new `any`; substantively editing a file means fixing the `any`s you touch in the same commit; ceiling drops as the count does. CLAUDE.md test section also updated: isolation suites now run against the dedicated Sentimetrx-Test project (stale "until a test project exists" note removed). Verified: fresh tsc clean, 1135 tests, lint:ci green at 3700. NEXT: promise-warning batch (329 misused + 204 floating, 118 files, all client components).

## 2026-07-02 — Lint burn-down batch 2: promise warnings 533→0, both rules promoted to error

All 329 `no-misused-promises` + 204 `no-floating-promises` fixed across 118 files (six parallel agents, disjoint slices) with a strictly zero-behavior policy: async JSX handlers wrapped `() => { void handler() }` (event params preserved), inline async arrows → void-IIFEs, floating statements prefixed `void ` — NEVER `await` (timing unchanged), no `.catch`, no restructuring. Extra care on public widgets (ChatBot, PulseIQ chat, /s, /b) and the frozen-adjacent chatCore fire-and-forgets. Both rules promoted warn→error; with consistent-type-imports that's 3 of the 4 typescript-eslint rules now un-regressable. Ceiling 3700→3170 (actual 3145 — now ~91% no-explicit-any, burned via the touch-it-fix-it convention, plus ~250 react-hooks god-component items). WHY: these two rules were the only warning classes that can hide real failures (unhandled rejections, lost errors in handlers) — the same disease as the silent-swallow class fixed earlier today; now new instances hard-fail CI. Verified: fresh tsc clean, 1135 tests green, lint:ci green at 3170.

## 2026-07-02 — Town-hall convergence unfreeze scoping (CONVERGENCE §4.2)

Owner asked whether to knock off the frozen town-hall refactor while prod has zero users — answer: yes, this is the cheapest window it will ever get. Scoped the actual gap by diffing the legacy orchestrator (1,085 lines) against chatCore's town-hall layer and verifying prod state: prod flags all OFF (legacy path serves 100% of traffic; new schema gets zero prod writes — the Tier-5 verification clock never started); legacy prod data is 1 session / 511 turns, so greenfield still holds. Good news: more is already rebuilt in chatCore than the freeze note implied (topic pool, cohort counts, pickNextTopic, standby, theme-detection triggers, mirroring w/ sentiment+content_en). The verified port list is 7 items — rounds pacing, session lifecycle, /pi client protocol adapter (+ the townhall_turns write-back decision), language switch + translation, strike guard, opening topic match, and the deterministic clarifier/disengagement engine (the big one, and a product decision: hybrid port as code caps + prompt hints). Tranche 1 (engine cutover + PulseIQ regression harness + flag flip + one real test town hall) ≈ 4–6 sessions; tranche 2 (rewire the 37 legacy-table readers, drop tables, Tier 5) ≈ 3–5 more, deferrable. Full table in CONVERGENCE §4.2.

## 2026-07-03 — Convergence item 0: pulseiq_* table rename approved

Owner tripped on the townhall_*-means-PulseIQ naming collision and asked if it's too late to fix. It isn't — but only because the new-schema tables (town_halls / town_hall_topics / town_hall_conversations) still have ZERO prod writes (flags off). Added as item 0 of the tranche-1 port list (CONVERGENCE §4.2): rename to pulseiq_events / pulseiq_topics / pulseiq_event_conversations BEFORE items 1–7 build on the old names. Legacy townhall_* tables deliberately NOT renamed (they're deleted in tranche 2). Code identifiers + /townhall slugs rename opportunistically during the port. WHY: the permanent PulseIQ schema must not carry the recordings product's name — this is the last near-zero-risk moment.

## 2026-07-03 — Convergence item 0 SHIPPED: pulseiq_* table rename (sql/148, applied)

The permanent PulseIQ schema no longer wears the recordings product's name: town_halls → pulseiq_events, town_hall_topics → pulseiq_topics, town_hall_conversations → pulseiq_event_conversations. sql/148 renames tables + indexes + RLS policies and leaves security_invoker compat VIEWS under the old names (bots→agents sql/079 precedent) so the deployed prod build — which still references old names — keeps working until the next release; views die in tranche 2. NO data moved (rename-in-place; 3 dev rows; legacy townhall_* tables holding real history untouched). townhall_participant_responses (the two-substrate bridge, 0 rows) keeps its name; its FK survived the rename by OID. Column names (town_hall_id) deliberately kept — compat views must expose identical columns. 170 code refs swept with word-boundary replace (town_hall_id untouched). Applied to BOTH prod and Sentimetrx-Test. Verified: fresh tsc clean, 1135 tests, rls 4/4 + egress 27/27 against the renamed schema, prod rows readable via both old and new names. Next: item 1 (round-based pacing → chatCore).

## 2026-07-03 — Convergence item 1 SHIPPED: round-based pacing in chatCore (sql/149, applied)

The last legacy-only PulseIQ feature is now on the unified engine. sql/149 ports round_number to pulseiq_topics (prod + test project; compat view refreshed). chatCore's town-hall all-covered branch now checks cohort_config.pacing_mode='rounds' → any later paused seed topics → round-hold standby (prompt-hint style, so the model still acknowledges the participant) + roundHold on the result; the Phase-4 shim returns round_hold to the /pi client, which already reads it. Two things the port surfaced: (1) VOCABULARY GAP — legacy topics use source='guide', the new schema CHECK-enforces 'seed'/'auto_detected'/'manual'; my first cut filtered on 'guide' and would never have matched — caught by live verification, mapping now documented in CONVERGENCE §4.2 for items 2–7. (2) The turn-mirror to conversations is fire-and-forget, so cohort counts can lag a turn — noted for item 3's write-back design. Verified LIVE on Sentimetrx-Test: seeded 2-round tasting event, 3-turn lifecycle passes (round-1 topic injected → hold with warm copy while round 2 paused → moderator advance → round-2 topic picked), real AI replies, seeded org cleaned up. tsc clean, 1135 tests, lint:ci green. Next: item 2 (session lifecycle: auto-end, turn caps, closing message).

## 2026-07-03 — Convergence item 2 SHIPPED: session lifecycle on the unified path

The Phase-4 shim now carries the legacy orchestrator's lifecycle: timed + inactivity auto-end (flips pulseiq_events to closed + stamps ended_at), closed → cohort_config.closing_message + is_final, paused → hold reply, and the per-participant turn cap (cohort_config.max_turns_per_participant, default 20) → wrap-up. chatCore's round hold gained the legacy turns-budget guard (hold only with ≥2 turns of budget left). The big catch this item surfaced: the shim's history read was pointed at townhall_turns, which the unified engine NEVER writes — every request looked like turn 1 (the engine had no memory). Reading the async, DUAL_WRITE-gated conversations mirror would race; the correct source is bot_conversation_turns, chatCore's synchronous store — now wired. Verified via the REAL route handler (POST /api/townhall/chat imported directly) against the test project: 5/5 — multi-turn memory ("What was my name?" → "Zebulon! 😊"), paused, closed w/ custom message, timed auto-end actually flips the row, turn cap closes at N. Flag checklist note: inactivity auto-end reads event-wide activity via the mirror, so cutover needs DUAL_WRITE_PHASE3=true in prod. scripts/_verify_lifecycle.ts kept untracked as the seed of the PulseIQ regression harness. tsc clean, 1135 tests, lint:ci green. Next: item 3 (client protocol adapter + write-back decision).

## 2026-07-03 — "PulseIQ sessions" language + discard decision reshapes the convergence (sql/150, applied)

Owner declared the product noun: **PulseIQ sessions** — so the just-renamed tables align once more while still near-empty: pulseiq_events → pulseiq_sessions, pulseiq_event_conversations → pulseiq_session_conversations (sql/150, prod + test, compat views re-pointed; 221 refs swept across code/specs). Both live verifications re-run green post-rename (round-hold 3-turn lifecycle + route-level lifecycle 5/5). Bigger: owner confirmed zero active PulseIQ sessions AND legacy PulseIQ data is discardable → CONVERGENCE item 3 collapses from "adapter + townhall_turns write-back layer" to "modernize the /pi client to the unified protocol directly"; tranche 2 becomes "port creation/console/exports onto the new substrate, then DELETE legacy tables + compat views + the 1,085-line orchestrator." Explicit boundary recorded: AGENT data (agents, bot_conversation_turns — Sarina/Mason/mvp-plus) is not part of any of this and never migrates; the discard applies only to townhall_* legacy tables, executed last, after the new path is proven.

## 2026-07-03 — Convergence item 3 SHIPPED: /pi client protocol live on the unified substrate (zero client changes)

Turned out smaller than scoped: join + responses were already substrate-aware via townHallAdapter (Phase-4/5 work had quietly covered them). What remained: (1) chat shim skip/done — skipped stores the legacy '[Skipped — participant declined…]' marker as the user turn (transcript-compatible AND tells the model to move on), '[done]' short-circuits to cohort_config.closing_message + is_final; (2) the resume endpoint (round-hold poller) gained a full unified-substrate branch — resolves pulseiq_sessions, history/turn-budget from bot_conversation_turns (synchronous store), topics from pulseiq_topics, cohort counts + discussed-set via the conversations mirror, and the served round question is written to the synchronous store then mirrored WITH its topic tag so cohort counts advance; item-aware lead-in ("Now for the Chardonnay — …") reads cohort_config.rounds[]. TownHallChat.tsx needed ZERO changes — the existing wire protocol now just works. Live-verified via real route handlers on the test project, 5/5: skip, done, hold, moderator-advance → round-2 serve with lead-in, and the served turn threading into the next chat's history (6 messages in the AI call). scripts/_verify_item3.ts joins the untracked regression set. tsc clean, 1135 tests, lint:ci green. Next: item 4 (language switch + translate-to-English).

## 2026-07-03 — Convergence item 4 SHIPPED: language switch + translate-to-EN on the unified path

Language switch now works on the unified substrate: the shim runs the shared detector (fast regex → AI classifier ≥95%) on short messages, confirms bilingually, replays the previous bot message translated, returns localized skip/done labels, and stores the exchange with the legacy '[Language switch: xx]' marker in both stores (doesn't count as a conversational turn). Translate-to-English deliberately went into chatCore's STORAGE block, not the shim — so 1:1 agents (Sarina's Spanish speakers) get it too: non-English user turns now carry content_en (3s-capped best-effort translate) and sentiment is scored on the translation, since the lexicon is English. Verification caught a real shared-lib bug: models now fence their JSON classifier verdicts (```json … ```), so EVERY AI-detected language switch was silently failing — including on the legacy path in prod. Fixed in lib/languageSwitch (strip fences before parse). Live-verified 4/4 on the test project: es switch with labels, marker stored, Spanish conversation flows, content_en + sentiment land on the stored turn. tsc clean, 1135 tests, lint:ci green. Remaining tranche-1: item 5 (strike-guard parity), item 6 (opening topic match), item 7 (clarifier engine — the big one).

## 2026-07-03 — Convergence item 5 SHIPPED: strike-guard parity (and two real agent-side bugs)

chatCore's content guard reached full legacy parity, and the item surfaced two genuine pre-existing bugs on the AGENT side: (1) strikes were keyed 'cbot_'+ip — at a venue where 200 participants share one NAT'd wifi IP, one heckler's strikes would have silenced the entire room (now per-participant in town-hall mode); (2) per-agent content-safety toggles (bot.config.content_safety, set via ContentSafetyEditor) were NEVER passed to checkMessage — agent operators' configured toggles were silently ignored (now merged: defaults ← agent config ← session cohort_config override). Also ported: legacy toneNudge (borderline-heated messages get a calm/de-escalate system hint instead of a block), violation audit trail ([filtered] + warning turns persisted to both stores), and the shim now propagates strike-shutdown as is_final + shutdown so the widget locks. Live-verified 5/5 on the test project incl. same-IP participant isolation and escalation-to-shutdown. tsc clean, 1135 tests, lint:ci green. Remaining tranche-1: item 6 (opening topic match), item 7 (clarifier engine).

## 2026-07-03 — Convergence item 6 SHIPPED: opening-response topic matching

The participant's first message now routes the conversation onto the topic they actually opened with. chatCore's town-hall layer runs a fast classification call (topic list + opening response → {"topic_number": N}, fence-tolerant JSON, 3s cap, best-effort) on first-turn messages; a match overrides the fewest-responses default and swaps the TOPIC FOCUS instruction to "build on their words, don't re-ask the question". Cleaner than the legacy matchResponseToTopic, which generated the follow-up inside the classification call — here the main engine still writes the reply, so persona/guardrails/language/toneNudge all apply to it. No-match falls through to pickNextTopic (keyword smart-probe = the legacy fallback). Live-verified 3/3 with a designed control: parking was seeded as the MOST-responded topic (picker would avoid it) and the parking complaint still landed there via the semantic match, while an unrelated "hi there!" fell through to fewest-responses. tsc clean, 1135 tests, lint:ci green. Tranche 1 remaining: item 7 only (clarifier/disengagement engine).

## 2026-07-03 — PulseIQ language hygiene in chatCore (owner directive)

Owner: the two products are now fully separated in his head — "town hall" must only ever mean the recordings product, in conversation, docs, everything. Sweeping chatCore's PulseIQ-layer strings surfaced a PARTICIPANT-VISIBLE leak: the standby instruction told the model "All discussion topics for this town hall have been covered…" — the model could echo "town hall" to a participant at, say, a restaurant tasting. Fixed: prompt copy now says "session"/"group discussion"; section markers PULSEIQ TOPIC FOCUS / PULSEIQ STANDBY; debug + log strings and comments follow. Memory rule escalated (feedback_product_naming): the naming discipline now covers conversation and internal docs, not just user-facing surfaces. Code identifiers (townHallContext, /townhall routes, TOWNHALL.md) still rename opportunistically as work touches them. tsc, 1135 tests, lint green.

## 2026-07-03 — Convergence item 7 SHIPPED: the facilitation policy — TRANCHE-1 PORT LIST COMPLETE

The clarifier/disengagement engine — the reason PulseIQ conversations feel facilitated rather than chatty — now lives in chatCore's PulseIQ layer, counting rules ported rule-for-rule as BINDING code decisions (the model executes, it doesn't vote): move-on regex, word-count trajectory decline, curt/skip-overload signals, frustration-aware clarifier caps (2, dropping to 1), dynamic per-topic caps (2–4 by answer substance), budget/coverage-aware word thresholds (12/8/10), 60% seed-budget organic preference, and the global-checkout chill standby. Per owner decision the constants became per-session knobs (cohort_config.pacing.*) with legacy values as defaults — a tasting session can now run max_clarifiers=0 by config, verified. The legacy bolt-on AI tone-check call became its own conservative default (subtle-disengage → move on, exactly the bias the legacy prompt demanded) plus tone guidance to the main engine. Clarifier turns are stamped source='clarifier' in both stores so caps count across requests. One faithful-port subtlety caught live: legacy stored the current answer BEFORE deciding, so all rolling signal windows include the message being handled — first cut counted only stored history and global checkout never fired; fixed. (Also found legacy's trajectory signal was mathematically dead code — it compared the current answer against itself; the port implements it as designed.) Live-verified 7/7 on the test project (clarifier fires/stamps/caps, move-on, rich-answer pass-through, checkout standby, knob=0) + items 3/6/lifecycle regression re-run green. 1:1 agent behavior untouched (everything is inside the townHallContext gate). tsc clean, 1135 tests, lint:ci green. Tranche 1 remaining: consolidate the regression harness, prod flag flip, one real end-to-end PulseIQ session (Phase 6 acceptance).

## 2026-07-03 — Tranche 2 unit 1: new PulseIQ sessions are BORN on the new substrate

The creation lynchpin: POST /api/townhall/sessions now creates a dedicated agent + a pulseiq_sessions row (status draft) instead of a legacy townhall_sessions row. The wizard config becomes cohort_config with the keys the unified engine reads lifted to top level (max_turns_per_participant, standby/chill messages, theme-detection cadence); the adapter's spread-projection keeps every legacy-shaped reader working. Slug validated for uniqueness across BOTH substrates (join/chat resolve either); generated from the name when absent (pulseiq_sessions requires one); agent cleanup on failed session insert. Discovery that shrank this unit dramatically: the console backend (sessions/[id] handlePhase3Patch) was ALREADY dual-substrate — status changes with seed-on-activate into pulseiq_topics, guide sync, restart, delete_participants all handled since Phase 5; only slug-edit/org-transfer remain legacy-only. Two cutover-safety changes ride along: (1) the unified chat path is now UNCONDITIONAL (TOWNHALL_VIA_AGENT_HANDLER dark-launch flag retired — an env mistake must not strand new sessions); (2) PulseIQ turn-mirroring is unconditional when townHallId is set (the mirror is the PRIMARY analytics store for new sessions, not an experiment; agent-side dual-write stays flag-gated for Tier 5). Verified: items 3+7 live scripts re-run green with ZERO env flags set — the flag-flip step just disappeared from the cutover checklist. Gate test updated for the two-insert shape (org-scoping asserted on both). tsc clean, 1135 tests, lint:ci green. Remaining tranche 2: exports/analyze/themes/round/live/status/search/duplicate readers + list-GET legacy leg removal + legacy deletion (orchestrator, tables, views, adapter).

## 2026-07-03 — Tranche 2 unit 2: fourteen legacy readers ported to the new substrate

WHY: with creation on pulseiq_sessions (unit 1), every facilitator/participant surface still reading townhall_* would show new sessions as empty — so the reader sweep is what makes the new substrate actually visible. Ported this unit: sessions list GET (legacy leg + sql/146 RPC fallback dropped — the adapter list is now the sole source), status (public; deletion candidate — no in-repo consumer, /pi polls join), live presenter (biggest piece: rebuilt at FULL legacy parity on conversation_turns — per-topic keyword matching, top-keywords, Trending Now, stored-sentiment-with-lexicon-fallback, skip counting via the [Skipped …] marker convention; the direct-PostgREST hack retired with the legacy leg), round advance (pulseiq_topics, source='seed'), themes moderation + themes/custom (legacy fallbacks deleted), conversation search (hall→links→turns ILIKE with marker exclusion), duplicate (copies the dedicated agent + inserts a draft pulseiq_sessions row, mirroring unit 1's two-insert shape; topics reseed on activate), favorites route + page, share (create-gate + public snapshot via getTownHallAsLegacy + one turn-text pass for avg-words), /m monitor, theme-detection cron (legacy scan dropped; lib/townhallThemeDetection survives only for the frozen orchestrator), admin usage name-lookups, admin org counts, agent-tester (cohort_config + pulseiq_topics with both state vocabs). Deliberately NOT touched: lib/orgSnapshot + lib/orgDelete keep their townhall_* entries until the tables physically drop (legacy rows are the rollback path), and join/resume/responses/chat shims ride until the orchestrator delete. townhall_participant_responses confirmed dual-substrate (sql/083) — it survives the drop. Verified: new scripts/_verify_tranche2.ts 5/5 live vs the test project (status visibility gate, live analytics with keyword-matched theme, search or-ilike, adapter list counts) + lifecycle 5/5 + item-3 5/5 regressions; mutation-gate tests updated to the new tables; fresh tsc clean, 1135 tests, lint ceiling ratcheted 3170→3160 (actual 3134, −11 net after typing the rewritten routes). Remaining tranche 2: sessions/[id] detail analytics pipeline + analyze + both exports, then the deletion commit (orchestrator, townhall_* tables, compat views, adapter dual-reads, snapshot/delete entries).

## 2026-07-03 — Tranche 2 unit 3: exports + analyze off the legacy tables

WHY: the export surfaces (CSV/XLSX/themes/json + PPTX deck) and the Ana analyze sync were the last big READ pipelines still sourcing townhall_turns — new-substrate sessions would export empty. Two new adapter projections carry the whole unit: fetchTurnsAsLegacy (conversation_turns → the legacy townhall_turns row shape; user turns pair with the preceding assistant turn, [Skipped …]/[filtered] markers become skipped=true, trailing unanswered questions emit with null user_message so the open question still shows) and fetchTopicsAsThemes (pulseiq_topics → legacy theme shape incl. round_number). Both export routes now resolve pulseiq_sessions only (uuid or slug) and the sheet/slide builders below are UNCHANGED — the projection preserves their input contract. The export json branch dropped its legacy seeding loop (turns is itself a conversation_turns projection now, so seeding would double-count every exchange); the Gap-#5c pairing block with flags/sentiment/personas is the sole source. Post-session responses read by town_hall_id (sql/083's phase-3 column). Analyze deleted runLegacyAnalyze + its insert helper (~140 lines) — the phase-3 path is the only path. One substrate-semantics note surfaced by live verification: on the unified path the participant speaks FIRST (the opener is client-rendered, not stored), so a conversation's first user turn legitimately has no paired bot_message — the harness assertion was wrong, not the code. Verified: _verify_tranche2.ts extended to 7/7 (adds pairing/skip-marker/theme-label + seed→guide projection checks), fresh tsc clean, 1135 tests (2 gate-test mocks repointed at pulseiq tables/adapter), lint ceiling 3160→3145 (actual 3120 — deleting the legacy analyze leg shed more warnings than the new adapter code added). Remaining tranche 2: sessions/[id] detail analytics pipeline, then the deletion commit (orchestrator, townhall_* tables, compat views, adapter dual-reads, snapshot/delete entries).

## 2026-07-03 — Database schema snapshot + data dictionary + architecture decision record

WHY: owner flagged the disaster-recovery/DD gap — no single authoritative schema artifact existed (prod DB was the only complete truth; sql/ history has gaps and is not cleanly replayable, per the bootstrap-test-db script's own warning). Closed with a GENERATED artifact, not a hand-written one: docs/db/schema.sql (7,786-line schema-only supabase db dump — 78 tables/106 policies/12 views/61 functions; psql -f recreates the empty database exactly), refreshed automatically by npm run migrate (best-effort, never masks a successful apply) + standalone npm run schema:snapshot. On top: docs/DATABASE.md — human data dictionary (every table's purpose grouped by domain, scoping/RLS conventions, legacy markers, views/functions explained) wired into the spec-drift map (sql/*.sql → DATABASE.md); and docs/ARCHITECTURE.md — 12-entry decision record (Decision/Why/Consequences) capturing the load-bearing choices: shared-schema tenancy, JSONB row blobs (one table, heterogeneous surveys), config-as-JSONB, unified chat substrate, two-tier turn storage, 50K full-data threshold, serverless-only, public slug widgets, AI cost accounting, dual branding, mock+real-DB test strategy, migration ledger + compat-view renames. CLAUDE.md migration instructions now route through npm run migrate.

## 2026-07-03 — Privacy notice shipped + trigger-mapped compliance checklist

WHY: owner asked whether the engineering audits had glossed over SSO/GDPR — review confirmed those were honestly DOCUMENTED as gaps (SSO reworded to roadmap, GDPR posture written) but nothing operationalized, and surfaced one genuinely new finding: the public widgets collect PII from the public with NO privacy notice anywhere in the app — the only compliance item already triggered today. Shipped: /privacy (app/privacy/page.tsx, static, no client JS) — honest controller/processor framing (the inviting org controls respondent data; Datanautix LLC processes), real subprocessor list matching SECURITY §8, US-residency disclosure, real contact (info@datanautix.com — already user-facing in agent deflections), truthful cookie claim (localStorage session id only, no tracking scripts — verified). Linked from every respondent surface: survey widget header wordmark + thank-you/closed footers (kiosk attract omitted — whole screen is a tap-to-begin button, anchor-in-button invalid), agent ChatBot header under the DATANAUTIX wordmark, PulseIQ chat header. Entity name corrected to Datanautix LLC per SECURITY_OVERVIEW (almost shipped "Inc."). Render-QC'd live: /privacy 200 with correct content, link present on /b/mvp-plus. And docs/COMPLIANCE.md: T0–T4 trigger-mapped checklist (already-triggered / first paying / first EU signal / first enterprise-gov / first DSR) consolidating the scattered TBDs — SSO not-built, missing DPAs (OpenAI+Deepgram audio = hard gov gates), DPIA, EU residency, DSR tooling, DR drill — each row pointing at its source doc. CLAUDE.md + SECURITY §7 + BOTS/SURVEYS/TOWNHALL specs updated. tsc clean, 1135 tests, lint green.

## 2026-07-03 — Spec-drift guard now enforces the schema snapshot

Committing any sql/*.sql migration now blocks unless docs/db/schema.sql is staged too (specMap entry) — the npm-run-migrate auto-refresh only protects the repo if the refreshed snapshot actually lands in the same commit.

## 2026-07-03 — One-off script cleanup: scripts/oneoff/ archive

WHY: 80 underscore-prefixed one-offs had accumulated — 65 committed loose in scripts/ (Vindman KB corpus SQL, Mason/Hope/MCO creators, NOWOCATS ingests, deck/brief generators, QC probes) mixed with operating scripts, plus 14 untracked. Moved all shipped one-offs (63 tracked + 7 untracked-now-committed, incl. the delivered county/EA/fed/VA-mail brief generators and the MVP+ demo tooling, secret-scanned first — env-only) into scripts/oneoff/ with a README stating the convention: provenance archive, not operating code; nothing in app/lib/CI imports from it. Relative imports fixed for the new depth; eslint ignores scripts/oneoff/** (archived one-offs shouldn't gate the promoted-to-error rules — they fell out of the old scripts/_*.ts ignore on move, briefly 8 errors, now 0). Stays in scripts/: the 2 server-only shims (used by active one-offs) + the untracked _verify_*.ts PulseIQ regression harness (standing decision). Doc paths updated (BOTS/MCO_AGENT/TESTING). Also: supabase/.temp/cli-latest de-tracked (was gitignored-but-tracked → perpetual dirty status). .gitignore now covers the active scripts/_* convention so git status stays clean. Verified: fresh tsc clean, 1135 tests, lint 0 errors/3120 warnings (unchanged).

## 2026-07-04 — Pre-push review: all 10 verified findings fixed

WHY: before shipping the 38-commit batch, a workflow-backed high-effort review (33 agents, adversarial verify) surfaced 10 distinct defects — all in unpushed code, so fixed before any customer exposure. (1) PostgREST 1000-row silent-cap CLASS: every multi-row town-hall read now pages via a shared fetchAllRows helper (adapter fetchTurnsAsLegacy/computeBasicStats + live/search/share/export-json/analyze) — a .limit(5000)/.range(0,49999) was a ceiling, not a fetch size, so big-session exports/stats silently truncated at 1000; same class in lib/orgDelete storage listing (right-to-delete would have reported clean while leaving files — SECURITY §7 updated). (2) Dedicated-agent lifecycle contract: created status='paused' + config.pulseiq_dedicated → never publicly chattable at /b while the session is draft/closed; chat shim resolves by bot_id with NO agent-status gate (pausing the agent in the Agents UI previously 404'd the whole live room); session DELETE removes the dedicated agent marker-gated (Sarina-style linked agents never touched); '-agent' slug collision (agents.slug is globally unique and used to survive session deletes) retries with a random suffix instead of an opaque 500. (3) Engine bookkeeping: language-switch exchanges stored source='language_switch' and excluded from the turn cap + opening-response counting (a Spanish-first participant no longer loses a turn and their first real answer still opening-matches); 'too_long' answers get a polite trim ask instead of being recorded as a [filtered] conduct violation with the text discarded. (4) Reader-port regressions: list GET regained a read-only legacy leg until the drop commit (NOWOCATS must not look deleted before the discard executes) and 500s on DB error (listTownHallsAsLegacy now returns null on error) instead of rendering a false-empty dashboard. (5) Mirror-consistency (the one PLAUSIBLE finding, confirmed by reading the storage block): in town-hall mode all mirrorTurns writes are AWAITED — the facilitation policy reads clarifier/topic/seed budgets from conversation_turns, so a lagging fire-and-forget write silently loosened the caps; 1:1 agent mode keeps the async fast path. Verified: fresh tsc clean, 1135 tests, lint 0 err/3127 warn, and ALL SIX live suites re-run green vs the test project (lifecycle 5/5, item3 5/5, item4 4/4, item5 5/5, item7 7/7, tranche2 7/7).

## 2026-07-03 — Dev-mode split: local dev defaults to the TEST project

WHY: client deployment approaching — local dev running ambiently against the production database (write-capable service role on every dev session) becomes untenable with real client data. Design decision from the owner discussion: prod becomes OPT-IN, not banned — reads against prod (report/deck QC) stay legitimate, they just become deliberate. `npm run dev` now exports the SUPABASE_TEST_* creds under the runtime names (process env beats .env.local in Next) and boots against Sentimetrx-Test; `npm run dev:prod` keeps the old behavior with a fixed red "PRODUCTION DATABASE" banner (dev-only, NODE_ENV-guarded — deploys never render it; test mode gets a small green TEST DB chip). scripts/seed-test-dev.ts seeds the test project with an admin org + owner login (idempotent; refuses to run if TEST url == prod url). Verified live: test-mode boot shows the chip AND the prod-only /b/mvp-plus agent 404s (proof the DB actually flipped); prod-mode boot on :3000 healthy. Discovered en route: Next 16 dev has a repo-level singleton lock — a second `next dev` silently defers to the running one, so mode switches require stopping the current server first. CLAUDE.md push-policy wording updated (it claimed dev runs against prod). Part 1 of the prod/test isolation block (next: org-clone-down restore = DR drill; promotion framework for agents/PulseIQ/surveys).

## 2026-07-03 — Org-clone-to-test + first successful restore drill; backup integrity bugs found and fixed

WHY: prod/test isolation block part 2 — "data flows down." scripts/clone-org-to-test.ts restores an org snapshot into the TEST project (default source = nightly S3 backup, so every routine clone proves the backup chain; --fresh dumps prod on demand through the same code path; clone renamed "[CLONE] …"; auth.users stubbed passwordless for org members AND external creators referenced by created_by/user_id; restore core extracted to lib/orgRestore shared with the admin route). Live-testing it against the Datanautix Demo org surfaced THREE real backup-integrity bugs, all fixed: (1) the nightly dump's .limit(cap) was silently truncated by PostgREST at 1000 rows/table — the same silent-cap class as the pre-push review findings, missed in that sweep — so since the 7/2 zero-rows fix, backups captured at most 1000 rows per table; now paged, with dataset_rows_flat paging per-dataset by row_index (index-served; cross-dataset ordering hit the statement timeout on 47K JSONB rows). (2) Four tables in TABLE_SPECS had wrong scoping and errored EVERY nightly run: entity_catalog(+refresh) are polymorphic-scoped, no org_id → now skipped as regenerable-via-entity-discovery; webhook_events is global → skipped; org_transfers scopes by to_org_id → new col_eq_org filter kind. (3) The admin restore route could never restore dataset_rows_flat anywhere: id is GENERATED ALWAYS AS IDENTITY, which rejects upsert-by-id — identity tables now restore as strip-id inserts with per-dataset replace. Final drill: 23,485 rows restored into test with 4 expected residuals (cross-org login/transfer audit rows). S3-sourced clones from a laptop need the BACKUP_* creds locally (Vercel Sensitive vars pull as empty) — owner-gated. Backup fixes are LOCAL — nightly backups stay 1000-row-capped until the next push.

## 2026-07-03 — Admin-org feature auto-grant: 17-site gate sweep (owner-found bug)

WHY: owner's first real test-DB session caught it — clicking a clone dataset on /analyze bounced to /dashboard ("No studies match this filter"). Root cause: 15 files gate features by selecting organizations(features) WITHOUT is_admin_org, so resolveOrg's admin-org auto-grant never applies; any admin org whose stored features JSON is sparse (the seeded dev org stores {}) fails every gate. Prod masked it because the prod admin org has features explicitly stored — but the bug was latent everywhere. Swept all 17 select sites (analyze textmine/charts/stats/taxonomy pages, datasets API, review/reddit/substack sources, social auto-config + cron, team features PATCH ceiling, studies campaigns page) to select is_admin_org AND route unwraps through resolveOrg — the 8 manual Array.isArray unwraps are gone; one code path for effective features. Also fixed a consistent-type-imports error in clone-org-to-test.ts. tsc clean, 1135 tests, lint 0 err.

## 2026-07-03 — PulseIQ creator parity: Organic Topic Discovery restored + mode actually honored (owner-found)

WHY: owner compared the new creator's Conversation step against the old session editor and caught missing controls. UI: Organic Topic Discovery card added to the wizard (Off/On Demand/Automatic pills + detect-every-N), session-end mode converted dropdown→pills (owner request), post-session copy inputs (messages.post_session_intro/demo) added to the Post-Session step; skip/done labels were present all along (below the fold). The REAL bug underneath: the unified engine ignored theme_detection_mode entirely — chatCore's count-trigger fired unconditionally and the cron defaulted to auto reading only the top level, so "Off"/"On Demand" still ran automatic detection. Fixed: sessions POST lifts theme_detection_mode; chatCore count-trigger + cron both gate on mode==='auto' with nested engine.* fallback. tsc clean, 1135 tests, lint 0 err.

## 2026-07-04 — Facilitation continuity + trending quality (owner dry-run findings)

WHY: owner's PulseIQ dry-run transcripts showed the agent acting "bipolar" — ask, get a good answer, jump topics ("we're actually focused on schools right now" after a rich transportation answer; participants literally called it out in-session). Root cause: the dynamic per-topic cap (2–4 assistant turns by substance, item 7) only gated CLARIFIERS. A substantive answer skipped the clarifier, the topic was already in participantDiscussed after one tagged exchange, pickNextTopic filters discussed topics → instant rotation. Good answers were PUNISHED with a jump; only short answers earned continuity. Fix: new binding continue branch — under the cap, no move-on/disengage/skip signals → stay on the topic with a reflect-and-build instruction; the rotation instruction now REQUIRES a one-sentence acknowledgment of the prior answer and forbids dismissive framing. Live-verified 4/4 via new scripts/_verify_continuity.ts (opening match → two continuation turns → move-on still rotates) + items 3/6/7/lifecycle all re-run green. Second finding: Trending Now showed one participant's typos ("teh","adn","aroudn") and seed-theme words as trends — trendingTerms now takes per-conversation sources (term must appear in ≥2 distinct conversations when the room has >1) and an exclude set of topic label/keyword words, wired in the live route.

## 2026-07-04 — Trending goes semantic + transition tone hardened (owner feedback round 2)

WHY: owner's follow-up on the dry-run: (1) the dismissiveness was the core sin — the agent solicited an answer then treated it as a detour ("before we go deeper on that, today's main topic…") = "poor-AI feel"; (2) trending words were "just words, not things of potential interest" — frequency is the wrong abstraction for interestingness. Fixes: transition instruction hardened (prior answer was PROMPTED — must engage with its substance in one sentence; dismissive framings explicitly forbidden, incl. the "before we go deeper" pattern); Trending Now is now an AI extraction of up to 5 short NEW-interest phrases from the recent window (excludes planned topics, cached 60s/session = one fast call/min, logged as trending_extract; word-count heuristic kept as fallback). Live proof: trending for the harness session = ["shuttle from lot c","parking garage congestion"] — concrete phrases, typos gone; continuity 4/4 re-run; tranche2 harness extended to 8 checks incl. trending sanity. tsc clean, 1135 tests, lint 0 err.

## 2026-07-04 — Closing message reaches idle participants (owner dry-run)

WHY: owner ended a test session and the participant window never showed the closing message. Root cause: TownHallChat stopped polling entirely once status hit active — only a SENT message would surface the closed state (the chat shim replies with the closing on closed sessions, but idle participants never send one). Fix: activation downshifts the 3s poll to a 15s end-watch poll (200-participant room = ~13 req/min — negligible); on ended-while-chatting, the closing message lands as a bot bubble and the post-session question flow starts, mirroring the is_final path. Needs owner visual re-test (client-side behavior; server paths already harness-covered).

## 2026-07-04 — Export turn semantics: exchanges, not raw message indexes (owner question)

WHY: owner's XLSX showed turn_number 0,2,4,…,16,17 — the pairing projection leaked the new store's per-message numbering (user messages land on even indexes). A "turn" in exports = one EXCHANGE (bot message + reply, one row). fetchTurnsAsLegacy + the export json branch now number exchanges 1,2,3,… per participant (trailing unanswered question = next number), and the exchange's `source` column now comes from the ASSISTANT side of the pair (clarifier/standby/deflect were invisible — every row said "normal" because the user side is always normal). Harness 8/8 + tests green.

## 2026-07-04 — PPTX surfaced in the PulseIQ Export menu (owner-found)

WHY: owner asked "where is the PPT?" — the session deck route existed (ported in tranche 2) but the shared DownloadButton hardcoded CSV/Excel, so the deck was only reachable via an odd link inside the conversation modal. DownloadButton now takes a formats prop (default unchanged for other consumers); the PulseIQ list offers CSV/Excel/PowerPoint deck, with PPTX going through the POST deck route in handleExport. tsc/tests/lint green.

## 2026-07-04 — Clone-to-test: identity remap + external-reference stubs (owner-found)

WHY: cloning the owner's real Datanautix org (surveys for local testing) zeroed out — 50K FK-cascade errors. Three structural causes, each now handled: (1) IDENTITY COLLISION — the owner's real account exists on test under a different id (seeded dev admin); the cloned users row collided on email and every created_by chain died. Clone now remaps colliding users by email to the existing test-side id across all snapshot rows. (2) LEGACY clients TABLE — snapshots skip it, but studies.client_id/users.client_id still FK it; referenced client ids get minimal stubs. (3) EXTERNAL REFERENCES — org transfers leave datasets pointing at studies in other orgs, and pulseiq_sessions can point at agents outside the org; both get minimal stub parents. Result: 61,427/61,634 rows restored (6 studies/40 responses/55 datasets/50K rows/12 collections usable on test); residual 207 = review/reddit ingestion configs referencing transferred datasets — deliberately left (sync configs, not content). Also confirmed for the owner: /dashboard has NO org filter for platform admins — admin sees all orgs everywhere; the empty state was a genuinely empty test DB.

## 2026-07-04 — Backup coverage audit: "everything" made true (owner-driven)

WHY: owner asked the right contract question — "backup and restore should be EVERYTHING, right?" — while chasing themes he thought were lost in a clone (they weren't: dataset_state carried all 25 theme models; prod=test 25/25). The audit the question forced found the backup inventory materially incomplete: the ENTIRE recordings/Town Hall module was unbacked (recordings/files/transcripts/extractions/config-versions), plus saved_views, dataset_row(_field)_taxonomy (ALSO truncated — prod 128K > 100K cap; caps→500K), question_batches/logged_questions, reo_gold_review, agent_impressions, conversation_reviews, org/user_features, user_favorites. All added to TABLE_SPECS (regenerable AI caches + demo scratch skipped WITH reasons; schema_migrations skipped as infra). Storage binaries (recording media, logos) documented as explicitly OUTSIDE DB snapshots — Supabase Storage durability is the posture, S3 bucket-sync is future hardening. Clone tool re-run: 262K rows restored incl. 100K+100K taxonomy, 520 gold reviews, 55 client questions. Residual quirk: collections upsert vs prior-run rows (brand_slug arbitration) — content present from earlier run; noted for the restore lib.

## 2026-07-04 — Promotion framework: config flows up test → prod (isolation block part 3)

WHY: with `npm run dev` defaulting to the test project, new agents/PulseIQ sessions/surveys get BUILT in test — they need a safe, reviewable road to prod (the counterpart of clone-org-to-test's "data flows down"). New `lib/promotion.ts`: versioned JSON manifests per entity — agent = the pre-existing `bot_export_version:1` route format (the export/import routes now delegate to the lib, so UI downloads and CLI files interoperate); PulseIQ = session config (cohort_config minus archived, discussion_guide, response_target) + the dedicated facilitator persona, NO topics (seed topics regenerate on activate, same as the duplicate route); survey = the allow-listed `studies` row (questions/branding all live in `config` jsonb). `scripts/promote.ts` CLI (`npm run promote`): `export <type> <id-or-slug>` from TEST → gitignored `promotions/*.json` (manifests can carry client prompts — the FILE is the review gate), `import <file> --org <uuid>` to PROD, which requires `--yes` and exits BEFORE any prod connection without it. Everything lands dormant: agents/surveys draft, PulseIQ session draft with its facilitator agent paused; surveys get a FRESH guid; slugs ride a `-copy[N]` ladder (fixed the route's off-by-one that skipped `-copy2`); imports strip all ids/org/client linkage and never carry runtime data. created_by resolves via --by email or the target org's earliest user (nullable everywhere). Verified: 12 unit tests (allow-lists, draft-forcing, fresh guid, PulseIQ agent rollback on failed session insert) + live round-trip on the test project — exported a real agent (Sarina, 1 KB chunk), the Test Planning Session, and the Olive Garden survey, imported all three into the dev org, 15/15 DB checks passed (status/org/config parity, chunk without embedding, no topics, fresh guid), then cleaned up. Docs: ARCHITECTURE D13 (data down / config up), ENGINEERING §12 promote runbook, BOTS.md route note, specMap entries. tsc clean, 1147 tests, lint ceiling ratcheted 3145→3117 (route refactor removed `any`s).

## 2026-07-04 — Snapshot v2: streamed, uncapped backups (client-readiness item)

WHY: owner-flagged 7/3 — the per-table caps (50K flat rows, 500K taxonomy, 100K default) existed only because snapshot v1 built one JSON document in serverless memory; 128K taxonomy rows exist PRE-production and one client pilot adds ~300K, so caps would truncate real tenants. Structural fix, not bigger numbers. New `lib/snapshotStore` (S3 store w/ multipart streaming via @aws-sdk/lib-storage + local-dir store for tests/laptop), `lib/orgSnapshot` refactored to an uncapped 1000-row page iterator (caps deleted), `lib/orgSnapshotV2` streams each table through gzip into its own NDJSON object with the manifest written LAST as the commit marker (listings hide manifest-less days), `lib/orgRestore` core now consumes a streaming SnapshotSource in ≤500-row batches (v1 in-memory wrapper stays for the clone script, which materializes). Cron + admin snapshot/restore routes + clone-org-to-test all ported; both formats restore forever (openSnapshot reads v1 and v2); clone --fresh streams v2 to S3 or a local scratch dir when BACKUP_* creds are absent. LIVE VERIFICATION EARNED ITS KEEP — caught SIX real bugs: (1-3) org_features/user_features/user_favorites have NO id column, so the pager's order-by-id errored (would have turned every prod nightly red on deploy) AND restore had silently skipped them forever → now paged by real PK columns and restored replace-per-parent; (4) social_moderation_log spec'd parent_via connection_id but is org_id-scoped; (5) reddit_source_threads FK is reddit_source_id not source_id — both 4+5 are LIVE IN PROD today erroring nightly for any org with social/reddit sources; (6) real review text contains U+2028 LINE SEPARATOR, which JSON.stringify legally leaves raw and Node readline treats as a line break → a streaming restore crashed mid-table; writer now escapes U+2028/2029 and the reader splits on \n only. Cost of bug 6's crash: ~8.2K dataset_rows_flat rows lost from the [CLONE] Datanautix Demo org on test (delete-before-reinsert had run; re-cloneable, owner informed). Verified end-to-end: 7 unit tests (round trip incl. U+2028 row, delete-once-per-parent, replace-mode zombie deletion, manifest grouping) + live vs test project: demo org dump→restore→recount row-for-row clean; 262K-row org (2×100K taxonomy) dumped uncapped in ~60s at ~200MB RSS. This also OBSOLETES the scoped "clone per-dataset top-up" item — clones from v2 snapshots are uncapped by construction. Owner TODO in BACKUPS.md: add s3:AbortMultipartUpload + AbortIncompleteMultipartUpload lifecycle rule in the AWS console. tsc clean, 1154 tests, lint ceiling 3117→3113.

## 2026-07-04 — PulseIQ detail analytics on the new substrate (tranche-2 unit 4)

WHY: the last data-bearing tranche-2 unit — the facilitator detail page's ?analytics=true branch was legacy-only; new-substrate sessions got an "empty shell" from the adapter that ALSO had the wrong shape (top-level fields instead of the `analytics` object TownHallAnalyticsPanel reads), so the panel rendered blank. Extracted the ~290-line legacy analytics pipeline VERBATIM into pure `lib/townhallAnalytics.computeSessionAnalytics` (seed themes count AI-tagged turns + keyword supplement; organic themes keyword-first; sentiment_score preferred with lexicon backfill; auto/pinned time buckets; ≥2× shift detection; cumulative sentiment trends). The legacy route now calls it (byte-identical response; its turns fetch also gained fetchAllRows paging — yet another missed instance of the 1000-row PostgREST silent-cap class) and the adapter's analytics mode feeds it `fetchTurnsAsLegacy` projections, so both substrates produce identical analytics. Verified: 9 unit tests on the pure pipeline; 10-check live verify against the test project's Test Planning Session (real matches/quotes per theme, breakdown sums, trend point per response, stale shell keys gone); tranche-2 regression harness 8/8 (first run failed 4 checks — my env was missing ANTHROPIC_API_KEY so the harness's chat-route seeding silently produced zero turns; full-env recipe documented in the harness memory). Remaining tranche 2: legacy DELETE after owner Phase-6 acceptance. tsc clean, 1163 tests, lint at ceiling 3113 (any-count net-neutral — the moved code carried its warnings).

## 2026-07-04 — Taxonomy embed: verdicts move into the row blob (pre-client scale fix)

WHY: owner-driven scale call (scoped 7/3) — the taxonomy sidecar tables minted one row per (row, field) verdict: 128K rows pre-production, and a single 1M-comment client dataset would mint 5–7M; worse, sidecars have an independent lifecycle every backup/clone/delete had to remember (they were MISSED by backups until 7/3, and this session found the org-clone restore never remapped sidecar row_ids — every cloned verdict on test was silently dangling). Verdicts now embed in `dataset_rows_flat.data._tx` per fieldKey (axes projection + alerts + full assertions + provenance; NO version history — owner confirmed re-classify overwrites, replace-then-spot-check is the workflow) with rollups stored at classify time in `dataset_state.analytics.taxonomy`, so dashboards read aggregates, drill-downs get text+tags from ONE row, and lifecycle coverage is structural (ARCHITECTURE D14). sql/151 ports every RPC (aggregates/crosstabs/theme-chips/comments-filter/pending/drill/counts + new batch writer) with a per-dataset sidecar fallback so it's safe to apply before deploy; it also fixes the FTS trigger + search headline to skip `_tx` (else tag names/evidence pollute search). `_tx` is a reserved key — schema detect, the rows API (strips it from every client payload; TextMine's 50K bulk fetch must not ship assertions), Ana context, and the search detail all skip it (`lib/taxonomyEmbed.isReservedRowKey`). reviewSync's auto-classify gate moved off the legacy-table count to the stored rollups and now keeps EVERY classified field-combo current (was review_text-only). Backfill (`scripts/backfill-taxonomy-embed.ts`, idempotent, PostgREST-cap-aware) + drop migration sql/152 WRITTEN but gated: prod apply + backfill + parity verify, THEN drop (sidecars stay in TABLE_SPECS as rollback until then). Live verify on the test project (fresh-classify mode, since the cloned sidecars are dangling): 44/44 checks — rollup==stored==JS-recompute, drill/sub_counts/axis-crosstab/filters parity, pending drains to 0, tsv byte-identical to a `_tx`-skipping recompute on 1000 rows — and it CAUGHT a real bug: classify pages the same table it now UPDATEs, so row_index ties reshuffled mid-run (10,651 classify events over 10,648 rows) → deterministic `(row_index, id)` ordering. Prod inventory confirmed ZERO legacy-only verdicts (per-field table covers all 128K), so the legacy-table RPC leg is gone, not ported. tsc clean, 1171 tests, lint ceiling 3113→3111. OWNER-GATED next: apply sql/151 to prod (auto-mode blocked `npm run migrate` — correctly), run the prod backfill + parity verify, push, re-run backfill, then sql/152.

## 2026-07-04 — Taxonomy sidecar drop (sql/152): the embed migration completes

WHY: with sql/151 live on prod, the 155K-verdict backfill parity-verified 102/102 (re-verified once more post-deploy, still clean), and the deployed code reading only `data._tx`, the sidecar tables and the transitional RPC fallback legs were pure dead weight — and dead weight with a cost: the prod sidecar had already accumulated 2,745 orphaned verdicts pointing at deleted rows. sql/152 replaces every taxonomy RPC with its blob-only body and drops dataset_row_taxonomy + dataset_row_field_taxonomy; this commit removes both from lib/orgSnapshot TABLE_SPECS (verdicts back up with the rows now) and lib/orgDelete ORG_SCOPED_TABLES. ORDERING NOTE: the drop is applied to TEST now but prod apply is gated on THIS commit deploying first — the currently-deployed backup cron still pages both tables nightly and snapshot v2 is deliberately fail-loud, so dropping under it would turn nightlies red. Verified post-drop on TEST: fresh-classify harness 44/44 (blob-only RPCs, drill/aggregates/filters/pending all green), tsc clean, 1171 tests. Pre-2026-07-04 snapshots still carry the two tables' NDJSON parts; restores report them as unknown tables — ignorable (same verdicts restore with dataset_rows_flat), noted in BACKUPS.md.

## 2026-07-04 — The legacy PulseIQ substrate is gone (tranche-2 finale; convergence COMPLETE)

WHY: owner ran the Phase 6 acceptance session on prod (real end-to-end: creation → activation gate → 2 participants → skip/done/idle → semantic trending → close → closing-message-to-idle → exports → detail analytics) and accepted — the gate on the legacy delete. Executed: the ~870-line legacy orchestrator leg deleted from the chat route (1,242 → ~370 lines; unified dispatch + language switch + shared helpers only, no-match → 404); join/resume/responses/sessions-list/sessions-detail shims lost their legacy legs (detail route 824 → ~390); lib/townhallThemeDetection + themes/detect route + dead phase4Flags deleted. sql/153 (TEST applied; PROD GATED on this commit deploying — the deployed backup cron pages the legacy tables nightly and snapshot v2 is fail-loud): drops the town_hall* compat views, townhall_sessions/themes/turns, two legacy functions, and executes the owner-approved data discard. townhall_participant_responses survives new-substrate-only — and its backup anchor moved to town_hall_id→pulseiq_sessions, fixing a REAL latent gap: the old townhall_sessions anchor silently excluded every new-substrate participant response from backups. Parity fix: per-participant delete now removes the participant's response row (FK only cascades on session delete). Owner-flagged URL rename done: /townhall → /pulseiq (permanent redirects; API paths + /pi unchanged). Verified post-drop on TEST: tranche-2 harness 8/8, lifecycle 5/5, item3 5/5, 1,171 tests, fresh tsc clean; lint ceiling 3111→3066 (dead code carried 45 warnings). NOTE: the 'collections brand_slug upsert quirk' from the 7/3 queue memory does not exist in the tree (repo-wide grep zero hits) — stale note, dropped.
